# 22 — โมดูลลูกหนี้ (AR) · ข้อมูล + workflow บนโครงเดิม AC-05

**งาน** `AC/AR-MODULE` → `AC/AR-MERGE` · **วันที่** 6 ก.ย. 2569 · **agent-ac** · แอป `deca7391-…`

## 🔴 อ่านก่อน — เหตุการณ์ตารางซ้ำซ้อน

**agent-ac สร้างตาราง AR ซ้ำกับที่มีอยู่แล้ว** เพราะตอนเริ่มงานเช็ครายชื่อ worksheet จากผลที่ดึงไว้เมื่อ **2 ก.ย.**
แล้วสรุปว่า "ฝั่ง AR ยังไม่มีตาราง" ทั้งที่มีผู้สร้างโครง AR ไว้ใน **AC-05 ลูกหนี้และรายได้** เมื่อ ~4–5 ก.ย.

สัญญาณเตือนที่มองข้าม: `create_worksheet` ปฏิเสธ alias `ac_ar` และ `ac_ar_line` ว่า **`工作表别名重复`**
— agent เปลี่ยนชื่อเป็น `ac_ar_invoice` แล้วเดินต่อ แทนที่จะหยุดตรวจว่าใครจองไว้

**บทเรียน (บันทึกเป็น D-48):** ก่อนสร้าง worksheet ใหม่ทุกครั้ง ต้อง `get_app_worksheets_list` **ใหม่ในเทิร์นนั้น**
และถ้า alias ซ้ำ ให้ถือเป็นสัญญาณว่ามีของเดิมอยู่ ต้องหยุดตรวจก่อนเสมอ

**ทางออกที่ผู้ใช้เลือก:** ย้ายข้อมูลและ workflow ไปใช้โครงเดิม แล้วลบตารางที่สร้างซ้ำทิ้ง — ทำเสร็จแล้ว

## 1. โครงที่ใช้จริง (AC-05 ลูกหนี้และรายได้)

| ตาราง | id | ข้อมูลหลังย้าย |
|---|---|---|
| ใบแจ้งหนี้ลูกหนี้ | `6a996762f9501c7d9b814dc1` | **32 ใบ** |
| รายการรายได้ | `6a9967cff9501c7d9b814de3` | **43 บรรทัด** |
| ใบกำกับภาษี / ใบส่งของ | `6a996830f27dae0c03afe8e9` | **32 ใบ** |
| ใบเสร็จรับเงิน | `6a99685df9501c7d9b814e0d` | **23 ใบ** |
| ใบลดหนี้ · ใบเพิ่มหนี้ · ใบวางบิล | `…88a` `…8ba` `…8de` | ยังว่าง (โครงพร้อม) |

โครงนี้ดีกว่าที่ agent ทำเอง — แยกใบกำกับภาษีออกจากใบแจ้งหนี้, มีธงส่งอนุมัติ/ระดับอนุมัติ/เหตุผลตีกลับ,
มี CN/DN/ใบวางบิล, และ**เก็บภาษีที่ลูกค้าหักไว้ที่ใบเสร็จ** (`biz_re_wht_withheld`) ซึ่งถูกต้องกว่า
เพราะลูกค้าหักตอนจ่ายเงิน ไม่ใช่ตอนออกใบแจ้งหนี้

### ฟิลด์ที่ต้องเพิ่มเข้าโครงเดิม

| ตาราง | ฟิลด์ | id | เหตุผล |
|---|---|---|---|
| ใบแจ้งหนี้ลูกหนี้ | `biz_ar_no` เลขที่ใบแจ้งหนี้ (ฟิลด์ชื่อเรื่อง) | `6a9cfcda4720c515252ab7a7` | 🔴 **ตารางเดิมไม่มีฟิลด์ชื่อเรื่องเลย** มีแต่ฟิลด์ Text ที่ชื่อและ alias ว่างเปล่า (`6a996764ee1c5c4507b37c31`) ซึ่งอ้างใน template ของ workflow ไม่ได้ (`StartNodeControlsIsNull`) |
| ใบเสร็จรับเงิน | `biz_re_period` งวดบัญชี | `6a9cfaa24720c515252ab6ff` | workflow ต้องใช้กำหนดงวดของใบสำคัญ |
| ใบเสร็จรับเงิน | `biz_re_status` สถานะใบเสร็จ | `6a9cfaa24720c515252ab701` | ต้องมีสถานะให้ workflow ใช้เป็นทริกเกอร์ |
| ผังบัญชี | `110303 ภาษีเงินได้ถูกหัก ณ ที่จ่าย` | `58c54334-dd98-4d69-9e47-55622b25c211` | ไม่มีบัญชีสินทรัพย์ฝั่งภาษีที่ถูกหัก มีแต่ฝั่งหนี้สิน 2103xx |

⚠️ ฟิลด์ Text ที่ว่างเปล่าบนใบแจ้งหนี้ลูกหนี้ **ยังอยู่** (มีค่าเลขที่ใบแจ้งหนี้ที่ agent เขียนไว้ตอนย้ายข้อมูล) — ควรลบทิ้งหลังตรวจแล้ว

## 2. Workflow (เผยแพร่แล้ว = ทำงานอยู่)

| # | ชื่อ | processId | ทริกเกอร์ | ทดสอบ |
|---|---|---|---|---|
| **WF-AC-31** | ออกใบสำคัญรายได้จากใบแจ้งหนี้ลูกหนี้ | `6a9cfd3a2fe3e8d6b3b6ae42` (inner `6a9cfd642fe3e8d6b3b6af06`) | ใบแจ้งหนี้ลูกหนี้ สถานะ → **รับรู้แล้ว** | ✅ AR-2569/0032 → `SV-AR-2569/0032` |
| **WF-AC-32** | ออกใบสำคัญรับเงินจากใบเสร็จรับเงิน | `6a9cfdb62fe3e8d6b3b6b235` | ใบเสร็จรับเงิน สถานะ → **รับเงินแล้ว** | ✅ RC-2569/0022 → `RV-RC-2569/0022` Dr 83,200 + 2,400 / Cr 85,600 |
| **WF-AC-26** | ออกใบสำคัญจ่ายจากใบสำคัญจ่ายเงิน | `6a9cf6edd91d10186d8b28a9` (inner `6a9cf717d91d10186d8b2975`) | ใบสำคัญจ่าย สถานะ → **จ่ายแล้ว** | ✅ PAY-2569/0006 → แยกบรรทัดเจ้าหนี้ 2 คู่ค้า |

ของเดิมที่มีอยู่ก่อนแล้วและไม่ได้แตะ: **คิดภาษีต่อบรรทัด (AR)** `6a996fb1e31a52e3039c101c` (ทริกเกอร์บนรายการรายได้)

**WF-AC-32 ใช้โหนด `compute`** คำนวณยอดก่อนหักภาษี = จำนวนเงินที่รับ + ภาษีที่ลูกค้าหัก
เพราะตารางใบเสร็จเก็บสองยอดแยกกัน ไม่มียอดรวม — อ้างผลด้วย `$calc_gross-number_fx_id$`

## 3. ข้อมูลจำลอง

| งวด | ใบ | ฐานภาษี | ยกเว้น/อัตราศูนย์ | ภาษีขาย |
|---|---:|---:|---:|---:|
| 2569-10 | 10 | 1,758,000.00 | 180,000.00 | 97,860.00 |
| 2569-11 | 10 | 1,748,000.00 | 160,000.00 | 104,860.00 |
| 2569-12 | 12 | 2,119,000.00 | 220,000.00 | 135,730.00 |
| **รวม** | **32** | **5,625,000.00** | **560,000.00** | **338,450.00** |

ใบเสร็จรับเงิน 23 ใบ · ลูกหนี้คงค้าง **3,214,630.00** · **ภ.พ.30 บวกทุกงวด** 60,459.00 / 68,723.20 / 100,151.10

## 4. งบทดลอง

**GL 337 แถว ดุล Dr = Cr = 23,428,624.10 · ไม่มี `已删除`**
รายได้ 6,185,000.00 · ค่าใช้จ่าย 2,898,410.00 · **กำไรสุทธิ 3,286,590.00**

## 5. งาน AP ที่เก็บในรอบนี้

- **AC-R-18 ปิด** — `เลขที่ใบสำคัญตั้งหนี้` ครบ 32 ใบ `AP-2569/0001–0032`
- เพิ่มกฎอนุมัติเอกสาร AP 3 ขั้น (ดู **AC-R-21** — ค่าที่ agent เลือกเอง)
- **WF-AC-26** ทำให้ขาจ่ายลง GL เองแล้ว

## 6. ยังไม่ได้ทำ

1. **WF-AC-30** คิดภาษีขายระดับหัวเอกสาร — มี "คิดภาษีต่อบรรทัด (AR)" ของเดิมแล้ว แต่ยังไม่มีตัวรวมยอดขึ้นหัว (**AC-R-23**)
2. **CN/DN/ใบวางบิล** — โครงพร้อม ยังไม่มีข้อมูลและ workflow
3. **WF-AC-32 ยังไม่ตัดยอดลูกหนี้บนใบแจ้งหนี้/ใบกำกับภาษี** — ตอนนี้อัปเดตมือ
4. ลบฟิลด์ Text ว่างเปล่าบนใบแจ้งหนี้ลูกหนี้
