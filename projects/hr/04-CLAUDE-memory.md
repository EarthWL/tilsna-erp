# Memory — โมดูลทรัพยากรบุคคล (HR) · TILSNA ERP (Nocoly)

_อัปเดตล่าสุด: 28 สิงหาคม 2569 (ต่อเนื่อง 17) — 🔴🔴🆕 **แก้ข้อสรุปของ D-17 ที่เขียนผิดไว้เมื่อรอบก่อน + พบว่าปัญหายังไม่ถูกแก้ทั้งโมดูล (D-19)** — จากการ scrutinize การแก้ D-17: (1) ข้อสรุปเดิม *"defaultValue ไม่ถูกใช้เมื่อสร้าง record ผ่าน MCP"* **ผิด** — สำรวจด้วย `hap worksheet fields --raw` พบว่า**ฟิลด์ธงทุกตัวในทุกตารางมี `default:""`** ไม่มีตัวไหนได้ `defaultValue:0` จริงเลย รวมถึงตัวที่ D-14 บันทึกว่าตั้งแล้ว ⇒ สาเหตุจริงคือ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (แก้ข้อความในเอกสารครบ 7 จุด 3 ไฟล์แล้ว) (2) **MCP `get_worksheet_structure` ไม่คืนค่า `defaultValue` เลย** ต้องใช้ hap CLI `--raw` เท่านั้น — นี่คือเหตุผลที่ D-14 ถูกเชื่อว่าแก้แล้วมา 4 วัน (3) 🔴 **D-19: D-17 แก้ไปแค่ 2 record ทดสอบ ไม่ได้แก้รูปแบบนี้ทั้งโมดูล — WF-HR-02 จะไม่ตัดสิทธิเมื่อ To-do ที่ค้างอยู่ถูกกด Approve** WF-HR-03/05/13 เสี่ยงเหมือนกัน (4) **path ผู้ใช้จริงผ่านฟอร์มยังไม่เคยทดสอบเลย** — ดู Known Issues ต่อเนื่อง 17 และ D-19 ใน Roadmap §11 — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 16) — 🟢 **D-17 แก้ไขแล้ว — ไม่ใช่ปัญหาแพลตฟอร์ม/session เลยตลอด 3 รอบก่อนหน้า** ผู้ใช้ส่งภาพ Workflow History ของ `TEST-LR-D15-REVERIFY` มาถามชื่อ workflow → เปิดดูพบสถานะ "Done" จริง เข้า node `gate` (branch) ครบ ไม่ใช่ "ไม่ยิง" — ตรวจ `get_workflow_structure` พบเงื่อนไข path `leg_supervisor` ต้องการ `submitted_flag` ne "1" — ตรวจ `get_record_details` พบว่า `submitted_flag`/`hr_ot_submitted_flag` เป็นค่าว่างจริง (ไม่ใช่ 0) บน record ทดสอบทั้งสองใบ ⇒ ตรงกับบั๊ก "`ne` เทียบฟิลด์ว่างไม่ผ่าน" ที่เคยพบมาก่อน (เหมือน D-14) จึงตกไป path "ไม่ทำอะไร" เงียบ ๆ — **สรุปใหม่สำคัญ: ฟิลด์ธงเหล่านี้**ไม่มี `defaultValue` เลย** (`default:""` — ยืนยันด้วย `hap worksheet fields <ws_id> --raw` 28 ส.ค. 2569) รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกไว้ว่าตั้ง `defaultValue:0` แล้วเมื่อ 27 ส.ค. ⇒ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องกับบั๊กที่รู้อยู่แล้วว่า `editFields` เขียนทับทั้ง field object) — ยังพิสูจน์ไม่ได้ว่า API เพิกเฉยต่อ default เพราะไม่มีฟิลด์ไหนมี default ให้ทดสอบเลย ⇒ ในทางปฏิบัติ ต้องส่งค่าฟิลด์ธงตรง ๆ เองเสมอตอน seed** — ทดสอบแก้จริง (set flag=0 แล้ว re-trigger) ทั้ง WF-HR-01/WF-HR-04 ยิงสำเร็จทันที `_updatedBy`=`user-workflow`, มี To-do จริงถึง Wanadtapong.l — WF-HR-01/WF-HR-04 ทำงานได้ปกติมาตลอด ไม่เคยเสียหายจาก D-15/BR-08.3 — ขออภัยที่แนะนำผิดให้ผู้ใช้ไปตรวจสถานะแพลตฟอร์ม/subscription รอบก่อน — บทเรียน: ต้องเปิด Workflow History (Browser) ตรวจก่อนสรุปว่า "ไม่ยิง" เสมอ อย่าพึ่ง `_updatedBy`/`get_approval_list_by_row` อย่างเดียว — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 15) — 🔴🔴🆕 **D-17 re-verify ครั้งที่ 2/3 (ตามคำสั่ง "ดำเนินการต่อไปก่อน") ยังไม่คลี่คลาย — ยืนยันข้ามช่วง session แล้ว (>7 ชม.)** — reset `TEST-LR-D15-REVERIFY` (WF-HR-01) และ `TEST-OT-SYSCHK-01` (WF-HR-04) กลับ Draft แล้วตั้งเป็น Pending supervisor อีกครั้งผ่าน `update_record(triggerWorkflow:true)` → รอ >60 วินาที → ทั้งคู่ยังคง `_updatedBy`=`user-api`, ไม่มีฟิลด์ workflow ถูกเติมเลย, `get_approval_list_by_row` ว่าง — **นี่คือความล้มเหลวครั้งที่ 3 ติดต่อกัน (ครั้งที่ 1: ต่อเนื่อง 11 · ครั้งที่ 2: นี้) และครั้งแรกที่คร่อมขอบเขต session** (record ทดสอบสร้างไว้ ~11:15 ของ session ก่อนหน้า, re-check นี้ ~18:46-18:47 ของ session ปัจจุบัน) ⇒ **ไม่ใช่ปัญหาชั่วคราวของ session/เวลาเดียวอีกต่อไป — แนะนำให้ผู้ใช้ตรวจสอบสถานะแพลตฟอร์ม Nocoly/HAP โดยตรง** (subscription/billing, platform support, หรือเปิด Browser เช็คว่า workflow ขึ้นสถานะ Enable/Active จริงหรือไม่) — เกินขอบเขตที่ agent แก้เองผ่าน MCP/CLI ได้แล้ว ดูรายละเอียดเต็มใน Known Issues (D-17) · ✅🆕 **P5-6 เสร็จบางส่วน (2/4 คน)** — seed `hr_salary_structure` ให้ TEST-EMP1 (30,000 บาท) และ TEST-EMP2 (35,000 บาท) ผ่าน `batch_create_records`(`triggerWorkflow:false`) ผูก pay component SALARY, effective 2026-01-01 — ยืนยันด้วย `get_record_list` เห็น relation ถูกต้อง ไม่ orphan — ตัวเลขเป็นค่าทดสอบสมมติ ไม่ใช่เงินเดือนจริง (ตามหลักปฏิบัติเดิมของโปรเจกต์) — เหลือ TEST-EMP3/4 รอ P2-6 seed พนักงานเพิ่ม (บล็อกบางส่วนโดย G-06) · **P5-5 (posting rule หลายบรรทัด) พิจารณาแล้วตัดสินใจไม่ทำในรอบนี้** — `ac_posting_rule` schema รองรับแค่ debit/credit account เดียวต่อ 1 rule แต่ WF-HR-10 ต้องการใบสำคัญ 5 บรรทัด — สร้างตอนนี้ต้องเดา mapping ธุรกิจเอง ขัดกฎ "ห้ามเดา" ต้องรอตัดสินใจร่วมกับฝ่ายบัญชี/ผู้ใช้ก่อน — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 14) — 🟡🆕 **G-05 cross-check กับกฎหมายทางการแล้ว (ตามคำขอผู้ใช้ "ดึงข้อมูลจากในเน็ตได้ไหม เอาตามกฎหมายก่อน")** — ตรวจผ่าน WebSearch/WebFetch: อัตราเงินสมทบประกันสังคม 5%(นายจ้าง)/5%(ลูกจ้าง) + เพดานฐานค่าจ้างใหม่ 17,500 บาท/เดือน มีผล 1 ม.ค. 2569 ตามราชกิจจานุเบกษา 12 ธ.ค. 2568 · ขั้นบันไดภาษี 8 ขั้น (สูงสุด 2,000,001-5,000,000=30%, over 5,000,000=35%) ตามหน้าทางการกรมสรรพากร (rd.go.th/59670.html) — **ตรวจ `hr_sso_rate`/`hr_tax_bracket` ที่ seed ไว้แล้ว (26 ส.ค.) พบว่าตรงกับกฎหมายจริงทุกจุดอยู่แล้ว ไม่ต้องแก้ไขค่าใด ๆ** — ⚠️ **นี่คือ cross-check กับกฎหมายสาธารณะ ไม่ใช่การเดา แต่ก็ยังไม่ใช่ "ยืนยัน/เซ็นรับรองจากฝ่ายบัญชี" ที่ฝ่ายบัญชีขอไว้ชัดเจนใน response document — ต้องขอ sign-off อย่างเป็นทางการก่อน go-live จริง** · **`hr_company_tax_id`/`hr_sso_employer_no` เป็นเลขเฉพาะของบริษัท TILSNA หาจากอินเทอร์เน็ตไม่ได้ ต้องได้จากเอกสารทะเบียนจริงของบริษัทเท่านั้น** — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 13) — 🟢🆕 **P5-4/Gap G-02 ปิดครบ 6/6!** ฝ่ายบัญชีตอบกลับ (`tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`) สร้างบัญชีใหม่ 3 รายการในผังบัญชีจริง (SSO_ER `530102` · SSO_EE `210402` · WHT `210304`) + เพิ่ม `event_code`=`PAYROLL_ACCRUAL`/`PAYROLL_PAYMENT` ใน `ac_posting_rule` (แก้ไขความเข้าใจผิดเดิม: เป็น inline field แก้ผ่าน MCP ได้ ไม่ใช่ Browser-only) — HR ผูก `hr_pay_component.biz_coa_account` ต่อสำเร็จครบ 6/6 ผ่าน `update_record`(fieldId `6a8ff3049762533b5b7193a2`, `triggerWorkflow:false`) ยืนยันด้วย `get_record_details` เห็นชื่อบัญชีจริงทั้ง 3 record · **WF-HR-10 source-of-truth ยืนยันแล้ว:** `ac_posting_rule` หลัก (จับคู่ event_code+conditions) / `hr_pay_component.biz_coa_account` fallback · ⚠️ **ยังเปิดอยู่ ห้ามแตะ:** อัตรา SSO/ภาษี WHT/เลขทะเบียนบริษัท (G-05/P0-5) — ฝ่ายบัญชีระบุชัดเจนห้ามเดาตัวเลขเหล่านี้ — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 12) — 🟢🆕 **P4-4 (WF-HR-05) และ P4-6 (WF-HR-06) สร้าง+publish สำเร็จผ่าน MCP ทั้งคู่!** ทำตาม "ดำเนินการต่อตามที่แนะนำ" — สร้าง node graph จริงของทั้งสอง workflow: WF-HR-05 main `6a910184730d20c5b7710fa8` v1 + inner `6a910197730d20c5b7711028` v1 (sub_process สรุปสถานะรายวันทีละ `hr_attendance`) · WF-HR-06 main `6a910222730d20c5b77115e5` v1 + inner `6a9102345f8564a68c449ee7` v1 (sub_process แจ้งเตือน SLA ค้างอนุมัติ) — **`validate_process`/`publish_process` ทั้ง 4 process (main×2 + inner×2) สำเร็จ 0 error** ยืนยันว่า D-16 fix (publish inner ก่อน outer) ใช้ได้จริงกับ build งานจริง ไม่ใช่แค่ minimal test — **ปรับโครงสร้างจากดราฟต์เดิมในหลายจุด** เพราะ `hr_shift.hr_start_time`/`hr_end_time` เป็น Text "HH:mm" ไม่ใช่ Time field: ใช้ node `code` (JavaScript) แทน `Function calculation`/`Duration` สำหรับคำนวณสาย/ออกก่อน/ชั่วโมงทำงาน (คำนวณผ่าน epoch ms + offset +7h เอง กัน timezone bug จาก runtime) · ตัดขั้น "Get associated records employee→shift" ออกเพราะ `hr_attendance` มี `hr_att_shift` อยู่แล้ว · ส่ง `hr_setting` (SLA/grace minutes) เข้า sub_process ผ่าน subprocess parameter แทนการ query ซ้ำทุก record — **พบ gap ใหม่ D-18: `hr_ot_request` ไม่มีฟิลด์ `submitted_at`** ทำให้ WF-HR-06 v1 ต้องจำกัดขอบเขตเหลือแค่ `hr_leave_request` (ตัด OT/claim ออกจากรอบนี้ รอผู้ใช้ตัดสินใจว่าจะเพิ่มฟิลด์หรือใช้ `_createdAt` แทน) — ⚠️ **ทั้งคู่ยังไม่ได้ live-fire test เพราะ D-17 ยังไม่คลี่คลาย** (schedule trigger ยังไม่เคยพิสูจน์ว่ากระทบจาก D-17 ด้วยหรือไม่ — ต้องตรวจตอนถึงรอบ Execute now/01:00-09:00 จริง) — รายละเอียดเต็มดู Known Issues ด้านล่างและ `02-BuildSpec-FRS.md` WF-HR-05/06 — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 11) — 🔴🆕 **D-17: re-verify WF-HR-01 พบว่า "ไม่ยิง" เป็นปัญหาระดับระบบ ไม่ใช่เฉพาะ WF-HR-01!** สร้าง record ทดสอบใหม่ล้วน ๆ (`TEST-LR-D15-REVERIFY`) ยิง WF-HR-01 → ไม่ทำงาน (รอ 68+ วินาที, log มีแต่ `user-api`) — เพื่อแยกว่าเป็นปัญหาเฉพาะ WF-HR-01 หรือทั้งระบบ **ทดสอบซ้ำกับ WF-HR-04 (ที่เคยยืนยันทำงานสำเร็จ 27 ส.ค. และไม่ถูกแตะต้องเลยตั้งแต่นั้น) ด้วย record ใหม่ (`TEST-OT-SYSCHK-01`) → ก็ไม่ทำงานเช่นกัน** ทั้งที่โครงสร้างทั้งคู่ตรวจสอบแล้วว่า published:true ปกติ ไม่มี warning ⇒ **สรุป: นี่ไม่ใช่ความเสียหายจากการแก้ไข WF-HR-01 (BR-08.3/D-15) — เป็นปัญหาระดับ session/environment/platform ที่ worksheet_event workflow ทั้งหมดไม่ยิงตอนนี้** เก็บ record ทดสอบทั้ง 2 ไว้เป็นหลักฐาน (ตั้งชื่อชัดเจนว่าเป็น test) — **คำแนะนำ: ต้องรอ/ตรวจสอบใหม่ใน session ถัดไปก่อนเชื่อว่า workflow ใด ๆ ทำงานได้ ไม่ใช่แค่ WF-HR-01** ดูรายละเอียดเต็มใน Known Issues — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 10) — 🟢🆕 **D-13 คลี่คลายแล้ว! ไม่ใช่บั๊กแพลตฟอร์ม — เป็นขั้นตอนที่ขาดไปเอง:** ตรวจสอบเอกสาร skill `hap-mcp-app-builder` (คนละ skill กับที่ session นี้ใช้หลัก) ตามคำขอผู้ใช้ "ลองยืนยันข้อมูลใน skill ชุด hap" พบว่ามีการบันทึกไว้ชัดเจนว่า **ต้อง `publish_process` ที่ inner processId ของ sub_process/approval_block ก่อน แล้วจึงค่อย publish outer/main process** — ไม่งั้นจะได้ `NodeAppIsNull` แน่นอน — ตรวจ Known Issues เดิมของ D-13 พบว่าทุกครั้งที่ทดสอบก่อนหน้านี้ (รวมถึงรอบ reconfirm ล่าสุด) เรา**แค่ `validate_process` (valid:true) ของ inner แต่ไม่เคยเรียก `publish_process` บน inner ก่อนเลย** — **ทดสอบซ้ำจริงด้วย minimal process ใหม่ (`6a90fb59fdab77a41c63662d` + inner `6a90fb5dfdab77a41c636699`) โดยเพิ่มขั้น publish inner ก่อน แล้วจึง publish outer — สำเร็จทั้งคู่ ไม่มี error เลย!** ลบ process ทดสอบทั้ง 2 (outer+inner) ทิ้งสะอาดแล้ว — **สรุป: `sub_process` ใช้งานได้ผ่าน MCP ปกติ ตราบใดที่ publish inner ก่อน outer เสมอ — P4-4 (WF-HR-05) และ P4-6 (WF-HR-06) ปลดบล็อกแล้ว พร้อมสร้างจริง** — ดูรายละเอียดเต็มใน Known Issues ด้านล่าง (แก้ไข D-13 entry) — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 9) — 🔴🆕 **D-15: (a) ยืนยันซ้ำบั๊ก `sub_process`→`NodeAppIsNull` เป็นครั้งที่ 2 ผ่าน minimal isolated test — บล็อก P4-4/WF-HR-05 และ P4-6/WF-HR-06 ทั้งคู่ (b) พบพฤติกรรม "auto-relink" ใหม่ของ `batch_create_process_nodes` เมื่อแทรก node กลาง edge ที่ปลายทางเป็น branch node — `validate_process` ไม่จับ ต้องเช็ค `prevNode` ด้วย `get_workflow_structure` เองเสมอหลังแทรก node (c) ลองเพิ่ม BR-08.3 เข้า WF-HR-01 จริง (publish v4) แล้วพบว่า workflow **หยุดยิงบน trigger ใหม่ทั้งหมด** (ไม่มี `user-workflow` log เลย) — revert คืนโครงสร้างเดิมครบ (`delete_process_node`×4, publish v5) แต่ **อาการไม่ยิงยังคงอยู่แม้หลัง revert** — สาเหตุยังไม่ทราบ (ไม่ยืนยันว่าเกี่ยวกับการแก้ไขหรือเป็นปัญหาเฉพาะหน้า) — ยืนยันว่า approval instance จริงของ TEST-LR-0001 **ไม่ถูกกระทบ** ตลอดกระบวนการ — 🔴 **สถานะ 🟢 เดิมของ WF-HR-01 (checklist #12) ต้องตรวจสอบซ้ำก่อนเชื่อ ห้ามแก้ node ของ WF-HR-01 เพิ่มจนกว่าจะเข้าใจสาเหตุ** — ดูรายละเอียดเต็มใน Known Issues ด้านล่างและ `05-Roadmap-Tracker.md` D-15 — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 8) — 🔶 **P5-4 seed `hr_pay_component` 6 รายการสำเร็จผ่าน MCP + verify แล้ว** — ผูก `coa_account` ได้ 3/6 (`SALARY`/`OT`/`ABSENCE_DEDUCT`→`530101 เงินเดือน`) ส่วน `SSO_ER`/`SSO_EE`/`WHT` เว้นว่างเพราะ **ผังบัญชีจริงไม่มีบัญชีรองรับ** (ตรวจ `ac_coa` ครบ 79 record แล้ว ไม่ใช่ agent มองข้าม) — ดูแถว #27 และ Known Issues ต่อเนื่อง 8 — บล็อกบางส่วนของ P5-5 จนกว่าฝ่ายบัญชี/ผู้ใช้จะเพิ่มบัญชี — ก่อนหน้านั้น: 27 สิงหาคม 2569 (ต่อเนื่อง 7) — ✅🔴 **ตรวจสอบตามคำขอผู้ใช้พบ+แก้บั๊กครบทั้ง 3 จุด:** (1) `entityName`("Data Name") ของ `hr_pay_component`/`hr_pay_period`/`hr_salary_structure` เคยเป็น alias อังกฤษ — แก้ด้วย MCP `update_worksheet(name+alias)` (2) **ทั้ง 7 ตารางกลุ่มเงินเดือนมี view "全部" (จีน) ค้างอยู่ — แก้ครบผ่าน hap CLI** `worksheet view update` (3) **หลังผู้ใช้ส่งภาพจับได้ว่า breadcrumb หัวตารางยังอังกฤษอยู่ พบว่า `name` จริงเป็นคนละ field กับ `entityName` ที่ MCP แก้ได้ — ต้องใช้ hap CLI `worksheet update --name` แก้แยกต่างหาก** ทำครบทั้ง 3 ตารางแล้ว — กลุ่มเงินเดือนตอนนี้ไม่มี object ภาษาอังกฤษ/จีนค้างเลยทั้ง `alias`/`entityName`/`name`/view — ดู Known Issues ด้านบนสุด — ก่อนหน้านั้น: 🆕🟢 **P5-3 สร้าง `hr_pnd1_filing`/`hr_wht_cert` ครบ (13+9 ฟิลด์) — กลุ่มเงินเดือนครบ 7/7 ตารางแล้ว (P5-1+P5-2+P5-3)** — ดูแถว #26 และ "ID ตารางภาษี/หนังสือรับรอง (P5-3)" ด้านล่าง — ก่อนหน้านั้น: 🟢 P5-2 สร้าง `hr_payslip`/`hr_payslip_line` ครบ (25+8 ฟิลด์, relation bidirectional 6 จุด, `Currency` แทน `Number` สำหรับยอดเงิน, `AutoNumber` สำหรับ `payslip_no`) — ดูแถว #25 และ "ID ตารางสลิปเงินเดือน (P5-2)" ด้านล่าง — ก่อนหน้านั้น: P3-4 เสร็จ+ยืนยัน (seed hr_leave_balance ครบ 2 พนักงาน×6 ประเภท) · P4-1 `hr_attendance` และ P4-2 `hr_ot_request` สร้างโครงสร้างครบผ่าน MCP workaround + hap CLI (view rename + Data Name ไทย) — ดูแถว #15 ด้านล่างและ ID ตารางใหม่ · 🔴🆕 พยายามทำ D-8 แล้วชนบั๊กแพลตฟอร์มใหม่ `sub_process`→`NodeAppIsNull` — revert กลับสถานะเดิมปลอดภัย ดู Known Issues + D-13 · 🆕🟢 **P4-3/WF-HR-04 (อนุมัติ OT 2 ระดับ) สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ 27 ส.ค. 2569** — เพิ่มฟิลด์ `hr_ot_rate.day_type` ที่ขาดหายไปสำเร็จผ่าน API ก่อน แล้วพบ+แก้บั๊กใหม่ 2 ตัว (`dateDiff` ตัด `precision` ทิ้งเงียบ · flag field ไม่มี defaultValue เจอซ้ำ) + คิดค้นเทคนิคใหม่แชร์ inner approval_block ข้าม 2 branch path — ดูแถว #16 และ ID Workflow ใหม่ด้านล่าง** — สรุปสถานะก่อนหน้า: กลุ่ม HR 8 กลุ่ม + optionset 24 ชุด + worksheet HR-00 ครบ 10 ตารางสร้างแล้ว ผ่าน workaround `create_app_items`+`addFields` + seed ข้อมูลตั้งค่า 55 record ใน 9/10 ตาราง เสร็จแล้ว — เหลือ `hr_approval_rule` รอ role · หา hap CLI ใช้ rename view ได้จริง (live-tested) + สรุปเรื่อง Data Name/Record Name จบแล้ว ดู Known Issues · P2-1 เสร็จ (seed hr_position 8 + hr_job_level 6) · hr_employee 36 ฟิลด์ยืนยันครบ · สร้าง hr_employment_contract/event/dependent/bank_account ครบ 4 ตาราง (P2-6 บางส่วน) · ปิดการสอบสวน hap CLI optionset-binding แล้ว (Browser-only ยืนยันชัดเจน) · **P3-1 เสร็จ (โครงสร้าง)** — สร้าง `hr_leave_balance`/`hr_leave_request`/`hr_leave_ledger` ครบ 3 ตาราง ดู ID ด้านล่างและ `02b-BuildSpec-Addendum-P3-1.md` · ✅ **แก้ Data Name ภาษาจีน (`记录`) ของทั้ง 3 ตารางใบลาเป็นไทยแล้ว** ผ่าน `update_worksheet.addRecordButtonName` + ยืนยันด้วย `hap worksheet info` · **พบเพิ่มและแก้แล้ว 26 ส.ค. 2569:** อีก 4 ตาราง P2-6 (`hr_employment_contract`/`hr_employment_event`/`hr_dependent`/`hr_bank_account`) ก็ยังเป็น `记录` เหมือนกัน (สร้างด้วย workaround เดียวกัน) — ตั้ง `addRecordButtonName` ให้ครบแล้วทั้ง 4 ตาราง ยืนยันด้วย `hap worksheet info` · ตรวจ `hr_position`/`hr_job_level`/`hr_employee` และ HR-00 ทั้ง 10 ตารางซ้ำแล้ว **ไม่มีปัญหา** (Data Name ไทยถูกต้องอยู่แล้ว) — สรุป: ทุกตาราง HR ที่มีอยู่ตอนนี้ (20 ตาราง) มี Data Name ไทยครบ 100% · **ตามด้วยพบว่า view "全部" (default view ที่ `create_app_items` สร้างให้อัตโนมัติ) ก็ยังเป็นภาษาจีนเหมือนกันใน 10 ตาราง** (ใบลา 3 + P2-6 4 + position/job_level/employee 3) — แก้เป็น "ทั้งหมด" ครบแล้วผ่าน `hap worksheet view update` (hap CLI เท่านั้น MCP ไม่มี `update_view`) ยืนยันด้วย `hap worksheet view list` ทุกตาราง · 27 ส.ค. 2569 WF-HR-01 อนุมัติใบลา 2 ระดับสำเร็จขั้นหัวหน้างาน)
> ไฟล์นี้ build agent โหลด **ทุก session ก่อนเริ่มงาน** — เก็บเฉพาะของจำเป็น
> รายละเอียดเต็ม (ทุก field / option key / workflow node) อยู่ใน `02-BuildSpec-FRS.md` — **และ `02b-BuildSpec-Addendum-P3-1.md` สำหรับ 3 ตารางใบลาที่สร้างล่าสุด** (ไฟล์หลักมีขนาดใหญ่ จึงแยกส่วนอัปเดตล่าสุดไว้ต่างหากแทนการเขียนทับ)
> 🔴 **โมดูลนี้อยู่ในแอปเดียวกับโมดูลบัญชี** — อ่าน `tilsna-accounting/04-CLAUDE-memory.md` ควบคู่ด้วยก่อนแตะ object ของบัญชี

---

## ⚠️ ข้อกำหนดหลัก (CRITICAL CONSTRAINTS)

1. **ทุก business logic ต้องอยู่บน Nocoly เท่านั้น** — ห้ามใช้สคริปต์หรือไฟล์ภายนอกเป็นส่วนหนึ่งของระบบที่ส่งมอบ
2. **Greenfield ในแอป Brownfield** — HR ยังไม่มี object แม้แต่ชิ้นเดียว แต่แอปมี **โมดูลบัญชี 34 worksheet · 35 optionset · 8 custom role** ทำงานอยู่ 🔴 **ห้ามสร้างซ้ำ ห้ามแก้โครงสร้างของบัญชี** เปิด BuildSpec §1.4 ก่อนแตะอะไรที่ขึ้นต้น `ac_`
3. **การอนุมัติทำผ่าน MCP ไม่ได้** — ผู้อนุมัติต้องกดใน To-do จริง ⇒ **ห้ามปิดงาน ✅ ถ้า approval ยัง pending**
4. **ห้ามใช้ org-auth API กดอนุมัติแทนคน** (A-HR-15 — ผู้ใช้ยังไม่อนุญาต เช่นเดียวกับโมดูลบัญชี)
5. **ชื่อ worksheet และชื่อฟิลด์ต้องเป็นภาษาไทย** ให้เข้าชุดกับโมดูลบัญชี · alias เป็นอังกฤษ snake_case ขึ้นต้น `hr_`
6. **ข้อมูล HR เป็นข้อมูลส่วนบุคคล (PDPA)** — เงินเดือน · เลขบัตรประชาชน · เอกสารการลาป่วย · บัญชีธนาคาร ต้องปิดบังตาม §1.7 · เอกสารที่อนุมัติแล้ว **ห้ามลบ** ใช้ยกเลิก/กลับรายการแทน
7. **SoD บังคับ** — R5 Payroll และ R8 Admin **ห้ามมีสิทธิ์อนุมัติเด็ดขาด** · R5 แก้ทะเบียนพนักงานไม่ได้

---

## 🚨 กับดัก `update_worksheet` (อ่านก่อนแตะฟิลด์ทุกครั้ง — พิสูจน์จากการยิงจริงในแอปนี้)

1. ส่ง `name` (ระดับตาราง) พร้อม `editFields` ในคอลเดียว → **`name` ถูกเมินเงียบ ๆ** ต้องแยกคอล
2. `editFields` **รีเซ็ตทุก attribute ที่ไม่ได้ส่งไป** — `required`, `isTitle`, `precision`, `defaultValue` และ `subType` ของ Relation / Date / DateTime / Attachment / Collaborator
3. ฟิลด์ **Number ห้ามส่ง `subType`** (รั่วลง `unit` เป็นขยะท้ายตัวเลข) · **Relation ที่ `subType=2` ก็ห้ามส่ง**
4. **Text หลายบรรทัด** ส่งผ่าน `config:{textMode:"multiLine"}` ไม่ใช่ `subType`
5. ฟิลด์ชนิด **`OrgRole` ห้ามส่ง `type`** (ไม่อยู่ใน enum ของ API)
6. `get_worksheet_structure` **คืนชื่อตารางเป็นค่าเก่าเสมอ** — ตรวจชื่อตารางด้วย `get_app_worksheets_list`
7. 🔴 **Rollup — ห้ามใส่ลง `editFields` เด็ดขาด** จะเปลี่ยนเป็น "จำนวนบันทึก" และหยุดคำนวณ · ซ่อมได้เฉพาะผ่านหน้าจอ
8. 🔴 **Dropdown ที่ผูก optionset ส่วนกลาง — ห้ามใส่ลง `editFields`** การผูกหลุด **ถาวร** · ทางเดียวคือลบฟิลด์แล้วสร้างใหม่
9. 🔴 **แก้ไข 26 ส.ค. 2569 — ผูก shared optionset ผ่าน API ทำไม่ได้เลยในทุกกรณี** (ทั้ง `create_worksheet` และ `addFields`) — `addFields` รับได้แค่ `options[]` inline (สร้าง option ใหม่แยกเฉพาะฟิลด์ ไม่ผูกของเดิม) ตาม anti-drift-playbook §11 · การผูกกับ shared optionset ที่มีอยู่แล้วทำได้ **เฉพาะใน Browser UI** เท่านั้น ไม่ว่าจะเป็นฟิลด์ใหม่หรือฟิลด์เดิม — **ยืนยันซ้ำอีกครั้งกับ 3 ตารางใบลาที่สร้างล่าสุด (P3-1)**: `leave_unit`/`leave_status`/`ledger_type` ทั้งหมดสร้างเป็น inline options เท่านั้น (ดู `02b-BuildSpec-Addendum-P3-1.md`) · **ยืนยันซ้ำอีกครั้ง 27 ส.ค. 2569 กับ P4-1/P4-2**: `hr_attendance.att_status`/`att_source` และ `hr_ot_request.day_type`/`ot_status` ก็สร้างเป็น inline options เช่นกัน (label ตั้งเป็นภาษาอังกฤษให้ตรงกับ optionset กลาง `OS_HR_ATT_STATUS`/`OS_HR_ATT_SOURCE`/`OS_HR_OT_DAY_TYPE`/`OS_HR_REQUEST_STATUS` แต่ key/GUID เป็นคนละชุด — ดู D-12 ใน Roadmap) · **เพิ่ม 27 ส.ค. 2569 (ต่อเนื่อง — P4-3):** พบว่า `hr_ot_rate` (ตารางตั้งค่า HR-00) ขาดฟิลด์ `day_type` ไปทั้งหมดตั้งแต่สร้าง (ไม่ใช่แค่ค้าง bind optionset) — เพิ่มสำเร็จผ่าน `addFields` เป็น inline options เช่นเดียวกัน (fieldId `6a8fd7608b6633ef76f13156`) แล้ว backfill 3 seed record เดิมให้มีค่าถูกต้อง
10. ✅ **แก้ไข 26 ส.ค. 2569 (เคยเข้าใจผิด) — `addRecordButtonName` ("Data Name"/"Record Name") ใช้ได้ทั้งตอนสร้าง (`create_worksheet`) และแก้ย้อนหลัง (`update_worksheet`) แม้จะไม่อยู่ในเอกสาร schema ของ `update_worksheet` ก็ตาม** — ยืนยันแล้วว่า HR-00 ทั้ง 10 ตารางถูกตั้งค่าไทยไปแล้ว (พร้อมกับ 34 ตารางบัญชี รวม 44/44) ดูรายละเอียดเต็มใน Known Issues ด้านล่างและ `tilsna-accounting/04-CLAUDE-memory.md` กับดักข้อ 15 — **ไม่มีงานค้างสำหรับตารางที่มีอยู่ แต่ตารางใหม่ควรใส่ตั้งแต่ตอน `create_worksheet` เลยจะได้ไม่ต้องแก้ทีหลัง** · ✅ **P3-1 ตั้งค่านี้ให้ 3 ตารางใบลาใหม่เสร็จแล้ว 26 ส.ค. 2569** ผ่าน `update_worksheet.addRecordButtonName` ตามหลัง (เพราะสร้างด้วย workaround `create_app_items` ซึ่งไม่มีช่องนี้ตอนสร้าง) — ยืนยันด้วย `hap worksheet info` ครบทั้ง 3 ตาราง · ✅ **26 ส.ค. 2569 (รอบ 2) — พบว่า `hr_employment_contract`/`hr_employment_event`/`hr_dependent`/`hr_bank_account` (P2-6) ก็ยังค้างเป็น `记录` เหมือนกัน** (คนละรอบสร้างกับใบลา แต่ workaround เดียวกันจึงพลาดจุดเดียวกัน) — แก้ครบแล้วทั้ง 4 ตาราง · ✅ **27 ส.ค. 2569 (P4-1/P4-2) — ยืนยันซ้ำอีกครั้งว่าพารามิเตอร์นี้ยังใช้ได้ปกติ** แม้ `update_worksheet` MCP schema เวอร์ชันนี้จะไม่ลิสต์พารามิเตอร์ `addRecordButtonName` ไว้เลย และ hap CLI (`worksheet update`/`form-settings`) ก็ไม่มีช่องนี้เช่นกัน — ทดสอบยิงตรงผ่าน MCP tool call ก็ยังสำเร็จปกติ (`hr_ot_request`→"ใบขออนุมัติล่วงเวลา", `hr_attendance`→"บันทึกลงเวลา") ยืนยันด้วย `hap worksheet info` → `entityName` **บทเรียน:** พารามิเตอร์นี้เป็น "undocumented แต่ใช้งานได้จริง" เสมอ — อย่าเชื่อว่าใช้ไม่ได้แค่เพราะไม่เจอใน schema/CLI help ให้ลองยิงตรงก่อน · **บทเรียน:** เวลาสร้างตารางใหม่ผ่าน workaround `create_app_items` ต้องเช็ค `addRecordButtonName` ทุกตารางที่สร้างในรอบนั้น ๆ ไม่ใช่แค่ชุดล่าสุด
11. ✅ **ยืนยันใหม่ 26 ส.ค. 2569 (P3-1) — ส่ง `removeFields` และ `addFields` ในคำสั่ง `update_worksheet` เดียวกันได้** ไม่จำเป็นต้องแยก 2 คำสั่งตามที่เข้าใจไว้เดิม (ทดสอบกับทั้ง 3 ตารางใบลา สำเร็จทุกตาราง — และซ้ำอีกครั้งกับ `hr_ot_request`/`hr_attendance` 27 ส.ค. 2569 สำเร็จเช่นกัน) — ทำให้ workaround สร้างตารางใหม่เหลือ 3 ขั้นจริง ๆ (create_app_items → update_worksheet[remove+add] → verify) แทน 4 ขั้น

> **สรุปกฎปฏิบัติ:** แตะฟิลด์ผ่าน API ได้เฉพาะฟิลด์ธรรมดา · Rollup / Formula / Dropdown-ผูก-optionset ทำผ่านหน้าจอ · ส่ง `name` + `alias` คู่กันเสมอ
> 🔴 **ผลต่อ HR โดยตรง:** optionset 24 ชุดต้องสร้างก่อน แล้วผูกตอน `addFields` **ครั้งแรกครั้งเดียว** — พลาดแล้วต้องลบฟิลด์สร้างใหม่ (โมดูลบัญชีเคยพลาดจนต้องลบสร้างใหม่ 20 ฟิลด์)

---

## ID สำคัญ

| รายการ | ค่า |
|---|---|
| App ID | `deca7391-1761-424b-9af3-c8d043004ad3` (**ERP - TILSNA**) |
| Organization | `9680d433-5b6d-45d7-b6df-d05d3095f82f` |
| **MCP connector** | **`ERP_-_TILSNA`** ⚠️ session นี้มี connector อื่นชี้เซิร์ฟเวอร์เดียวกัน (`API-Lab`, `MCP-ASM`, `-_WFA_System`, `hap-mcp-Demo_-`) — **`get_app_info` ยืนยันก่อน write แรกทุก session** |
| Host | `https://www.nocoly.com` |
| Surface R (org-auth API) | ❌ ไม่ใช้ |
| **object ของ HR** | ✅ กลุ่ม (Section) 8 กลุ่ม + ✅ optionset 24 ชุด + ✅ worksheet HR-00 10 ตาราง + ✅ seed ข้อมูลตั้งค่า 55 record (9/10 ตาราง) + ✅ hr_position (8 seed) + hr_job_level (6 seed) + hr_employee (36 ฟิลด์ + supervisor self-relation) + hr_employment_contract/event/dependent/bank_account (4 ตาราง โครงสร้างครบ) + ✅ hr_leave_balance/hr_leave_request/hr_leave_ledger (3 ตาราง โครงสร้างครบ 26 ส.ค. 2569 — P3-1, seed balance ครบ 27 ส.ค. 2569 — P3-4) + hr_ot_request/hr_attendance (2 ตาราง โครงสร้างครบ 27 ส.ค. 2569 — P4-1/P4-2) + hr_pay_component/hr_pay_period/hr_salary_structure (3 ตาราง โครงสร้างครบ 27 ส.ค. 2569 — P5-1) + hr_payslip/hr_payslip_line (2 ตาราง โครงสร้างครบ 27 ส.ค. 2569 — P5-2) + 🆕 **hr_pnd1_filing/hr_wht_cert (2 ตาราง โครงสร้างครบ 27 ส.ค. 2569 — P5-3, กลุ่มเงินเดือนครบ 7/7 ตารางแล้ว)** — ดู BuildSpec §1.2 / §1.3 / §1.6 / §1.6.1 / §2 และ `02b-BuildSpec-Addendum-P3-1.md` · 🔴 role/workflow ของกลุ่มเงินเดือนยังเป็น `<TBD>` ทั้งหมด — **หลัง create ทุกครั้ง อ่าน ID กลับแล้วเติมทันที** |

### 🆕 ID ตารางใบลา (P3-1 — สร้าง 26 ส.ค. 2569 · รายละเอียดฟิลด์ครบใน `02b-BuildSpec-Addendum-P3-1.md`)

| ตาราง | ws ID | view "ทั้งหมด" |
|---|---|---|
| `hr_leave_request` (ใบลา) | `6a8f2dbaae2a0e3743a0beaa` | `6a8f2dbaae2a0e3743a0beae` |
| `hr_leave_balance` (สิทธิและยอดคงเหลือการลา) | `6a8f2dba353e1b0e4a507757` | `6a8f2dba353e1b0e4a50775b` |
| `hr_leave_ledger` (รายการเคลื่อนไหวสิทธิลา) | `6a8f2dba9762533b5b718675` | `6a8f2dba9762533b5b718679` |

**🆕 view ทั้ง 4 มุมมองของ `hr_leave_request` (P3-7 — สร้าง+ยืนยันแล้ว 27 ส.ค. 2569):**

| view | id |
|---|---|
| ใบลาของฉัน | `6a8fb8451378964f9984a0b5` |
| รออนุมัติ (หัวหน้างาน) | `6a8fb845ae2a0e3743a0c34f` |
| รออนุมัติ (HR) | `6a8fb8458b6633ef76f12f64` |
| ปฏิทินการลาของทีม | `6a8fb845353e1b0e4a507c38` |

⚠️ **ทั้ง 3 ตารางยังไม่มี:** unique index จริง (IX-08.1 — **ตัดสินใจไม่เปิดใช้ 27 ส.ค. 2569 ดูเหตุผลใน Known Issues** / IX-09.1) · DV-08.1 (**ทำผ่าน UI ไม่ได้ ต้องย้ายไป WF-HR-01 — ดู Known Issues**) · การผูก Dropdown (`leave_unit`/`leave_status`/`ledger_type`) เข้า shared optionset · ✅ seed `hr_leave_balance` ครบทั้ง 6 ประเภท×2 พนักงาน 27 ส.ค. 2569 (P3-4) · `hr_leave_request`/`hr_leave_ledger` ยังไม่ seed record ทดสอบเพิ่ม — ✅ **BR-08.1/8.4/8.5 สร้าง+ยืนยันผ่าน Role Debugging แล้ว 27 ส.ค. 2569** (✅ `addRecordButtonName` ตั้งเป็นไทยครบแล้วทั้ง 3 ตาราง 26 ส.ค. 2569)

### 🆕 ID ตารางลงเวลา/OT (P4-1/P4-2 — สร้าง 27 ส.ค. 2569 · field ตามสเปกเต็มใน `02-BuildSpec-FRS.md` FR-HR-10/FR-HR-11B)

| ตาราง | ws ID | view "ทั้งหมด" | Data Name ไทย |
|---|---|---|---|
| `hr_ot_request` (ใบขออนุมัติล่วงเวลา) | `6a8fcca48b6633ef76f13033` | `6a8fcca48b6633ef76f13037` | ✅ "ใบขออนุมัติล่วงเวลา" |
| `hr_attendance` (บันทึกลงเวลา) | `6a8fcd67353e1b0e4a507d32` | `6a8fcd67353e1b0e4a507d36` | ✅ "บันทึกลงเวลา" |

- **ลำดับสร้าง:** `hr_ot_request` ก่อน (ไม่มี dependency ขาเข้านอกจาก `hr_employee`) แล้วค่อย `hr_attendance` (ต้องอ้าง `hr_ot_request` สำหรับ field `ot_request`)
- **Relation ที่ผูกสำเร็จ:** `hr_ot_request.employee`→`hr_employee` (`6a8efa5e9762533b5b7185c1`) · `hr_attendance.employee`→`hr_employee` · `hr_attendance.shift`→`hr_shift` (`6a8eebf79762533b5b7184c5`) · `hr_attendance.leave_request`→`hr_leave_request` (`6a8f2dbaae2a0e3743a0beaa`) · `hr_attendance.ot_request`→`hr_ot_request` — ทั้งหมด `bidirectional:true` สร้าง reverse field อัตโนมัติ
- **Dropdown ที่สร้างเป็น inline options (label อังกฤษให้ตรงกับ optionset กลาง แต่ key/GUID คนละชุด — ยังไม่ bind จริง):** `hr_attendance.att_status` (Present/Late/Absent/On leave/Holiday/Weekly holiday/Half day ตรงกับ `OS_HR_ATT_STATUS`) · `hr_attendance.att_source` (Device import/Manual entry/Self check-in/System generated ตรงกับ `OS_HR_ATT_SOURCE`) · `hr_ot_request.day_type` (Working day/Holiday within hours/Holiday outside hours ตรงกับ `OS_HR_OT_DAY_TYPE` — **แก้จากภาษาไทยผิดพลาดครั้งแรกด้วย `editFields` แล้ว** ดู Known Issues) · `hr_ot_request.ot_status` (Draft/Pending supervisor/Pending HR/Approved/Rejected/Cancelled ตรงกับ `OS_HR_REQUEST_STATUS`)
- ⚠️ **ยังไม่มี:** unique index IX-10.1 (`employee`+`work_date` บน `hr_attendance`) / IX-11.1 (`employee`+`ot_date`+`ot_from` บน `hr_ot_request`) — Browser-only · Business Rules BR-10.1/10.2/11.1/11.2/11.3 · `hr_ot_request.pay_period` relation (รอ P5-1) · seed record ทดสอบ

### 🆕 ID ตารางเงินเดือน (P5-1 — สร้าง 27 ส.ค. 2569 · field ตามสเปกเต็มใน `02-BuildSpec-FRS.md` FR-HR-12 12A/12B/12C)

| ตาราง | ws ID | view "全部" | Data Name ไทย |
|---|---|---|---|
| `hr_pay_component` (องค์ประกอบค่าจ้าง) | `6a8ff2868b6633ef76f13871` | `6a8ff2868b6633ef76f13875` | ✅ "องค์ประกอบค่าจ้าง" |
| `hr_pay_period` (งวดจ่ายเงินเดือน) | `6a8ff2878b6633ef76f1387b` | `6a8ff2878b6633ef76f1387f` | ✅ "งวดจ่ายเงินเดือน" |
| `hr_salary_structure` (โครงสร้างเงินเดือน) | `6a8ff287353e1b0e4a508550` | `6a8ff287353e1b0e4a508554` | ✅ "โครงสร้างเงินเดือน" |

- **ลำดับสร้าง:** `hr_pay_component` ก่อน (ไม่มี dependency ขาเข้านอกจาก `ac_coa`) → `hr_pay_period` (อ้าง `ac_period`/`ac_voucher` ของบัญชี) → `hr_salary_structure` (อ้าง `hr_employee` และ `hr_pay_component`)
- **Relation ที่ผูกสำเร็จ (ทั้งหมด `bidirectional:true` สร้าง reverse field อัตโนมัติ):** `hr_pay_component.biz_coa_account`→`ac_coa` (`6a85516e1049edca1eecd9b7`, reverse `6a8ff3049762533b5b7193a3`) · `hr_pay_period.biz_ac_period_ref`→`ac_period` (`6a8434d5055f2288c5b6d4b8`, reverse `6a8ff311ae2a0e3743a0ccb1`) · `hr_pay_period.biz_voucher_ref`→`ac_voucher` (`6a85fb2e9b6999a714d2a53d`, reverse `6a8ff311ae2a0e3743a0ccbb`) · `hr_salary_structure.biz_employee`→`hr_employee` (`6a8efa5e9762533b5b7185c1`, reverse `6a8ff3149762533b5b7193c9`) · `hr_salary_structure.biz_pay_component`→`hr_pay_component` (`6a8ff2868b6633ef76f13871`, reverse `6a8ff3149762533b5b7193cb` — auto-created on `hr_pay_component`)
- **Field ID หลักที่ workflow (WF-HR-07/08/09/10) จะต้องอ้าง:** `hr_pay_period.biz_period_status` `6a8ff311ae2a0e3743a0ccb2` (inline options: Open=`f10e686c-9381-4a5a-9406-bead7012bf51` · Calculating=`d4c51a3a-2c41-4959-bd97-cac24ae9db48` · Pending approval=`4b772a20-1b19-4f49-b635-f4b809e97b1b` · Approved=`3c7aa8cd-6a7c-446a-9d9e-b123dbc1cbd9` · Posted=`a40f98e5-d1a8-408c-a0a5-03c4eeafe5e8` · Closed=`c227fad9-0a9f-4869-90e6-b1b2a15db991` · Cancelled=`30fb7cad-9653-4ce3-a230-6a8b57bd9725`) · `biz_generated_flag` `6a8ff311ae2a0e3743a0ccbe` · `biz_pp_submitted_flag` `6a8ff311ae2a0e3743a0ccbf` · `biz_post_flag` `6a8ff311ae2a0e3743a0ccc0` · `hr_pay_component.biz_component_type` `6a8ff3049762533b5b71939e` (Earning=`3d7c0516-9963-401f-94d5-af77b411ddb0` · Deduction=`b2f76390-5726-49d6-b9c1-2df381b605e8` · Employer contribution=`c0fa8294-f4f1-43dc-8033-71560716d14a` · Informational=`a50efeb5-a2e7-41f9-9184-7b121f6918ba`)
- **Dropdown ที่สร้างเป็น inline options (label อังกฤษให้ตรงกับ optionset กลาง แต่ key/GUID คนละชุด — ยังไม่ bind จริง):** `hr_pay_component.biz_component_type`↔`OS_HR_COMPONENT_TYPE` · `hr_pay_component.biz_calc_method`↔`OS_HR_COMPONENT_CALC` · `hr_pay_period.biz_period_status`↔`OS_HR_PAY_PERIOD_STATUS`
- ⚠️ **ยังไม่มี:** unique index IX-12.1 (`biz_period_year`+`biz_period_month` บน `hr_pay_period`) / IX-12.2 (`biz_employee`+`biz_pay_component`+`biz_effective_from` บน `hr_salary_structure`) — Browser-only · Business Rules BR-12.1/12.2/12.3 · `hr_leave_type.pay_component`→`hr_pay_component` relation ที่ค้างจาก FR-HR-07 (ตารางปลายทางมีแล้ว แต่ยังไม่เพิ่มฟิลด์บน `hr_leave_type`) · `hr_ot_request.pay_period`→`hr_pay_period` relation ที่ค้างจาก P4-2 (ตอนนี้ทำได้แล้ว — ตารางปลายทางพร้อมแล้ว) · seed record P5-6 (โครงสร้างเงินเดือน 4 พนักงานทดสอบ) ยังไม่ได้ทำ
- ✅ **28 ส.ค. 2569 (P5-4) — ปิดครบ 6/6 (ต่อเนื่อง 13):** seed `hr_pay_component` 6 รายการเสร็จผ่าน `batch_create_records`(`triggerWorkflow:false`) — verify ด้วย `get_record_list`/`get_record_details` ผ่านครบ: `SALARY`(rowId `4f6e08db-9316-4d22-88ac-a3656c29a182`)/`OT`(`485f15df-1467-4af6-b072-43c027237e7a`)/`SSO_ER`(`68d66b6b-cfcb-404b-a7c6-35d324c7ca7d`)/`SSO_EE`(`b8e92933-ba55-488b-b6fa-bd2e0c0044f7`)/`WHT`(`ad802ca3-fcb4-4aba-909c-a6f2ae786835`)/`ABSENCE_DEDUCT`(`63ed6dd9-9d2b-435f-a823-28935b0dff8a`) · ผูก `biz_coa_account`(fieldId `6a8ff3049762533b5b7193a2`)→`ac_coa` ครบ **6/6**: `SALARY`/`OT`/`ABSENCE_DEDUCT`→`530101 เงินเดือน`(rowId `43ee2889-b2e5-4620-85b7-53f6db1c8e5b`, 26-27 ส.ค.) · `SSO_ER`→`530102`(rowId `233626b4-d272-4cb5-8f77-9d8d2ddef804`) · `SSO_EE`→`210402`(rowId `c6386fe7-1b3c-44db-ab80-9dfd3308a6a5`) · `WHT`→`210304`(rowId `777ea001-5300-4f5c-857a-71a45db2076b`) — บัญชี 3 รายการหลังนี้ฝ่ายบัญชีสร้างให้ 28 ส.ค. 2569 ตามที่ HR ส่งเรื่องไป (ดู Known Issues)

### 🆕 ID ตารางสลิปเงินเดือน (P5-2 — สร้าง 27 ส.ค. 2569 · field ตามสเปกเต็มใน `02-BuildSpec-FRS.md` FR-HR-12 12D/12E)

| ตาราง | ws ID | view "全部" | Data Name ไทย |
|---|---|---|---|
| `hr_payslip` (สลิปเงินเดือน) | `6a904c85353e1b0e4a50ba05` | `6a904c85353e1b0e4a50ba09` | ✅ "สลิปเงินเดือน" (add-record button ตั้งแล้ว) |
| `hr_payslip_line` (รายการในสลิปเงินเดือน) | `6a904c858b6633ef76f16a0b` | `6a904c858b6633ef76f16a0f` | ✅ "รายการในสลิปเงินเดือน" (add-record button ตั้งแล้ว) |

- **Relation ที่ผูกสำเร็จ (ทั้งหมด `bidirectional:true`):** `hr_payslip.biz_employee`→`hr_employee` (`6a8efa5e9762533b5b7185c1`, reverse `6a904d67353e1b0e4a50ba11`) · `hr_payslip.biz_pay_period`→`hr_pay_period` (`6a8ff2878b6633ef76f1387b`, reverse `6a904d67353e1b0e4a50ba13`) · `hr_payslip.biz_cost_center`→`ac_cost_center` (`6a85452b9b6999a714d26720`, reverse `6a904d67353e1b0e4a50ba15`) · `hr_payslip.biz_bank_account`→`hr_bank_account` (`6a8efd1fae2a0e3743a0bd9d`, reverse `6a904d67353e1b0e4a50ba26`) · `hr_payslip_line.biz_payslip`→`hr_payslip` (`6a904c85353e1b0e4a50ba05`, reverse `6a904d6d9762533b5b71c4e2` auto-created on `hr_payslip`) · `hr_payslip_line.biz_pay_component`→`hr_pay_component` (`6a8ff2868b6633ef76f13871`, reverse `6a904d6d9762533b5b71c4e4`)
- **Field ID หลักที่ workflow (WF-HR-07/08) จะต้องอ้าง:** `hr_payslip.biz_payslip_no` `6a904d67353e1b0e4a50ba0f` (AutoNumber, title, rule "PS-"+yyyyMM+sequence4) · `biz_recalc_flag` `6a904d67353e1b0e4a50ba28` (🔴 Trigger Field ของ WF-HR-08 ที่ยังไม่ได้สร้าง) · `biz_calculated_flag` `6a904d67353e1b0e4a50ba29` · `biz_payslip_status` `6a904d67353e1b0e4a50ba27` (inline: Draft=`73dcdbaf-283c-4182-9072-3f59b4336f79` · Calculated=`740b4fba-e506-4e23-853f-10b17b3d7210` · Approved=`325b8942-70b4-4ffa-9e78-ce8d65937156` · Paid=`f1ed2aa6-f2d8-4e8b-804e-118625af0d72` · Cancelled=`a846cbb5-75a1-4515-bc96-908d5ce7d8cf`) · totals `biz_total_earning`/`biz_total_deduction`/`biz_net_pay` = `6a904d67353e1b0e4a50ba1d`/`...1e`/`...1f`
- **⚠️ เปลี่ยนจากสเปกเดิม:** ยอดเงินทั้งหมด (`base_salary`/totals/`sso_ee`/`sso_er`/`wht_amount`/`ytd_*`/`hr_payslip_line.rate`/`amount`) สร้างเป็น **`Currency` (code THB)** แทน `Number` ตามที่ระบุใน BuildSpec ฉบับก่อนหน้า (ปรับ BuildSpec ให้ตรงของจริงแล้ว) · `payslip_no` ใช้ **`AutoNumber`** แทน `Text` (รองรับ isTitle + auto-generate ได้ในตัว)
- flag/total ทุกตัวตั้ง `defaultValue:[{source:"static",value:"0"}]` เชิงรุกตามแบบแผนที่เจอบั๊กซ้ำมาก่อน (D-9/D-13 pattern)
- ⚠️ **ยังไม่มี:** unique index IX-12.3 (`biz_employee`+`biz_pay_period` บน `hr_payslip`) — Browser-only · Business Rules BR-12.4 (`pay_period.period_status` in Posted/Closed → read-only)/BR-12.5 (R1 เห็นเฉพาะ Published/Paid) · ผูก `biz_payslip_status` เข้า `OS_HR_PAYSLIP_STATUS` `f77b582b-187e-4df0-8578-69b2078d9045` จริง (label "Published" ของ optionset เดิมกับ "Paid" ที่สร้างใหม่ไม่ตรงกัน ต้องเทียบก่อนผูก) · Dynamic default snapshot ของ `biz_cost_center` · WF-HR-07 (สร้างสลิปทั้งงวด)/WF-HR-08 (คำนวณสลิป) ยังไม่ได้สร้าง

### 🆕 ID ตารางภาษี/หนังสือรับรอง (P5-3 — สร้าง 27 ส.ค. 2569 · field ตามสเปกเต็มใน `02-BuildSpec-FRS.md` FR-HR-12 12I/12J · กลุ่มเงินเดือนครบ 7/7 ตารางแล้ว)

| ตาราง | ws ID | view "全部" | Data Name ไทย |
|---|---|---|---|
| `hr_pnd1_filing` (การยื่นภาษีเงินได้หัก ณ ที่จ่าย) | `6a904e72ae2a0e3743a0fd06` | `6a904e72ae2a0e3743a0fd0a` | ✅ "การยื่นภาษีเงินได้หัก ณ ที่จ่าย" |
| `hr_wht_cert` (หนังสือรับรองการหักภาษี ณ ที่จ่าย) | `6a904e73353e1b0e4a50ba83` | `6a904e73353e1b0e4a50ba87` | ✅ "หนังสือรับรองการหักภาษี ณ ที่จ่าย" |

- **Relation ที่ผูกสำเร็จ (ทั้งหมด `bidirectional:true`):** `hr_pnd1_filing.biz_pay_periods`→`hr_pay_period` (`6a8ff2878b6633ef76f1387b`, multi, reverse `6a904e96353e1b0e4a50ba93`) · `hr_wht_cert.biz_employee`→`hr_employee` (`6a8efa5e9762533b5b7185c1`, reverse `6a904ea1353e1b0e4a50bac0`) · `hr_wht_cert.biz_pnd1_filing`→`hr_pnd1_filing` (`6a904e72ae2a0e3743a0fd06`, reverse `6a904ea1353e1b0e4a50bac3` auto-created on `hr_pnd1_filing`)
- **Field ID หลักที่ WF-HR-10/WF-HR-20 (ยังไม่สร้าง) จะต้องอ้าง:** `hr_pnd1_filing.biz_filing_status` `6a904e96353e1b0e4a50ba97` (inline: Draft=`9cfae3d7-8705-413f-b406-a0d538aaf406` · Approved=`e1aa329d-b0b2-4576-b23c-29f0740e0d7d` · Filed=`b214da57-0f11-4165-acba-139be95bf93a` · Paid=`a16148b3-68f1-4032-892a-bfb1a688bf91` · Amended=`260f4c54-e005-4c62-8118-2a5be8b92483`) · `biz_issue_cert_flag` `6a904e96353e1b0e4a50ba9b` (🔴 Trigger Field ของ WF-HR-20) · `hr_wht_cert.biz_cert_status` `6a904ea1353e1b0e4a50bac8` (inline: Issued=`48242102-ee8a-44a4-bec5-482b16b8729e` · Printed=`27a8ee8a-f6e2-4bfa-aa2a-da709d93ada6` · Included in filing=`e608e791-438d-40fa-acb3-fb339ef41eee` · Cancelled=`8b45923b-7541-4417-9cec-295d4c37e962`)
- **⚠️ เปลี่ยนจากสเปกเดิม:** `total_income`/`total_wht` ทั้ง 2 ตารางสร้างเป็น **`Currency` (THB)** แทน `Number` (ปรับ BuildSpec ให้ตรงของจริงแล้ว — สอดคล้องกับแนวทางเดียวกับ `hr_payslip` ใน P5-2)
- `biz_payee_legal_form` ของ `hr_wht_cert` สร้างเป็น inline options มีแค่ค่า "Individual" (`08216df9-8694-4a85-a961-a61c7f1646f6`) ตรงกับที่สเปกระบุว่าใช้เฉพาะค่านี้ — key ต่างจาก `OS_LEGAL_FORM` ของโมดูลบัญชี (`1aeb1a04-1957-4905-92d9-c506d8bbdccc`) ยังไม่ผูกจริง
- ⚠️ **ยังไม่มี:** unique index IX-12.5 (`biz_form_type`+`biz_tax_year`+`biz_tax_month` บน `hr_pnd1_filing`) / IX-12.6 (`biz_employee`+`biz_tax_year` บน `hr_wht_cert`) — Browser-only · ผูก dropdown 4 ฟิลด์เข้า shared optionset จริง (`form_type`/`filing_status`/`payee_legal_form`/`cert_status`) · WF-HR-10 (ผ่านรายการเข้าบัญชี, ⛔ บล็อกด้วย G-02)/WF-HR-20 (ออกหนังสือรับรองประจำปี) ยังไม่ได้สร้าง

### 🆕 ID Workflow WF-HR-02 / WF-HR-03 (P3-6 — สร้าง+publish+ทดสอบยิงจริงสำเร็จ 27 ส.ค. 2569)

| Workflow | processId | publish version | node สำคัญ |
|---|---|---|---|
| **WF-HR-02** ตัดสิทธิลาเมื่ออนุมัติ | `6a8fa97cfdab77a41c51cb3f` | v3 (v1/v2 มีบั๊ก ledger balance_after ผิด/relation ขยะ — แก้แล้วใน v3) | `gate`(branch)→`upd_deducted`→`get_emp`→`calc_neg_days`→`get_bal`→`br_bal_found`(branch: `bal_missing`→`add_bal`+`calc_used_m`+`calc_remaining_m`+`add_ledger_m`+`upd_bal_m`+`notify_m` / `bal_found`→`calc_used_f`+`calc_remaining_f`+`add_ledger_f`+`upd_bal_f`+`notify_f`) |
| **WF-HR-03** คืนสิทธิลาเมื่อยกเลิกใบลา | `6a8fab800e97bf440dddb744` | v1 (ถูกต้องตั้งแต่รอบแรก เพราะเรียนรู้บั๊กจาก WF-HR-02 มาก่อน) | `gate`(branch)→`upd_returned`→`get_emp`→`get_bal`(ifEmpty:stop)→`calc_used_r`→`calc_remaining_r`→`add_ledger_r`→`upd_bal_r`→`notify_emp_r`→`notify_approver_r` |

**หลักการสำคัญที่ทั้งคู่ใช้ (เขียนไว้ใน Known Issues ด้วย):** (1) `add_record` ของ ledger ต้องรันก่อน `update_record` ของ balance เสมอ (ไม่งั้น compute node ที่อ้างซ้ำจะคำนวณผิดจากค่าที่ update ไปแล้ว) (2) เขียนค่าลง Relation field ที่ชี้ไปยัง record ของ node ต้นน้ำ ใช้ `{kind:"field", node:{nodeAlias:<node>}, fieldId:"rowid"}` ไม่ใช่ `{kind:"record", node:{...}}`

> 🔴🆕 **D-8 ลองทำแล้ว 27 ส.ค. 2569 (รอบเย็น) แต่ชนบั๊กใหม่ `sub_process`→`NodeAppIsNull` — revert กลับสถานะเดิมแล้ว (publish v4/v2)** — `hr_attendance` มีตารางแล้ว แต่การเพิ่ม node sync สถานะ "On leave" กลับเข้า WF-HR-02/WF-HR-03 ต้องใช้ node ชนิด `sub_process` (`sequential_each`) เพื่อวน update ทีละ attendance record ที่ทับช่วงวันลา — สร้างสำเร็จแต่ publish ไม่ได้ ดูรายละเอียดเต็มใน Known Issues ด้านล่าง (หัวข้อ "sub_process → NodeAppIsNull") — **ยังบล็อกอยู่จริง ไม่ใช่พร้อมทำ**

### 🆕 ID Workflow WF-HR-04 (P4-3 — สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ 27 ส.ค. 2569 ต่อเนื่อง — ไม่ต้องใช้ `sub_process`)

| Process | processId | publish version | node สำคัญ |
|---|---|---|---|
| **Main** WF-HR-04 | `6a8fd8d35f8564a68c3c1909` | v2 (v1 มีบั๊ก `dateDiff` precision — แก้แล้วใน v2) | `gate`(branch: `leg_supervisor`/`leg_hr`/`leg_default` ตาม `hr_ot_status`)→[leg_supervisor] `get_emp`(reverse-relation+`op:eq` หา `hr_employee`)→`chk_supervisor`(branch missing/ok)→[sup_missing]`rej_no_supervisor`+notify / [sup_ok]`calc_ot_minutes`(compute dateDiff→นาที)→`calc_ot_hours`(compute number หาร 60, precision:2)→`get_holiday`→`chk_holiday`(branch is_workday/is_holiday)→[is_holiday]`get_rate_holiday`→`upd_daytype_holiday`→`appr_block_1a`(approval_block, `mode:"use_existing"` ชี้ processId เดียวกับ 1b) / [is_workday]`get_rate_workday`→`upd_daytype_workday`→`appr_block_1b`(approval_block, `mode:"create"` — ตัวที่สร้าง inner flow จริง) · [leg_hr] `appr_block_2`(approval_block, `mode:"use_existing"` ชี้ inner HR flow) |
| **Inner** supervisor-approval (ใช้ร่วม 2 เส้นทาง day-type) | `6a8fd9325f8564a68c3c1cec` | v1 | `approve_l1`(approvers=`approval_start.hr_approver_user`)→`res_branch_1`(branch r_pass/r_reject)→[r_pass]`upd_pendinghr`(ot_status→Pending HR, approval_step=2) / [r_reject]`upd_rejected_l1`+`notify_rejected_l1` |
| **Inner** HR-approval | `6a8fd9325f8564a68c3c1ced` | v1 (สร้างแล้ว **ยังไม่ทดสอบ** — รอ `hr_approval_rule` seed) | `get_rule`(get_single `hr_approval_rule` filter `hr_doc_kind`=OT key `402a261b-ba3f-4e76-941a-0791d5ceea84` AND `hr_approval_level`="2" AND `hr_rule_is_active` checked, ifEmpty:stop)→`approve_l2`(approvers=`get_rule.hr_approver_role`)→`res_branch_2`(branch)→[pass]`upd_approved`(ot_status→Approved, approval_step=3) / [reject]`upd_rejected2`+`notify_final_rejected` |

**🆕 เทคนิคใหม่ — แชร์ inner approval_block ข้าม 2 branch path ที่ไม่มีทาง merge กัน (บันทึกไว้ใน Known Issues ด้วย):** แพลตฟอร์มนี้ไม่มี node ชนิด join/merge — ทุก node มี `prevNode` ได้แค่ 1 เดียว ⇒ เมื่อ 2 เส้นทาง (holiday/workday) ต้องไปจบที่ approval_block เดียวกัน ให้สร้าง approval_block ตัวแรกด้วย `config.process.mode:"create"` (เก็บ inner `processId` ที่ตอบกลับมาใน `createdNodes`) แล้วสร้าง approval_block ตัวที่สองด้วย `mode:"use_existing"` อ้าง processId เดียวกัน — ได้ผลลัพธ์เหมือน merge จริงในแง่ logic (ทั้ง 2 เส้นทางส่งเข้า approval flow เดียวกัน) โดยไม่ต้องสร้าง inner flow ซ้ำ

**🆕 ID ปุ่ม + view P4-5 (สร้าง+verify ผ่านจริง 27 ส.ค. 2569 ต่อเนื่อง 2):**

| รายการ | บน worksheet | ID | หมายเหตุ |
|---|---|---|---|
| ปุ่ม "ส่งขอล่วงเวลา" | `hr_ot_request` | `6a8fde0aae2a0e3743a0c62f` | `updateCurrentRecord`, `enableWhen: hr_ot_status=Draft`, `runWorkflowAfterSubmit:true` — Scope ตั้งเป็น "All Records" แล้วผ่าน Browser |
| view "OT รออนุมัติ" | `hr_ot_request` | `6a8fde0b353e1b0e4a50803e` | filter `hr_ot_status` in (Pending supervisor, Pending HR) |
| view "บันทึกลงเวลาที่ผิดปกติ" | `hr_attendance` | `6a8fde0c1378964f9984a42c` | filter `hr_att_status` in (Late, Absent) |

**🆕 ทดสอบยิงจริง 3 เคส (เก็บไว้เป็นหลักฐาน ไม่ลบ):** `OT-TEST-HOLIDAY-01` (rowid `079c9d4d-439b-4536-9670-4d753d2af47f`, 4h ณ Holiday outside hours → `applied_multiplier`=3.0) · `OT-TEST-WORKDAY-01` (rowid `6384d1bc-dfb0-40b5-af86-e5b399725ec0`, 2.5h ณ Working day → ×1.5) · `OT-TEST-FRACTIONAL-01` (rowid `ecfa8e8f-b65e-4e60-ab5b-1dded2d3761a`, 2.5h → ยืนยัน `hr_ot_hours`="2.50" ถูกต้องหลังแก้บั๊ก precision) — ทั้ง 3 มี `_updatedBy`=`user-workflow` และ To-do จริงถึง Wanadtapong.l (`get_approval_list_by_row`) ✅ **AC-19 ส่วนแรกผ่าน** (`applied_multiplier` ดึงจากตาราง `hr_ot_rate` จริง ไม่ hardcode — ยืนยันจาก 2 ค่าต่างกันตาม day_type)

### 🔴 ตารางของบัญชีที่ HR ต้องเขียนถึง (ID ดึงจริงแล้ว — ใช้ได้เลย ห้ามแก้โครงสร้าง)

| ตาราง | ws ID | HR ใช้ทำอะไร |
|---|---|---|
| **ใบสำคัญ** `ac_voucher` | `6a85fb2e9b6999a714d2a53d` | WF-HR-10 สร้างใบสำคัญเงินเดือน · 🔴 required 6 ฟิลด์ (`description` `voucher_date` `journal` `voucher_type` `period` `currency`) |
| **รายการในใบสำคัญ** `ac_voucher_line` | `6a85fb3933560633b8cd9f40` | บรรทัดเดบิต/เครดิต · relation กลับ = `6a85fb399b6999a714d2a557` |
| **ใบขออนุมัติเบิกจ่าย** `ac_pay_req` | `6a8677b19b6999a714d2aa83` | WF-HR-12 ส่งใบเบิกสวัสดิการเข้าไปจ่าย |
| **หน่วยงาน/ศูนย์ต้นทุน** `ac_cost_center` | `6a85452b9b6999a714d26720` | ปลายทาง relation "หน่วยงาน" · ✅ มีฟิลด์ `hrms_unit_id` `6a85452b055f2288c5b741bc` เตรียมไว้แล้ว |
| **ผังบัญชี** `ac_coa` | `6a85516e1049edca1eecd9b7` | ปลายทางของ `hr_pay_component.coa_account` (79 record) |
| **กฎการผ่านรายการ** `ac_posting_rule` | `6a85518c33560633b8cd6a15` | 🔴 คู่บัญชีเงินเดือน — **ห้าม hard-code รหัสบัญชีใน workflow** |
| **งวดบัญชี** `ac_period` | `6a8434d5055f2288c5b6d4b8` | ผูกงวดจ่ายเงินเดือน · 🔴 **ห้ามลบสร้างใหม่** |

**Option key ที่ใช้บ่อย (จำไว้ ไม่ต้องเปิดหา)**
- `ac_voucher.source_module` `6a86021833560633b8cd9fb1` → **Payroll = `914f5226-dc4c-4572-bd4d-18bb278414b5`** 🔴 WF-HR-10 ต้องเขียนค่านี้
- `ac_voucher.voucher_status` `6a86016b1049edca1eed028a` → Draft = `3536165d-460c-4942-8bec-6f381209d8da`
- `ac_period.period_status` `6a851f70055f2288c5b73edf` → Open = `f662571c-3de0-4e4c-9828-9172e337d223`
- `ac_pay_req.biz_preq_status` `6a8ec5be353e1b0e4a506d75` → Draft = `08092993-906a-4956-b0ff-6f91f766fe61`

---

## 🔴 Blocker ที่ต้องปลดก่อน (ดู Roadmap P0)

| รหัส | เรื่อง | ผลถ้าไม่ปลด |
|---|---|---|
| **G-06** | **มีบัญชีผู้ใช้ในองค์กรเพียง 2 บัญชี** (`Kunlasatri.c` `202df0a6-…` · `Wanadtapong.l` `4dc40ac1-…`) | ⛔ ทดสอบอนุมัติ 2 ระดับและสิทธิ์ 8 บทบาทไม่ได้เลย (AC-01 AC-11 AC-12 AC-13) — **ขอเพิ่ม 4 บัญชีก่อน** |
| **G-02** | `ac_posting_rule.event_code` ไม่มี `PAYROLL_ACCRUAL` / `PAYROLL_PAYMENT` | ⛔ WF-HR-10 จะจบที่ branch ตรวจกฎทุกครั้ง — **เพิ่ม option ผ่านหน้าจอเท่านั้น** |
| **G-01** | `ac_voucher.source_doc_type` `6a85fb7033560633b8cd9f56` ไม่มีค่าสำหรับ HR | ใช้ `source_module` = Payroll แทนได้ชั่วคราว |
| **G-03** | `OS_FORM_TYPE` `7b605ddd-…` ไม่มี ภ.ง.ด.1 | สร้าง `OS_HR_FILING_FORM` แยก **ห้ามแก้ optionset เดิมที่ผูก 4 ตารางของบัญชี** |
| **G-05** | ยังไม่ยืนยันอัตราประกันสังคม เพดานฐาน และขั้นบันไดภาษีที่บังคับใช้ | ยอดสลิปอาจผิด — เก็บเป็น record แบบมีวันที่มีผล จึงแก้ทีหลังได้ |
| **G-07** | `create_view` / `create_custom_actions` / `create_chart` **มี tool แต่ยังไม่เคยเรียกสำเร็จในแอปนี้** | Surface = `MCP (unverified)` · **ทุก DoD ต้องมีขั้นเปิดหน้าจอดูว่าโผล่จริง** |

---

## Known Issues / Workarounds

- 🟡🆕 **28 ส.ค. 2569 (ต่อเนื่อง 14) — G-05: cross-check `hr_sso_rate`/`hr_tax_bracket` กับกฎหมายทางการผ่าน WebSearch/WebFetch (ตามคำขอผู้ใช้ "ดึงข้อมูลจากในเน็ตได้ไหม เอาตามกฎหมายก่อน"):** ตรวจสอบ 2 เรื่อง — (1) **อัตราเงินสมทบประกันสังคม ม.33:** 5%(นายจ้าง)/5%(ลูกจ้าง) + เพดานฐานค่าจ้างใหม่ **17,500 บาท/เดือน** (ขึ้นจาก 15,000 เดิม) มีผล **1 ม.ค. 2569** ตามราชกิจจานุเบกษาที่ประกาศ 12 ธ.ค. 2568 — ปรับ 3 เฟส: เฟส 1 (2569-2571) เพดาน 17,500/เงินสมทบสูงสุด 875 บาท · เฟส 2 (2572-2574) เพดาน 20,000/1,000 บาท · เฟส 3 (2575+) เพดาน 23,000/1,150 บาท (2) **ขั้นบันไดภาษีเงินได้บุคคลธรรมดา 8 ขั้น** ยืนยันกับหน้าทางการของกรมสรรพากร (`rd.go.th/59670.html`) — ขั้นสุดท้ายคือ **2,000,001-5,000,000=30%** และ **over 5,000,000=35%** (มีผลตั้งแต่ปีภาษี 2560 ต่อเนื่องถึงปัจจุบัน ไม่เปลี่ยน) — **ผลตรวจ: `hr_sso_rate` (record `81acf446-…`) และ `hr_tax_bracket` (8 record) ที่ seed ไว้ตั้งแต่ 26 ส.ค. 2569 ตรงกับตัวเลขกฎหมายจริงทุกจุดอยู่แล้ว ไม่ต้องแก้ไขค่าใด ๆ** — ⚠️ **ข้อควรระวัง: พบแหล่งข้อมูลรองที่ขัดแย้งกัน** — เอกสาร AEC tax comparison PDF ของ `rd.go.th` เอง และเว็บ Statrys ระบุขั้นบันไดสุดท้ายผิดเป็น 2,000,001-4,000,000/over 4,000,000 (คนละตัวเลขกับหน้าอัตราภาษีทางการโดยตรง) — **บทเรียน: เมื่อแหล่งรองขัดแย้งกับหน้าเอกสารทางการที่เจาะจงเรื่องนั้นโดยตรง ให้เชื่อหน้าเจาะจงมากกว่า** (ในกรณีนี้คือ `rd.go.th/59670.html` ซึ่งเป็นหน้าอัตราภาษีเงินได้บุคคลธรรมดาโดยเฉพาะ ไม่ใช่เอกสารเปรียบเทียบภาษี AEC ที่กว้างกว่า) — **สรุปสถานะ G-05:** ยกระดับจาก "ไม่มีการยืนยันเลย" เป็น "cross-check กับกฎหมาย/ประกาศทางการแล้วตรงกัน" — **แต่ยังไม่ใช่ "การยืนยัน/เซ็นรับรองจากฝ่ายบัญชี" ตามที่ฝ่ายบัญชีระบุไว้ชัดเจนใน `tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`** ("ขอให้ทุกฝ่ายงดใช้ค่าที่ไม่ได้มาจากการยืนยันจริง") — แนะนำให้ขอ sign-off อย่างเป็นทางการจากฝ่ายบัญชีก่อน go-live จริงอยู่ดี · **`hr_company_tax_id`/`hr_sso_employer_no` เป็นเลขทะเบียนเฉพาะของบริษัท TILSNA เอง (ไม่ใช่ตัวเลขที่กฎหมาย/สาธารณะกำหนด) หาจากอินเทอร์เน็ตไม่ได้เลย — ต้องได้จากเอกสารทะเบียนจริงของบริษัทเท่านั้น ยังคงเป็น placeholder ต่อไป**

**🟢 ยืนยันครบแล้ว 26 ส.ค. 2569 — view rename ทำได้จริง (live-tested) · Data Name/Record Name ของ HR ทั้ง 10 ตาราง "เสร็จไปแล้ว" ตั้งแต่ก่อนเริ่มสอบสวนนี้ (ทำโดยงานฝั่งบัญชี) — ดูการแก้ไขข้อผิดพลาดด้านล่าง**

- ผู้ใช้เพิ่ม skill ตระกูล `hap-*` เข้าบัญชี — ห่อหุ้ม **hap CLI** (`pip install hap-cli`, คนละตัวกับ MCP connector `ERP_-_TILSNA` ต้อง `hap auth login` แยกต่างหาก) มี auth 2 ชั้นที่ต้องผ่านก่อนใช้งานจริง: (1) `--token` รับเฉพาะ **session cookie `md_pss_id`** จากเบราว์เซอร์ที่ login nocoly.com แล้ว — **ไม่ใช่ PAT** (PAT ใช้กับ REST API v3 เท่านั้น, header `Bearer` คนละแบบ) (2) org-level **"CLI access" toggle** ต้องเปิดโดยแอดมินองค์กรก่อน (ดูใน settings ของ nocoly.com) — ผู้ใช้เปิดให้แล้ว 26 ส.ค. 2569 (`hap auth list-my-orgs` → `VBix CLI:on`) → ผ่านทั้งสองด่านแล้ว ใช้งานได้ปกติ
- ✅ **เปลี่ยนชื่อ view ยืนยัน LIVE แล้ว** (ไม่ใช่แค่จากเอกสาร) — ทดสอบครั้งแรกกับ `hr_setting` (`6a8eebbd353e1b0e4a507477` view `6a8eebbd353e1b0e4a50747b`): `hap worksheet view update <ws_id> <view_id> --name "ชื่อใหม่" -a <app_id>` คืนค่า 200 พร้อม object ที่อัปเดตแล้ว — แก้ปัญหาที่ MCP ทำไม่ได้ (ไม่มี `update_view`, มีแค่ `create_view` — ดู `tilsna-accounting/04-CLAUDE-memory.md` กับดักข้อ 11) **hap CLI คือทางเดียวที่แก้ view เดิมได้โดยไม่ต้องเข้า Browser** · 🔴 **26 ส.ค. 2569 (สำคัญ) — ตรวจพบว่า view `全部` ที่ `create_app_items` สร้างอัตโนมัติเป็นภาษาจีนเสมอ ไม่ใช่ Thai** (เอกสารก่อนหน้าที่เขียนว่า worksheet ใหม่ "มี view 'ทั้งหมด' ให้อัตโนมัติ" นั้นเข้าใจผิด — ชื่อจริงคือ `全部` ต้องมาแก้เองด้วย `hap worksheet view update` ทุกครั้ง) — **แก้ครบแล้วทั้ง 10 ตาราง** ที่ยังค้าง (`hr_leave_request`/`hr_leave_balance`/`hr_leave_ledger`/`hr_employment_contract`/`hr_employment_event`/`hr_dependent`/`hr_bank_account`/`hr_position`/`hr_job_level`/`hr_employee`) ยืนยันด้วย `hap worksheet view list` ทุกตาราง — **กฎใหม่: ตารางที่สร้างผ่าน `create_app_items` ต้องเช็คทั้ง (1) `addRecordButtonName` และ (2) ชื่อ view default เป็น 2 จุดเสมอ ไม่ใช่แค่จุดเดียว** — ✅ **ทำซ้ำสำเร็จอีกครั้งกับ `hr_ot_request`/`hr_attendance` 27 ส.ค. 2569 ทั้ง 2 จุด**
- 🔴 **สรุปเรื่อง Data Name / Record Name (สรุปจบแล้ว):** `update_worksheet.addRecordButtonName` ใช้ได้จริงกับตารางที่มีอยู่แล้ว และ **10 ตาราง HR-00 ทั้งหมดถูกตั้งค่านี้เป็นไทยไปแล้วตั้งแต่ 26 ส.ค. 2569** พร้อมกับ 34 ตารางของบัญชี (รวม 44/44 ตาราง) — ยืนยันผ่านหน้าจอแล้ว (More Settings → Data Name) ในตัวอย่างสุ่ม 4 ตาราง — ดู `tilsna-accounting/04-CLAUDE-memory.md` กับดักข้อ 15 และ checklist ข้อ 21 · "Record Name" (field ที่ `isTitle:true`) เป็น pattern ปกติที่ทำอยู่แล้วทุกตาราง · ✅ **3 ตารางใบลาใหม่ (P3-1) ตั้ง `addRecordButtonName` เสร็จแล้วเช่นกัน 26 ส.ค. 2569** (สร้างผ่าน `create_app_items` ซึ่งไม่มีช่องนี้ตอนสร้าง จึงตามไปตั้งด้วย `update_worksheet.addRecordButtonName` ทีหลัง แล้วยืนยันด้วย `hap worksheet info` ทั้ง 3 ตาราง) · ✅ **26 ส.ค. 2569 (รอบ 2 — ตามคำขอ "แก้ภาษาจีนตาม Data Name ให้หน่อย" อีกครั้ง):** ตรวจซ้ำทุกตาราง HR แล้วพบว่า `hr_employment_contract`/`hr_employment_event`/`hr_dependent`/`hr_bank_account` (P2-6) ยังเป็น `记录` อยู่ — แก้ครบทั้ง 4 ตารางด้วยวิธีเดียวกัน · ตรวจ `hr_position`/`hr_job_level`/`hr_employee`/HR-00 ทั้ง 10 ตารางซ้ำแล้วยืนยันว่าถูกต้องอยู่แล้ว ไม่ต้องแก้ — **รวมทั้งแอปตอนนี้ Data Name ไทยครบ 54/54 ตาราง (44 เดิม + 3 ใบลา + 3 position/job_level/employee + 4 P2-6)** · ✅ **27 ส.ค. 2569 (P4-1/P4-2):** ตั้งครบอีก 2 ตาราง (`hr_ot_request`→"ใบขออนุมัติล่วงเวลา", `hr_attendance`→"บันทึกลงเวลา") — **รวมทั้งแอปตอนนี้ 56/56 ตาราง** — ระหว่างตรวจสอบพบว่า hap CLI (`worksheet update`/`form-settings`) ไม่มีช่องนี้เลย ชวนสงสัยว่า MCP อาจใช้ไม่ได้แล้วเช่นกัน แต่ทดสอบยิงตรงก็ยังสำเร็จปกติ — **สรุป: พารามิเตอร์นี้ยังคง undocumented-but-working เหมือนเดิม ไม่ต้องสงสัยซ้ำอีกในอนาคต แค่ลองยิงตรง**
- ✅ **กฎปฏิบัติสำหรับตารางใหม่ (P2 เป็นต้นไป):** ตอนใช้ `create_worksheet` (ถ้ากลับมาใช้งานได้) ให้ใส่ `addRecordButtonName` ทุกครั้งตั้งแต่ตอนสร้าง · ถ้าใช้ workaround `create_app_items` (เพราะ `create_worksheet` ยังพัง) จะไม่มีช่องนี้ให้ใส่ตอนสร้าง — **ต้องตามไปตั้งด้วย `update_worksheet.addRecordButtonName` เป็นขั้นที่ 4 เพิ่มจาก workaround เดิม**
- ⚠️ credential ที่ผู้ใช้ส่งมา (ทั้ง PAT และ cookie `md_pss_id`) เป็นของสด **ไม่บันทึกค่าจริงไว้ที่ไหนในเอกสารนี้หรือไฟล์ project ใด ๆ**
- 🔴🆕 **27 ส.ค. 2569 (ต่อเนื่อง 5) — พบว่า `addRecordButtonName` ไม่ได้แก้ "ชื่อตาราง" ที่แสดงในเมนูแอป (top-level `name`) เป็นคนละจุดกับที่เข้าใจไว้เดิม:** ตรวจสอบกลุ่ม HR-04 Payroll ตามคำขอผู้ใช้ "ตรวจสอบชื่อชีทที่ยังเป็น id" พบว่า `hr_pay_component`/`hr_pay_period`/`hr_salary_structure` (P5-1) ยังแสดงชื่อดิบ `hr_pay_component`/`hr_pay_period`/`hr_salary_structure` ใน `get_app_info`/เมนูแอป **ทั้งที่เอกสาร (checklist #24 เดิม) เคยระบุว่า "`addRecordButtonName` ตั้งสำเร็จทั้ง 3 ตาราง" แล้ว** — สรุปคือ `addRecordButtonName` ตั้งค่าสำเร็จจริง (ปุ่ม "เพิ่มระเบียนใหม่" ในฟอร์ม) แต่ **ไม่ได้เปลี่ยนชื่อตารางที่แสดงในเมนู/นำทางแอป (top-level `name`)** ซึ่งเป็นคนละ field กัน — ต้องแก้ด้วย `update_worksheet` ส่ง **`name`+`alias` คู่กัน แยกคอลจาก `editFields`** (ตาม DO #8 เดิม) ต่างหาก · ตารางที่ถูกสร้างด้วย `create_app_items` โดยระบุชื่อไทยตรง ๆ ตั้งแต่ต้น (`hr_payslip`/`hr_payslip_line`/`hr_pnd1_filing`/`hr_wht_cert` — P5-2/P5-3) ไม่มีปัญหานี้ · **แก้ไขแล้ว 27 ส.ค. 2569** ด้วย `update_worksheet(name+alias)` ทั้ง 3 ตาราง ยืนยันด้วย `get_app_worksheets_list` — **กฎใหม่: หลัง `create_app_items` ด้วยชื่อ non-Thai ต้องแก้ `name` ก่อน (ไม่ใช่พึ่ง `addRecordButtonName` อย่างเดียว)**
- 🔴🆕 **27 ส.ค. 2569 (ต่อเนื่อง 7) — ผู้ใช้ส่งภาพหน้าจอพบว่า breadcrumb หัวหน้าตาราง (ข้าง "…") ยังเป็น `hr_pay_component` ทั้งที่เมนูซ้ายเป็นไทยแล้ว → ไขปริศนาสุดท้าย: worksheet มี "ชื่อ" จริง 3 field แยกกัน ไม่ใช่ 2** — ตรวจด้วย `hap worksheet info <ws_id>`: `alias` (English snake_case, ใช้ในสูตร/`addFields`) · `entityName` (= "Data Name" ที่ MCP `update_worksheet.name` แก้ได้จริง และเมนูซ้าย/`get_app_worksheets_list` อ่านค่านี้) · **`name` (ชื่อ worksheet ที่แท้จริง แสดงที่ breadcrumb หัวตาราง — คนละ field กับ `entityName` แต่ MCP `update_worksheet.name` กลับไม่แตะ field นี้เลย)** — ต้องแก้ด้วย **hap CLI `hap worksheet update <ws_id> --name "ชื่อไทย" -a <app_id>`** เท่านั้นถึงจะเปลี่ยน breadcrumb ได้จริง — แก้ครบทั้ง 3 ตาราง (`hr_pay_component`/`hr_pay_period`/`hr_salary_structure`) ยืนยันด้วย `hap worksheet info` เห็นทั้ง `name` และ `entityName` เป็นไทยตรงกันแล้ว · ตาราง P5-2/P5-3 ทั้ง 4 (`hr_payslip`/`hr_payslip_line`/`hr_pnd1_filing`/`hr_wht_cert`) ไม่มีปัญหานี้เพราะ `create_app_items` ตั้ง `name` ที่แท้จริงเป็นไทยตั้งแต่สร้าง — **กฎใหม่ที่ต้องจำ: worksheet ใน HAP มีชื่อ 3 ระดับ `alias`/`entityName`/`name` — MCP `update_worksheet.name` แก้ได้แค่ `entityName` เท่านั้น ต้องใช้ hap CLI `worksheet update --name` เพื่อแก้ `name` จริง**
- ✅ **ปิดแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 13) — ฝ่ายบัญชีตอบกลับและสร้างบัญชีให้ครบแล้ว** (`tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`): เพิ่มบัญชีใหม่ในผังบัญชีจริง 3 รายการ — `530102` (ค่าใช้จ่ายสมทบ ปกส. นายจ้าง, rowId `233626b4-d272-4cb5-8f77-9d8d2ddef804`) · `210402` (เจ้าหนี้สมทบ ปกส. ลูกจ้าง, rowId `c6386fe7-1b3c-44db-ab80-9dfd3308a6a5`) · `210304` (เจ้าหนี้ WHT เงินเดือน ภ.ง.ด.1 โดยเฉพาะ แยกจาก ภ.ง.ด.3/53/54 เดิม, rowId `777ea001-5300-4f5c-857a-71a45db2076b`) — HR ผูก `hr_pay_component.biz_coa_account` (fieldId `6a8ff3049762533b5b7193a2`) ต่อสำเร็จครบ 6/6 ผ่าน `update_record`(`triggerWorkflow:false`) ยืนยันด้วย `get_record_details` เห็นชื่อบัญชีจริง — **P5-4 ปิดงานสมบูรณ์** · **แก้ไขความเข้าใจผิดเดิมของ HR ด้วย:** `ac_posting_rule.event_code` (fieldId `6a85518c055f2288c5b7430b`) เป็น **inline SingleSelect ไม่ใช่ shared optionset** — ฝ่ายบัญชีแก้ผ่าน MCP `editFields` ได้ตรง ๆ ไม่ต้องผ่าน Browser (P0-3 เดิมเข้าใจผิดว่าต้อง Browser-only) — เพิ่ม option `PAYROLL_ACCRUAL`(key `ad4850c4-f1c1-4a65-adbb-fb0054a6e0c8`)/`PAYROLL_PAYMENT`(key `04a14b13-73c2-408b-a5fb-bb81ff630a06`) ให้แล้ว พร้อมใช้ P5-5 · **WF-HR-10 source-of-truth ยืนยันแล้วโดยฝ่ายบัญชี:** `ac_posting_rule` เป็นหลัก (จับคู่ `event_code`+conditions), `hr_pay_component.biz_coa_account` เป็น fallback เมื่อไม่มีกฎตรง — ตรงกับที่ HR เสนอไว้ในเอกสาร handoff เดิม ไม่ต้องแก้ node design ของ WF-HR-10 ที่วางแผนไว้ · **ยังเปิดอยู่ ห้ามแตะ (ฝ่ายบัญชีระบุชัดเจน "ห้ามเดา"):** อัตราเงินสมทบ SSO (นายจ้าง+ลูกจ้าง)/เพดานฐานค่าจ้าง/ขั้นบันไดภาษี WHT/`hr_company_tax_id`/`hr_sso_employer_no` — ยังไม่มีตัวเลขที่ฝ่ายบัญชีเซ็นรับรอง (G-05/P0-5 ยังเปิดต่อไป) — ~~ปัญหาเดิมที่บันทึกไว้ก่อนปิด~~:
- ~~🔴🆕 28 ส.ค. 2569 (ต่อเนื่อง 8) — ช่องว่างผังบัญชีจริง กระทบ P5-4/P5-5 (ไม่ใช่บั๊ก แต่เป็นข้อมูลที่ต้องรอฝ่ายบัญชี/ผู้ใช้):~~ ตรวจ `ac_coa` ครบทั้ง 79 record (ผ่าน `get_record_list`) เพื่อหาบัญชีผูกกับ `hr_pay_component.biz_coa_account` พบว่า **มีบัญชีเงินเดือนที่ชัดเจนเพียงบัญชีเดียวคือ `530101 เงินเดือน`** (rowId `43ee2889-b2e5-4620-85b7-53f6db1c8e5b`, type Expense, group Administrative, is_active) ใช้ผูกกับ `SALARY`/`OT`/`ABSENCE_DEDUCT` ได้ · **ไม่มีบัญชีรองรับ 3 รายการนี้เลย:** (1) ค่าใช้จ่ายเงินสมทบประกันสังคมฝั่งนายจ้าง (2) เจ้าหนี้เงินสมทบประกันสังคมค้างจ่าย (3) เจ้าหนี้ภาษีหัก ณ ที่จ่ายเงินเดือนค้างนำส่ง (ภ.ง.ด.1 — ผังบัญชีมีแต่ ภ.ง.ด.3/53/54 ซึ่งเป็นหัก ณ ที่จ่ายประเภทอื่น ไม่ใช่เงินเดือน) — จึงเว้น `coa_account` ของ `SSO_ER`/`SSO_EE`/`WHT` ว่างไว้ **ตาม DO/DON'T เดิม (ห้ามสร้างบัญชีใหม่/ห้ามแก้โครงสร้างบัญชีเอง ห้าม hard-code รหัสบัญชีในworkflow)** — ต้องรอฝ่ายบัญชีเพิ่มผังบัญชี 3 รายการนี้ก่อนจึงจะผูกได้ครบ 6/6 · **บล็อกบางส่วนของ P5-5** (สร้าง `ac_posting_rule`) เพราะกฎผ่านรายการของ SSO/WHT จะไม่มีบัญชีปลายทางให้ระบุ — ส่วน SALARY/OT/ABSENCE_DEDUCT ทำ posting rule ได้เลย · **ข้อสังเกตเพิ่มเติม (ยังไม่แก้ ต้องพิจารณาตอน P5-5):** BuildSpec ระบุใน remark ของ `biz_coa_account` ว่า "WF-HR-10 อ่านรหัสบัญชีจากที่นี่ ไม่ hard-code" แต่ node-by-node catalog ของ WF-HR-10 ที่มีอยู่กลับสื่อว่า debit/SSO-payable/WHT-payable account มาจาก `ac_posting_rule` ผ่าน "กฎแยก event" แทน — ต้องยืนยันให้ชัดตอนสร้าง WF-HR-10 จริงว่าฟิลด์ไหนเป็น source of truth
- 🔴🔴 **[ใหม่ 28 ส.ค. 2569 (ต่อเนื่อง 9) — D-15 — สำคัญมาก ต้องอ่านก่อนแตะ WF-HR-01/WF-HR-05/WF-HR-06 ใด ๆ] สามเรื่องที่พบระหว่างพยายามทำ P4-4→P4-6→P3-8/BR-08.3 ตามลำดับที่แนะนำ:**
  1. **(a) ยืนยันซ้ำบั๊ก `sub_process`→`NodeAppIsNull` เป็นครั้งที่ 2** — สร้าง process ทดสอบแยกต่างหาก (ไม่แตะ workflow จริง) มีแค่ trigger+`get_multiple`+`sub_process`(1 node update_record ข้างใน) แล้วยิง `validate_process`/`publish_process` — ได้ error เดิมทุกประการ (`NodeAppIsNull`) ทั้งที่ inner child process เอง `valid:true, canPublish:true` — สรุป: บั๊กยังไม่ถูกแพตช์ ยังคงเป็นบล็อกเกอร์เต็มรูปแบบสำหรับ P4-4 (WF-HR-05 ต้องวนคำนวณสายลงเวลาทีละ record) และ P4-6 (WF-HR-06 ออกแบบให้ต้องใช้ `sub_process` เช่นกัน) — ลบ process ทดสอบทิ้งสะอาดแล้ว (`delete_process`) ไม่มีของค้าง — **ทั้ง P4-4/P4-6 ตั้งเป็น ⛔ ใน Roadmap Tracker แล้ว รอแพลตฟอร์มแพตช์หรือหา workaround ที่ไม่ใช้ `sub_process`**
  2. **(b) ค้นพบใหม่ — "auto-relink" ของ `batch_create_process_nodes` เมื่อแทรก node กลาง edge ที่ปลายทางเป็น branch node** — เมื่อสร้าง node ใหม่โดยอ้าง `prevNode` เป็น node/path ที่มี node ปลายทางอยู่แล้วพอดี 1 ตัว (คือแทรกเข้ากลาง A→B) ระบบจะ auto-relink `prevNode` ของ B ให้ชี้มาที่ node/chain ใหม่ที่เพิ่งแทรกให้อัตโนมัติ — **แต่ถ้าปลายของ chain ใหม่ที่แทรกเป็น `branch` node (ไม่ใช่ node ธรรมดา) ระบบจะ relink B ให้ชี้ไปที่ nodeId ของตัว branch node เองตรง ๆ** (ไม่ใช่ path/alias เฉพาะที่ตั้งใจ) — เกิดขึ้นเงียบ ๆ `validate_process` ไม่จับ (คืน clean) ⇒ **กฎใหม่บังคับ: หลังแทรก node กลาง chain ทุกครั้ง ต้อง `get_workflow_structure` แล้วตรวจ `prevNode` ของทุก node ที่อยู่ถัดจากจุดแทรกด้วยมือเสมอ ห้ามเชื่อแค่ `validate_process` ผ่าน**
  3. **(c) ลองเพิ่ม BR-08.3 (เช็คไม่ให้ลาซ้อนทับ) เข้า WF-HR-01 จริง (main process `6a8f36bc0e97bf440dda64eb`) แล้วพบว่า workflow หยุดยิงทั้งหมดหลัง publish** — เพิ่ม 4 node (`chk_doc`/`br_document`/`reject_doc`/`notify_doc`) → `validate_process` ผ่าน clean → `publish_process` สำเร็จเป็น **v4** → ทดสอบยิงจริงด้วย record ใหม่ 2 ใบ (set `leave_status`="Pending supervisor" ผ่าน `update_record(triggerWorkflow:true)`) → **ไม่มี `user-workflow` log เลยแม้แต่รายการเดียว ไม่มี approval instance เกิดขึ้น** (`get_record_logs`/`get_approval_list_by_row` ว่างเปล่าทั้งคู่ รอ 3+ นาทีสะสมแล้วยังว่าง) — **revert:** `delete_process_node`×4 (ลำดับ notify_doc→reject_doc→br_document→chk_doc) → `validate_process` clean → `publish_process` เป็น **v5** (ยืนยันโครงสร้าง = v3 เดิมทุกจุด รวม `set_submitted.prevNode` กลับไปอ้าง "sufficient" branch path เหมือนเดิม) → **ทดสอบยิงซ้ำบน v5 ที่โครงสร้างเหมือนเดิม 100% — อาการไม่ยิงยังคงอยู่เหมือนเดิม** (ไม่ใช่ผลจากการแก้ไข BR-08.3 โดยตรง เพราะ revert แล้วก็ยังเป็น) — **สาเหตุจริงยังไม่ทราบ** อาจเป็นปัญหา session/environment ชั่วคราว หรือเกี่ยวกับการ publish ซ้ำเอง (ไม่ใช่ตัวเนื้อหาที่แก้) — **ยืนยันด้วยว่า approval instance จริงที่ pending อยู่ก่อนหน้า (TEST-LR-0001, rowId `4a226f18-5f2b-4af4-b212-4c016f50b9ec`, To-do ถึง Wanadtapong.l) ไม่ถูกกระทบเลยตลอดกระบวนการ** (ตรวจก่อน-หลังด้วย `get_approval_list_by_row` ทั้งคู่ ยังคง status=1/pending เหมือนเดิม) ⇒ **ผลกระทบ: (1) BR-08.3 ยังไม่ได้เพิ่มจริง (revert แล้ว) (2) checklist #12 "WF-HR-01 🟢 tested and working" ต้องตรวจสอบซ้ำก่อนเชื่อ — สถานะ 🟢 เดิมอ้างอิงจากการทดสอบ 27 ส.ค. ก่อนเหตุการณ์นี้ (3) ห้ามแก้ node ของ WF-HR-01 (หรือ workflow HR อื่นที่คล้ายกัน) เพิ่มจนกว่าจะเข้าใจสาเหตุการไม่ยิง** — วิธีตรวจสอบซ้ำที่แนะนำ session หน้า: ลองยิง test record ใหม่อีกครั้งแบบ isolated (ไม่แก้อะไรก่อน) รอให้นานพอ (5+ นาที) แล้วดู log/approval list — ถ้ายังไม่ยิง ให้สงสัย environment/platform-side ก่อนสงสัยโครงสร้าง workflow
- ✅ **แก้ครบแล้ว 27 ส.ค. 2569 (ต่อเนื่อง 6) — hap CLI พร้อมใช้งานจริงใน session นี้** (`hap auth whoami` ยืนยัน login อยู่แล้ว, `hap app list-managed` เห็น ERP - TILSNA `deca7391-…` 68 ตาราง) ทำให้ไม่ต้องพึ่ง Browser — rename view ทั้ง 7 ตารางกลุ่มเงินเดือนจาก "全部" เป็น "ทั้งหมด" สำเร็จครบด้วย `hap worksheet view update <ws_id> <view_id> --name "ทั้งหมด" -a deca7391-1761-424b-9af3-c8d043004ad3`: `hr_pay_component`/`hr_pay_period`/`hr_salary_structure`/`hr_payslip`/`hr_payslip_line`/`hr_pnd1_filing`/`hr_wht_cert` ยืนยันด้วย response `"name":"ทั้งหมด"` ทุกตาราง — **กลุ่มเงินเดือนตอนนี้ไม่มี object ภาษาจีนค้างแล้วทั้งชื่อตารางและชื่อ view** — **กฎที่ยืนยันซ้ำ: session ที่มี `hap` binary ให้เช็ค `hap auth whoami` ก่อนเสมอ ก่อนจะบอกว่า "ต้องใช้ Browser" — บางครั้ง hap CLI login ค้างอยู่จาก session ก่อนหน้าแล้ว**

**🔴 `create_worksheet` (สร้างตารางใหม่) พังทั้งระบบตั้งแต่ 26 ส.ค. 2569 — มี WORKAROUND ที่พิสูจน์แล้วว่าใช้ได้ (และปรับปรุงให้สั้นลงอีกใน P3-1)**

- ทุกครั้งที่เรียก `create_worksheet` คืน error `Validation failed: structuredContent does not match tool outputSchema... required property 'appId' not found` และยืนยันด้วย `get_app_info`/`get_app_worksheets_list` ว่าไม่สร้างตารางเลย — สาเหตุอยู่ที่ตัว tool เอง ไม่ใช่ payload (ยังไม่ได้ probe ซ้ำใน P3-1/P4-1/P4-2 ว่ากลับมาใช้ได้หรือยัง — ใช้ workaround ต่อไปเลยเพื่อความชัวร์)
- ✅ **WORKAROUND ล่าสุด (ทดสอบซ้ำสำเร็จกับ P3-1 และ P4-1/P4-2 — เหลือแค่ 3 ขั้นจริง ไม่ใช่ 4):**
  1. `create_app_items` (`type:"worksheet", name, sectionId`) → ได้ worksheet เปล่า พร้อม field default 3 ตัว (名称=isTitle Text · 描述=Text multiLine · 附件=Attachment) ที่ ID = **wsid+1 / +2 / +3 เมื่อนับเป็นเลขฐาน 16**
  2. `update_worksheet` **คำสั่งเดียว** ส่ง `removeFields` (ลบ 3 field default) **พร้อมกับ** `addFields` (เติมฟิลด์จริงทั้งหมด รวม relation ที่ dataSource ชี้ตารางที่มีอยู่แล้วได้เลย) — 🆕 **ยืนยันแล้วว่ารวมเป็นคำสั่งเดียวได้ ไม่ต้องแยก 2 คำสั่งเหมือนที่เข้าใจไว้เดิม** (ทดสอบกับ `hr_leave_balance`/`hr_leave_request`/`hr_leave_ledger` และล่าสุด `hr_ot_request`/`hr_attendance` — สำเร็จหมด)
  3. `get_worksheet_structure` verify ID/type/option key ครบ
  4. `update_worksheet.addRecordButtonName` ตั้ง Data Name ไทย — ทำแล้วสำหรับ P3-1 ทั้ง 3 ตาราง และ P4-1/P4-2 ทั้ง 2 ตาราง
  5. `hap worksheet view update <ws_id> <view_id> --name "ทั้งหมด"` — แก้ view default จาก `全部` เป็นไทย (ดู Known Issues ด้านบน)
  - **Checkbox และ Role (OrgRole) สร้างผ่าน `addFields` ได้จริง ไม่ต้อง Browser** — บทเรียนเดิม "Checkbox สร้างผ่าน API ไม่ได้" ใช้ได้กับ field array ตอน `create_worksheet` เท่านั้น (คนละ code path กับ `addFields`)
  - **ยังทำผ่าน API ไม่ได้จริง (ไม่เปลี่ยนจากเดิม — ยืนยันซ้ำกับ P3-1 และ P4-1/P4-2):** ผูก field Dropdown เข้ากับ **shared optionset ที่มีอยู่แล้ว** — `addFields` ให้แค่ `options[]` inline ต้องทำใน Browser UI เท่านั้น — งานค้างล่าสุด: `hr_attendance.att_status`/`att_source`, `hr_ot_request.day_type`/`ot_status` (เพิ่มจากเดิม `hr_leave_request.leave_unit`/`leave_status`, `hr_leave_ledger.ledger_type`, `hr_leave_type.carry_policy`, `hr_ot_rate.day_type`/`employment_type`)
  - 🆕 **[27 ส.ค. 2569 — P4-2] ระวังคัดลอก label ของ option ผิดชุด:** ตอนสร้าง `hr_ot_request.day_type` พลาดครั้งแรกเป็นการคัดลอก label จาก **ชื่อ record จริง** ของ `hr_ot_rate` (`hr_rate_name`, ภาษาไทย เช่น "OT วันทำงาน") แทนที่จะเป็น label ของ **optionset** `OS_HR_OT_DAY_TYPE` ที่ field สเปกอ้างถึงจริง (ภาษาอังกฤษ: "Working day"/"Holiday within hours"/"Holiday outside hours") — สองอย่างนี้คนละความหมายกันแม้จะดูเกี่ยวข้องกัน (rate table มี "ชื่ออัตรา" ที่เป็น Text ธรรมดา ไม่ใช่ optionset) ⇒ **ก่อนสร้าง inline options ให้ตรวจ option-key GUID mapping ใน BuildSpec §1.6.1 เสมอ อย่าเดาจากชื่อ record ที่ดูคล้ายกัน** — แก้ด้วย `editFields` (ปลอดภัยเพราะยังไม่มี record ใช้ field นี้)
  - **AutoNumber ยังทำผ่าน API ไม่ได้ (ยืนยันซ้ำ P3-1):** `hr_leave_ledger.ledger_no` ที่สเปกกำหนดเป็น AutoNumber ถูกสร้างเป็น **Text ชั่วคราวแทน** (เพราะตารางต้องมี isTitle field ตั้งแต่สร้าง และ AutoNumber ต้องทำใน Browser) — งานค้าง: ตัดสินใจว่าจะแปลงเป็น AutoNumber จริงใน Browser ทีหลังหรือใช้ Text ต่อไป (workflow เขียนสตริงเองได้)
- **ตรวจก่อนใช้ทุก session:** ลอง probe (`create_app_items` 1 item) ก่อน ถ้า `create_worksheet` กลับมาใช้ได้ปกติแล้วให้ใช้ตัวนั้นแทนได้ (เร็วกว่า) แต่ยังต้องเช็คว่า Checkbox/Role field ยังสร้างผ่าน field array ตอน creation ไม่ได้เหมือนเดิมหรือเปล่า — ปลอดภัยสุดคือใช้ `addFields` เติมทีหลังเสมอ

**บทเรียนจากโมดูลบัญชีในแอปเดียวกัน (ใช้ได้กับ HR ทันที)**

- 🔴 **ห้ามใส่ `filter` ใน trigger ของ `worksheet_event`** — ยิงจริงแล้ว workflow **ไม่ทำงานเลยและไม่มี error** · ใส่ `triggerFields` อย่างเดียว แล้วเอาเงื่อนไขไปไว้ใน branch node แรก (branch เทียบ option key ได้ถูกต้อง)
- 🔴 **เงื่อนไข `ne` กับฟิลด์ที่ค่าว่างไม่ผ่าน** — ฟิลด์ธงทุกตัวต้องตั้ง **default 0** และ backfill record เดิม
- 🔴 **`update_record` คัดลอกค่าฟิลด์ชนิด OrgRole ไม่ได้** — เขียนค่าว่างเงียบ ๆ ⇒ approval abort ทันที · **ทางแก้:** `get_single` หากฎ **ภายในสายอนุมัติ** แล้ว `approve.approvers` อ้าง `{kind:"field", node:<get_single>, fieldId:<approver_role_n>}` ตรง ๆ
- 🔴 **start node ของ approval_block ไม่มี alias** — `approval_start` ใช้ไม่ได้ (`找不到节点别名`) · ต้อง `get_workflow_structure(<inner processId>)` แล้วอ้างด้วย `nodeId`
- 🔴 **branch `approval_result` ต้องอยู่ในสายอนุมัติ ต่อจาก approve node** — วางนอก block จะได้ error
- ✅ **`sub_process` โหมด `sequential_each` ใช้แทน node Loop ได้** (WF-AC-02 พิสูจน์แล้ว) ⇒ งานวนทีละ record (สร้างสลิปทุกคน · ยกยอดสิทธิลาทุกคน) ทำบน MCP ได้ ไม่ต้องเข้า Browser
- ✅ **workflow สร้าง/validate/publish ผ่าน MCP ได้ครบวงจร** และ **publish = เปิดใช้งานทันที ไม่มีขั้น Enable**
- ⚠️ **`get_record_logs` แสดง operator ตามผู้จุดชนวน (อาจเป็น `user-api`) แม้ workflow เป็นคนเขียน** — หลักฐานที่เชื่อถือได้คือ **`_updatedBy` จาก `get_record_details(includeSystemFields:true)`** ต้องเป็น `user-workflow`
  - 🟢 **[V] 28 ส.ค. 2569 — วัดจริงแล้วรู้ว่าแยกเป็น 2 ชนิดชัดเจน ไม่ใช่ "สุ่มเป็น user-api":** บน `hr_leave_request` `72e70392-…` หลัง WF-HR-01 ยิงสำเร็จ `get_record_logs` คืน 3 รายการในวินาทีเดียวกัน — รายการที่เขียน **ฟิลด์กรอบอนุมัติ** (`wfstatus`/`wfcuaids`/`wfname`) operator = `user-workflow` ✅ แต่รายการที่เขียน **ฟิลด์ธุรกิจจริงของ node `set_submitted`** (`submitted_flag` 0→1 · `approver_user` · `submitted_at` · `balance_snapshot`) operator = **`user-api`** ❌ ทั้งที่เป็นงานของ workflow ล้วน ๆ ⇒ **ถ้าตรวจว่า node ของเราทำงานไหมโดยมองหา `user-workflow` ใน log จะได้ผลลบลวงเสมอ** ขณะที่ `_updatedBy` ของ record เดียวกันคืน `user-workflow` ถูกต้อง และ `_processName` คืนชื่อ workflow ให้ด้วย
  - ✅ `get_record_logs` **ยังมีประโยชน์** สำหรับดู "ฟิลด์ไหนเปลี่ยนจากอะไรเป็นอะไรเมื่อไร" (audit trail · AC-18) — แค่ห้ามใช้คอลัมน์ operator ตัดสินว่า workflow ทำงานหรือไม่
- ⚠️ **`_updatedAt` ของหัวเอกสารไม่ขยับเมื่อ Rollup คำนวณใหม่** — ห้ามใช้ `_updatedAt` จับการเปลี่ยนแปลงของยอดรวมทั้งใน trigger และการตรวจด้วยมือ
- ✅ **`get_role_list` บน tenant นี้คืน custom role ครบ** — ต่างจากที่ playbook เตือน (ข้อค้นพบ E-4 ของโมดูลบัญชี)
- 🔴 **`get_workflow_list` คืนเฉพาะ PBP** — คืน `[]` แม้มี `worksheet_event` ที่ publish แล้วทำงานอยู่ · ยืนยันที่หน้า **Automated Workflow** หรือ `get_workflow_structure(processId)`
- **แก้ node ที่สร้างแล้วไม่ได้** → `delete_process_node` แล้วสร้างใหม่ · `validate_process` ทุกครั้งหลัง batch
- **ใช้ Trigger Field แทน Trigger Condition** (field-cache bug ทำให้ Publish error "1 nodes with abnormal")
- **เลี่ยง Formula field ใหม่** → ใช้ Number + node `Function calculation` / `Numerical operations` / `Duration` (Duration ไม่มีปัญหา timezone)
- **Number → Formula แปลงตรงไม่ได้** → ลบแล้วสร้างใหม่ (field ID เปลี่ยน)
- **DateTime ผ่าน API ต้องระบุ `+07:00` เสมอ** (ไม่งั้นเซิร์ฟเวอร์ตีเป็น UTC+8 เร็วไป 1 ชม.)
- **กันซ้ำจริงใช้ Unique index** ไม่ใช่ `isUnique` (API bypass ได้) · ⚠️ ฟิลด์ใน index ว่างได้แค่ 1 แถวทั้งตาราง ⇒ ต้อง required ก่อน · ล้างถังขยะก่อนสร้าง index
- **ล็อก record หลังอนุมัติใช้ Business Rule** ไม่ใช่ role/workflow · ทดสอบกฎฟอร์มด้วย **Role Debugging** ไม่ใช่ `get_record_logs`
- **Approve node:** ผู้อนุมัติต้องมาจากฟิลด์ **Collaborator บนตารางหลัก** · Update Record เขียนค่าก่อน Initiate Approval · ใช้ **Data Update tab** แทน Branch ผลอนุมัติ · ตั้ง **empty-approver policy = Delegate by the workflow owner** ทุกครั้ง · ปิด "Notify when approved/rejected" ถ้ามี Send Internal Notification ใน flow นอกแล้ว
- **worksheet trigger ทดสอบด้วย `create_record`/`update_record` (`triggerWorkflow:true`)** ไม่ใช่ `trigger_workflow`
- **seed ข้อมูลใช้ `batch_create_records` + `triggerWorkflow:false`**
- **ปุ่ม `updateCurrentRecord` เขียนค่าเป็น operator `user-self`** — หลักฐานว่าปุ่มทำงาน = log `user-self` ตามด้วย `user-workflow` ของ WF ที่รับช่วง (ดูสองบรรทัดคู่กัน)
- ⚠️ **`find_member` / `find_department` ถูกถอดออกจาก connector แล้ว** — หา userId จากชื่อคนต้องทำใน UI ⇒ WF-HR-17 กรอก `emp_user` เองไม่ได้ ต้องแจ้งเตือนให้ HR กรอก
- **tool schema deferred** → `ToolSearch` โหลด schema ก่อนเรียก

**เฉพาะโมดูล HR**

- 🔴 **`OS_HR_REQUEST_STATUS` ใช้ร่วม 4 ตาราง** (`hr_leave_request` · `hr_ot_request` · `hr_claim` · `hr_job_requisition`) — ต้องเป็น shared optionset และผูกตอน `addFields` เท่านั้น — ⚠️ **ยังไม่ผูกกับ `hr_leave_request.leave_status` หรือ `hr_ot_request.ot_status`** (ทั้งคู่สร้างเป็น inline options ไปก่อน — ดู addendum และ ID ตาราง P4-1/P4-2 ด้านบน)
- 🔴 **`hr_employee.supervisor_user` (Collaborator) คือหัวใจของทุกสายอนุมัติ** — ถ้าว่าง WF-HR-01/04/11 จะสร้าง approval ที่ไม่มีผู้อนุมัติ ⇒ ทุก workflow ต้องมี branch ตรวจก่อน + empty-approver policy
- 🔴 **`hr_employee.supervisor` เป็น relation ชี้ตัวเอง** — สร้างตารางก่อน แล้วเพิ่มฟิลด์ด้วย `update_worksheet.addFields` ภายหลัง
- 🔴 **WF-HR-08 node "ลบบรรทัดเดิม" สำคัญที่สุด** — ถ้าไม่ลบ การกดคำนวณสลิปใหม่จะทำให้บรรทัดซ้อนและยอดคูณสอง
- 🔴 **`hr_ot_request.paid_flag` ต้องเซ็ตในขั้นที่สร้างบรรทัด OT** ไม่ใช่ตอนจบ flow ไม่งั้นการคำนวณใหม่ในงวดเดิมจะข้าม OT ไป
- 🔴 **WF-HR-10 กันสร้างใบสำคัญซ้ำ 3 ชั้น**: `post_flag` = 1 AND `period_status` = Approved AND `voucher_ref` **is empty**
- ⚠️ **นโยบายสิทธิลาที่ช่วงอายุงานซ้อนทับกัน** — WF-HR-14 ต้องเรียงตาม `min_service_months` มาก→น้อย แล้วเอาแถวแรก
- ⚠️ **Rollup ใช้เงื่อนไข "วันนี้" ไม่ได้** — รายงานที่ต้องเทียบวันปัจจุบันให้ใช้ Chart หรือ Aggregated Table
- ⚠️ **`AutoNumber` สร้างผ่าน API ไม่ได้** — เจอซ้ำกับ `hr_leave_ledger.ledger_no` (P3-1) เหมือนที่คาดไว้เดิมสำหรับ `hr_leave_ledger` — ใช้ Text ชั่วคราวแทนไปก่อน (ดู Known Issues ด้านบน)
- ✅ **`create_record`/`batch_create_records` เขียนค่า SingleSelect/MultipleSelect ด้วย option key (GUID) ได้ถูกต้อง** — ทดสอบกับ `hr_setting.hr_leave_year_basis` แล้วอ่านกลับด้วย `get_record_details` ตรง (ใช้ `row_id` ไม่ใช่ `record_id` เป็นชื่อ parameter — เรียกผิดชื่อจะได้ error schema เหมือน `create_worksheet`) — ใช้ pattern นี้กับ seed ทุกตารางที่มี SingleSelect
- ✅ **`get_record_pivot_data` ต้องส่ง `viewId` เสมอ** — ไม่ส่งจะ error `430028 required parameter 'viewId' is missing` แม้ tool description จะบอกว่า optional — ใช้ pattern นี้ยืนยัน P3-4 (group by employee, COUNT rowid = 6 ทั้งสองคน) สำเร็จ
- 🔴🔴 **[ใหม่ 26 ส.ค. 2569 — สำคัญมาก ใช้ได้ทุกโปรเจกต์] `get_single`/`get_multiple` filter ด้วย `fieldId:"rowid"` + `op:"in"` เทียบกับค่าที่มาจาก Relation field ของ node อื่น (เช่น `trigger.employee`) ล้มเหลวเงียบ ๆ — ไม่ error แต่ได้ผลลัพธ์ว่างเสมอ (`ifEmpty:"continue"` จึงทำให้ downstream คิดว่า "ไม่พบ" ทั้งที่ record มีจริง)** ยืนยันด้วยการยิงจริง WF-HR-01 ซ้ำ 3 รอบ (ลอง `right` เป็น bare ValueRef และเป็น array-wrapped ValueRef — ผลเหมือนกันทั้งคู่ คือว่างเปล่า) ⇒ **ห้ามใช้ `rowid`+`in` เพื่อ "ดึง record ที่ relation field ของ node อื่นชี้มา"** ทางแก้ที่พิสูจน์แล้วว่าทำงานจริง (ยิงจริง 27 ส.ค. 2569, `_updatedBy`=`user-workflow` + ค่าที่ดึงมาถูกต้อง 100%): กรอง **reverse-relation field บนตารางปลายทาง** (ฟิลด์ auto-created ที่ระบบสร้างให้อัตโนมัติเมื่อสร้าง relation field ต้นทาง — หาได้จาก `sourceField` ใน `get_worksheet_structure`) ด้วย `op:"contains"` เทียบกับ `trigger.rowid` (array-wrap ด้วย `right:[{...}]` ตามที่ schema `contains` ต้องการ) เช่น get_single บน `hr_employee` เพื่อหาแถวที่ `hr_leave_request.employee` ชี้มา ให้กรองด้วย `fieldId:<reverse-relation field id บน hr_employee>` `op:"contains"` `right:[{kind:"field",node:{nodeAlias:"trigger"},fieldId:"rowid"}]` แทนการกรอง `rowid in trigger.employee`
- ⚠️ **field ที่สร้างด้วย `batch_create_process_nodes` แล้วละเว้น `node` ใน `ValueRef{kind:"field",...}` (ลืมใส่ node เฉย ๆ) ไม่ error ตอน `validate_process`/`publish_process`** — เขียนค่าว่างเงียบ ๆ ตอนรัน (คนละสาเหตุกับกับดัก OrgRole ที่มีอยู่แล้ว แต่ผลเหมือนกัน) ⇒ ตรวจทุก `FieldPatch.value.kind=="field"` ว่ามี `node` เสมอก่อน publish ด้วย `get_workflow_structure` อ่านสอบ
- 🔴 **`delete_process_node` ที่ branch node จะ cascade ลบทุก node ที่อยู่ใต้ paths ของมันทั้งหมด** (ไม่ใช่แค่ node เดียว) — ยืนยันจริง: ลบ `chk_supervisor` (1 branch) ไปพร้อมลูกหลาน 24 node ที่ผูกอยู่ใต้ทั้งสอง path ⇒ ก่อนแก้ node กลาง flow ให้ประเมินก่อนว่าจะเสีย subtree เท่าไร บาง case คุ้มกว่าจะ mock ทดสอบ filter แยกก่อนแล้วค่อยประกอบเข้า flow จริง
- ⚠️ **prevNode ของ node ที่อยู่ถัดจาก node ที่ถูกลบ จะ auto-relink ไปยัง node ใหม่ที่สร้างด้วย alias เดิม** (ยืนยันแล้ว) **แต่เนื้อหา filter/condition ภายใน node อื่นที่อ้างอิง field ของ node ที่ถูกลบ (เช่น branch path filter อ้าง `{node:{nodeAlias:"X",nodeId:"<เดิม>"}}`) จะไม่ auto-relink** — ต้อง `delete_process_node`+สร้างใหม่ทุก node ที่มี filter อ้างถึง node ที่เปลี่ยน nodeId ด้วย (ไม่ใช่แค่ node ที่ต่อ sequence โดยตรง) — เจอเป็น `StartNodeControlsIsNull` ตอน `validate_process`
- 🔴🔴 **[ใหม่ 27 ส.ค. 2569 — สำคัญมาก ใช้ได้ทุกโปรเจกต์ — พบระหว่างสร้าง WF-HR-02] `compute`/`get_single` node ไม่ใช่ snapshot แบบ freeze ณ จุดที่มันรัน — ค่าที่ downstream node อ้างถึงซ้ำ (`{kind:"field",node:{nodeAlias:<compute หรือ get_single>},fieldId:...}`) จะถูก "คำนวณใหม่" จากค่าปัจจุบันของฟิลด์ต้นทางในฐานข้อมูล ณ เวลาที่ node ปลายทางรัน ไม่ใช่ค่า ณ ตอนที่ compute node เองรันครั้งแรก** — ยืนยันจริง: WF-HR-02 มี compute `calc_remaining` (get_bal.total_days − calc_used.number_fx_id) ที่ `upd_bal` (update balance) และ `add_ledger` (create ledger, อ้าง `calc_remaining-number_fx_id` เป็น `balance_after`) อ้างถึงร่วมกัน — เมื่อวาง `add_ledger` **หลัง** `upd_bal` (ตามลำดับที่เขียนก่อน update แล้วค่อย log ซึ่งดูเป็นลำดับปกติ) ผลคือ `add_ledger` อ่านค่า `get_bal.used_days` ที่ **update ไปแล้ว** (เพราะ `upd_bal` รันไปก่อนหน้า) ทำให้คำนวณซ้ำเป็นค่าใหม่ที่ผิด (พบจริง: balance จริง=9.0 แต่ ledger บันทึก 8.0 เพราะไปหัก used_days ซ้ำสองรอบ) ⇒ **ห้ามวาง node ที่ต้องใช้ผลลัพธ์ compute เดิม (โดยเฉพาะที่อ้างอิงฟิลด์ผ่าน get_single) ไว้หลัง node ที่เขียนทับฟิลด์ต้นทางของ compute นั้น** ทางแก้ที่พิสูจน์แล้วว่าถูกต้อง (ยิงจริง 27 ส.ค. 2569 ledger กับ balance ตรงกัน 100%): **สลับลำดับให้ node ที่ต้อง "อ่าน" ผลลัพธ์ (เช่น `add_record` ของ ledger) รันก่อน node ที่ "เขียน" ทับฟิลด์ต้นทาง (เช่น `update_record` ของ balance) เสมอ** — ใช้ได้กับทุก pattern ที่มี compute node ถูกอ้างอิงมากกว่า 1 จุดในเส้นทางเดียวกัน
- 🔴🔴 **[ใหม่ 27 ส.ค. 2569 — สำคัญมาก ใช้ได้ทุกโปรเจกต์ — พบระหว่างสร้าง WF-HR-02] `FieldPatch.value{kind:"record"}` เขียนลง Relation field แล้วได้ค่าขยะ ไม่ error ทั้งตอน `validate_process`/`publish_process` และตอนรันจริง** — ค่าที่บันทึกจริงกลายเป็น literal string ที่มี **node ID ภายในของ workflow** (เช่น `["6a8fa9a9fdab77a41c51cd9e"]`) ไม่ใช่ rowid ของ record จริงที่ node นั้นสร้าง/ดึงมา ⇒ เมื่ออ่านกลับด้วย `get_record_details`/`get_record_list` relation field จะแสดง `已删除` ("ถูกลบ"/ไม่พบ) เพราะ node ID ไม่ตรงกับ rowid จริงในตาราง ยืนยันจริง: `add_record` ของ `hr_leave_ledger` ใช้ `{fieldId:<balance>, value:{kind:"record", node:{nodeAlias:"get_bal"}}}` เพื่อผูก relation กลับไปยังแถว balance ที่เพิ่งดึงมา — readback และข้อมูลจริงหลังยิงแสดงค่าขยะ (node ID ของ `get_bal`) แทนที่จะเป็น rowid ของแถว balance จริง ⇒ **ห้ามใช้ `kind:"record"` เพื่อเขียนค่าเข้า Relation field ที่ต้องการชี้ไปยัง "record ที่ node ต้นน้ำผลิต/ดึงมา"** ทางแก้ที่พิสูจน์แล้วว่าถูกต้อง (ยิงจริง 27 ส.ค. 2569 relation แสดงชื่อ record จริงถูกต้อง ไม่ใช่ `已删除`): ใช้ `{kind:"field", node:{nodeAlias:<upstream node>}, fieldId:"rowid"}` แทน — อ้าง system field `rowid` ของ node ต้นน้ำตรง ๆ (ทำงานได้ทั้งกับ `get_single`/`add_record`/`trigger`) — หมายเหตุ: การ **copy ค่าจาก Relation field ที่มีอยู่แล้ว** (เช่น คัดลอก `trigger.employee` ไปยัง `add_record.employee`) ยังคงใช้ `kind:"field"` อ้าง fieldId ของ relation field ต้นทางตามปกติได้ (ทดสอบแล้วใช้งานได้ถูกต้อง) — บั๊กนี้เกิดเฉพาะกรณีใช้ `kind:"record"` เพื่ออ้างอิง "ทั้ง record" ของ node อื่นเท่านั้น
- 🔴🔴 **[ใหม่ 27 ส.ค. 2569 — สำคัญมาก ใช้ได้ทุกโปรเจกต์ — พบระหว่าง P3-8] "rejected Agent tool call" อาจยังทิ้ง side-effect ไว้จริงบนเซิร์ฟเวอร์** — เซสชันก่อนหน้าเรียก Agent tool สร้าง view/custom action ที่ผู้ใช้ปฏิเสธ (reject) 2 ครั้ง แต่กลับพบว่ามี **ghost views ซ้ำ 8 อัน (2 ชุด × 4 view)** ค้างอยู่จริงในแอปเมื่อกลับมาตรวจ — สมมติฐานเดิมที่ว่า "reject = ไม่มีอะไรถูกสร้าง" ผิด ⇒ **หลังทุกครั้งที่ Agent tool call ถูก reject หรือถูก cancel กลางคัน ต้องเปิดหน้าจอ "All Views" (หรือ list ที่เทียบเท่า) ของ object ที่เกี่ยวข้องเพื่อตรวจว่ามี object ซ้ำ/ขยะหลงเหลือหรือไม่** ก่อนเชื่อว่าสถานะสะอาด — วิธีล้าง: hover แต่ละ view ที่ซ้ำ → "..." → Delete → ยืนยัน URL ตรงกับ ID ที่จะลบก่อนกดยืนยันทุกครั้ง (กันลบผิดตัว)
- ⚠️ **[ใหม่ 27 ส.ค. 2569 — UI quirk, Business Rules panel] เลือก "Dynamic Value" ในช่อง value-type ของเงื่อนไข (สำหรับเทียบฟิลด์กับฟิลด์ เช่น `leave_to` เทียบ `leave_from`) ด้วยการคลิกเมาส์ตรง ๆ ที่ตัวเลือก "Dynamic Value" ล้มเหลวซ้ำ ๆ** — dropdown จะปิดแล้ว "Fixed Value" ก็ยังถูกเลือกอยู่เหมือนเดิม โดยไม่มี error ใด ๆ ⇒ **วิธีแก้ที่ใช้ได้จริง:** คลิกเปิด dropdown ก่อน แล้วใช้ปุ่มลูกศร **Down** ตามด้วย **Return/Enter** บนคีย์บอร์ด (ห้ามคลิกเมาส์เลือกตรง ๆ) — ทดสอบยืนยันซ้ำได้ผลเหมือนกันทั้งกับ Date field (BR-08.1) และ Number field
- ⚠️ **[ใหม่ 27 ส.ค. 2569 — UI quirk, Business Rules panel] กดปุ่ม Escape ขณะแผงสร้าง Validation/Interaction rule เปิดอยู่ จะปิดทั้งแผงทันทีและทิ้งข้อมูลที่กรอกไปทั้งหมดที่ยังไม่ได้ Save** (ไม่ใช่แค่ปิด dropdown ย่อยที่เปิดอยู่ ณ ขณะนั้น) — เจอซ้ำ 2 ครั้งระหว่างสร้าง BR-08.1 และ BR-08.5 จนต้องเริ่มสร้างกฎใหม่ตั้งแต่ต้น ⇒ **ห้ามใช้ Escape เพื่อปิด dropdown/เมนูย่อยขณะอยู่ในแผงสร้างกฎ** ให้คลิกที่อื่นบนหน้าจอหรือใช้ปุ่ม Cancel ของแผงแทนเสมอ
- 🔴🔴 **[ใหม่ 27 ส.ค. 2569 — สำคัญมาก ใช้ได้ทุกโปรเจกต์ — พบระหว่าง P3-8/DV-08.1] Relation-type field ของฟอร์มปัจจุบันใช้เป็นค่าเปรียบเทียบ (Dynamic Value) ไม่ได้ทั้งใน Business Rules และใน Dynamic Default "Query worksheet"** — ทดสอบสร้าง DV-08.1 จริง (field `approver_user` → Default Value → Query worksheet → worksheet `ทะเบียนพนักงาน` → Query conditions → `Record ID equals [Dynamic Value] [Select fields from current form]`) แล้วเปิด field picker ของ "current form" พบว่า **แสดงเฉพาะฟิลด์ชนิด Text/Date/Number/etc. เท่านั้น ไม่มีฟิลด์ Relation (`employee`, `leave_type`, `backup_person`) ให้เลือกเลย** แม้จะค้นหาด้วยชื่อฟิลด์ตรง ๆ ก็ไม่เจอ ("No results found") — ยืนยันว่าเป็นข้อจำกัดเดียวกับที่เจอใน Business Rules field picker (relation-field lookup ไม่รองรับ) ไม่ใช่ปัญหาเฉพาะจุด ⇒ **lookup ใด ๆ ที่ต้องอ้างอิง "ค่าของ Relation field บนฟอร์มปัจจุบัน" เพื่อไปกรอง/เทียบค่าบนตารางอื่น ทำผ่าน Business Rule หรือ Dynamic Default ไม่ได้เลย ไม่ว่าจะเป็น validation, interaction, หรือ query-worksheet default ก็ตาม — ต้องย้ายไปทำที่ระดับ workflow เท่านั้น** (query worksheet node + update_record node) — ใช้ได้กับทุกโปรเจกต์ Nocoly ที่ต้องเติมค่า default จาก related-record's field
- 🔴🔴 **[ใหม่ 28 ส.ค. 2569 (ต่อเนื่อง 11) — D-17 — สำคัญมากที่สุดตอนนี้ ต้องอ่านก่อนเชื่อว่า workflow ใด ๆ ในแอปนี้ทำงาน] worksheet_event workflow ทั้งหมด (ไม่ใช่แค่ WF-HR-01) ไม่ยิงเลยตอนนี้ — เป็นปัญหาระดับ session/environment/platform ไม่ใช่ความเสียหายจากการแก้ไขที่เราทำ** — ทำตามคำแนะนำ "ทำตามที่แนะนำ" (re-verify WF-HR-01 ก่อนเชื่อสถานะ 🟢 เดิมตาม D-15) โดยสร้าง record ทดสอบใหม่สะอาด (`hr_leave_request` rowId `72e70392-6833-4921-932c-5db3fa446b50`, leave_no `TEST-LR-D15-REVERIFY`) ไม่แตะ record เดิมใด ๆ → set `leave_status`="Pending supervisor" ผ่าน `update_record(triggerWorkflow:true)` → รอ 68+ วินาที → `get_record_details`/`get_record_logs`/`get_approval_list_by_row` ล้วนไม่มีร่องรอย workflow ทำงานเลย (`_updatedBy`=`user-api` ตลอด, todoList ว่าง) — **เพื่อแยกว่าเป็นปัญหาเฉพาะ WF-HR-01 (อาจเกี่ยวกับ D-15 revert) หรือทั้งระบบ จึงทดสอบซ้ำกับ WF-HR-04 ที่ไม่เคยถูกแตะต้องเลยตั้งแต่ยืนยันทำงานสำเร็จ 27 ส.ค.** สร้าง record OT ทดสอบใหม่สะอาดเช่นกัน (`hr_ot_request` rowId `f9d1b750-4a25-4642-82f5-552524a8ea62`, `hr_ot_no` `TEST-OT-SYSCHK-01`) → set `hr_ot_status`="Pending supervisor" → **ก็ไม่ยิงเช่นกัน (ไม่มี log, ไม่มี approval, `_updatedBy`=`user-api`)** — ตรวจ `get_workflow_structure` ทั้ง WF-HR-01 (main + inner appr_block_1) ยืนยันว่า `published:true`, version ถูกต้อง, trigger config ถูกต้องทุกจุด (worksheetId/event/triggerFields ตรง), ไม่มี warning ใด ๆ — **โครงสร้างสมบูรณ์ 100% แต่ execution engine ไม่ทำงาน** ⇒ **สรุปที่สำคัญที่สุด: ปัญหานี้ไม่เกี่ยวกับการแก้ไข WF-HR-01/BR-08.3 ที่ทำไปก่อนหน้า (D-15) เลย — เป็นปัญหาที่กว้างกว่านั้นมาก (อาจเป็น session/environment ปัจจุบัน หรือ platform-side outage ชั่วคราว)** — เก็บ record ทดสอบทั้ง 2 ไว้เป็นหลักฐาน (ตั้งชื่อให้ชัดว่าเป็น test ไม่ใช่ข้อมูลจริง) ไม่ลบ ⇒ **กฎใหม่บังคับ: ก่อนเริ่มงานที่ต้องพึ่งพา worksheet_event trigger ทำงานจริง (ทดสอบ AC ใด ๆ, ทดสอบ workflow ใหม่ที่เพิ่งสร้าง) ต้อง sanity-check ด้วย workflow ที่เคยพิสูจน์ทำงานมาก่อน (เช่น WF-HR-04) ก่อนเสมอ — ถ้า sanity-check ก็ไม่ยิง แปลว่าเป็นปัญหาระดับ environment ไม่ใช่ workflow ที่กำลังทดสอบ อย่าเสียเวลาสืบสวน/แก้ไขโครงสร้าง workflow เจาะจงตัวใดตัวหนึ่งก่อน** — 🔴🔴 **อัปเดต 28 ส.ค. 2569 (ต่อเนื่อง 15) — re-verify ครั้งที่ 2/3 (ตามคำสั่ง "ดำเนินการต่อไปก่อน") ยังไม่คลี่คลาย ยืนยันข้ามช่วง session แล้ว:** ทำซ้ำขั้นตอนเดิมทุกประการ — reset `TEST-LR-D15-REVERIFY` (`hr_leave_request`) จาก Draft→Pending supervisor ผ่าน `update_record(triggerWorkflow:true)` → รอ >60 วินาที → `get_record_details`/`get_approval_list_by_row` ยังคง `_updatedBy`=`user-api`, `submitted_at`/`approver_user` ว่าง, `todoList` ว่าง — sanity-check ซ้ำด้วย `TEST-OT-SYSCHK-01` (`hr_ot_request`, WF-HR-04): reset `hr_ot_day_type`="Working day" แล้วตั้ง `hr_ot_status`="Pending supervisor" → **ก็ไม่ยิงเช่นกันทุกประการ** — **นี่คือความล้มเหลวครั้งที่ 3 ติดต่อกัน (ครั้งที่ 1: ต่อเนื่อง 11 · ครั้งที่ 2: นี้) และครั้งแรกที่ยืนยันคร่อมขอบเขต session จริง** (record ทดสอบเดิมสร้างไว้ ~11:15 ของ session ก่อนหน้า ซึ่งจบไปแล้ว, re-check รอบนี้เกิดขึ้น ~18:46-18:47 ของ session ปัจจุบัน คนละ session กัน ห่างกัน >7 ชั่วโมง) ⇒ **ข้อสรุปเปลี่ยนไป: ไม่ใช่ปัญหาชั่วคราวของ session/เวลาเดียวอีกต่อไป — สิ่งที่ agent ทำได้ผ่าน MCP/CLI มีแค่นี้ (ยืนยันซ้ำว่าไม่ใช่ปัญหาจากโครงสร้าง/การแก้ไขของเราเอง) ขั้นต่อไปต้องให้ผู้ใช้ช่วยตรวจสอบสถานะแพลตฟอร์ม Nocoly/HAP โดยตรง** — เช่น สถานะ subscription/billing ขององค์กร, ติดต่อ platform support ของ Nocoly, หรือเปิด Browser เข้าไปดูว่า workflow (เช่น WF-HR-01/WF-HR-04) ขึ้นสถานะ "Enabled"/"Active" จริงหรือไม่ (ซึ่งเป็นสิ่งที่ Claude in Chrome ทำได้แต่ agent ยังไม่ได้ลองในรอบนี้ — ควรลองเป็นขั้นต่อไปถ้ายังไม่คลี่คลายอีก) — เก็บ record ทดสอบทั้ง 2 ไว้เป็นหลักฐานต่อไป ไม่ลบ · 🟢🆕 **[แก้ไข 28 ส.ค. 2569 (ต่อเนื่อง 16) — D-17 ไม่เคยเป็นปัญหาแพลตฟอร์ม/session เลยตลอด 3 รอบที่บันทึกไว้ข้างต้น — ข้อสรุปทั้งหมดก่อนหน้านี้ผิด]** ผู้ใช้ส่งภาพหน้าจอ Workflow History ของ `TEST-LR-D15-REVERIFY` มาถามว่า "ใช่ workflow ที่พูดถึงไหม" — เปิดดูพบว่า process มีสถานะ **"Done" จริง** (ไม่ใช่ "ไม่ยิง") เข้า node `trigger`→`gate` (branch) ครบตามลำดับ — ไปตรวจ `get_workflow_structure` ของ WF-HR-01 พบว่า `gate` node มี path `leg_supervisor` ที่ต้องการเงื่อนไข **AND กัน 2 ข้อ**: `leave_status`=Pending supervisor **และ** `submitted_flag` ne "1" — ตรวจ `get_record_details` ของ record นี้พบว่า `submitted_flag` เป็น **ค่าว่างจริง ("" ไม่ใช่ 0)** ⇒ ตรงกับบั๊กที่เคยพบมาก่อนแล้วเป๊ะ ("เงื่อนไข `ne` กับฟิลด์ที่ค่าว่างไม่ผ่าน" — ดู Known Issues ด้านล่าง) — ค่าว่างไม่ match `ne "1"` เลยตกไป path `leg_default` ("อื่นๆ ไม่ทำอะไร") แบบเงียบ ๆ ไม่ error — ตรวจ WF-HR-04/`TEST-OT-SYSCHK-01` พบสาเหตุเดียวกันทุกประการ: `hr_ot_submitted_flag` เป็นค่าว่างจริงเช่นกัน **ทั้งที่ D-14 เคยบันทึกไว้ว่าแก้ด้วย `editFields` ใส่ `defaultValue:0` ให้ฟิลด์นี้ไปแล้วตั้งแต่ 27 ส.ค.** ⇒ **สรุปใหม่ที่สำคัญมาก: ฟิลด์ธงเหล่านี้**ไม่มี `defaultValue` เลย** (`default:""` — ยืนยันด้วย `hap worksheet fields <ws_id> --raw` 28 ส.ค. 2569) รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกไว้ว่าตั้ง `defaultValue:0` แล้วเมื่อ 27 ส.ค. ⇒ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องกับบั๊กที่รู้อยู่แล้วว่า `editFields` เขียนทับทั้ง field object) — ยังพิสูจน์ไม่ได้ว่า API เพิกเฉยต่อ default เพราะไม่มีฟิลด์ไหนมี default ให้ทดสอบเลย ⇒ ในทางปฏิบัติ — ต้องส่งค่าฟิลด์ธงเข้าไปตรง ๆ เองเสมอเมื่อ seed record ผ่าน API ห้ามพึ่ง `defaultValue` ของฟิลด์อย่างเดียวเด็ดขาด** (⚠️ **ยังไม่ทดสอบ**ว่า record ที่สร้างผ่าน **ฟอร์ม UI** จะได้ค่า default หรือไม่ — และตอนนี้ทดสอบไม่ได้เพราะไม่มีฟิลด์ไหนเหลือ default ให้ทดสอบเลย) — **ทดสอบแก้จริง (28 ส.ค. 2569 ต่อเนื่อง 16):** `update_record` ตั้ง `submitted_flag`/`hr_ot_submitted_flag`=0 ทั้ง 2 record (`triggerWorkflow:false`) → ตั้ง `leave_status`/`hr_ot_status`=Draft (`triggerWorkflow:false`) → ตั้งกลับเป็น Pending supervisor (`triggerWorkflow:true`) → รอ 20 วินาที → **ทั้งคู่ยิงสำเร็จทันที**: WF-HR-01 `_updatedBy`=`user-workflow`, `submitted_flag`=1, `approver_user`=Wanadtapong.l, `balance_snapshot`=10.0, `submitted_at` ถูกเติม, `get_approval_list_by_row` มี To-do จริงถึง Wanadtapong.l · WF-HR-04 `_updatedBy`=`user-workflow`, `hr_ot_submitted_flag`=1, `hr_ot_hours`=2.00, `hr_applied_multiplier`=1.50, `hr_approver_user`=Wanadtapong.l, มี To-do จริงเช่นกัน ⇒ **WF-HR-01 และ WF-HR-04 ทำงานได้ปกติสมบูรณ์มาตลอด ไม่เคยเสียหายจากการแก้ไข BR-08.3/D-15 เลยแม้แต่น้อย** — 3 รอบ re-verify ก่อนหน้าที่สรุปว่า "ไม่ยิง"/"เป็นปัญหาระบบ" ล้วนเป็นการตีความข้อมูลผิด เพราะดูแค่ `_updatedBy`/`get_record_logs`/`get_approval_list_by_row` (ซึ่งว่างเปล่าเหมือนกันทั้งกรณี "ไม่ยิงเลย" และกรณี "ยิงแล้วแต่ไปจบที่ path ที่ไม่ทำอะไร") โดยไม่เคยเปิด **Workflow History** (Browser) ดูสถานะ process จริงเลยสักครั้ง — และเคยแนะนำผู้ใช้ให้ไปตรวจสอบสถานะ subscription/platform support ทั้งที่ไม่จำเป็นเลย ขออภัยในความผิดพลาดนี้ ⇒ **กฎใหม่บังคับ: ก่อนสรุปว่า worksheet_event workflow "ไม่ยิง" ต้องเปิด Workflow History (Browser) ตรวจสถานะ process จริงก่อนเสมอ — ถ้าสถานะเป็น "Done"/"In progress" แปลว่า trigger ทำงานแล้ว ปัญหาอยู่ที่ branch/condition logic ไม่ใช่ platform/session** และ **ทุกครั้งที่ seed record ทดสอบผ่าน MCP `create_record`/`batch_create_records` สำหรับ worksheet ที่มี flag field ใช้ใน `ne`/`eq` condition ของ workflow ต้องส่งค่า flag นั้นตรง ๆ เสมอ (เช่น `submitted_flag`/`deducted_flag`/`hr_ot_submitted_flag`/`hr_paid_flag` ทั้งหมด =0) ห้ามปล่อยว่างแล้วหวังพึ่ง `defaultValue`**
- 🧹 **[ใหม่ 28 ส.ค. 2569 (ต่อเนื่อง 18) — วิธีหาและล้าง orphan workflow + ข้อควรระวังของ `hap workflow list`]**
  1. **`delete_process_node` ที่ลบ node ชนิด `sub_process` ทิ้ง จะทิ้ง inner process เป็น orphan เสมอ** (ยืนยันแล้ว: การ revert D-8 เมื่อ 27 ส.ค. ทิ้ง orphan ไว้ 8 ตัว และการสร้าง WF-HR-05 รอบแรกทิ้งอีก 1 ตัว) ⇒ **ทุกครั้งที่ลบ `sub_process` node ต้องลบ inner processId ทิ้งด้วยมือเสมอ** (เหมือนกฎเดิมของ `delete_process` ที่ไม่ cascade)
  2. 🔴 **`hap workflow list <app_id>` ให้ผลไม่คงที่และไม่ครบ** — เรียก 3 ครั้งได้ 50 / 37 / 36 รายการ · inner ของ WF-HR-05 (`6a910197730d20c5b7711028`) **ไม่ปรากฏในการเรียกครั้งแรกเลย** ทั้งที่มีอยู่จริงและถูกอ้างอิงอยู่ · orphan บางตัวโผล่มาเฉพาะการเรียกครั้งหลัง ⇒ **ห้ามใช้ผลจาก list เป็นหลักฐานว่า "ไม่มี" อะไร** (คู่กับกฎเดิมที่ MCP `get_workflow_list` เห็นเฉพาะ PBP) · 🔴 **และห้ามใช้ `hap workflow structure <id>` ตรวจว่ามีอยู่จริงหรือไม่ด้วย — มันคืนโครงสร้างเต็มแม้ process ถูกลบไปแล้ว** (ยืนยันจริง: process ที่ `delete_process` ลบสำเร็จแล้ว `workflow structure` ยังคืนข้อมูลครบเหมือนตัวที่ยังใช้งานอยู่ ไม่มีอะไรบอกความต่างเลย — เกือบสรุปผิดว่า "ลบไม่สำเร็จ") ⇒ **เครื่องมือเดียวที่เชื่อถือได้คือ `hap workflow get <id>` แล้วดูฟิลด์ `deleted` (true/false) และ `enabled`**
  2b. **`delete_process` เป็น soft-delete (ถังขยะ) ไม่ใช่ลบถาวร** — หลังลบ `workflow get` จะได้ `deleted:true, enabled:false` และหายจาก `workflow list` แต่ object ยังอยู่และดึงด้วย id ได้ · ผลเชิงปฏิบัติคือใช้งานไม่ได้แล้ว ปลอดภัย แต่**อย่าเขียนในเอกสารว่า "ลบถาวร"**
  3. **คีย์ที่ใช้อ้าง inner process ต่างกันตามเครื่องมือ:** MCP `get_workflow_structure` → `config.process.processId` · hap CLI `workflow structure` → `flowNodeMap.<nodeId>.subProcessId` — เขียน script หา orphan ต้องจับให้ถูกคีย์ (รอบแรกจับผิดคีย์ ได้ผล "referenced = 0" ซึ่งแปลว่า script พัง ไม่ใช่ทุกตัวเป็น orphan)
  4. **วิธีหา orphan ที่ใช้ได้จริง:** ดึง id ทุกตัวจาก list (เรียกหลายครั้งแล้ว union) → `workflow structure` ทีละตัว → **grep หา id เป้าหมายแบบ full-text ทั้ง output** (ไม่เจาะจงคีย์ เพราะ approval sub-flow ผูกผ่าน `triggerId` ไม่ใช่ `subProcessId`) → ตัวที่ไม่มีใครอ้างถึงเลย = orphan · **สำรองด้วย `workflow structure` ลงไฟล์ก่อนลบเสมอ**
  5. **ผลการล้าง 28 ส.ค. 2569:** ลบ orphan 9 ตัว (`Sync attendance to On leave` ×6 · `Revert attendance sync` ×2 จากการ revert D-8 · `WF-HR-05 inner - summarise one attendance record` ×1 จากการสร้าง WF-HR-05 รอบแรก) — ยืนยันก่อนลบว่าไม่มี process ใดใน 51 ตัวอ้างถึงเลย · หลังลบยืนยัน workflow HR ครบ 12/12 โครงสร้างสมบูรณ์ · สำรองไว้ที่ `/home/claude/wf_backup/` (ชั่วคราวใน session)
- 🔴🔴 **[ใหม่ 28 ส.ค. 2569 (ต่อเนื่อง 17) — พบจากการ scrutinize การแก้ D-17 — สำคัญมาก ใช้ได้ทุกโปรเจกต์ Nocoly] ฟิลด์ธงทุกตัวในทุกตาราง HR มี `default:""` และ MCP มองไม่เห็นค่านี้เลย**
  1. **MCP `get_worksheet_structure` ไม่คืนคีย์ `defaultValue`/`default` เลย** — ทั้ง `responseFormat:"json"` และ `"md"` field object มีแค่ `id`/`name`/`alias`/`type`/`subType`/`required`/`options`/`dataSource`/... ⇒ **ห้ามใช้ MCP ยืนยันว่าตั้ง `defaultValue` สำเร็จแล้วเด็ดขาด** ต้องใช้ `hap worksheet fields <ws_id> --raw -a <app_id>` เท่านั้น — **นี่คือเหตุผลที่ D-14 ถูกเชื่อว่า "แก้แล้ว" มา 4 วันทั้งที่ไม่เคยสำเร็จจริง**
  2. **สำรวจครบแล้ว 28 ส.ค. 2569 — ธงทุกตัวยังเป็น `default:""`:** `hr_leave_request.submitted_flag`/`deducted_flag`/`returned_flag` · `hr_ot_request.hr_ot_submitted_flag`/`hr_paid_flag` · `hr_attendance.hr_summarised_flag` · `hr_employee.probation_alert_flag` — ไม่มีตัวไหนได้ `defaultValue:0` จริงสักตัว
  3. **ผลกระทบที่ยังไม่ได้แก้ (ดู D-19 ใน Roadmap §11):** ทุก branch ที่ใช้ `<flag> ne "1"` จะตกไป path "ไม่ทำอะไร" เงียบ ๆ สำหรับ record ที่ธงยังว่าง — **WF-HR-02 (`deducted_flag`) จะไม่ตัดสิทธิเมื่อ To-do ที่ค้างอยู่ถูกกด Approve** · WF-HR-03 (`returned_flag`) · WF-HR-05 (`hr_summarised_flag`) · WF-HR-13 (`probation_alert_flag`) เสี่ยงเหมือนกันทั้งหมด
  4. **path ผู้ใช้จริงยังไม่เคยทดสอบเลย:** record ทดสอบทุกใบในโปรเจกต์นี้สร้างผ่าน API — ยังไม่เคยมีใบลาที่พนักงานกรอกผ่าน **ฟอร์มจริง** สักใบ จึงยังไม่รู้ว่า path ผู้ใช้จริงเจอปัญหาเดียวกันหรือไม่ ⇒ **ต้องทดสอบก่อน demo/go-live** (กฎ "ต้องใส่ค่าธงตอน seed" ป้องกันเฉพาะ record ที่ agent สร้าง ไม่ช่วย path ผู้ใช้เลย)
  5. **ทางแก้เชิงโครงสร้างที่แนะนำ (ยังไม่ทำ — รอผู้ใช้ตัดสินใจ):** แทนที่จะไล่เติม default/backfill ทุก record ตลอดไป ให้**สลับโครง branch**: path แรก = "ทำไปแล้ว" (`flag eq "1"`) → ไม่ทำอะไร · path หลัง = catch-all (เงื่อนไขว่าง) → ทำงาน — ผลเชิงตรรกะเหมือนเดิมทุกประการ แต่ค่าว่างจะตกเข้า catch-all ได้ถูกต้อง **ไม่ต้องพึ่ง `defaultValue` ไม่ต้อง backfill ไม่ต้องจำกฎตอน seed** และปิดช่อง path ผู้ใช้จริงไปด้วยในตัว · ทุก gate ในโมดูลนี้มี fallback path ว่างอยู่แล้ว จึงเป็นการแก้เล็ก
- 🟡 **[ใหม่ 28 ส.ค. 2569 (ต่อเนื่อง 12) — D-18 — gap ต้องตัดสินใจก่อนขยาย WF-HR-06] `hr_ot_request` ไม่มีฟิลด์ "วันที่ส่งคำขอ" (`submitted_at`)** — ตอนสร้าง WF-HR-06 (เตือน SLA ค้างอนุมัติ) พบว่า `hr_leave_request` มีฟิลด์ `submitted_at` (DateTime) ให้ใช้คำนวณ SLA ได้ตรงความหมาย แต่ `hr_ot_request` มีแค่ `hr_approved_at` (วันที่อนุมัติ) ไม่มีฟิลด์บันทึก "วันที่ส่งคำขอ" เลย — ใช้ `_createdAt` ระบบแทนได้แค่คร่าว ๆ (ไม่ตรงความหมายถ้าผู้ใช้สร้าง Draft ทิ้งไว้นานก่อนกดส่งจริง) ⇒ **ตัดสินใจชั่วคราว: WF-HR-06 v1 จำกัดขอบเขตเหลือแค่ `hr_leave_request` เท่านั้น** ตัด `hr_ot_request`/`hr_claim` ออก (`hr_claim` ยังไม่มีตารางอยู่แล้ว P6) — **ต้องตัดสินใจก่อนขยาย:** (a) เพิ่มฟิลด์ `submitted_at` จริงใน `hr_ot_request` ผ่าน MCP `addFields` (ทำได้ทันทีถ้าผู้ใช้ยืนยัน) แล้วให้ WF-HR-04 เขียนค่านี้ตอน submit เหมือน `hr_leave_request`, หรือ (b) ยอมรับ `_createdAt` เป็นตัวแทนคร่าว ๆ — ไม่ตัดสินใจเองเพราะเป็นการเปลี่ยน schema ที่ผู้ใช้ควรรับทราบ

- 🟢🆕 **[แก้ไข 28 ส.ค. 2569 (ต่อเนื่อง 10) — D-13 ไม่ใช่บั๊กแพลตฟอร์ม เป็นขั้นตอนที่ขาดไปเอง — คลี่คลายแล้ว!] Root cause ที่แท้จริง: ต้อง `publish_process` ที่ inner processId ของ `sub_process` ก่อน แล้วจึง `validate_process`/`publish_process` ที่ outer/main process — ไม่ใช่แค่ `validate_process` ที่ inner (valid:true อย่างเดียวไม่พอ)** ทุกครั้งที่ผ่านมา (รวมการ reconfirm 28 ส.ค. เช้า) เรา validate inner แล้วเห็น `valid:true, canPublish:true` ก็เข้าใจว่า "โครงสร้างข้างในถูกต้องแล้ว" แล้วข้ามไป validate/publish outer ตรง ๆ โดยไม่เคยเรียก `publish_process` บน inner processId จริง ๆ เลยสักครั้ง — พบวิธีแก้จากเอกสารอ้างอิงของ skill **`hap-mcp-app-builder`** (คนละ skill กับ `nocoly-hybrid-builder-v2` ที่ session นี้ใช้หลัก) ไฟล์ `workflow_rules.md` ระบุชัดเจนว่า "含子流程/审批块时的发布顺序（违反必报 NodeAppIsNull 致命错误）... 正确流程：创建内部节点后 → 调 publish_process（内部 processId）立即发布该子流程 → 所有子流程发布成功后 → 调 publish_process（主流程 processId）最后发布主流程" — และ `nocoly-hybrid-builder-v2/references/workflow-authoring-mcp.md` บรรทัด 155 ก็มีกฎเดียวกันเขียนสั้น ๆ ไว้อยู่แล้ว ("Publish inner flows before the outer one, or the outer publish throws NodeAppIsNull") แต่เราตีความ/ปฏิบัติผิดมาตลอดว่า "แค่ validate inner ให้ผ่านก็พอ" **ทดสอบยืนยันจริง 28 ส.ค. 2569 (ต่อเนื่อง 10):** สร้าง process ทดสอบใหม่ (outer `6a90fb59fdab77a41c63662d` มี trigger+get_multiple(`hr_employee`)+sub_process(`sequential_each`), inner `6a90fb5dfdab77a41c636699` มี 1 update_record) → `validate_process`(inner) = valid:true → **`publish_process`(inner) = published:true, v1 สำเร็จ** → `validate_process`(outer) = valid:true → **`publish_process`(outer) = published:true, v1 สำเร็จ ไม่มี `NodeAppIsNull` เลย!** ลบ process ทดสอบทั้ง 2 ตัวทิ้งสะอาดแล้ว (`delete_process` แยกทั้ง outer และ inner เพราะลบ outer ไม่ cascade ลบ inner ให้อัตโนมัติ — inner จะกลายเป็น orphan process ค้างอยู่ถ้าไม่ลบเอง) ⇒ **กฎใหม่บังคับที่ต้องจำตลอดไป: ทุกครั้งที่มี `sub_process`/`approval_block` ต้อง `publish_process` ที่ inner processId ก่อนเสมอ (ไม่ใช่แค่ validate) แล้วจึงค่อย validate/publish ที่ outer — เรียงตามลำดับ inner→outer ทุกครั้งไม่มีข้อยกเว้น** — **P4-4 (WF-HR-05) และ P4-6 (WF-HR-06) ปลดบล็อกแล้ว พร้อมสร้างจริงด้วยขั้นตอนที่ถูกต้องนี้** — เดิมเข้าใจผิดว่าเป็นบั๊กแพลตฟอร์มที่แก้ไม่ได้ (D-13 เดิมด้านล่างนี้เป็นบันทึกความพยายามที่ผิดพลาดไว้เป็นหลักฐาน):

- ~~🔴🔴 [เดิม, เข้าใจผิด] `sub_process` container node → `NodeAppIsNull` ตอน `validate_process`/`publish_process` (บั๊กแพลตฟอร์ม ยังไม่มีทางแก้จากฝั่ง MCP)~~ — สร้าง `sub_process` node (mode `sequential_each`, `target` ชี้ไปยัง `get_multiple` node ที่สร้างไว้ก่อนหน้า) ผ่าน `batch_create_process_nodes` **สำเร็จทุกครั้งในแง่ create** (คืน `nodeId`+internal `processId` ปกติ) แต่ `validate_process` ของ workflow แม่ค้าง error `{code:"NodeAppIsNull", message:"节点关联对象不存在或不可用"}` ที่ตัว `sub_process` container node เสมอ (reproducible 3 ครั้งติดกันด้วย config ต่างกัน) — **สิ่งที่ตัดทิ้งไปแล้วว่าไม่ใช่สาเหตุ:** (1) ไม่ใช่ missing `worksheetId` — ลองเพิ่มทั้งที่ `config.process.start.worksheetId` และที่ระดับบนสุดของ `config` เอง (นอกสเปกที่ระบุไว้ แต่ schema เป็น `additionalProperties:true` จึงลองได้) ไม่ช่วย — และ `worksheetId` ไม่จำเป็นด้วยซ้ำเพราะ `target` ที่อ้างถึง `get_multiple` node ก็ทำให้ internal child's `sub_trigger.config.worksheetId` ถูก infer ถูกต้องอยู่แล้ว (ตรวจสอบด้วย `get_workflow_structure` บน internal processId) (2) ไม่ใช่ nodeAlias-only vs nodeId+alias สำหรับ `target` — ลองทั้งสองแบบ ผลเหมือนกัน (3) ไม่ใช่ eventual-consistency/timing lag — เรียก `validate_process` ซ้ำหลังจากรอ (คั่นด้วย tool call อื่น) ยัง error เหมือนเดิมทุกครั้ง (4) ไม่เกี่ยวกับมี `input`/`inputFields` (subprocess parameters) หรือไม่ — ทดสอบทั้งแบบมีพารามิเตอร์ (WF-HR-02 ทั้ง 2 จุด) และไม่มีเลย (WF-HR-03) error เหมือนกันหมด · **สิ่งที่ยืนยันแล้ว:** internal child process ของ sub_process ที่สร้างไว้ (เรียก `validate_process`/`get_workflow_structure` ตรงด้วย internal processId จาก `config.process.processId` ที่ตอบกลับมาตอน create) **valid+canPublish:true ปกติทุกครั้ง** — โครงสร้างข้างในถูกต้อง 100% (sub_trigger มี worksheetId ถูกต้อง, update_record ข้างในมี target+fields ถูกต้อง) ⇒ **บั๊กอยู่ที่การผูก "app/worksheet association" ของตัว container node เองในฝั่ง parent workflow ไม่ใช่ logic ข้างในผิด** — ลักษณะคล้ายเป็น bug ในขั้นตอน commit metadata ของ node ระดับ container (sub_process/อาจรวมถึง approval_block ด้วยแม้ยังไม่ทดสอบ) เมื่อสร้างผ่าน `batch_create_process_nodes` โดยเฉพาะ ⇒ **ทางแก้ที่ยังไม่ได้ลอง:** (a) สร้าง `sub_process` ผ่าน Browser UI แทน MCP แล้วดูว่า UI สร้างสำเร็จ publish ได้หรือไม่ (ถ้าได้ = ยืนยันว่าบั๊กเฉพาะฝั่ง MCP tool) (b) รอแพลตฟอร์มแพตช์แล้วลองใหม่เป็นระยะ (c) ออกแบบ workaround เลี่ยง `sub_process` ทั้งหมด (เช่น ใช้ `branch`+`get_single` วนทีละ record แบบ manual ถ้าจำนวน record ต่อครั้งมีจำกัดจริง — ไม่ scale แต่ใช้ได้ชั่วคราว) — **สิ่งที่ทำไปแล้วเมื่อเจอบั๊ก (สำคัญ อย่าลืมทำซ้ำถ้าเจอบั๊กแบบนี้อีก):** `delete_process_node` ลบ node ที่เพิ่มทั้งหมดคืนสถานะเดิม (ลบ `sub_process` ก่อน แล้วค่อยลบ `get_multiple` ที่กลายเป็น orphan) → `validate_process` เช็คว่าสะอาด (issueCount:0) → `publish_process` คืนสถานะ `published:true` — **ห้ามปล่อยให้ workflow ค้างอยู่ใน draft ที่ publish ไม่ได้บนแอป production แม้ published version เดิมจะยังทำงานอยู่ก็ตาม** (draft ที่ publish ไม่ได้จะกลายเป็นกับดักสำหรับ session ถัดไปที่มาแตะ workflow เดียวกันด้วยเหตุผลอื่น) — ดู D-13 (Roadmap) และ G-13 (RTM) สำหรับสถานะและแผนต่อ

- 🔴🔴 **[ใหม่ 27 ส.ค. 2569 (ต่อเนื่อง) — สำคัญมาก ใช้ได้ทุกโปรเจกต์ — พบระหว่างสร้าง WF-HR-04] `compute` node ชนิด `computeType:"dateDiff"` ตัดพารามิเตอร์ `precision` ทิ้งเงียบ ๆ เมื่อ `outputUnit` เป็นหน่วยที่มีเศษ (เช่น `"h"` ชั่วโมง) — ไม่ error ทั้งตอน `validate_process`/`publish_process`** — ส่ง `precision:2` พร้อม `outputUnit:"h"` ตอนสร้าง node แล้วอ่านค่ากลับด้วย `get_workflow_structure` พบว่า node config ที่บันทึกจริงไม่มี `precision` เลย ⇒ ผลลัพธ์ถูกปัดเศษทิ้ง (ตัดทศนิยม ไม่ใช่ปัดเลข) เสมอ — ยืนยันจริง: กะเวลา 18:00–20:30 (2.5 ชั่วโมง) คำนวณผ่าน `dateDiff(outputUnit:"h", precision:2)` ได้ผลลัพธ์ `"2.00"` แทนที่จะเป็น `"2.50"` **ทางแก้ที่พิสูจน์แล้วว่าถูกต้อง** (ยิงจริง 27 ส.ค. 2569 ได้ `"2.50"` ตรงเป๊ะ): คำนวณเป็นหน่วยที่เล็กที่สุดที่ไม่มีเศษสำหรับ input จริง (`outputUnit:"m"` นาที — ไม่มีเศษนาทีในเคสจริงทั่วไป) ด้วย `dateDiff` compute node ตัวแรก แล้วต่อด้วย compute node ตัวที่สองชนิด `computeType:"number"` (`expression:"$<alias ของ node แรก>-number_fx_id$ / 60"`, `precision:2`) เพื่อแปลงเป็นชั่วโมงทศนิยม — **numeric compute (`computeType:"number"`) เคารพ `precision` ปกติ ต่างจาก `dateDiff`** ⇒ กฎใหม่: **ห้ามพึ่ง `precision` ของ `dateDiff` compute node เมื่อ `outputUnit` อาจมีเศษ (h/d) เสมอ ให้แตกเป็น dateDiff หน่วยเล็กสุด + numeric compute หารแปลงหน่วยแทนทุกครั้ง**
- 🔴🔴 **[ยืนยันซ้ำเป็นครั้งที่ 2 — 27 ส.ค. 2569 (ต่อเนื่อง) — สำคัญมาก] `editFields` ล้าง `name`/`alias` เป็นค่าว่างเมื่อไม่ได้ส่งมาในคำสั่งเดียวกัน เกิดซ้ำอีกครั้งจริง ไม่ใช่แค่เคยเตือนไว้ในทฤษฎี** — ระหว่างแก้ `hr_ot_request.hr_ot_submitted_flag`/`hr_paid_flag` เพื่อเติม `defaultValue:0` ส่งแค่ `defaultValue` ไปเพียงอย่างเดียว ผลคือ `name`/`alias` ของทั้ง 2 ฟิลด์ถูกล้างเป็นค่าว่างจริง (ตรวจพบด้วย `get_worksheet_structure` หลังแก้) — ต้องเรียก `editFields` ซ้ำอีกครั้งพร้อมส่ง `name`/`alias`/`type`/ทุก attribute ที่ต้องคงไว้ครบถ้วนเพื่อกู้คืน ⇒ **กฎนี้พิสูจน์แล้วว่าไม่ใช่ edge case ครั้งเดียว แต่เป็นพฤติกรรมแน่นอนของ `editFields` ทุกครั้ง — ทุกคำสั่ง `editFields` ต้อง treat เป็น "เขียนทับทั้ง field object" เสมอ ไม่ใช่ "patch เฉพาะ key ที่ส่ง" ห้ามส่งแค่ field ที่ต้องการเปลี่ยนโดยเด็ดขาด**
- 🔴🔴 **[ยืนยันซ้ำเป็นครั้งที่ 2 — 27 ส.ค. 2569 (ต่อเนื่อง) — สำคัญมาก] ฟิลด์ธง/Number ที่สร้างผ่าน workaround `create_app_items`+`addFields` ไม่มี `defaultValue` อัตโนมัติ ⇒ เงื่อนไข branch แบบ `ne` ที่เทียบกับฟิลด์นี้จะไม่ match ค่าว่างเงียบ ๆ ทำให้ workflow ทั้งเส้นไม่ยิงเลยโดยไม่มี error ใด ๆ** — เกิดซ้ำกับ `hr_ot_request.hr_ot_submitted_flag`/`hr_paid_flag` ระหว่างทดสอบ WF-HR-04 รอบแรก (`get_record_details`/`get_record_logs`/`get_approval_list_by_row` ว่างเปล่าทั้งหมด มีแค่ operator `user-api` จากคำสั่ง `update_record` ของ agent เอง) — root cause เดียวกับที่เคยเจอกับ `hr_leave_request.submitted_flag` ตอนสร้าง WF-HR-01 มาก่อนแล้ว **นี่คือครั้งที่ 2 ที่เจอ pattern เดียวกันบนตารางคนละใบ** ⇒ ตั้งเป็นกฎบังคับ: **ทุกฟิลด์ธง/Number ที่สร้างผ่าน workaround นี้ต้องตรวจ+ตั้ง `defaultValue` ทันทีหลังสร้าง ก่อนเริ่มสร้าง workflow ที่อ้างอิงฟิลด์นั้นเสมอ อย่ารอให้เจอบั๊กแล้วค่อยแก้** — เปิด D-14 (Roadmap) ให้ audit ฟิลด์ธงของ `hr_attendance` (สร้างพร้อมกันในรอบ P4-1) ก่อนเริ่ม WF-HR-05
- 🔴🔴 **[ใหม่ 27 ส.ค. 2569 (ต่อเนื่อง 2) — สำคัญมาก ใช้ได้ทุกโปรเจกต์ — เกิดซ้ำจากโมดูลบัญชี, งาน P4-5] ปุ่มที่สร้างผ่าน `create_custom_actions` มี Scope เริ่มต้น "Unassigned View" เสมอ — ไม่โผล่บนหน้าจอเลยจนกว่าจะเข้า Browser ตั้งเป็น "All Records" หรือ "Specified View"** — เจอกับดักเดียวกับที่ `tilsna-accounting/04-CLAUDE-memory.md` เคยบันทึกไว้ (26 ส.ค.) ตอนสร้างปุ่ม "ส่งอนุมัติ"/"ยกเลิกใบสำคัญ" บน `ac_voucher` — คราวนี้เจอซ้ำกับปุ่ม "ส่งขอล่วงเวลา" บน `hr_ot_request` (`create_custom_actions` คืน `success:true`+`actionId` ปกติ แต่เปิด record ไม่เห็นปุ่มเลย) ⇒ **ยืนยันว่าเป็นพฤติกรรมแน่นอน 100% ของทุกปุ่มที่สร้างผ่าน MCP ไม่มีข้อยกเว้น ไม่ใช่บั๊กเฉพาะโมดูลบัญชี** **ทางแก้ที่ใช้ซ้ำได้:** เปิด record ใดก็ได้ของตาราง → เมนู "..." → **Edit Custom Action** → หน้า Form Settings → Custom Action → คลิกลิงก์ **Scope** ท้ายแถวปุ่มนั้น → เลือก **All Records** (หรือ Specified View + view ที่ต้องการ) → บันทึกอัตโนมัติทันทีที่คลิก (ไม่มีปุ่ม Save แยก) ⇒ **ทุกปุ่มที่สร้างผ่าน `create_custom_actions` ต้องเข้า Browser ตั้ง Scope เป็นขั้นตอนที่สองเสมอ ก่อนจะนับว่า DoD "เปิด worksheet เห็นปุ่มโผล่จริงตาม enableWhen" ผ่าน** — ครั้งนี้ทำผ่าน **Claude in Chrome** สำเร็จ (พบว่ามี browser session ที่ login ค้างอยู่แล้วจาก `nocoly.com`, ไม่ต้อง login ใหม่ — เข้าแอปตรงได้ทันทีด้วย URL `/app/<appId>/<sectionId>/<worksheetId>`)
- ⚠️ **[ใหม่ 27 ส.ค. 2569 (ต่อเนื่อง) — พบระหว่างทดสอบ WF-HR-04] การ execute ของ workflow หลัง `update_record(triggerWorkflow:true)` ไม่ synchronous — ใช้เวลาประมาณ 5–8 วินาที** — อ่าน `get_record_details` ทันทีหลังยิง `update_record` บางครั้งได้ค่าเก่า (ยังไม่ทันรัน) ทำให้เข้าใจผิดว่า workflow ไม่ยิง ⇒ **วิธี verify ที่เชื่อถือได้:** รอสักครู่ก่อนอ่าน (`sleep`) หรือใช้ `get_record_logs` (มี `_createdAt` ละเอียดต่อการเปลี่ยนแปลง + `operator.accountId`="user-workflow" ยืนยันชัดเจนกว่า) ร่วมกับ `get_approval_list_by_row` (เห็น To-do ที่เกิดขึ้นจริงแบบ live) แทนการเชื่อ `get_record_details` ที่อ่านทันทีเพียงอย่างเดียว
- 🔴 **[ใหม่ 27 ส.ค. 2569 — พบระหว่าง P3-8/IX-08.1] Unique Index บนฟิลด์ที่ไม่ required: ระบบเองยืนยัน (tooltip) ว่าทั้งตารางมีได้แค่ 1 record ที่ฟิลด์นั้นว่าง** — ข้อความ tooltip จริงจากหน้า Search Acceleration → Create Index → hover ไอคอน (i) ข้าง "Unique Index": *"Once a unique index is established, the value of the field cannot be duplicated. If the field is not required, the entire worksheet can only have one empty record to ensure the uniqueness of the data."* ⇒ ฟิลด์แบบ `leave_no` (isTitle, Text ธรรมดา, ไม่ required, ไม่มีกลไก auto-generate ตอนสร้าง record) **ห้ามเปิด unique index ก่อนมีกลไกเติมค่าอัตโนมัติทันทีตอนสร้าง** ไม่งั้นจะสร้าง record ใหม่ (Draft) ที่ฟิลด์นั้นยังว่างได้แค่ใบเดียวทั้งระบบ ใบที่ 2 เป็นต้นไปจะถูกปฏิเสธทันที — ต้องตรวจแพทเทิร์นนี้ก่อนเปิด unique index ทุกครั้งที่ฟิลด์เป้าหมายไม่ required — **หมายเหตุเพิ่ม 27 ส.ค. 2569:** `hr_attendance.att_name` (isTitle, Text, ไม่ required — เขียนโดย workflow) และ `hr_ot_request.ot_no` (isTitle, Text, required=true) มีความเสี่ยงคล้ายกัน — `hr_attendance` ต้องมีกลไก auto-generate ก่อนเปิด unique index ใด ๆ บนฟิลด์นี้ (แต่ IX-10.1 จริงอยู่บน `employee`+`work_date` ซึ่งเป็น required ทั้งคู่อยู่แล้ว ไม่ติดกับดักนี้)

---

## สถานะงานล่าสุด (checklist — 🔴 sync กับ `05-Roadmap-Tracker.md` เสมอ)

| # | งาน | สถานะ |
|---|---|---|
| 1 | ดึงโครงสร้างจริงของจุดเชื่อมโมดูลบัญชี 6 ตาราง มาทำ ID Registry | ✅ 26 ส.ค. 2569 |
| 2 | ทำเอกสารชุด BRD / BuildSpec / RTM / Roadmap / memory / Demo | ✅ 26 ส.ค. 2569 |
| 3 | ยืนยันข้อสมมติ 18 ข้อ (A-HR-01…18) กับผู้ใช้ | ⬜ ใช้ค่า default ไปก่อน |
| 4 | **ขอเพิ่มบัญชีทดสอบ 4 บัญชี (G-06)** | ⬜ 🔴 **บล็อกการทดสอบทั้งหมด** |
| 5 | เพิ่ม option `PAYROLL_*` ใน `ac_posting_rule` (G-02) | ✅ **28 ส.ค. 2569 (ต่อเนื่อง 13) — ฝ่ายบัญชีทำแล้วผ่าน MCP** (`event_code` เป็น inline field ไม่ใช่ shared optionset ตามที่เข้าใจผิดเดิม) — `PAYROLL_ACCRUAL` key `ad4850c4-f1c1-4a65-adbb-fb0054a6e0c8` / `PAYROLL_PAYMENT` key `04a14b13-73c2-408b-a5fb-bb81ff630a06` |
| 6 | ยืนยันอัตราประกันสังคมและขั้นบันไดภาษี (G-05) | ⬜ |
| 7 | สร้างกลุ่มในแอป 8 กลุ่ม | ✅ 26 ส.ค. 2569 — `6a8ee668e56d2e6eb7bd6cb3` + 7 ลูก |
| 8 | สร้าง optionset 24 ชุด | ✅ 26 ส.ค. 2569 — ID + option key ครบใน BuildSpec §1.6 / §1.6.1 (ยืนยันด้วย `get_optionset_list`) |
| 9 | สร้างตารางตั้งค่า HR-00 (10 ตาราง) + seed | 🟢 **10/10 ตารางสร้างแล้ว + seed เสร็จ 9/10 ตาราง** 26 ส.ค. 2569 (ผ่าน `create_app_items`+`addFields` — ดู Known Issues) 55 record ยืนยันด้วย `get_record_pivot_data` COUNT · ID ครบใน BuildSpec §1.3/§2 · ⬜ `hr_approval_rule` ยังไม่ seed (รอ role §18) · ⬜ ค้าง 3 dropdown ที่ผูก shared optionset ต้องทำใน Browser (`carry_policy`/`day_type`/`employment_type`) · ⬜ `job_level`/`pay_component` relation รอสร้างตารางปลายทาง |
| 10 | สร้าง `hr_employee` + `hr_position` + `hr_job_level` (+ `hr_employment_contract`/`event`/`dependent`/`bank_account`) | 🟢 โครงสร้างครบ 26 ส.ค. 2569 — hr_position 8 seed + hr_job_level 6 seed + hr_employee 36 ฟิลด์ (ยืนยัน `get_worksheet_structure`) + 4 ตาราง P2-6 (contract/event/dependent/bank_account) สร้างครบ · ✅ `addRecordButtonName` (Data Name ไทย) ครบทุกตารางแล้ว 26 ส.ค. 2569 (4 ตาราง P2-6 พบเป็น `记录` ค้างและแก้แล้วรอบ 2 · position/job_level/employee ถูกต้องอยู่แล้วไม่ต้องแก้) · ⬜ ยังไม่ seed พนักงานทดสอบ EMP-T1…T4 (รอ G-06) · ⬜ ยังไม่ผูก Dropdown 4 ฟิลด์ (gender/marital_status/employment_type/emp_status) เข้า shared optionset (Browser-only ยืนยันแล้ว) |
| 11 | สร้างกลุ่มการลา 3 ตาราง (`hr_leave_balance`/`hr_leave_request`/`hr_leave_ledger`) | 🟢 **โครงสร้างครบ 26 ส.ค. 2569 (P3-1)** — สร้างผ่าน `create_app_items`→`update_worksheet`(removeFields+addFields รวมคำสั่งเดียว)→verify ทั้ง 3 ตาราง · ID ครบใน `02b-BuildSpec-Addendum-P3-1.md` · ✅ `addRecordButtonName` (Data Name ไทย) ตั้งครบแล้วทั้ง 3 ตาราง 26 ส.ค. 2569 · ✅ **seed `hr_leave_balance` ครบ 27 ส.ค. 2569 (P3-4)** — 6 ประเภท × 2 พนักงาน (`get_record_pivot_data` COUNT=6 ยืนยันทั้งคู่) · ⬜ `hr_leave_request`/`hr_leave_ledger` ยังไม่ seed เพิ่ม · ⛔ **ตัดสินใจไม่เปิด unique index (IX-08.1/IX-09.1)** ดูเหตุผลใน Known Issues (แถว #14 ด้านล่างมีรายละเอียดเดียวกัน) · ⬜ ยังไม่ผูก Dropdown 3 ฟิลด์เข้า shared optionset (`leave_unit`/`leave_status`/`ledger_type`) |
| 12 | **WF-HR-01 อนุมัติใบลา 2 ระดับ** | 🟢 **D-17 แก้ไขแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 16) — ไม่ใช่ปัญหาระบบ เป็นบั๊ก `submitted_flag` ว่างบน record ทดสอบ (defaultValue ไม่ถูกใช้ตอนสร้าง record ผ่าน API) — แก้แล้วยิงสำเร็จจริง ขั้นหัวหน้างานผ่าน มี To-do ถึง Wanadtapong.l อีกครั้ง ยืนยันว่าโครงสร้างทำงานปกติมาตลอด** — ก่อนหน้านั้น 28 ส.ค. 2569 (ต่อเนื่อง 11) ยิง record ทดสอบใหม่สะอาด (`TEST-LR-D15-REVERIFY`) ไม่ทำงาน → ทดสอบ sanity-check กับ WF-HR-04 ด้วย record ใหม่ (`TEST-OT-SYSCHK-01`) ก็ไม่ทำงานเช่นกัน → **สรุปว่าเป็นปัญหา session/environment/platform กว้างกว่า WF-HR-01** โครงสร้างทั้ง main+inner ตรวจสอบแล้วว่า published ปกติ ไม่มี warning — **ต้องรอ/ตรวจสอบใหม่ใน session ถัดไปก่อนเชื่อว่า workflow ใด ๆ (ไม่ใช่แค่ WF-HR-01) ทำงานได้จริง** — หลักฐานเดิม (27 ส.ค.) ที่เคยผ่าน: 🟢 **สร้าง+publish+ทดสอบยิงจริงสำเร็จ 27 ส.ค. 2569** — 3 process (main `6a8f36bc0e97bf440dda64eb` v3 + inner approval `6a8f373b730d20c5b7647131` ขั้นหัวหน้างาน + `6a8f3757730d20c5b7647392` ขั้น HR) · **สถาปัตยกรรม:** ออกแบบใหม่เป็น single-flow gate-branch (ไม่ใช้ nested approval_block ที่ไม่เคยพิสูจน์) — top-level `branch` "gate" แยกเส้นทางตาม `leave_status` ปัจจุบัน (`leg_supervisor`/`leg_hr`/`leg_default`) อาศัยการที่ workflow-internal update ค่า `leave_status` (เช่น → Pending HR) จะ re-trigger flow เดิมโดยอัตโนมัติเพราะ `triggerFields:[leave_status]` ⇒ ขั้น HR กลายเป็น top-level pass ใหม่แทนการ nest · **หลักฐานยิงจริง (ขั้นหัวหน้างาน):** seed พนักงานทดสอบ 2 คนผูกบัญชีจริง (`TEST-EMP1`→Kunlasatri.c `3664d2b8-b497-4aef-b349-20d33c007367` เป็นผู้ยื่น, `TEST-EMP2`→Wanadtapong.l `ede04ab2-d9cd-4906-a3da-68d0277e5e29` เป็นหัวหน้า) + `hr_leave_balance` 10 วัน (`56c008e2-bf03-4a6e-bf0e-2ccbb2fbf863`) → ยิง `hr_leave_request` `4a226f18-5f2b-4af4-b212-4c016f50b9ec` (Draft→Pending supervisor, `triggerWorkflow:true`) → `_updatedBy`=**`user-workflow`** ✅ · `submitted_flag`=1 · `approver_user`=Wanadtapong.l (ดึงถูกต้องจาก `hr_employee.supervisor_user`) ✅ · `balance_snapshot`=10.0 ✅ · `get_approval_list_by_row` มี To-do จริงส่งถึง Wanadtapong.l ✅ · **ระหว่างทางแก้บั๊ก 2 ตัวที่กลายเป็นบทเรียนใหม่ใน Known Issues** (rowid+in ใช้ไม่ได้กับ relation value ต้องใช้ reverse-relation+contains แทน · FieldPatch ลืมใส่ node เขียนค่าว่างเงียบ) · **ยังไม่ทดสอบ:** ขั้น HR (`leg_hr`/`appr_block_2`) — ต้องรอ `hr_approval_rule` seed (บล็อกด้วย role §18 ซึ่งบล็อกด้วย G-06) และต้องให้ **มนุษย์กด Approve จริงใน To-do ของ Wanadtapong.l** ก่อน (A-HR-15 ห้าม org-auth API กดแทน) เพื่อดันสถานะไป Pending HR แล้วดูว่า flow re-trigger เข้า `appr_block_2` ถูกจริงไหม · path ปฏิเสธ (`rej_no_supervisor`/`rej_overlap`/`rej_no_balance`/`rej_insufficient`) และ `chk_overlap`/`chk_balance`(relation-to-relation `in`) ยังไม่ถูกยิงผ่านในเคสจริง (ผ่านแค่ `br_remaining`/`sufficient` เคสเดียว) — ควรทดสอบเพิ่มก่อนถือว่าสมบูรณ์ 100% |
| 13 | WF-HR-02 / WF-HR-03 ตัด-คืนสิทธิลา | 🟢 **สร้าง+publish+ทดสอบยิงจริงสำเร็จทั้งคู่ 27 ส.ค. 2569** — WF-HR-02 `6a8fa97cfdab77a41c51cb3f` (publish v3, 17 node) · WF-HR-03 `6a8fab800e97bf440dddb744` (publish v1, 10 node) · ใช้ record ทดสอบเดิมจาก WF-HR-01 (`4a226f18-…` TEST-LR-0001 + balance `56c008e2-…` TEST-EMP1-ANNUAL-2026) · **ลำดับทดสอบและหลักฐาน:** ตั้ง `leave_status`=Approved → WF-HR-02 ตัดสิทธิ balance 10.0→9.0 (`_updatedBy`=`user-workflow`) + ledger แถวใหม่ (`ledger_days`=-1.0, `balance_after`=9.0, `ledger_type`=Leave taken) → ยิงซ้ำ Approved อีกครั้ง (ทดสอบ idempotency: `deducted_flag` กันซ้ำถูกต้อง balance/ledger ไม่เปลี่ยน) → ตั้ง `leave_status`=Cancelled → WF-HR-03 คืนสิทธิ balance 9.0→10.0 + ledger แถวที่ 2 (`ledger_days`=+1.0, `balance_after`=10.0, `ledger_type`=Leave returned) · ledger รวม 2 แถวคู่กันตาม DoD ทั้งคู่ relation ชี้ `balance`/`leave_request` ถูกต้อง (แสดงชื่อจริง ไม่ใช่ `已删除`) · **เลื่อนออกจากขอบเขต (แก้แล้ว — ดูแถว #15):** ตัด node sync สถานะ "On leave" เข้า `hr_attendance` ออกจากทั้ง 2 workflow (ตารางยังไม่มี รอ P4-1) — บันทึกเป็น D-8 ใน Roadmap · **พบบั๊กแพลตฟอร์มใหม่ 2 ตัวระหว่างทาง แก้ได้ทั้งคู่ก่อน publish จริง** (ดู Known Issues 2 ข้อใหม่ด้านบน: compute/get_single ไม่ใช่ snapshot-freeze ต้องเรียง add_record ledger ก่อน update_record balance เสมอ · `kind:"record"` เขียน Relation field ได้ค่าขยะ ต้องใช้ `kind:"field"`+`fieldId:"rowid"` แทน) |
| 14 | **P3-7 (ปุ่ม+view) + P3-8 (Business Rules/DV-08.1/IX-08.1)** | ✅/🔶 **27 ส.ค. 2569** — **P3-7 เสร็จ+ยืนยัน:** ล้าง ghost views ซ้ำ 8 อันจาก Agent tool call ที่ถูก reject ก่อนหน้า (เหลือ 5 view ถูกต้อง) · ยืนยันปุ่ม "ส่งคำขอ"/"ยกเลิกใบลา" render ตาม `enableWhen` จริง (toggle `leave_status` ผ่าน MCP แล้วเปิดฟอร์มดู) · **P3-8 บางส่วนเสร็จ:** ✅ BR-08.1 (validation `leave_to`<`leave_from`)/BR-08.4 (interaction read-only all เมื่อ≠Draft)/BR-08.5 (interaction hide `leave_to`/show `leave_hours` เมื่อ≠Full day) สร้างและ**ยืนยันผ่าน Role Debugging (role Member)** แล้วทั้ง 3 ข้อ — เปิด record จริงเห็นทั้งฟอร์ม read-only เมื่อ Cancelled และเห็น hide/show ถูกต้องเมื่อสลับ `leave_unit`=Hourly ชั่วคราว · ⛔ BR-08.2/BR-08.3 ยืนยันซ้ำว่าทำเป็น Business Rule ไม่ได้ (relation-field lookup ไม่รองรับ) ต้องย้ายไป WF-HR-01/WF-HR-06 · ⛔ **DV-08.1 พบว่าทำผ่าน Dynamic Default UI ไม่ได้เช่นกัน** (field picker ไม่แสดง Relation field ของฟอร์มปัจจุบันให้เลือกเป็นค่าเปรียบเทียบ) → บันทึกเป็น D-9 ย้ายไป WF-HR-01 · ⛔ **IX-08.1 ตัดสินใจไม่เปิดใช้** (tooltip ระบบยืนยัน: ฟิลด์ไม่ required เปิด unique index ได้แค่ 1 record ว่างทั้งตาราง จะบล็อกการสร้าง Draft ใบที่ 2) → บันทึกเป็น D-10 รอ auto-generate `leave_no` ก่อน — ดู Known Issues 4 ข้อใหม่ (ghost object หลัง reject · Dynamic Value ต้องใช้คีย์บอร์ด · Escape ทิ้งข้อมูลในแผงกฎ · relation-field ใช้เป็น dynamic value ไม่ได้ทั้ง BR และ DV) |
| 15 | **P3-4 seed hr_leave_balance + P4-1/P4-2 กลุ่มลงเวลา/OT** | ✅/🔶 **27 ส.ค. 2569** — **P3-4 เสร็จ+ยืนยัน:** seed `hr_leave_balance` ครบ 6 ประเภท × TEST-EMP1/TEST-EMP2 (`get_record_pivot_data` COUNT=6 ยืนยันทั้งคู่) ผ่าน `batch_create_records`(`triggerWorkflow:false`) · **P4-1/P4-2 โครงสร้างเสร็จ+ยืนยัน:** สร้าง `hr_ot_request` (`6a8fcca48b6633ef76f13033`, 16 ฟิลด์ตาม FR-HR-11B) ก่อน แล้ว `hr_attendance` (`6a8fcd67353e1b0e4a507d32`, 16 ฟิลด์ตาม FR-HR-10 รวม relation `ot_request`→`hr_ot_request`) ผ่าน workaround `create_app_items`→`update_worksheet`(remove+add)→verify เดิม · ตั้ง view "ทั้งหมด" + `addRecordButtonName` ไทยครบทั้ง 2 ตาราง (ยืนยัน `addRecordButtonName` ยังใช้งานได้แม้ไม่อยู่ใน schema/CLI help) · แก้ `hr_ot_request.day_type` ที่สร้างผิดเป็นภาษาไทยครั้งแรกให้เป็นอังกฤษตรงกับ optionset กลาง · 🔶 **D-8 ลองทำแล้ว 27 ส.ค. 2569 (รอบเย็น) แต่ชนบั๊ก `sub_process`→`NodeAppIsNull` — revert กลับ publish v4(WF-HR-02)/v2(WF-HR-03) เดิมปลอดภัยแล้ว** ดู Known Issues + D-13/G-13 · ⬜ ยังไม่ทำ: unique index IX-10.1/IX-11.1 (Browser-only) · Business Rules BR-10.x/BR-11.x · WF-HR-04/WF-HR-05 (ทั้งคู่ยังทำไม่ได้ในส่วนที่ต้องใช้ `sub_process`) · dropdown rebind เข้า shared optionset จริง (D-12) |
| 16 | **D-8 sync "On leave" เข้า `hr_attendance`** | 🟢 **ปลดบล็อกแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 10)** — D-13 คลี่คลาย (root cause = ไม่ได้ publish inner ก่อน ไม่ใช่บั๊กแพลตฟอร์ม — ดู Known Issues) — พร้อมกลับมาสร้าง node sync "On leave" ใหม่ในรอบถัดไป โดยต้อง publish inner sub_process ก่อน publish outer เสมอ — ⚠️ **หมายเหตุ D-17 (ต่อเนื่อง 11): สร้าง/publish node ได้ปกติ แต่การทดสอบยิงจริงจะพิสูจน์ไม่ได้จนกว่า D-17 (worksheet_event ทั้งระบบไม่ยิง) จะคลี่คลาย** |
| 28 | **P4-4 (WF-HR-05) / P4-6 (WF-HR-06)** | 🟢 **สร้าง+publish สำเร็จจริง 28 ส.ค. 2569 (ต่อเนื่อง 12)** — WF-HR-05 main `6a910184730d20c5b7710fa8` v1 + inner `6a910197730d20c5b7711028` v1 · WF-HR-06 main `6a910222730d20c5b77115e5` v1 + inner `6a9102345f8564a68c449ee7` v1 — validate/publish ทั้ง 4 process สำเร็จ 0 error ยืนยัน D-16 fix ใช้ได้กับ build จริง — ปรับโครงสร้างจากดราฟต์เดิม (ใช้ `code` node แทน Duration/Function calculation เพราะ `hr_shift` เวลาเป็น Text · ตัด "get associated shift" เพราะมี `hr_att_shift` อยู่แล้ว · ส่ง SLA/grace minutes เข้า sub_process ผ่าน subprocess parameter) — **WF-HR-06 v1 จำกัดเหลือแค่ `hr_leave_request`** เพราะพบ gap ใหม่ D-18 (`hr_ot_request` ไม่มีฟิลด์ `submitted_at`) — ⚠️ **ยังไม่ได้ live-fire test เพราะ D-17** (schedule trigger ยังไม่เคยพิสูจน์ว่ากระทบด้วยหรือไม่ — ต้องตรวจตอนถึงรอบจริงหรือกด Execute now) — รายละเอียดเต็มดู Known Issues ต่อเนื่อง 12 และ `02-BuildSpec-FRS.md` |
| 17 | กลุ่มเงินเดือน 7 ตาราง + WF-HR-07…10 | 🔶 **P5-1+P5-2+P5-3+P5-4(บางส่วน) 28 ส.ค. 2569** — โครงสร้างครบ 7/7 ตาราง (`hr_pay_component`/`hr_pay_period`/`hr_salary_structure`/`hr_payslip`/`hr_payslip_line`/`hr_pnd1_filing`/`hr_wht_cert`) ผ่าน `create_app_items`→`update_worksheet`(remove+add)→verify เดิม รวม relation bidirectional ครบ 14 จุด + `addRecordButtonName` ไทยครบทั้ง 7 · seed `hr_pay_component` 6 รายการแล้ว (P5-4) ผูก `coa_account` ได้ 3/6 (ดู checklist #27 และ Known Issues ต่อเนื่อง 8) (ดู "ID ตารางเงินเดือน (P5-1)"/"ID ตารางสลิปเงินเดือน (P5-2)"/"ID ตารางภาษี/หนังสือรับรอง (P5-3)" ด้านบน) · ⬜ ยังไม่ทำ: unique index IX-12.1…12.6 ทั้งชุด (Browser-only) · dropdown rebind เข้า shared optionset (`OS_HR_COMPONENT_TYPE`/`OS_HR_COMPONENT_CALC`/`OS_HR_PAY_PERIOD_STATUS`/`OS_HR_PAYSLIP_STATUS`/`OS_HR_FILING_FORM`/`OS_HR_FILING_STATUS`/`OS_LEGAL_FORM`/`OS_HR_CERT_STATUS`) · ✅ seed P5-6 บางส่วนแล้ว (2/4 คน — ดูแถว #29) · WF-HR-07…10 + WF-HR-20 ทั้งหมด |
| 24 | **P5-1 สร้าง `hr_pay_period`/`hr_pay_component`/`hr_salary_structure`** | 🔶 **27 ส.ค. 2569** — สร้างทั้ง 3 ตารางด้วยวิธี `create_app_items`(type worksheet)→`update_worksheet`(removeFields 3 default + addFields ครบ)→`get_worksheet_structure` verify เดิมที่ใช้มาตั้งแต่ P1 · ลำดับสร้าง: `hr_pay_component` (11 ฟิลด์, ไม่มี dependency นอกจาก `ac_coa`) → `hr_pay_period` (22 ฟิลด์, อ้าง `ac_period`+`ac_voucher`) → `hr_salary_structure` (9 ฟิลด์, อ้าง `hr_employee`+`hr_pay_component`) · relation ทั้งหมดตั้ง `bidirectional:true` สำเร็จ (reverse field auto-created 5 จุด) · flag/total Number field ทุกตัวตั้ง `defaultValue:0` เชิงป้องกันตั้งแต่แรก (ใช้บทเรียนจาก checklist #22 ที่เจอปัญหานี้ซ้ำ 2 ครั้งกับ `hr_ot_request`) — ไม่ต้องแก้ทีหลังเหมือนรอบก่อน · `addRecordButtonName` ตั้งสำเร็จทั้ง 3 ตาราง · **02-BuildSpec-FRS.md §1.3/FR-HR-12 sync แล้วด้วย ID จริงจาก `get_worksheet_structure`** (แก้ไฟล์ที่เคยเผลอ truncate เนื้อหาส่วนอื่นทิ้งระหว่างแก้ — restore กลับครบ 100% แล้วก่อน push) · ⬜ **ยังไม่ทำ:** unique index IX-12.1/12.2 (Browser-only) · dropdown 3 ฟิลด์ยังเป็น inline options ไม่ได้ผูก shared optionset จริง · `hr_leave_type.pay_component`/`hr_leave_policy.job_level` relation ที่ค้างจาก FR-HR-07 (ตารางปลายทางพร้อมแล้ว แต่ยังไม่เพิ่มฟิลด์) · `hr_ot_request.pay_period` relation ที่ค้างจาก P4-2 (ตอนนี้ทำได้แล้วเช่นกัน) · seed record P5-4/P5-6 |
| 25 | **P5-2 สร้าง `hr_payslip`/`hr_payslip_line`** | 🔶 **27 ส.ค. 2569** — สร้างด้วยวิธี `create_app_items`(type worksheet)→`update_worksheet`(removeFields 3 default + addFields ครบ)→`get_worksheet_structure` verify เดิม · `hr_payslip` 25 ฟิลด์ (รวม reverse relation อัตโนมัติ 1) + `hr_payslip_line` 8 ฟิลด์ · relation ทั้งหมดตั้ง `bidirectional:true` สำเร็จ (reverse field auto-created 6 จุด รวม `hr_payslip_line.payslip`→`hr_payslip` self ที่สร้าง reverse บน `hr_payslip` อัตโนมัติ) · flag/total ทุกตัวตั้ง `defaultValue:0` เชิงป้องกันตั้งแต่แรก · เงินทุกยอดใช้ `Currency`(THB) แทน `Number` ตามสเปกเดิม (ดีกว่าเพราะแสดงหน่วยเงินชัดเจน) · `payslip_no` ใช้ `AutoNumber` (prefix "PS-"+yyyyMM+sequence4) แทน `Text` เดิม · `addRecordButtonName` ตั้งสำเร็จทั้ง 2 ตาราง · **02-BuildSpec-FRS.md §1.3/FR-HR-12 12D/12E sync แล้วด้วย ID จริง** · ⬜ **ยังไม่ทำ:** unique index IX-12.3 (Browser-only) · `payslip_status` ยังเป็น inline options ไม่ได้ผูก `OS_HR_PAYSLIP_STATUS` จริง (ต้องเทียบ label "Published"↔"Paid" ก่อนผูก) · Business Rules BR-12.4/12.5 · Dynamic default snapshot ของ `cost_center` · WF-HR-07/08 |
| 26 | **P5-3 สร้าง `hr_pnd1_filing`/`hr_wht_cert`** | 🔶 **27 ส.ค. 2569** — สร้างด้วยวิธี `create_app_items`→`update_worksheet`(removeFields 3 default + addFields ครบ)→`get_worksheet_structure` verify เดิม · `hr_pnd1_filing` 13 ฟิลด์ (รวม reverse relation อัตโนมัติ 1) + `hr_wht_cert` 9 ฟิลด์ · relation ทั้งหมดตั้ง `bidirectional:true` สำเร็จ (`pay_periods` เป็น multi-relation subType 2 ไปยัง `hr_pay_period`) · `total_income`/`total_wht` ทั้ง 2 ตารางใช้ `Currency`(THB) แทน `Number` ตามสเปกเดิม (สอดคล้อง P5-2) · `addRecordButtonName` ตั้งสำเร็จทั้ง 2 ตาราง · **กลุ่มเงินเดือนครบ 7/7 ตารางแล้ว** · BuildSpec §1.3/FR-HR-12 12I/12J sync แล้วด้วย ID จริง · ⬜ **ยังไม่ทำ:** unique index IX-12.5/12.6 (Browser-only) · dropdown 4 ฟิลด์ยังเป็น inline options ไม่ได้ผูก shared optionset จริง (`form_type`/`filing_status`/`payee_legal_form`/`cert_status`) · seed record P5-4/P5-6 · WF-HR-07…10/WF-HR-20 ทั้งหมด |
| 29 | **P5-6 seed `hr_salary_structure`** | 🔶 **2/4 คน — 28 ส.ค. 2569 (ต่อเนื่อง 15)** — seed TEST-EMP1 (30,000 บาท, rowId `f36de845-596d-4a87-b8b1-55f19f1bd790`) และ TEST-EMP2 (35,000 บาท, rowId `14efbacf-3f36-426d-a98a-7d714cebb14f`) ผ่าน `batch_create_records`(`triggerWorkflow:false`) ผูก `biz_pay_component`=SALARY, `biz_effective_from`=2026-01-01, `biz_is_active`=1 — ยืนยันด้วย `get_record_list` เห็น relation employee/pay component ถูกต้อง — ตัวเลขเป็นค่าทดสอบสมมติ ไม่ใช่เงินเดือนจริง · ⬜ เหลือ TEST-EMP3/4 รอ P2-6 seed พนักงานทดสอบเพิ่ม (บล็อกบางส่วนโดย G-06) · **P5-5 พิจารณาแล้วตัดสินใจไม่ทำในรอบนี้ — schema `ac_posting_rule` รองรับแค่ debit/credit เดียวต่อ 1 rule แต่ WF-HR-10 ต้องการใบสำคัญ 5 บรรทัด ต้องรอตัดสินใจออกแบบร่วมกับฝ่ายบัญชี/ผู้ใช้ก่อน ไม่เดาเอง** |
| 27 | **P5-4 seed `hr_pay_component` 6 รายการ + ผูก `coa_account`** | ✅ **ปิดครบ 28 ส.ค. 2569 (ต่อเนื่อง 13)** — seed ผ่าน `batch_create_records`(`triggerWorkflow:false`) สำเร็จ 6/6 record: `SALARY`/`OT`/`SSO_ER`/`SSO_EE`/`WHT`/`ABSENCE_DEDUCT` — verify ด้วย `get_record_list` ผ่านครบทุกฟิลด์ (type/calc_method เป็น option key ตรงกับที่ตั้งใจ, taxable/sso_base/active ตรงตามออกแบบ) · ผูก `biz_coa_account`→`ac_coa` ครบ **6/6**: `SALARY`/`OT`/`ABSENCE_DEDUCT`→`530101 เงินเดือน` (26-27 ส.ค.) และ **28 ส.ค. 2569 (ต่อเนื่อง 13) ผูกอีก 3 รายการหลังฝ่ายบัญชีสร้างบัญชีให้:** `SSO_ER`→`530102`, `SSO_EE`→`210402`, `WHT`→`210304` ผ่าน `update_record`(`triggerWorkflow:false`) + ยืนยันด้วย `get_record_details` เห็นชื่อบัญชีจริง — **AC-P5-4 ("ทุก component มี `coa_account`") ผ่านเต็ม 100% แล้ว** · ⬜ ยังไม่ทำ: seed P5-6 (โครงสร้างเงินเดือนพนักงานทดสอบ) |
| 18 | กลุ่มสวัสดิการ + WF-HR-11 / WF-HR-12 | ⬜ |
| 19 | กลุ่มสรรหา/ประเมิน/อบรม + WF-HR-15…19 | ⬜ |
| 20 | สร้าง role 8 บทบาท + permission matrix | ⬜ ⛔ ต้องมี #4 ก่อน |
| 21 | รายงาน · print template · App Lock | ⬜ |
| 22 | **P4-3 / WF-HR-04 อนุมัติ OT 2 ระดับ** | 🟢 **D-17 แก้ไขแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 16) — `hr_ot_submitted_flag` ของ `TEST-OT-SYSCHK-01` ว่างจริงทั้งที่เคยตั้ง defaultValue:0 ไว้แล้ว (D-14) เพราะ defaultValue ไม่ถูกใช้ตอนสร้าง record ผ่าน API — แก้แล้วยิงสำเร็จจริง ขั้นหัวหน้างานผ่าน มี To-do ถึง Wanadtapong.l** — หลักฐานเดิม (27 ส.ค.) ที่เคยผ่าน: 🟢 **สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ 27 ส.ค. 2569 (ต่อเนื่อง)** — main `6a8fd8d35f8564a68c3c1909` v2 · inner supervisor `6a8fd9325f8564a68c3c1cec` v1 (แชร์ร่วม 2 เส้นทาง day-type ผ่าน `mode:"use_existing"`) · inner HR `6a8fd9325f8564a68c3c1ced` v1 (สร้างแล้วยังไม่ทดสอบ รอ role/G-06) · เพิ่มฟิลด์ `hr_ot_rate.day_type` ที่ขาดหายไปสำเร็จผ่าน API ก่อนเริ่ม · ทดสอบยิงจริง 3 เคส ยืนยัน AC-19 ส่วนแรกผ่าน (ดู ID Workflow WF-HR-04 ด้านบนสำหรับรายละเอียดเต็ม) · พบ+แก้บั๊กใหม่ 2 ตัว (`dateDiff` precision, defaultValue ฟิลด์ธง — ดู Known Issues) + คิดค้นเทคนิคแชร์ inner approval_block |
| 23 | **P4-5 ปุ่ม "ส่งขอล่วงเวลา" + view OT รออนุมัติ/ลงเวลาผิดปกติ** | ✅ **สร้าง+verify ผ่านจริง 27 ส.ค. 2569 (ต่อเนื่อง 2)** — ปุ่ม `6a8fde0aae2a0e3743a0c62f` + view `6a8fde0b353e1b0e4a50803e`/`6a8fde0c1378964f9984a42c` สร้างผ่าน MCP สำเร็จ · เจอกับดัก Custom Action Scope="Unassigned View" ซ้ำจากโมดูลบัญชี — แก้ผ่าน **Claude in Chrome** (มี session login ค้างอยู่แล้ว) · verify ครบด้วยการ toggle สถานะ record ทดสอบชั่วคราวแล้วดูปุ่มโผล่จริงตาม `enableWhen` (ดู ID ปุ่ม+view P4-5 ด้านบน) |

---

## โหมดส่งมอบให้ `nocoly-hybrid-builder-v2` (Handoff)

- **ข้าม Phase 1 Interview** — spec ตอบครบแล้ว · ถ้าขาดค่าใด ถามเฉพาะจุดนั้นแล้ว **เขียนกลับ BuildSpec ทันที**
- **Phase 2 Confirm จาก spec** — สรุป checklist + Mermaid ของ workflow ที่จะทำ **จาก BuildSpec §3** ให้ผู้ใช้ยืนยัน ไม่ต้องถามใหม่
- **Phase 3 ทำตามคอลัมน์ Surface** ใน §1.8 / §1.9 / §3 · หลัง `create_*` ทุกครั้ง **อ่าน ID กลับแล้วเติมแทน `<TBD>` ใน §1 และ §2 ทันที** (สำหรับ FR-HR-08/09 ให้เติมใน `02b-BuildSpec-Addendum-P3-1.md` แทนไฟล์หลัก — ดูหมายเหตุในไฟล์นั้น)
- **ลำดับการสร้างที่ปลอดภัย** อยู่ใน BuildSpec §6 — relation ต้องมีปลายทางก่อนเสมอ
- ปิดงาน ✅ ได้เมื่อ Test recipe ของงานนั้นผ่านตาม BuildSpec §5 (`_updatedBy` = `user-workflow` + approval ถ้ามี) แล้วอัปเดต `05-Roadmap-Tracker.md` + `03-RTM-Status.md` + checklist ข้างบนให้ตรงกัน

---

## กฎทำงาน (สั้น)

1. เปิด **BuildSpec §1 ID Registry** (และ `02b-BuildSpec-Addendum-P3-1.md` สำหรับ 3 ตารางใบลา) ก่อนพิมพ์ ID ใด ๆ — ID ของ HR ที่เหลือยังเป็น `<TBD>` · ID ของบัญชีใช้ได้เลย
2. `get_app_info` ยืนยันแอปก่อน write แรกของทุก session
3. **อ่านหัวข้อ "กับดัก `update_worksheet`" ให้จบก่อนแตะฟิลด์ทุกครั้ง**
4. **Backup ก่อนงานที่แตะโครงสร้างตารางทุกครั้ง** (NFR-HR-13)
5. **Verify ก่อน claim** เสมอ — ผลว่างจาก tool ≠ ไม่มีของ
6. แก้ object แล้วอัปเดต BuildSpec (หรือ addendum ที่เกี่ยวข้อง) + ไฟล์นี้ **ทันที**
7. ห้ามเลื่อนงานเป็น ✅ ถ้ายังไม่มีหลักฐาน — ใช้ ⚠️ จนกว่าจะพิสูจน์ได้
8. บทเรียนใหม่ที่ค้นพบ (กับดัก UI/API) เขียนกลับหัวข้อ Known Issues ของไฟล์นี้ทันที
9. 🆕 **หลัง Agent tool call ใด ๆ ถูก reject/cancel กลางคัน ให้เปิดหน้าจอตรวจ object ที่เกี่ยวข้องว่ามีของค้าง (ghost object) หรือไม่ก่อนเชื่อว่าสถานะสะอาด**
</content>
