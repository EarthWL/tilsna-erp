# Roadmap & Progress Tracker — โมดูล HR · TILSNA ERP
> ไฟล์ติดตามความคืบหน้า **ทั้งโปรเจกต์** แบบรวมศูนย์ — build agent ต้องอัปเดต**ทุกครั้งที่เริ่มหรือปิดงาน** แล้ว sync ตาราง "สถานะงานล่าสุด" ใน `04-CLAUDE-memory.md` ให้ตรงกัน
> อัปเดตล่าสุด: **30 สิงหาคม 2569 (ต่อเนื่อง 20)** · อัปเดตโดย: build agent (hap CLI + MCP) — ✅ **D-19 ปิดแล้ว 5 workflow** (`"" ne "1"` = FALSE พิสูจน์ด้วย A/B controlled test → patch ธงว่างเป็น 0 + เพิ่ม OR group `is empty` เข้า **WF-HR-01/02/03/04 branch + WF-HR-05 filter** แล้ว publish ครบ) · ยิงจริงยืนยัน 3/5 (ตัดสิทธิ ✅ คืนสิทธิ ✅ เปิด approval ✅) · **WF-HR-04/05 แก้แล้วแต่ยังไม่ได้พิสูจน์** · 🔴 บทเรียน: รอบแรกสรุปว่า "WF-HR-05 ไม่กระทบ" **ผิด** เพราะ `hap workflow structure` ไม่คืน `filters` ของ node ชนิด search/get_multiple ⇒ **สแกนเงื่อนไขต้องใช้ `hap workflow node get` ทีละ node** · ✅ ปิด MIGRATION §A/§B/§C ฝั่ง HR (memory 217 KB → 93 KB โดยย้ายของที่อยู่ผิดที่ ไม่ใช่ย่อเนื้อหา · แยก `17-ID-Registry-HR.md`) · ✅ เติม **WF-HR-05/06 เข้า ID Registry** ที่ตกหล่นมาตั้งแต่ 28 ส.ค. (เจอจาก handoff ฝั่งบัญชี) · รายละเอียดรายหัวข้ออยู่ใน §11 (D-13…D-19) และ Change Log ท้ายไฟล์
>
> 📜 _ประวัติอัปเดตรอบก่อน ๆ (ต่อเนื่อง 1–19) เคยซ้อนอยู่ในบรรทัดนี้ 9 ชั้น = 12,406 bytes (89% ของบรรทัด) — **ยุบเมื่อ 30 ส.ค. 2569** ตามแพทเทิร์นที่ agent-ac เสนอใน `handoff/AC-DOC-SLIM.md` §2 · **ตรวจก่อนตัดแล้ว: ตัวระบุทุกชนิดในข้อความที่ตัด (ID 24hex/uuid · D-nn · G-nn · Pn-n · WF-xx-nn · BR/IX/DV/AC-nn · alias `hr_`/`ac_` · `TEST-*`) รวม 43 ตัว มีอยู่ที่อื่นครบทุกตัว ไม่มีตัวไหนหายถาวร** (ข้อจำกัด: พิสูจน์ได้แค่ "ตัวระบุมีที่อื่น" ไม่ได้พิสูจน์ว่าคำอธิบายรอบนั้นมีที่อื่น) · อ่านข้อความเต็มย้อนหลัง: `git log -p --follow -- projects/hr/05-Roadmap-Tracker.md` · 🔴 **กฎตั้งแต่นี้ไป: บรรทัดนี้เก็บรอบล่าสุดรอบเดียว ห้ามซ้อนประวัติอีก — ประวัติอยู่ใน git และ Change Log §ท้ายไฟล์**_
> สถานะ: ⬜ ยังไม่เริ่ม · 🚧 กำลังทำ · 🔶 บางส่วน · ⚠️ มีแต่ยังไม่พิสูจน์ทำงาน · ✅ เสร็จ+ยืนยัน · ⛔ ถูกบล็อก
> 🔴 **ห้ามเลื่อนงานเป็น ✅ ถ้ายังไม่มีหลักฐาน verify** — ใช้ ⚠️ จนกว่าจะพิสูจน์ได้

---

## 1. ภาพรวมความคืบหน้า

| เฟส | ชื่อเฟส | งานทั้งหมด | ✅ | 🚧 | ⬜ | ⛔ | % |
|---|---|---|---|---|---|---|---|
| P0 | เตรียมความพร้อมและปลดบล็อก | 6 | 1 | 0 | 5 | 0 | 17% |
| P1 | โครงสร้างพื้นฐานและตารางตั้งค่า | 5 | 3 | 1 | 1 | 0 | 70% |
| P2 | Core HR — ทะเบียนพนักงาน | 6 | 2 | 0 | 4 | 0 | 33% |
| P3 | การลา (โมดูล demo แรก) | 8 | 5 | 2 | 1 | 0 | 62% |
| P4 | ลงเวลาและล่วงเวลา | 6 | 4 | 0 | 2 | 0 | 67% |
| P5 | เงินเดือนและภาษี | 11 | 1 | 1 | 8 | 1 | 9% |
| P6 | สวัสดิการและการเบิกจ่าย | 6 | 0 | 0 | 6 | 0 | 0% |
| P7 | สรรหา ประเมิน อบรม | 8 | 0 | 0 | 8 | 0 | 0% |
| P8 | สิทธิ์ รายงาน เอกสาร go-live | 8 | 0 | 0 | 5 | 3 | 0% |
| **รวม** | | **64** | **16** | **4** | **41** | **3** | **25%** |

**Critical path:** P0#2 (บัญชีทดสอบ) → P2 (ทะเบียนพนักงาน) → P3 (การลา) → P4 (ลงเวลา) → P5 (เงินเดือน) → P8 (สิทธิ์)
**สิ่งที่บล็อกหนักที่สุดตอนนี้:** มีบัญชีผู้ใช้ในองค์กรเพียง 2 บัญชี ⇒ ทดสอบการอนุมัติ 2 ระดับและสิทธิ์ 8 บทบาทไม่ได้เลย
**ปลดบล็อกสำเร็จ 26 ส.ค. 2569:** `create_worksheet` ยังพังอยู่ แต่พบทางเลือกสำรองที่ใช้งานได้ 100% (`create_app_items`→`update_worksheet`[removeFields+addFields รวมคำสั่งเดียว]) — ใช้สร้าง HR-00 ครบ 10 ตาราง และล่าสุด 3 ตารางใบลา (P3-1) สำเร็จอีกครั้ง
**ปลดบล็อกเพิ่ม 27 ส.ค. 2569:** WF-HR-02/WF-HR-03 สร้าง+publish+ทดสอบยิงจริงสำเร็จ — พบและแก้บั๊กแพลตฟอร์มใหม่ 2 ตัวระหว่างทาง (ดู Change Log และ `04-CLAUDE-memory.md`/`nocoly-shared/00-HAP-Working-Guide.md`)
**พบข้อจำกัดแพลตฟอร์มใหม่ 27 ส.ค. 2569 (P3-8):** ทั้ง Business Rules และ Dynamic Default ("Query worksheet") ไม่รองรับการอ้างอิง **Relation-type field ของฟอร์มปัจจุบัน** เป็นค่าเปรียบเทียบ (Dynamic Value) เลย — field picker ไม่แสดงให้เลือก ⇒ lookup ข้ามตารางที่อ้างอิง relation field ต้องย้ายไปทำที่ระดับ workflow เสมอ (ดูรายละเอียดในแถว P3-8 และ Known Issues ของ `04-CLAUDE-memory.md`)
**ทำต่อ 27 ส.ค. 2569 (ตามคำขอผู้ใช้ "เน้นงานที่ผ่าน MCP/CLI ได้ก่อน"):** P3-4 seed ครบ · P4-1 `hr_attendance` และ P4-2 `hr_ot_request` สร้างโครงสร้างครบผ่าน MCP + hap CLI (view rename) — ดูรายละเอียดแถว P4-1/P4-2 และ Change Log
**ทำต่อ 27 ส.ค. 2569 (ต่อเนื่อง — หลัง D-8/D-13 บล็อก เปลี่ยนไปทำงานที่ไม่ต้องใช้ `sub_process`):** เพิ่มฟิลด์ `hr_ot_rate.day_type` ที่ขาดหายไปสำเร็จผ่าน `addFields`+`editFields` (ไม่ต้องรอ Browser) · **P4-3/WF-HR-04 (อนุมัติ OT 2 ระดับ) สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ** ด้วยสถาปัตยกรรม gate-branch เดียวกับ WF-HR-01 บวกเทคนิคใหม่ "share inner approval flow ข้าม 2 เส้นทาง day-type ผ่าน `mode:use_existing`" — พบ+แก้บั๊กใหม่ `dateDiff` compute node ตัด `precision` ทิ้งเงียบ ๆ (workaround: คำนวณเป็นนาทีก่อนด้วย `dateDiff`, แล้วหารเป็นชั่วโมงด้วย compute node ชนิด `number` ที่มี `precision` แยกอีกตัว) — ดูรายละเอียดแถว P4-3 และ Change Log

---

## 2. P0 — เตรียมความพร้อมและปลดบล็อก 🔴 ทำก่อนทุกอย่าง

| # | งาน | สถานะ | effort | ขึ้นกับ | ผู้รับผิดชอบ | หลักฐานที่ต้องมี |
|---|---|---|---|---|---|---|
| P0-1 | ยืนยันข้อสมมติ 18 ข้อ (A-HR-01…18) กับผู้ใช้ แล้วเขียนกลับ BRD §11 | ⬜ | S | — | ผู้ใช้ + agent | BRD §11 ไม่มีคำว่า "ยังไม่ได้รับการยืนยัน" |
| P0-2 | **ขอเพิ่มบัญชีผู้ใช้ทดสอบ 4 บัญชี** (พนักงาน · หัวหน้า · HR · Payroll) | ⬜ | S | ผู้ดูแลองค์กร | ผู้ใช้ | `get_role_list` เห็นบัญชีใหม่ |
| P0-3 | เพิ่ม option `PAYROLL_ACCRUAL` / `PAYROLL_PAYMENT` ใน `ac_posting_rule.event_code` | ✅ **28 ส.ค. 2569 (ต่อเนื่อง 13) — ฝ่ายบัญชีทำแล้ว** | S | Backup ก่อน | ฝ่ายบัญชี (ทีม tilsna-accounting) | ✅ **แก้ไข: ไม่ใช่ Browser-only อย่างที่ HR เข้าใจผิดตอนแรก** — `event_code` เป็น **inline SingleSelect** (fieldId `6a85518c055f2288c5b7430b`) ไม่ใช่ shared optionset จึงแก้ผ่าน MCP `editFields` ได้ตรง ๆ — ฝ่ายบัญชีเพิ่มให้แล้ว: `PAYROLL_ACCRUAL` (key `ad4850c4-f1c1-4a65-adbb-fb0054a6e0c8`), `PAYROLL_PAYMENT` (key `04a14b13-73c2-408b-a5fb-bb81ff630a06`) — อ้างอิง `tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md` |
| P0-4 | เพิ่ม option สำหรับ HR ใน `ac_voucher.source_doc_type` **ผ่านหน้าจอ** (ทางเลือก — ถ้าไม่ทำใช้ `source_module` แทน) | ⬜ | S | Backup | agent | เห็น option ใหม่ |
| P0-5 | ยืนยันอัตราประกันสังคม เพดานฐานค่าจ้าง และขั้นบันไดภาษีที่บังคับใช้ กับฝ่ายบัญชี | 🟡 **28 ส.ค. 2569 (ต่อเนื่อง 7)** | M | — | ผู้ใช้ | ✅ cross-check กับกฎหมายทางการแล้ว (ราชกิจจานุเบกษา + rd.go.th) ตรงกับที่ seed ไว้ทุกจุด — ⚠️ ยังไม่ใช่ตัวเลขที่ฝ่ายบัญชี**ลงนามรับรองอย่างเป็นทางการ** ยังต้องขอ sign-off ก่อน go-live · `hr_company_tax_id`/`hr_sso_employer_no` หาจากเน็ตไม่ได้ ยังเป็น placeholder |
| P0-6 | **Backup แอปก่อนเริ่มสร้าง object** | ⬜ | S | — | agent | มีจุด restore ในวันที่เริ่ม |

---

## 3. P1 — โครงสร้างพื้นฐานและตารางตั้งค่า

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P1-1 | สร้างกลุ่มในแอป 8 กลุ่ม (HR + HR-00…HR-06) | ✅ | S | P0-6 | `get_app_info` ยืนยันแล้ว 26 ส.ค. 2569 — Section ID เติมใน BuildSpec §1.2 ครบ |
| P1-2 | สร้าง optionset ใหม่ 24 ชุด | ✅ | M | — | ยืนยันแล้ว 26 ส.ค. 2569 — `get_optionset_list` เห็นครบ 24 · option key ทุกตัวคัดลงใน BuildSpec §1.6 / §1.6.1 แล้ว |
| P1-3 | สร้างตารางตั้งค่า HR-00 ทั้ง 10 ตาราง (ผูก optionset ตอน `addFields`) | 🚧 | M | P1-2 | ✅ **10/10 ตารางสร้างแล้ว 26 ส.ค. 2569** ผ่านทางเลือกสำรอง `create_app_items`→`get_worksheet_structure`→`removeFields`→`addFields`→verify (เพราะ `create_worksheet` ยังพัง) · ws ID + field ID ครบใน BuildSpec §1.3/§2 · ⚠️ ค้าง 2 ฟิลด์ Dropdown ที่ต้องผูก shared optionset (`hr_leave_type.carry_policy` · `hr_ot_rate.employment_type`) — ทำได้เฉพาะใน Browser · ✅ **27 ส.ค. 2569 (ต่อเนื่อง) — `hr_ot_rate.day_type` เพิ่มสำเร็จผ่าน API** (พบว่าตารางนี้ขาดฟิลด์นี้ไปตั้งแต่แรก ไม่ใช่ปัญหา Browser-only) ผ่าน `addFields` เป็น inline options (label อังกฤษตรงกับ `OS_HR_OT_DAY_TYPE` — ลบ+สร้างใหม่ 1 ครั้งเพื่อแก้ label ที่พลาดเป็นไทยรอบแรก) fieldId `6a8fd7608b6633ef76f13156` — seed 3 record เดิมอัปเดต `day_type` ครบแล้ว — ยังไม่ bind shared optionset จริง (เหมือน D-11/D-12) · ⚠️ ค้าง relation 2 ฟิลด์รอ P2-1 สร้างตารางปลายทาง (`hr_leave_type.pay_component`→`hr_pay_component`, `hr_leave_policy.job_level`→`hr_job_level`) |
| P1-4 | seed ข้อมูลตั้งค่า: `hr_setting` 1 record · วันหยุด 1 ปี · กะ 1 กะ · ประเภทการลา 6 · นโยบายสิทธิ · อัตรา OT 3 · อัตรา SSO · ขั้นบันไดภาษี · ค่าลดหย่อน | ✅ | M | P1-3 · P0-5 | ✅ **เสร็จ 26 ส.ค. 2569** — 55 record ใน 9/10 ตาราง (`hr_approval_rule` ยกไปรอ role) ยืนยันด้วย `get_record_pivot_data` COUNT ทุกตาราง · รายละเอียดใน `21-FRS-Modules-HR.md` ต่อ FR (เดิม BuildSpec §2) · ⚠️ ตัวเลข SSO/ภาษียังเป็น "ยืนยันจากแหล่งข่าวสาธารณะ" ไม่ใช่ตัวเลขที่ฝ่ายบัญชีเซ็นรับรอง (G-05 ยังเปิดอยู่) · ⚠️ `hr_company_tax_id`/`hr_sso_employer_no` เป็น placeholder |
| P1-5 | สร้าง unique index ของตารางตั้งค่า (IX-01.1 · IX-02.1 · IX-07.1) | ⬜ | S | P1-4 | `create_record` ซ้ำถูกปฏิเสธ |

---

## 4. P2 — Core HR (ทะเบียนพนักงาน)

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P2-1 | สร้าง `hr_position` · `hr_job_level` + seed | ✅ | S | P1-3 | ws ID เติมใน §1.3/§2 แล้ว · 6 job_level + 8 position record seeded 26 ส.ค. 2569 ยืนยันด้วย `get_record_pivot_data` COUNT |
| P2-2 | สร้าง `hr_employee` (36 ฟิลด์) — ฟิลด์ `supervisor` ชี้ตัวเองเพิ่มทีหลังด้วย `addFields` | ✅ | L | P2-1 | `get_worksheet_structure` ยืนยันครบ 36 ฟิลด์ + supervisor self-relation 26 ส.ค. 2569 · field ID เติมใน §2 FR-HR-03 แล้ว |
| P2-3 | ผูกฟิลด์ Dropdown ของ `hr_employee` (`gender`/`marital_status`/`employment_type`/`emp_status`) เข้ากับ shared optionset ที่มีอยู่ (🔴 Browser UI เท่านั้น) | ⬜ | M | P2-2 | ยืนยันข้อจำกัดแล้ว 26 ส.ค. 2569 ผ่าน hap CLI + ตรวจ WireControl JSON schema โดยตรง — สรุปชัดเจนว่าไม่มีทาง API/CLI ใด ๆ ทำได้ ต้องทำผ่าน Browser UI เท่านั้น |
| P2-4 | ตั้ง default 0 ให้ฟิลด์ธงทุกตัว (Browser) | ⬜ | S | P2-2 | เปิดฟอร์มเห็นค่า 0 |
| P2-5 | สร้าง unique index IX-03.1 (`emp_code`) · IX-03.2 (`national_id`) + ทดสอบผ่าน API | ⬜ | S | P2-2 | **AC-15 ส่วนแรก** |
| P2-6 | สร้าง `hr_employment_contract` · `hr_employment_event` · `hr_dependent` · `hr_bank_account` + seed พนักงานทดสอบ 4 คน | 🔶 | M | P2-2 · P0-2 | ✅ ทั้ง 4 ตารางสร้างแล้ว 26 ส.ค. 2569 (ws/field ID เติมใน §1.3/§2 แล้ว) · ✅ **แก้ Data Name ภาษาจีน (`记录`) เป็นไทยแล้ว 26 ส.ค. 2569** (พบค้างตอนตรวจซ้ำรอบ 2 ตามคำขอผู้ใช้ — ใช้ `update_worksheet.addRecordButtonName` เหมือนตารางใบลา) · ⬜ ยังไม่ seed พนักงานทดสอบ EMP-T1…T4 — บล็อกบางส่วนโดย G-06 (มีบัญชีผู้ใช้จริงในองค์กรแค่ 2 บัญชี) |

---

## 5. P3 — การลา 🎯 **โมดูลที่ควรทำ demo แรก**

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P3-1 | สร้าง `hr_leave_balance` · `hr_leave_request` · `hr_leave_ledger` | 🚧 | M | P1-3 · P2-2 | ✅ **โครงสร้างฟิลด์ครบทั้ง 3 ตาราง 26 ส.ค. 2569** ผ่านทางเลือกสำรอง `create_app_items`→`update_worksheet`(removeFields+addFields รวมคำสั่งเดียว — ยืนยันใหม่ว่ารวมได้ ไม่ต้องแยก 2 คำสั่ง)→verify · ws ID + field ID ครบใน `02b-BuildSpec-Addendum-P3-1.md` (ไม่ได้แก้ไฟล์ `02-BuildSpec-FRS.md` หลักตรง ๆ เพื่อลดความเสี่ยงเขียนทับ — ไฟล์ addendum override ค่า `<TBD>` ของแถว #20–22 ใน §1.3 และฟิลด์ FR-HR-08/09) · ⚠️ **ยังไม่เสร็จสมบูรณ์:** ยังไม่ seed record ทดสอบเพื่อยืนยัน defaultValue จริง (readback md ไม่แสดงค่า default) · `leave_unit`/`leave_status`/`ledger_type` เป็น **inline options** ยังไม่ผูก shared optionset (`OS_HR_LEAVE_UNIT`/`OS_HR_REQUEST_STATUS`/`OS_HR_LEDGER_TYPE`) — Browser-only · `hr_leave_ledger.ledger_no` เป็น **Text ชั่วคราว** แทน AutoNumber ที่สเปกกำหนด (API ทำ AutoNumber ไม่ได้) · ✅ ตั้ง `addRecordButtonName` (Data Name ไทย) ครบแล้วทั้ง 3 ตาราง 26 ส.ค. 2569 ผ่าน `update_worksheet.addRecordButtonName` ยืนยันด้วย `hap worksheet info` |
| P3-2 | สร้างฟิลด์ `AutoNumber` ของ ledger ใน Browser + ทดสอบเลขจริง | ⬜ | S | P3-1 | 🔴 **เปลี่ยนสถานะ:** ตอนนี้ `hr_leave_ledger.ledger_no` เป็น Text ชั่วคราวแล้ว (ใช้งานได้จริงแม้ไม่ใช่ AutoNumber) — งานนี้กลายเป็น "ทางเลือก" ไม่ใช่ blocker: ตัดสินใจว่าจะแปลงเป็น AutoNumber จริงใน Browser หรือใช้ Text ต่อไปโดยให้ workflow เขียนสตริงเอง |
| P3-3 | สร้าง unique index IX-08.1 · IX-09.1 | ⛔ | S | P3-1 | **27 ส.ค. 2569 — ตัดสินใจ "ยังไม่เปิดใช้" IX-08.1** ดูรายละเอียดเหตุผลในแถว P3-8 (สรุป: `leave_no` ไม่ required + ยังไม่ auto-generate ⇒ เปิด unique index ตอนนี้จะบล็อกการสร้างใบลา Draft ใบที่ 2 ขึ้นไป) — รอ D-9/D-10 ก่อน |
| P3-4 | seed `hr_leave_balance` ให้พนักงานทดสอบครบทุกประเภท | ✅ | S | P3-1 · P2-6 | ✅ **เสร็จ+ยืนยัน 27 ส.ค. 2569** — TEST-EMP1 และ TEST-EMP2 มีครบ 6 record/คน (UNPAID=30, ORDINATION=15, MATERNITY=98, PERSONAL=3, SICK=30, ANNUAL=6 ตามนโยบายที่ seed ไว้ — ยกเว้น TEST-EMP1.ANNUAL คงค่าเดิม 10 ที่ใช้ทดสอบ WF-HR-02/03 ไปแล้วไว้เหมือนเดิม ไม่แตะ) ผ่าน `batch_create_records`(`triggerWorkflow:false`) · ยืนยันด้วย `get_record_pivot_data`(group by employee, COUNT rowid) = 6 ทั้งสองคน |
| P3-5 | **WF-HR-01 อนุมัติใบลา 2 ระดับ** | 🟢 **D-17 แก้ไขแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 16) — ไม่ใช่ปัญหาระบบ เป็นบั๊ก `submitted_flag` ว่างบน record ทดสอบ (ดู §11 D-17) — ยิงสำเร็จแล้วหลังแก้ ขั้นหัวหน้างานผ่านจริง มี To-do ถึง Wanadtapong.l อีกครั้ง** ~~🔴 D-17: re-verify 28 ส.ค. 2569 (ต่อเนื่อง 11) พบว่าไม่ยิง — แต่เป็นปัญหาระดับระบบ ไม่ใช่ WF-HR-01 เจาะจง~~ | L | P3-4 | หลักฐานเดิม (27 ส.ค.) ที่เคยผ่าน: `_updatedBy` = **`user-workflow`** ✅ (record `4a226f18-5f2b-4af4-b212-4c016f50b9ec`) · `get_approval_list_by_row` มี To-do จริงถึง Wanadtapong.l ✅ · **AC-01 (ขั้นหัวหน้างาน) ผ่าน** · ⬜ ขั้น HR (`appr_block_2`) ยังไม่ทดสอบ — รอ `hr_approval_rule` seed (ต้องมี role ก่อน P8-1←G-06) และรอมนุษย์กด Approve จริงใน To-do (A-HR-15) · รายละเอียดสถาปัตยกรรมและบั๊กที่แก้ระหว่างทางอยู่ใน `04-CLAUDE-memory.md` แถว #12 และ `nocoly-shared/00-HAP-Working-Guide.md` §2 ข้อ 16-17 · 🔴 **workflow ต้องอ้าง option key แบบ inline ของ `leave_status`/`leave_unit` ที่บันทึกใน `02b-BuildSpec-Addendum-P3-1.md` ไม่ใช่ key ของ `OS_HR_REQUEST_STATUS`/`OS_HR_LEAVE_UNIT` ใน BuildSpec §1.6.1 (คนละชุดกันจนกว่าจะรีไบนด์ผ่าน Browser)** · **28 ส.ค. 2569 (ต่อเนื่อง 11): re-verify ด้วย record ทดสอบใหม่สะอาด (`TEST-LR-D15-REVERIFY`) พบว่าไม่ยิงเลย (68+ วินาที ไม่มี `user-workflow`) แต่โครงสร้าง (main+inner) ตรวจสอบผ่าน `get_workflow_structure` แล้วว่า published/สมบูรณ์ 100% ไม่มี warning — sanity-check กับ WF-HR-04 (แถว P4-3) ก็ไม่ยิงเช่นกัน ⇒ สรุปว่าเป็นปัญหา session/environment/platform กว้างกว่า WF-HR-01 เอง ไม่ใช่ความเสียหายจาก BR-08.3/D-15 — ต้อง re-verify ซ้ำอีกครั้งใน session ถัดไปก่อนเชื่อสถานะเดิม** |
| P3-6 | **WF-HR-02 ตัดสิทธิ** + **WF-HR-03 คืนสิทธิ** | 🟢 **สร้าง+publish+ทดสอบยิงจริงสำเร็จทั้งคู่ 27 ส.ค. 2569** | M | P3-5 | **AC-01 (ส่วนตัดสิทธิ) · AC-03 ผ่านทั้งคู่** — WF-HR-02 `6a8fa97cfdab77a41c51cb3f` (publish v3) · WF-HR-03 `6a8fab800e97bf440dddb744` (publish v1) · หลักฐาน: balance `56c008e2-…` 10.0→9.0 (ตัด)→10.0 (คืน) ทั้งคู่ `_updatedBy`=`user-workflow` · ledger มีคู่กัน 2 แถว (`-1.0` Leave taken balance_after=9.0 · `+1.0` Leave returned balance_after=10.0) ทั้งคู่ relation ชี้ balance/leave_request ถูกต้อง (ไม่ใช่ "已删除") · ทดสอบยิงซ้ำ (idempotency) แล้วไม่ตัดซ้ำ (balance/ledger ไม่เปลี่ยน) · 🔴 **ระหว่างทางพบบั๊กแพลตฟอร์มใหม่ 2 ตัว** (ดู Change Log ด้านล่างและ `04-CLAUDE-memory.md`/`00-HAP-Working-Guide.md`) — แก้ในทั้ง 2 workflow แล้ว · ⚠️ **เลื่อนออกจากขอบเขตรอบนี้ (ลองแก้ 27 ส.ค. 2569 รอบเย็นแล้วชนบั๊กใหม่ — ดู D-8/D-13):** node เดิมของสเปกที่อ้าง `hr_attendance` ตอนนั้นยังไม่มีตาราง — P4-1 เสร็จแล้วแต่การเพิ่ม node กลับต้องใช้ `sub_process` ซึ่งชนบั๊ก `NodeAppIsNull` ใหม่ที่ยังไม่มีทางแก้ — revert กลับ publish v4/v2 (logic เดิมไม่เปลี่ยน) รอทางแก้ |
| P3-7 | ปุ่ม "ส่งคำขอ" / "ยกเลิกใบลา" + view 4 มุมมองของใบลา | ✅ | M | P3-1 | ✅ **ยืนยันแล้ว 27 ส.ค. 2569** — เปิดฟอร์ม TEST-LR-0001 แล้วดูปุ่มไม่โผล่ตอน `leave_status`=Cancelled (ถูกต้อง) → ตั้งชั่วคราวเป็น Draft ผ่าน MCP แล้วเปิดฟอร์มซ้ำ → ปุ่ม "ส่งคำขอ" และ "ยกเลิกใบลา" โผล่ทั้งคู่ตาม `enableWhen` จริง → revert กลับ Cancelled · view ครบ 4 มุมมองตามสเปก (ใบลาของฉัน `6a8fb8451378964f9984a0b5` · รออนุมัติ หัวหน้างาน `6a8fb845ae2a0e3743a0c34f` · รออนุมัติ HR `6a8fb8458b6633ef76f12f64` · ปฏิทินการลาของทีม `6a8fb845353e1b0e4a507c38`) · 🧹 **ทำความสะอาด ghost views ซ้ำ 8 อัน (2 ชุด × 4)** ที่ค้างเป็นผลข้างเคียงจาก Agent tool call ที่ถูก reject ในเซสชันก่อนหน้า — ลบครบแล้วเหลือ 5 view ที่ถูกต้อง (ทั้งหมด + 4 มุมมองจริง) — ดู Known Issues ใน `04-CLAUDE-memory.md` |
| P3-8 | Business Rules ของใบลา (BR-08.1…5) + Dynamic default DV-08.1 + unique index IX-08.1 | 🔶 | M | P3-1 | 🔶 **บางส่วนเสร็จ 27 ส.ค. 2569 — AC-02 บางส่วน** · ✅ **BR-08.1** (validation: `leave_to` is before `leave_from` → block) · ✅ **BR-08.4** (interaction: `leave_status` ≠ Draft → Read-only all) · ✅ **BR-08.5** (interaction: `leave_unit` ≠ Full day → Hide `leave_to` / Show `leave_hours`) — ทั้ง 3 ข้อสร้างและ **ยืนยันผ่าน Role Debugging (role Member)** แล้ว: BR-08.4/8.5 ยืนยันด้วยการเปิด record จริงเห็นฟอร์ม read-only + hide/show ตามเงื่อนไข, BR-08.1 ยืนยันด้วยการอ่านค่ากลับจากแผง Business Rules (condition ตรงสเปก) · ⛔ **BR-08.2** (บล็อกเมื่อ `leave_days` > สิทธิคงเหลือ) และ **BR-08.3** (บังคับแนบไฟล์ตาม `leave_type.require_document`) **ทำเป็น Business Rule ไม่ได้** — field picker ของ Business Rules ไม่รองรับการอ้างอิงฟิลด์บนตารางที่เชื่อมผ่าน Relation field (ทดสอบแล้ว: ค้นหาฟิลด์ของ `leave_type` ในเงื่อนไข ได้ "No search results") ⇒ ต้องย้ายไปบังคับที่ **WF-HR-01** (BR-08.2 มีแผนอยู่แล้วใน BuildSpec) และ **WF-HR-06 หรือโหนดใหม่** (BR-08.3) แทน · ⛔ **DV-08.1 ทำไม่ได้ผ่าน UI Dynamic Default เช่นกัน (พบใหม่)** — ทดสอบจริงที่ Field "ผู้อนุมัติที่ระบบกำหนด" (`approver_user`) → Default Value → Query worksheet → เลือก worksheet `ทะเบียนพนักงาน` ได้ปกติ แต่ในขั้น Query conditions ช่อง "Dynamic Value" ที่ต้องเทียบ `Record ID` ของ hr_employee กับฟิลด์ `พนักงาน` (`employee`, Relation) ของฟอร์มปัจจุบัน **ไม่แสดง field ชนิด Relation ให้เลือกเลย** (ค้นหาด้วยชื่อฟิลด์ตรง ๆ ก็ไม่เจอ — เห็นเฉพาะ Text/Date/Number) ⇒ ข้อจำกัดเดียวกับ Business Rules ขยายมาถึง Dynamic Default ด้วย — ต้องย้ายทั้ง `approver_user` (จาก `employee.supervisor_user`) และ `balance_snapshot` (จาก query `hr_leave_balance` filter employee+leave_type+leave_year) ไปทำที่ **WF-HR-01** แทน (query worksheet + update_record) · ⛔ **IX-08.1 ตัดสินใจ "ยังไม่เปิดใช้" (พบใหม่)** — เปิดหน้า Search Acceleration → Create Index → hover tooltip ของ "Unique Index" ยืนยันข้อความจริงจากระบบ: *"Once a unique index is established, the value of the field cannot be duplicated. If the field is not required, the entire worksheet can only have one empty record to ensure the uniqueness of the data."* เนื่องจาก `leave_no` เป็น Text ธรรมดา **ไม่ required และไม่ auto-generate ตอนสร้าง record** (ผู้ใช้ต้องปล่อยว่างตอน Draft แล้วรอระบบ/workflow ใส่เลขทีหลัง) การเปิด unique index ตอนนี้จะทำให้สร้างใบลา Draft ได้แค่ใบเดียวทั้งระบบที่ `leave_no` ว่าง — **ไม่เปิดใช้จนกว่าจะมีกลไก auto-generate `leave_no` ทันทีตอนสร้าง record** (ดู D-9/D-10) · 🔴🆕 **28 ส.ค. 2569 — ลองย้าย BR-08.3 เข้า WF-HR-01 จริง พบปัญหา 2 ชั้นแล้ว revert กลับสถานะเดิมปลอดภัย (รายละเอียดเต็มดู D-15)**: (1) แทรก branch node กลางเส้นทางเดี่ยวที่มีปลายทางเดิมอยู่แล้ว ทำให้ปลายทางเดิม (`set_submitted`) ถูก auto-relink ไปอ้าง node ของ branch เองแทน path ย่อยที่ตั้งใจ (`validate_process` ผ่านสะอาดแม้แปลก) (2) **ทดสอบยิงจริงด้วย record ใหม่ 2 ใบไม่พบ `_updatedBy`=`user-workflow` เลยแม้รอเกิน 3 นาที ทั้งก่อน revert (v4) และหลัง revert กลับโครงสร้างเดิม 100% (v5)** — ตรวจแล้วว่า approval instance เดิมของ TEST-LR-0001 ไม่กระทบ (`get_approval_list_by_row` ยังเห็น To-do เดิมถึง Wanadtapong.l ปกติ) — **สรุป: BR-08.3 ยังไม่ได้เพิ่ม (revert สำเร็จทุกจุด) แต่พบประเด็นใหม่ต้องเฝ้าระวัง: ต้องตรวจสอบซ้ำ session หน้าว่า WF-HR-01 ยิง trigger จริงได้ปกติหรือไม่ ก่อนเชื่อสถานะ 🟢 เดิม** |

---

## 6. P4 — ลงเวลาและล่วงเวลา

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P4-1 | สร้าง `hr_attendance` + unique index IX-10.1 (`employee`, `work_date`) | 🔶 | M | P2-2 | ✅ **โครงสร้างฟิลด์ครบ 27 ส.ค. 2569** — สร้างผ่าน `create_app_items`(`6a8fcd67353e1b0e4a507d32`)→`update_worksheet`(removeFields 3 default + addFields 16 ฟิลด์ครบตาม FR-HR-10)→`get_worksheet_structure` verify · relation ทุกตัวชี้ถูก (`employee`→`hr_employee` `6a8efa5e9762533b5b7185c1` · `shift`→`hr_shift` `6a8eebf79762533b5b7184c5` · `leave_request`→`hr_leave_request` `6a8f2dbaae2a0e3743a0beaa` · `ot_request`→`hr_ot_request` `6a8fcca48b6633ef76f13033` — ตัวหลังนี้ต้องสร้าง P4-2 ก่อนจึงเรียงลำดับสร้าง `hr_ot_request` ก่อน `hr_attendance`) · view "全部"→"ทั้งหมด" (`6a8fcd67353e1b0e4a507d36`) แก้ผ่าน hap CLI แล้ว · ⚠️ **ยังไม่เสร็จ:** IX-10.1 unique index (Browser-only) · `att_status`/`att_source` เป็น **inline options** (label อังกฤษตรงกับ `OS_HR_ATT_STATUS`/`OS_HR_ATT_SOURCE` แต่ยังไม่ผูก shared optionset จริง — Browser-only) · BR-10.1/BR-10.2 (Business Rules) · ✅ `addRecordButtonName` (Data Name ไทย) = "บันทึกลงเวลา" ตั้งสำเร็จ (ยืนยันด้วย `hap worksheet info` → `entityName`) — **AC-15** · 🔶 **D-8 ลองทำแล้ว 27 ส.ค. 2569 (รอบเย็น) แต่ชนบั๊ก `sub_process`→`NodeAppIsNull` ใหม่ — ยังไม่ปิดงานได้ ดู D-13** |
| P4-2 | สร้าง `hr_ot_request` + IX-11.1 | 🔶 | M | P2-2 · P1-3 | ✅ **โครงสร้างฟิลด์ครบ 27 ส.ค. 2569** — สร้างผ่าน `create_app_items`(`6a8fcca48b6633ef76f13033`)→`update_worksheet`(removeFields 3 default + addFields 16 ฟิลด์ครบตาม FR-HR-11B)→`get_worksheet_structure` verify · relation `employee`→`hr_employee` ถูกต้อง · `day_type`/`ot_status` เป็น **inline options** (label อังกฤษตรงกับ `OS_HR_OT_DAY_TYPE`/`OS_HR_REQUEST_STATUS` แต่ยังไม่ผูก shared optionset จริง — Browser-only; **แก้ไข 27 ส.ค. 2569:** สร้าง `day_type` เป็นภาษาไทยผิดพลาดครั้งแรก ["OT วันทำงาน"ฯลฯ ตามชื่อ record ของ `hr_ot_rate`] แล้วแก้เป็นภาษาอังกฤษให้ตรง optionset ภายหลังด้วย `editFields`) · view "全部"→"ทั้งหมด" (`6a8fcca48b6633ef76f13037`) แก้ผ่าน hap CLI แล้ว · ⚠️ **ยังไม่เสร็จ:** IX-11.1 unique index (`employee`,`ot_date`,`ot_from`) (Browser-only) · BR-11.1/11.2/11.3 · `pay_period` relation (รอ P5-1 สร้าง `hr_pay_period`) · ✅ `addRecordButtonName` = "ใบขออนุมัติล่วงเวลา" ตั้งสำเร็จ (ยืนยันด้วย `hap worksheet info` → `entityName`) |
| P4-3 | **WF-HR-04 อนุมัติ OT 2 ระดับ** (พร้อมหา `day_type` และตัวคูณจากตาราง) | 🟢 **D-17 แก้ไขแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 16) — sanity-check ที่ "ไม่ยิง" เกิดเพราะ record ทดสอบ `TEST-OT-SYSCHK-01` มี `hr_ot_submitted_flag` ว่างจริง (ไม่ใช่ 0) ทั้งที่ field ตั้ง defaultValue:0 ไว้แล้ว — **สรุปใหม่: ฟิลด์ธงเหล่านี้**ไม่มี `defaultValue` เลย** (`default:""` — ยืนยันด้วย `hap worksheet fields <ws_id> --raw` 28 ส.ค. 2569) รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกไว้ว่าตั้ง `defaultValue:0` แล้วเมื่อ 27 ส.ค. ⇒ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องกับบั๊กที่รู้อยู่แล้วว่า `editFields` เขียนทับทั้ง field object) — ยังพิสูจน์ไม่ได้ว่า API เพิกเฉยต่อ default เพราะไม่มีฟิลด์ไหนมี default ให้ทดสอบเลย ⇒ ในทางปฏิบัติ ต้องส่งค่าฟิลด์ธงตรง ๆ เองเสมอเมื่อ seed record ผ่าน API** — แก้แล้วยิงสำเร็จ ขั้นหัวหน้างานผ่านจริง (ดู §11 D-17)** ~~🔴 D-17: ใช้เป็น sanity-check 28 ส.ค. 2569 (ต่อเนื่อง 11) แล้วพบว่าไม่ยิงเช่นกัน — ยืนยันว่าปัญหาไม่ยิงเป็นระดับระบบ~~ | L | P4-2 | หลักฐานเดิม (27 ส.ค.) ที่เคยผ่าน: ตัวคูณเปลี่ยนตามตาราง ไม่ใช่ค่าฝัง (**AC-19 ส่วนแรก — ผ่านแล้ว**) — main process `6a8fd8d35f8564a68c3c1909` publish v2 · inner supervisor-approval `6a8fd9325f8564a68c3c1cec` publish v1 (ใช้ร่วมกัน 2 เส้นทาง day-type ผ่าน `mode:"use_existing"`) · inner HR-approval `6a8fd9325f8564a68c3c1ced` publish v1 (สร้างแล้วแต่**ยังไม่ทดสอบ** — รอ `hr_approval_rule` seed ซึ่งรอ role/G-06 เหมือน WF-HR-01) · **สถาปัตยกรรมเหมือน WF-HR-01** (single-flow gate-branch ตาม `hr_ot_status`) บวกเทคนิคใหม่: fork แยกตาม day_type (holiday/workday) ที่ไม่มี merge node ในแพลตฟอร์มนี้ ⇒ ทำซ้ำแค่ chain สั้น ๆ ต่อเส้นทาง แต่ใช้ inner approval_block ร่วมกันตัวเดียวผ่าน create-once-then-`use_existing` · **ทดสอบยิงจริง 3 เคส:** `OT-TEST-HOLIDAY-01` (4h, Holiday outside hours, ×3.0), `OT-TEST-WORKDAY-01` (2.5h, Working day, ×1.5), `OT-TEST-FRACTIONAL-01` (2.5h — ยืนยัน `hr_ot_hours`="2.50" ถูกต้องหลังแก้บั๊ก precision) ทั้ง 3 มี To-do จริงถึง Wanadtapong.l (`get_approval_list_by_row`) และ `_updatedBy`=`user-workflow` ครบ — เก็บไว้เป็นหลักฐานทดสอบ ไม่ลบ · 🔴 **พบ+แก้บั๊กใหม่ระหว่างทาง 2 ตัว** (ดู Known Issues `04-CLAUDE-memory.md`): (1) `dateDiff` compute node ตัด `precision` ทิ้งเงียบ ๆ — แก้ด้วย 2-step compute (นาที→หารเป็นชั่วโมงด้วย `number` compute ที่มี precision) (2) เจอซ้ำ: `hr_ot_submitted_flag`/`hr_paid_flag` ไม่มี defaultValue ทำให้ `ne` branch condition ไม่ match เงียบ ๆ workflow ไม่ยิงเลย — แก้ด้วย `editFields` ใส่ `defaultValue:0` |
| P4-4 | **WF-HR-05 สรุปเวลาทำงานรายวัน** | 🟢⬜ | L | P4-1 · P3-6 | **AC-04** — วันที่ลาแสดง On leave · ทดสอบ 5 เคส · 🟢🆕 **28 ส.ค. 2569 (ต่อเนื่อง 12) — สร้าง+publish สำเร็จจริง!** main `6a910184730d20c5b7710fa8` v1 + inner `6a910197730d20c5b7711028` v1 — validate สะอาด 0 error ทั้งคู่ ยืนยัน D-16 fix ใช้ได้กับ build จริง — ปรับจากดราฟต์เดิม: ใช้ `code` node (JS) แทน Duration/Function calculation เพราะ `hr_shift.hr_start_time`/`hr_end_time` เป็น Text "HH:mm" ไม่ใช่ Time field · ตัดขั้น get-associated-shift ออกเพราะมี `hr_att_shift` บน `hr_attendance` อยู่แล้ว · ส่ง grace/halfday minutes เข้า sub_process ผ่าน subprocess parameter — ⚠️ **ยังไม่ได้ live-fire test เพราะ D-17** (ดู §11) — Test recipe เดิม (seed 5 เคส + Execute now) รอรันจริงเมื่อ D-17 คลี่คลาย |
| P4-5 | ปุ่ม "ส่งขอล่วงเวลา" + view ลงเวลาผิดปกติ / OT รออนุมัติ | ✅ **สร้าง+verify ผ่านจริง 27 ส.ค. 2569 (ต่อเนื่อง)** | S | P4-2 | ปุ่ม "ส่งขอล่วงเวลา" บน `hr_ot_request` (`create_custom_actions` — actionId `6a8fde0aae2a0e3743a0c62f`) + view "OT รออนุมัติ" (`6a8fde0b353e1b0e4a50803e`, filter `hr_ot_status` in Pending supervisor/Pending HR) + view "บันทึกลงเวลาที่ผิดปกติ" บน `hr_attendance` (`6a8fde0c1378964f9984a42c`, filter `hr_att_status` in Late/Absent) — ทั้งหมดสร้างผ่าน MCP สำเร็จ · 🔴 **เจอกับดักเดิมของโมดูลบัญชีซ้ำในโมดูล HR เป็นครั้งแรก:** ปุ่มที่สร้างผ่าน `create_custom_actions` มี Scope เริ่มต้นเป็น "Unassigned View" (ไม่โผล่บนหน้าจอเลย) ต้องเข้า Browser (Form Settings → Custom Action → Scope → All Records) ก่อนถึงจะโผล่ — แก้แล้วผ่าน Claude in Chrome (มี session ที่ login ค้างอยู่แล้ว ไม่ต้อง login ใหม่) · **verify จริง:** toggle `hr_ot_status` ของ `OT-TEST-FRACTIONAL-01` เป็น Draft ชั่วคราวผ่าน MCP (`triggerWorkflow:false`) → เปิด record ใน Browser เห็นปุ่ม "ส่งขอล่วงเวลา" โผล่จริงตาม `enableWhen` → revert กลับ Pending supervisor เดิม · view "OT รออนุมัติ" แสดง 3 record ถูกต้อง · view "บันทึกลงเวลาที่ผิดปกติ" render คอลัมน์ถูกต้อง (0 record เพราะ `hr_attendance` ยังไม่มี seed data) |
| P4-6 | **WF-HR-06 เตือนคำขอค้างเกิน SLA** | 🟢⬜ | M | P3-5 · P4-3 | **AC-14** · 🟢🆕 **28 ส.ค. 2569 (ต่อเนื่อง 12) — สร้าง+publish สำเร็จจริง!** main `6a910222730d20c5b77115e5` v1 + inner `6a9102345f8564a68c449ee7` v1 — validate สะอาด 0 error — **v1 จำกัดขอบเขตเหลือแค่ `hr_leave_request`** เพราะพบ gap ใหม่ D-18: `hr_ot_request` ไม่มีฟิลด์ `submitted_at` ให้คำนวณ SLA ได้ตรงความหมาย (`hr_claim` ตัดออกโดยธรรมชาติเพราะยังไม่มีตาราง — P6) — ⚠️ **ยังไม่ได้ live-fire test เพราะ D-17** (ดู §11) — ต้องตัดสินใจเรื่อง D-18 ก่อนขยายขอบเขตไป OT/claim |

---

## 7. P5 — เงินเดือนและภาษี (เฟสที่หนักที่สุด)

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P5-1 | สร้าง `hr_pay_period` · `hr_pay_component` · `hr_salary_structure` | 🔶 | M | P2-2 | ✅ **โครงสร้างฟิลด์ครบทั้ง 3 ตาราง 27 ส.ค. 2569** — สร้างผ่าน `create_app_items`→`update_worksheet`(removeFields 3 default + addFields)→`get_worksheet_structure` verify: `hr_pay_component` (`6a8ff2868b6633ef76f13871`, view `6a8ff2868b6633ef76f13875`, 11 ฟิลด์ รวม `biz_coa_account`→`ac_coa` bidirectional) · `hr_pay_period` (`6a8ff2878b6633ef76f1387b`, view `6a8ff2878b6633ef76f1387f`, 22 ฟิลด์ รวม `biz_ac_period_ref`→`ac_period` และ `biz_voucher_ref`→`ac_voucher` ทั้งคู่ bidirectional) · `hr_salary_structure` (`6a8ff287353e1b0e4a508550`, view `6a8ff287353e1b0e4a508554`, 9 ฟิลด์ รวม `biz_employee`→`hr_employee` และ `biz_pay_component`→`hr_pay_component` ทั้งคู่ bidirectional) · flag/total Number fields ทั้งหมดตั้ง `defaultValue:0` เชิงป้องกันตั้งแต่แรก (บทเรียนจาก P4) · `addRecordButtonName` ตั้งสำเร็จทั้ง 3 ตาราง ("องค์ประกอบค่าจ้าง"/"งวดจ่ายเงินเดือน"/"โครงสร้างเงินเดือน") · ⚠️ **ยังไม่เสร็จ:** `biz_component_type`/`biz_calc_method`/`biz_period_status` เป็น **inline options** (label อังกฤษตรงกับ `OS_HR_COMPONENT_TYPE`/`OS_HR_COMPONENT_CALC`/`OS_HR_PAY_PERIOD_STATUS` แต่ยังไม่ผูก shared optionset จริง — Browser-only) · unique index IX-12.1 (`period_year`,`period_month`) และ IX-12.2 (`employee`,`pay_component`,`effective_from`) ยังไม่ตั้ง (Browser-only) · `hr_leave_type.pay_component` และ `hr_leave_policy.job_level` (relation ย้อนกลับที่ค้างจาก FR-HR-07) ยังไม่ได้เพิ่ม |
| P5-2 | สร้าง `hr_payslip` · `hr_payslip_line` + unique index IX-12.3 | 🔶 **27 ส.ค. 2569** | M | P5-1 | **AC-15** — โครงสร้างตารางสร้างครบผ่าน MCP (`hr_payslip` `6a904c85353e1b0e4a50ba05` 25 ฟิลด์ · `hr_payslip_line` `6a904c858b6633ef76f16a0b` 8 ฟิลด์) relation bidirectional ครบ 6 จุด · เงินยอดใช้ Currency(THB) แทน Number ตามสเปกเดิม · `payslip_no` เป็น AutoNumber · flag/total ตั้ง defaultValue:0 เชิงรุก · `addRecordButtonName` ตั้งครบ · BuildSpec §1.3/FR-HR-12 12D/12E sync แล้ว — ⬜ เหลือ unique index IX-12.3 (Browser-only, **AC-15 ยังไม่ผ่าน**) · ผูก `payslip_status`→`OS_HR_PAYSLIP_STATUS` จริง · BR-12.4/12.5 |
| P5-3 | สร้าง `hr_pnd1_filing` · `hr_wht_cert` + IX-12.5 · IX-12.6 | 🔶 **27 ส.ค. 2569** | M | P5-2 | โครงสร้างตารางสร้างครบผ่าน MCP (`hr_pnd1_filing` `6a904e72ae2a0e3743a0fd06` 13 ฟิลด์ · `hr_wht_cert` `6a904e73353e1b0e4a50ba83` 9 ฟิลด์) relation bidirectional ครบ 3 จุด (รวม multi-relation `pay_periods`) · ยอดเงินใช้ Currency(THB) แทน Number ตามสเปกเดิม · `addRecordButtonName` ตั้งครบ · **กลุ่มเงินเดือนครบ 7/7 ตารางแล้ว** · BuildSpec §1.3/FR-HR-12 12I/12J sync แล้ว — ⬜ เหลือ unique index IX-12.5/IX-12.6 (Browser-only) · ผูก dropdown 4 ฟิลด์เข้า shared optionset จริง |
| P5-4 | seed องค์ประกอบค่าจ้างพร้อมผูก `coa_account` เข้าผังบัญชีจริง | ✅ **28 ส.ค. 2569 (ต่อเนื่อง 13) — ปิดครบ 6/6** | M | P5-1 | seed `hr_pay_component` 6 รายการผ่าน MCP `batch_create_records`(`triggerWorkflow:false`) สำเร็จ: SALARY/OT/SSO_ER/SSO_EE/WHT/ABSENCE_DEDUCT — ผูก `coa_account` ครบ 6/6: SALARY/OT/ABSENCE_DEDUCT→`530101 เงินเดือน` (26-27 ส.ค.) · **28 ส.ค. 2569 (ต่อเนื่อง 13):** ฝ่ายบัญชีตอบกลับ (`tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`) สร้างบัญชีใหม่ 3 รายการในผังบัญชีจริงแล้ว → ผูกต่อสำเร็จ: SSO_ER→`530102` (rowId `233626b4-d272-4cb5-8f77-9d8d2ddef804`), SSO_EE→`210402` (rowId `c6386fe7-1b3c-44db-ab80-9dfd3308a6a5`), WHT→`210304` (rowId `777ea001-5300-4f5c-857a-71a45db2076b`) ผ่าน `update_record`(fieldId `6a8ff3049762533b5b7193a2`, `triggerWorkflow:false`) ทั้ง 3 record + ยืนยันด้วย `get_record_details` เห็นชื่อบัญชีจริงไม่ใช่ "已删除" — **P5-4 ปิดงานสมบูรณ์ ปลดบล็อก P5-5 เต็มที่** |
| P5-5 | สร้างกฎการผ่านรายการของเงินเดือนใน `ac_posting_rule` | ⬜ | S | ~~P0-3~~ · P5-4 | ปลดบล็อกแล้ว (P0-3 และ P5-4 เสร็จทั้งคู่) — ฝ่ายบัญชียืนยัน `ac_posting_rule` เป็น **source of truth หลัก** (จับคู่ด้วย `event_code`+conditions), `hr_pay_component.biz_coa_account` เป็น fallback เมื่อไม่มีกฎตรง — ใช้ `event_code`: `PAYROLL_ACCRUAL` (key `ad4850c4-f1c1-4a65-adbb-fb0054a6e0c8`)/`PAYROLL_PAYMENT` (key `04a14b13-73c2-408b-a5fb-bb81ff630a06`) · `get_record_list` เห็นกฎที่ `event_code` = PAYROLL_ACCRUAL |
| P5-6 | seed โครงสร้างเงินเดือนของพนักงานทดสอบ 4 คน | 🔶 **28 ส.ค. 2569 (ต่อเนื่อง 15) — 2/4 คน** | S | P5-1 | seed `hr_salary_structure` 2 record ผ่าน `batch_create_records`(`triggerWorkflow:false`): TEST-EMP1 (`f36de845-596d-4a87-b8b1-55f19f1bd790`, 30,000 บาท) · TEST-EMP2 (`14efbacf-3f36-426d-a98a-7d714cebb14f`, 35,000 บาท) — ทั้งคู่ผูก `biz_pay_component`=SALARY (`4f6e08db-9316-4d22-88ac-a3656c29a182`), `biz_effective_from`=2026-01-01, `biz_is_active`=1 — ยืนยันด้วย `get_record_list` เห็น relation ชี้ employee/pay component ถูกต้อง ไม่ใช่ "已删除" · ⚠️ **ตัวเลข 30,000/35,000 เป็นค่าทดสอบสมมติกลม ๆ สำหรับ QA เท่านั้น ไม่ใช่เงินเดือนจริงของพนักงาน** (ตามหลักปฏิบัติเดิมของโปรเจกต์ที่ใช้ตัวเลขสมมติสำหรับ test data — ไม่ใช่การ "เดา" ข้อมูลจริง) · ⬜ ยังมีแค่ 2/4 คน — TEST-EMP3/4 ยังไม่มี (รอ P2-6 seed พนักงานทดสอบเพิ่ม ซึ่งบล็อกบางส่วนโดย G-06 บัญชีผู้ใช้จริงในองค์กรมีแค่ 2 บัญชี) |
| P5-7 | **เตรียมกระดาษคำนวณ 3 เคสด้วยมือ** (เงินเดือนอย่างเดียว / มี OT / มีขาดงาน) | ⬜ | M | P0-5 | เอกสารกระดาษคำนวณที่ลงนามรับรอง |
| P5-8 | **WF-HR-07 สร้างสลิปทั้งงวด** | ⬜ | M | P5-2 · P5-6 | จำนวนสลิป = จำนวนพนักงาน active · ยิงซ้ำไม่เพิ่ม |
| P5-9 | **WF-HR-08 คำนวณสลิป** 🔴 XL — workflow ที่ซับซ้อนที่สุด 13 กลุ่ม node | ⬜ | XL | P5-8 · P5-7 · P4-3 | **AC-05 · AC-06 · AC-19** — ทุกยอดตรงกระดาษคำนวณ 100% |
| P5-10 | **WF-HR-09 อนุมัติงวดจ่าย** | ⬜ | L | P5-9 · P0-2 | approval 1 ขั้นโดย HR Manager · สลิปทุกใบเป็น Approved |
| P5-11 | **WF-HR-10 ผ่านรายการเข้าบัญชี** | ⛔ | L | **P0-3** · P5-10 | **AC-07 · AC-08** — ใบสำคัญดุล · `source_module` = Payroll · ยิงซ้ำไม่เกิดใบที่สอง |

---

## 8. P6 — สวัสดิการและการเบิกจ่าย

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P6-1 | สร้าง `hr_welfare_scheme` · `hr_welfare_balance` + IX-18.1 | ⬜ | M | P2-1 | |
| P6-2 | สร้าง `hr_claim` · `hr_claim_line` + Rollup RU-19.1 (🔴 **Browser** — ห้ามแตะผ่าน API อีก) | ⬜ | M | P6-1 | Rollup คำนวณจริง (แก้บรรทัดแล้วหัวขยับ) |
| P6-3 | seed สวัสดิการและวงเงินของพนักงานทดสอบ | ⬜ | S | P6-1 | |
| P6-4 | Business Rules ของใบเบิก (BR-19.1…4) + DV-19.1 | ⬜ | M | P6-2 | **AC-10 ส่วนแรก** — บล็อกเมื่อเกินวงเงิน |
| P6-5 | **WF-HR-11 อนุมัติใบเบิก + ตัดวงเงิน** | ⬜ | L | P6-4 | วงเงินลดถูกต้อง · ยิงซ้ำไม่ตัดซ้ำ |
| P6-6 | **WF-HR-12 ส่งใบเบิกเข้า `ac_pay_req`** | ⬜ | M | P6-5 | **AC-10 ส่วนหลัง** — เกิด `ac_pay_req` 1 ใบ · ยิงซ้ำไม่เกิดใบที่สอง |

---

## 9. P7 — สรรหา ประเมิน อบรม

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P7-1 | สร้าง `hr_job_requisition` · `hr_candidate` · `hr_interview` | ⬜ | M | P2-1 | |
| P7-2 | **WF-HR-15 อนุมัติใบขออัตรากำลัง** | ⬜ | M | P7-1 · P0-2 | approval 2 ขั้น |
| P7-3 | **WF-HR-16 เลื่อนสถานะผู้สมัคร + แจ้งนัดสัมภาษณ์** | ⬜ | M | P7-1 | แจ้งครั้งเดียว ยิงซ้ำไม่แจ้งซ้ำ |
| P7-4 | **WF-HR-17 สร้างทะเบียนพนักงานจากผู้สมัคร** | ⬜ | M | P7-1 · P2-2 | **AC-17** |
| P7-5 | สร้าง `hr_appraisal_cycle` · `hr_appraisal` · `hr_appraisal_item` + IX-22.1/2 | ⬜ | M | P2-2 | |
| P7-6 | **WF-HR-18 เปิดรอบประเมินสร้างแบบให้ทุกคน** | ⬜ | M | P7-5 | จำนวนแบบ = จำนวนพนักงาน |
| P7-7 | **WF-HR-19 เดินสถานะแบบประเมิน 3 ขั้น** | ⬜ | L | P7-6 · P0-2 | `final_score` ถ่วงน้ำหนักถูกต้อง |
| P7-8 | สร้าง `hr_course` · `hr_training` + IX-23.1 + view Kanban ผู้สมัคร | ⬜ | M | P2-2 | ⚠️ verify view Kanban โผล่จริง |

---

## 10. P8 — สิทธิ์ รายงาน เอกสาร และ go-live

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P8-1 | **สร้าง role 8 บทบาทพร้อม permission matrix** | ⛔ | L | **P0-2** · ทุกตาราง | `get_role_details` ตรง matrix §1.7 |
| P8-2 | ตั้ง field-level visibility ปิดบังข้อมูลอ่อนไหว (Browser) | ⛔ | M | **P0-2** · P8-1 | **AC-11** — Role Debugging ยืนยัน |
| P8-3 | ผูกบัญชีทดสอบเข้าบทบาทและทดสอบสิทธิ์จริงทั้ง 8 บทบาท | ⛔ | M | **P0-2** · P8-1 | **AC-11 · AC-12 · AC-13** |
| P8-4 | **WF-HR-13A/13B เตือนสัญญาและทดลองงาน** | ⬜ | M | P2-6 | แจ้งเตือนถึงผู้รับถูกคน |
| P8-5 | **WF-HR-14 ยกยอดสิทธิลาต้นปี** | ⬜ | L | P3-6 | **AC-20** — ทดสอบด้วย Execute now |
| P8-6 | **WF-HR-20 ออกหนังสือรับรองหักภาษีประจำปี** | ⬜ | M | P5-11 | 1 ใบต่อพนักงาน · ยอดตรงผลรวมทุกงวด |
| P8-7 | รายงานและแดชบอร์ด 7 รายการ | ⬜ | M | ทุกตาราง | ⚠️ `create_chart` ยังไม่เคยเรียกสำเร็จ — **เปิดหน้าจอดูทุกครั้ง** |
| P8-8 | Print template เอกสารราชการ 4 แบบ (🔴 Browser) + **App Lock** | ⬜ | M | P5-11 | **AC-16** — พิมพ์ออกมาถูกรูปแบบ |

---

## 11. หนี้ทางเทคนิคและงานที่ยกไปเฟสถัดไป

| # | รายการ | เหตุผล | ความเสี่ยงถ้าไม่ทำ |
|---|---|---|---|
| D-1 | workflow ลบข้อมูลผู้สมัครที่หมดอายุตาม PDPA (G-08) | ไม่อยู่ในขอบเขตเฟสแรก | ไม่เป็นไปตาม PDPA ในระยะยาว |
| D-2 | เชื่อมเครื่องบันทึกเวลาอัตโนมัติ | Out of scope | ต้องนำเข้าไฟล์ด้วยมือทุกเดือน |
| D-3 | ไฟล์นำส่งธนาคารและไฟล์ยื่นภาษีอิเล็กทรอนิกส์ | Out of scope | ยังต้องกรอกในเว็บหน่วยงานเอง |
| D-4 | หน้าแรกของพนักงาน (Custom Page) | Browser · ทำหลังโครงสร้างนิ่ง | NFR-HR-11 (ยื่นใบลา ≤ 3 คลิก) อาจไม่ผ่าน |
| D-5 | จัดลำดับฟิลด์บนฟอร์มให้อ่านง่าย | ต้องทำผ่านหน้าจอหลังสร้างครบ | ฟอร์มยาวและอ่านยาก |
| D-6 | 🆕 รีไบนด์ `leave_unit`/`leave_status`/`ledger_type` เข้า shared optionset ผ่าน Browser | inline options ใช้งานได้ชั่วคราว แต่ค่า key ไม่ตรงกับ `OS_HR_*` ใน registry กลาง | workflow/report ที่อ้าง key ของ `OS_HR_*` จะไม่ match กับข้อมูลจริงในตารางนี้ จนกว่าจะรีไบนด์ |
| D-7 | 🆕 ตัดสินใจ `hr_leave_ledger.ledger_no`: คงเป็น Text หรือแปลงเป็น AutoNumber จริงใน Browser | AutoNumber สร้างผ่าน API ไม่ได้ | ถ้าต้องการเลขที่รันอัตโนมัติจริงต้องแปลงทีหลัง (อาจต้องลบสร้างใหม่ ข้อมูลเดิมหาย) |
| D-8 | 🔶 **พยายามทำแล้ว 27 ส.ค. 2569 (รอบเย็น) แต่ชนบั๊กใหม่ — revert กลับสถานะเดิมปลอดภัยแล้ว** — เพิ่ม node sync สถานะ "On leave" เข้า `hr_attendance` กลับเข้า WF-HR-02/03 | `hr_attendance` มีตารางแล้ว แต่ node ชนิด `sub_process` (จำเป็นสำหรับวน update ทีละ attendance record) สร้างผ่าน `batch_create_process_nodes` แล้ว `validate_process` ค้าง error `NodeAppIsNull` เสมอที่ตัว container node เอง (internal child process ของมันเองกลับ valid+canPublish:true ปกติ) — ทดลองแก้ 3 รูปแบบ (worksheetId ที่ `process.start` / worksheetId ที่ config ระดับบนของ sub_process / target แบบ nodeAlias-only vs nodeId+alias) ยังไม่หาย ดู D-13 และ G-13 ใน RTM สำหรับรายละเอียดเต็ม | AC-04 ("วันที่ลาแสดงเป็น 'ลา' ไม่ใช่ 'ขาดงาน'") ยังทำไม่ได้จนกว่าจะแก้บั๊ก `sub_process`/`NodeAppIsNull` ได้ — งานนี้ยัง**บล็อกอยู่จริง** ไม่ใช่ "พร้อมทำ" อีกต่อไป |
| D-9 | 🆕 27 ส.ค. 2569 — ย้าย DV-08.1 (เติม `approver_user` จาก `employee.supervisor_user` + เติม `balance_snapshot` จาก `hr_leave_balance`) ไปทำใน **WF-HR-01** แทน Dynamic Default UI | Dynamic Default "Query worksheet" ไม่รองรับ Relation-type field ของฟอร์มปัจจุบันเป็นค่าเปรียบเทียบ (field picker ไม่แสดงให้เลือก — ทดสอบยืนยันแล้ว) | ถ้าไม่เพิ่ม node นี้ใน WF-HR-01 ฟิลด์ `approver_user`/`balance_snapshot` จะว่างเสมอตอนสร้างใบลาใหม่ (ตอนนี้ WF-HR-01 คำนวณ `approver_user`/`balance_snapshot` ในสายอนุมัติอยู่แล้วบางส่วน — ต้อง cross-check ว่าครอบคลุมครบ 100% หรือยังต้องเพิ่ม node เฉพาะสำหรับ default ตอนสร้าง) |
| D-10 | 🆕 27 ส.ค. 2569 — เปิดใช้ IX-08.1 (unique index บน `leave_no`) หลังมีกลไก auto-generate `leave_no` ทันทีตอนสร้าง record (workflow เขียนสตริงเอง หรือแปลงเป็น AutoNumber จริงใน Browser — ดู D-7) | เปิด unique index ตอนนี้ (ฟิลด์ไม่ required) จะจำกัดทั้งตารางให้มี record ที่ `leave_no` ว่างได้แค่ 1 แถว — ยืนยันจาก tooltip ของระบบเองที่หน้า Search Acceleration | ถ้าเปิดก่อนมี auto-generate จะบล็อกการสร้างใบลา Draft ใบที่ 2 เป็นต้นไปทันที (error ตอน `create_record`/บันทึกฟอร์ม) |
| D-11 | 🆕 27 ส.ค. 2569 — รีไบนด์ `hr_attendance.att_status`/`att_source` และ `hr_ot_request.day_type`/`ot_status` เข้า shared optionset จริง (`OS_HR_ATT_STATUS`/`OS_HR_ATT_SOURCE`/`OS_HR_OT_DAY_TYPE`/`OS_HR_REQUEST_STATUS`) ผ่าน Browser | inline options ที่สร้างไว้ใช้ label ภาษาอังกฤษตรงกับ optionset กลางแล้ว แต่ option key (GUID) เป็นคนละชุด — ใช้งานได้ชั่วคราวแต่ workflow/report ที่อ้าง key ของ `OS_HR_*` จะไม่ match | เหมือน D-6 — จนกว่าจะรีไบนด์ ทุก workflow ที่ต้องเทียบ key ต้องอ้างอิง key ของ inline options ในตารางนี้เอง ไม่ใช่ key ของ `OS_HR_*` ใน BuildSpec §1.6.1 — **27 ส.ค. 2569 (ต่อเนื่อง): เพิ่ม `hr_ot_rate.day_type` เข้ารายการรีไบนด์นี้ด้วย** (สร้างเป็น inline option เช่นกันตอนทำ WF-HR-04) |
| D-13 | 🟢 **ปิดแล้ว 28 ส.ค. 2569 — ไม่ใช่บั๊กแพลตฟอร์ม** (`sub_process`→`NodeAppIsNull`) · **กฎที่ยังบังคับใช้: ต้อง `publish_process` ที่ inner processId ก่อนเสมอ แค่ `validate` ไม่พอ แล้วจึง validate/publish outer** | บันทึกการสอบสวนเต็ม + ความพยายามที่ผิดทาง → `15-Investigations-D13-D17.md` | ไม่มี — ปิดแล้ว |
| D-16 | 🆕🟢 28 ส.ค. 2569 (ต่อเนื่อง 10) — พบ root cause ที่แท้จริงของ D-13 จากเอกสาร skill `hap-mcp-app-builder` (ตามคำขอผู้ใช้ "ลองยืนยันข้อมูลใน skill ชุด hap ทั้ง mcp และ cli") | ไฟล์ `workflow_rules.md` ของ skill นั้นระบุชัดว่า: **ต้อง `publish_process` ที่ inner processId ของ `sub_process`/`approval_block` ก่อนเสมอ แล้วจึงค่อย publish outer/main process — ถ้า publish outer ตรง ๆ โดยไม่ publish inner ก่อน จะได้ `NodeAppIsNull` แน่นอน** (`nocoly-hybrid-builder-v2/references/workflow-authoring-mcp.md` บรรทัด 155 ก็มีกฎเดียวกันบันทึกไว้สั้น ๆ อยู่แล้ว แต่ตีความ/ปฏิบัติผิดมาตลอดว่า validate อย่างเดียวพอ) — **ทดสอบยืนยันจริง:** สร้าง process ทดสอบใหม่ (outer `6a90fb59fdab77a41c63662d` trigger+get_multiple(`hr_employee`)+sub_process, inner `6a90fb5dfdab77a41c636699` 1 update_record) → validate(inner)=valid:true → **publish(inner)=published:true v1** → validate(outer)=valid:true → **publish(outer)=published:true v1 ไม่มี `NodeAppIsNull` เลย** — ลบ process ทดสอบทั้ง 2 ตัวทิ้งสะอาดแล้ว (`delete_process` ไม่ cascade ลบ inner อัตโนมัติ ต้องลบแยกทั้งคู่) | **P4-4 (WF-HR-05) และ P4-6 (WF-HR-06) ปลดบล็อกแล้ว** — กฎใหม่บังคับ: ทุกครั้งที่มี `sub_process`/`approval_block` ต้อง publish inner processId ก่อนเสมอ (ไม่ใช่แค่ validate) แล้วจึง validate/publish outer — D-8 (sync "On leave" เข้า `hr_attendance`) ก็พร้อมกลับมาทำใหม่ด้วยลำดับที่ถูกต้องนี้ |
| D-14 | 🆕 27 ส.ค. 2569 (ต่อเนื่อง) — ตรวจสอบ (audit) ฟิลด์ธง/Number ทุกตัวที่สร้างผ่าน workaround `create_app_items`+`addFields` ในตารางอื่น (โดยเฉพาะ `hr_attendance` ที่สร้างพร้อมกันกับ `hr_ot_request` ใน P4-1/P4-2) ว่ามี `defaultValue` ครบหรือยัง ก่อนเริ่มสร้าง WF-HR-05 | พบซ้ำเป็นครั้งที่ 2 ว่าฟิลด์ธง/Number ที่สร้างผ่าน workaround นี้ไม่ได้ตั้ง `defaultValue` อัตโนมัติ — คราวนี้เจอที่ `hr_ot_request.hr_ot_submitted_flag`/`hr_paid_flag` (ระหว่างสร้าง WF-HR-04) ทำให้ workflow ไม่ยิงเลยเงียบ ๆ (เพราะ `ne` branch condition เทียบกับค่าว่างไม่ match) — จุดเดิมเคยเจอกับ `submitted_flag` ของ WF-HR-01 มาแล้วครั้งหนึ่ง | ถ้าไม่ audit ก่อน WF-HR-05 อาจเจอบั๊ก "workflow ไม่ยิงเลย" แบบเดิมซ้ำเป็นครั้งที่ 3 บนฟิลด์ธงของ `hr_attendance` (เช่น `summarised_flag` หรือฟิลด์ธงอื่นที่สร้างพร้อมกัน) — เสียเวลาสืบสวนซ้ำโดยไม่จำเป็น ทั้งที่รู้ pattern แล้ว |
| D-18 | 🟡 **28 ส.ค. 2569 (ต่อเนื่อง 12) — พบระหว่างสร้าง WF-HR-06 — ต้องตัดสินใจก่อนขยายขอบเขต** `hr_ot_request` ไม่มีฟิลด์ "วันที่ส่งคำขอ" (`submitted_at`) | `hr_leave_request` มี `submitted_at` (DateTime) ให้ WF-HR-06 คำนวณ SLA ค้างอนุมัติได้ตรงความหมาย แต่ `hr_ot_request` มีแค่ `hr_approved_at` — ใช้ `_createdAt` ระบบแทนได้แค่คร่าว ๆ ไม่ตรงความหมายถ้า Draft ค้างไว้นานก่อนกดส่งจริง | WF-HR-06 v1 สร้างแล้วจำกัดขอบเขตเหลือแค่ `hr_leave_request` เท่านั้น — ต้องตัดสินใจ (a) เพิ่มฟิลด์ `submitted_at` จริงใน `hr_ot_request` ผ่าน MCP แล้วให้ WF-HR-04 เขียนค่าตอน submit หรือ (b) ยอมรับ `_createdAt` เป็นตัวแทน ก่อนจะขยาย WF-HR-06 ให้ครอบคลุม OT (และ `hr_claim` เมื่อสร้างตารางในเฟส P6) |
| D-17 | 🟢 **ปิดแล้ว 28 ส.ค. 2569 — ไม่ใช่ปัญหาแพลตฟอร์ม/session** สาเหตุจริงคือฟิลด์ธงเป็นค่าว่าง (ไม่ใช่ 0) ทำให้ branch ตกไป path ที่ไม่ทำอะไร ทั้งที่ workflow ยิงจริงและสถานะ "Done" · **กฎที่ยังบังคับใช้: ก่อนสรุปว่า workflow "ไม่ยิง" ต้องเปิด Workflow History ดูสถานะ process ก่อนเสมอ — `_updatedBy` ว่างได้ทั้งกรณี "ไม่ยิงเลย" และ "ยิงแล้วไปจบ path ที่ไม่ทำอะไร"** | บันทึกเต็ม (รวมข้อสรุปผิด 3 รอบและวิธีที่จับได้) → `15-Investigations-D13-D17.md` · ต้นตอเชิงระบบต่อยอดเป็น D-19 | ถ้าไม่เปิด History จะวินิจฉัยผิดซ้ำ |
| D-15 | 📜 **28 ส.ค. 2569 — ส่วน `sub_process` ถูกแทนที่ด้วย D-16 · ส่วน "หยุดยิง" อธิบายด้วย D-17** · **กฎที่ยังบังคับใช้: แทรก node กลางเส้นทางเดี่ยวที่มีปลายทางอยู่แล้ว อาจเกิด auto-relink ที่ไม่ตรงเจตนา และ `validate_process` ไม่จับ ⇒ ต้อง `get_workflow_structure` ตรวจ `prevNode` ของ node ถัดจากจุดแทรกด้วยมือทุกครั้ง** | รายละเอียดเต็ม → `15-Investigations-D13-D17.md` | ถ้าลืมตรวจ `prevNode` จะได้กราฟที่ผิดโดยไม่มี error |
| D-19 | ✅ **แก้แล้ว 30 ส.ค. · ปิดขอบเขต 31 ส.ค. 2569** — เงื่อนไข `<ธง> ≠ "1"` ประเมินเป็น FALSE เมื่อฟิลด์เป็นค่าว่าง 🔴 **ใน `operateCondition` ของ branch** (พิสูจน์ด้วย A/B controlled test 30 ส.ค.) ⇒ workflow ข้ามงานเงียบ ๆ ไม่มี error · 🆕 **31 ส.ค.: probe workflow พิสูจน์ว่าใน `filters` ของ get_multiple (typeId 13) `≠` ผ่านค่าว่างตามปกติ ไม่ได้เป็นบั๊ก** (trigger filter / search typeId 7 ยังไม่ทดสอบ) ⇒ **กระทบจริง 4 จุด (WF-HR-01/02/03/04 branch) ไม่ใช่ 5** · จุดที่ 5 (filter ของ WF-HR-05) ไม่เคยพัง — OR group `8` ที่เพิ่มไปเป็น superset เก็บไว้ได้ · ยิงจริงยืนยัน **3/4** | หลักฐาน ตารางจุดที่กระทบ ทางเลือกการแก้ กับดักตอนแก้ และ **§11 การแก้ขอบเขต + วิธีทำ probe workflow** → `16-D19-Empty-Flag-Branch.md` · **กฎที่ยังบังคับใช้: สแกนเงื่อนไขทั้ง workflow ต้องใช้ `hap workflow node get` ทีละ node — `workflow structure` และ `workflow node list` ไม่คืน `filters` ของ node ชนิด search/get_multiple** | 🔶 **WF-HR-04 แก้แล้วแต่ยังไม่ได้ยิงพิสูจน์** · 🔴 **WF-HR-05 ไม่เคยรันสำเร็จสักครั้ง** (`hr_attendance` มี 1 แถว · `สรุปแล้ว` ว่าง · `_updatedBy` ว่าง) และ `conditionId 42` ที่ยังไม่รู้ความหมายถูก AND อยู่ใน **ทั้งสอง OR group** ของ filter มัน ⇒ D-19 ไม่ใช่ปัญหาแล้ว แต่ตัว workflow ยังไม่ผ่านการพิสูจน์ · `defaultValue` ยัง persist ไม่ได้ ⇒ record ใหม่ยังเกิดพร้อมธงว่าง (workflow ทนแล้ว) |
| D-20 | 🧹 **28 ส.ค. 2569 (ต่อเนื่อง 18) — ส่งต่อฝ่ายบัญชี: พบ orphan/workflow ซ้ำฝั่งบัญชี 12 ตัว (HR ไม่แตะ)** | ระหว่างล้าง orphan ฝั่ง HR สำรวจทั้งแอปพบของฝั่งบัญชีที่ไม่มี parent อ้างถึงหรือเป็นฉบับซ้ำที่ปิดใช้งานไว้: **inner ไม่มี parent 7 ตัว** — `สร้างบรรทัดกลับรายการ` ×5 (`6a8f4286…`/`6a8f42d2…`/`6a8f4310…` ปิดอยู่ · `6a8f437d…`/`6a8f45ba…` **เปิดอยู่**) · `ทำเครื่องหมาย GL ว่าถูกกลับรายการ` ×1 (`6a8f43e4…` เปิดอยู่) · `สร้างรายการภาษีติดลบ` ×1 (`6a8f43c5…` เปิดอยู่) — และ **ฉบับซ้ำระดับบนที่ปิดใช้งาน 5 ตัว** — `WF-AC-02 ผ่านรายการใบสำคัญฯ` (`6a861cfe…`) · `ปิดงวด` (`6a903eb8…`) · `ปิดปีและยกยอด` ×2 (`6a90ef43…`/`6a90f50e…`) · `กลับรายการ` (`6a8fb9ea…`) · ✅ ตรวจแล้วว่า `อนุมัติใบสำคัญ (WF-AC-01)` (`6a8eaa76…`) **ไม่ใช่ตัวซ้ำ** — เป็น approval sub-flow ที่ WF-AC-01 เรียกผ่าน `triggerId` (`appType:9`) | HR **ไม่ลบให้** เพราะเป็น object ของโมดูลบัญชี และมี 3 ตัวที่ `enabled:true` ซึ่งอาจยังถูกอ้างผ่านเส้นทางที่ตรวจไม่พบ — ส่งเรื่องให้ฝ่ายบัญชีตัดสินใจเอง (เอกสาร `tilsna-hr/14-HR-to-Accounting-Orphan-Workflows.md`) · ถ้าปล่อยไว้: รก workflow list ทำให้ audit ยากขึ้น และเสี่ยงแก้ผิดตัวเวลามี process ชื่อซ้ำกัน 5-6 ตัว |

---

## 12. Change Log

📜 **ย้ายไป `19-Change-Log-HR.md` แล้ว (31 ส.ค. 2569)** — เนื้อหาครบเหมือนเดิม ไม่ได้ตัดทิ้ง · §12 เคยกิน 61,712 bytes = 38% ของไฟล์นี้

🔴 **กติกา: ทุกครั้งที่ปิดงาน ให้เขียน Change Log ลง `19-Change-Log-HR.md` ไม่ใช่ไฟล์นี้** — ไฟล์นี้เก็บ **สถานะปัจจุบัน** เท่านั้น
