# Roadmap & Progress Tracker — โมดูล HR · TILSNA ERP
> ไฟล์ติดตามความคืบหน้า **ทั้งโปรเจกต์** แบบรวมศูนย์ — build agent ต้องอัปเดต**ทุกครั้งที่เริ่มหรือปิดงาน** แล้ว sync ตาราง "สถานะงานล่าสุด" ใน `04-CLAUDE-memory.md` ให้ตรงกัน
> อัปเดตล่าสุด: 28 สิงหาคม 2569 (ต่อเนื่อง 17) · อัปเดตโดย: build agent (hap CLI + MCP) — 🔴🔴🆕 **แก้ข้อสรุป D-17 ที่ผิด + เปิด D-19 (ปัญหายังไม่ถูกแก้ทั้งโมดูล)** — จากการ scrutinize: ข้อสรุปเดิม *"defaultValue ไม่ถูกใช้ตอนสร้าง record ผ่าน MCP"* **ผิด** — `hap worksheet fields --raw` แสดงว่า**ฟิลด์ธงทุกตัวมี `default:""`** ไม่มีตัวไหนได้ `defaultValue:0` จริง รวมถึงตัวที่ D-14 อ้างว่าตั้งแล้ว ⇒ สาเหตุจริง = **`editFields` ไม่ persist ค่า default** (แก้ข้อความครบ 7 จุด 3 ไฟล์) · **MCP ไม่คืนค่า `defaultValue` เลย ต้องใช้ hap CLI `--raw`** · 🔴 **D-19: D-17 แก้แค่ 2 record ทดสอบ — WF-HR-02 จะไม่ตัดสิทธิเมื่อ To-do ที่ค้างถูกกด Approve** WF-HR-03/05/13 เสี่ยงเหมือนกัน · **path ผู้ใช้จริงผ่านฟอร์มยังไม่เคยทดสอบ** — ดู §11 D-19 — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 16) — 🟢 **D-17 แก้ไขแล้ว — ไม่ใช่ปัญหาแพลตฟอร์ม/session ตามที่เข้าใจผิดมาตลอด 3 รอบก่อนหน้า** ผู้ใช้ส่งภาพ Workflow History ของ `TEST-LR-D15-REVERIFY` มาถาม พบว่า process จริงสถานะ "Done" (ไม่ใช่ "ไม่ยิง") เข้า node `gate` (branch) แล้วตกไป path "อื่นๆ ไม่ทำอะไร" เพราะ `submitted_flag` ของ record นี้เป็นค่าว่างจริง (ไม่ใช่ 0) — ตรงกับบั๊กเดิมที่เคยพบแล้ว ("`ne` เทียบกับฟิลด์ว่างไม่ผ่าน") — **สรุปใหม่สำคัญ: ฟิลด์ธงเหล่านี้**ไม่มี `defaultValue` เลย** (`default:""` — ยืนยันด้วย `hap worksheet fields <ws_id> --raw` 28 ส.ค. 2569) รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกไว้ว่าตั้ง `defaultValue:0` แล้วเมื่อ 27 ส.ค. ⇒ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องกับบั๊กที่รู้อยู่แล้วว่า `editFields` เขียนทับทั้ง field object) — ยังพิสูจน์ไม่ได้ว่า API เพิกเฉยต่อ default เพราะไม่มีฟิลด์ไหนมี default ให้ทดสอบเลย ⇒ ในทางปฏิบัติ ต้องตั้งค่าฟิลด์ธงเองเสมอตอน seed** — ทดสอบแก้จริง (set flag=0 แล้ว re-trigger) ทั้ง WF-HR-01/WF-HR-04 ยิงสำเร็จทันที ขั้นหัวหน้างานผ่านจริง มี To-do ถึง Wanadtapong.l — ขออภัยที่แนะนำผิดให้ผู้ใช้ไปเช็คสถานะแพลตฟอร์ม/subscription รอบก่อน ดูรายละเอียดเต็มใน §11 D-17 — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 15) · อัปเดตโดย: build agent (MCP) — 🔴🔴 **D-17 re-verify ครั้งที่ 2/3 (ตามคำสั่ง "ดำเนินการต่อไปก่อน") ยังไม่คลี่คลาย** — reset+re-trigger `TEST-LR-D15-REVERIFY` (WF-HR-01) และ `TEST-OT-SYSCHK-01` (WF-HR-04) อีกครั้ง คนละ workflow กับที่เคยพิสูจน์ทำงานมาก่อน รอ 60+ วินาที ทั้งคู่ยังไม่มี `user-workflow` เลย — **ตอนนี้ปัญหายืนยันข้ามช่วง session แล้ว (>7 ชม.)** ยังไม่ใช่ปัญหาชั่วคราวของ session เดียว แนะนำผู้ใช้ตรวจสอบสถานะแพลตฟอร์ม/บัญชีสมัครสมาชิก Nocoly โดยตรง · ✅🆕 **P5-6 เสร็จ** — seed `hr_salary_structure` ให้ TEST-EMP1 (30,000 บาท) และ TEST-EMP2 (35,000 บาท) ผูก pay component SALARY ผ่าน `batch_create_records`(`triggerWorkflow:false`) ยืนยันด้วย `get_record_list` — เป็นตัวเลขทดสอบสมมติ ไม่ใช่เงินเดือนจริง · P5-5 (posting rule หลายบรรทัด) ยังไม่ทำ — พบข้อจำกัด schema ใหม่ (ดูแถว P5-5) ต้องรอการตัดสินใจออกแบบร่วมกับฝ่ายบัญชีก่อน ไม่เดาเอง — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 14) · อัปเดตโดย: build agent (WebSearch/WebFetch) — 🟡🆕 **G-05 cross-check กับกฎหมายทางการแล้ว** ตามคำขอผู้ใช้ "ดึงข้อมูลจากในเน็ตได้ไหม เอาตามกฎหมายก่อน" — อัตราประกันสังคม 5%/5%+เพดาน 17,500 บาท (ราชกิจจานุเบกษา 12 ธ.ค. 2568 มีผล 1 ม.ค. 2569) และขั้นบันไดภาษี 8 ขั้น (rd.go.th ทางการ) **ตรงกับตัวเลขที่ seed ไว้ในระบบทุกจุดอยู่แล้ว** — ยังไม่ใช่ accounting sign-off อย่างเป็นทางการ แนะนำขอยืนยันก่อน go-live · `hr_company_tax_id`/`hr_sso_employer_no` หาจากเน็ตไม่ได้ (เฉพาะบริษัท) ยังเป็น placeholder — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 13) — 🟢🆕 **P5-4/Gap G-02 ปิดครบ 6/6!** ฝ่ายบัญชีตอบกลับ (`tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`) สร้างบัญชีใหม่ 3 รายการ (SSO_ER `530102` / SSO_EE `210402` / WHT `210304`) + เพิ่ม `event_code`=PAYROLL_ACCRUAL/PAYROLL_PAYMENT ใน `ac_posting_rule` ให้แล้ว — HR ผูก `hr_pay_component.biz_coa_account` ต่อสำเร็จครบ 6/6 ผ่าน `update_record` ยืนยันด้วย `get_record_details` · WF-HR-10 source-of-truth ยืนยันแล้ว: `ac_posting_rule` หลัก / `hr_pay_component` fallback · ⚠️ ยังเปิดอยู่: อัตรา SSO/ภาษี WHT/เลขทะเบียนบริษัท (G-05/P0-5) — ฝ่ายบัญชีขอห้ามเดาตัวเลขเหล่านี้เด็ดขาด — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 12) — 🟢🆕 **WF-HR-05 และ WF-HR-06 สร้าง+publish สำเร็จจริงทั้งคู่ผ่าน MCP!** (main×2 + inner×2 process, validate สะอาด 0 error) ยืนยัน D-16 fix ใช้ได้กับ build งานจริง — WF-HR-06 v1 จำกัดขอบเขตเหลือแค่ `hr_leave_request` เพราะพบ gap ใหม่ **D-18** (`hr_ot_request` ไม่มีฟิลด์ `submitted_at`) — ⚠️ ทั้งคู่ยังไม่ได้ live-fire test เพราะ D-17 ยังไม่คลี่คลาย — ดูรายละเอียดใน §11 D-18 และ Change Log — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 11) — 🔴🔴 **D-17: worksheet_event workflow ทั้งหมดไม่ยิงเลยตอนนี้ — เป็นปัญหาระดับ session/environment/platform ไม่ใช่ WF-HR-01 เจาะจง!** ทำตามคำแนะนำ "ทำตามที่แนะนำ" โดย re-verify WF-HR-01 ก่อนเริ่ม WF-HR-05 — สร้าง record ทดสอบใหม่สะอาด (`TEST-LR-D15-REVERIFY`) ยิงไม่ทำงาน (68+ วินาที) → sanity-check ด้วย WF-HR-04 ที่ไม่เคยถูกแตะเลยตั้งแต่ยืนยันสำเร็จ 27 ส.ค. (`TEST-OT-SYSCHK-01`) ก็ไม่ทำงานเช่นกัน → โครงสร้าง WF-HR-01 ตรวจสอบแล้วสมบูรณ์ 100% ⇒ **สรุป: ไม่ใช่ความเสียหายจากการแก้ไข WF-HR-01/D-15 — เป็นปัญหากว้างกว่านั้นมาก ต้องรอ/ตรวจสอบใหม่ session หน้าก่อนเชื่อว่า workflow ใด ๆ ทำงานได้** — P4-4/P4-6 สร้างโครงสร้างทำต่อได้ปกติ แต่ทดสอบยิงจริงต้องรอ D-17 คลี่คลาย — ดู §11 D-17 — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 10) — 🟢🆕 **D-16: D-13 (`sub_process`→`NodeAppIsNull`) คลี่คลายแล้ว ไม่ใช่บั๊กแพลตฟอร์ม!** ตามคำขอผู้ใช้ "ลองยืนยันข้อมูลใน skill ชุด hap ทั้ง mcp และ cli" ตรวจสอบเอกสาร skill `hap-mcp-app-builder` พบ root cause จริง: ต้อง `publish_process` ที่ inner processId ของ sub_process ก่อนเสมอ แล้วจึง publish outer (เราเคยแค่ validate inner ไม่เคย publish จริง) — ทดสอบยืนยันด้วย minimal process ใหม่สำเร็จ 100% ลบทิ้งสะอาดแล้ว — **P4-4 (WF-HR-05) และ P4-6 (WF-HR-06) ปลดบล็อกแล้ว พร้อมสร้างจริง** — ก่อนหน้านั้น: 28 สิงหาคม 2569 — 🔴🆕 **D-15: (1) ยืนยันซ้ำ D-13 `sub_process`→`NodeAppIsNull` ยังไม่หาย → P4-4(WF-HR-05)/P4-6(WF-HR-06) เปลี่ยนเป็น ⛔ (2) ลองย้าย BR-08.3 เข้า WF-HR-01 แล้ว revert กลับสถานะเดิมปลอดภัย 100% แต่พบว่าทดสอบยิง trigger จริงไม่ทำงานเลยทั้งก่อน-หลัง revert (รอ >3 นาที) — ต้อง re-verify WF-HR-01 ใน session หน้าก่อนเชื่อสถานะ 🟢 เดิม, approval instance เดิมของ TEST-LR-0001 ไม่กระทบ** · P5-4 seed `hr_pay_component` 6 รายการสำเร็จ ผูก `coa_account` ได้ 3/6 (ส่งเรื่องช่องว่างผังบัญชีให้ผู้ใช้/ฝ่ายบัญชีแล้ว) — ก่อนหน้านั้น: 27 สิงหาคม 2569 (ต่อเนื่อง) — **P3-4 เสร็จ+ยืนยัน (seed hr_leave_balance ครบ 2 พนักงาน × 6 ประเภท) · P4-1/P4-2 เสร็จ+ยืนยัน (โครงสร้าง hr_attendance + hr_ot_request ครบผ่าน MCP workaround + view rename ผ่าน hap CLI) · 🆕 D-8 พยายามทำแล้วแต่ชนบั๊กแพลตฟอร์มใหม่ (`sub_process` container node → `NodeAppIsNull`) — revert กลับสถานะเดิมปลอดภัยแล้ว ดู D-13 · 🆕🟢 **P4-3/WF-HR-04 (อนุมัติ OT 2 ระดับ) สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ** (ไม่ต้องใช้ `sub_process`) — พบ+แก้บั๊กใหม่ `dateDiff` ตัด `precision` ทิ้งเงียบ (workaround: คำนวณเป็นนาทีแล้วหารเป็น compute node ที่สอง) และเพิ่มฟิลด์ `hr_ot_rate.day_type` ที่ขาดหายไปสำเร็จผ่าน API · 🆕✅ **P4-5 (ปุ่ม "ส่งขอล่วงเวลา" + view OT รออนุมัติ/ลงเวลาผิดปกติ) เสร็จ+verify ผ่านจริงด้วย Claude in Chrome** — พบกับดัก Custom Action Scope="Unassigned View" ซ้ำจากโมดูลบัญชี**
> สถานะ: ⬜ ยังไม่เริ่ม · 🚧 กำลังทำ · 🔶 บางส่วน · ⚠️ มีแต่ยังไม่พิสูจน์ทำงาน · ✅ เสร็จ+ยืนยัน · ⛔ ถูกบล็อก
> 🔴 **ห้ามเลื่อนงานเป็น ✅ ถ้ายังไม่มีหลักฐาน verify** — ใช้ ⚠️ จนกว่าจะพิสูจน์ได้

---

## 1. ภาพรวมความคืบหน้า

| เฟส | ชื่อเฟส | งานทั้งหมด | ✅ | 🚧 | ⬜ | ⛔ | % |
|---|---|---|---|---|---|---|---|
| P0 | เตรียมความพร้อมและปลดบล็อก | 6 | 1 | 0 | 5 | 0 | 17% |
| P1 | โครงสร้างพื้นฐานและตารางตั้งค่า | 5 | 3 | 1 | 1 | 0 | 70% |
| P2 | Core HR — ทะเบียนพนักงาน | 6 | 2 | 0 | 4 | 0 | 33% |
| P3 | การลา (โมดูล demo แรก) | 8 | 5 | 2 | 1 | 0 | 62% |
| P4 | ลงเวลาและล่วงเวลา | 6 | 4 | 0 | 2 | 0 | 67% |
| P5 | เงินเดือนและภาษี | 11 | 1 | 1 | 8 | 1 | 9% |
| P6 | สวัสดิการและการเบิกจ่าย | 6 | 0 | 0 | 6 | 0 | 0% |
| P7 | สรรหา ประเมิน อบรม | 8 | 0 | 0 | 8 | 0 | 0% |
| P8 | สิทธิ์ รายงาน เอกสาร go-live | 8 | 0 | 0 | 5 | 3 | 0% |
| **รวม** | | **64** | **16** | **4** | **41** | **3** | **25%** |

**Critical path:** P0#2 (บัญชีทดสอบ) → P2 (ทะเบียนพนักงาน) → P3 (การลา) → P4 (ลงเวลา) → P5 (เงินเดือน) → P8 (สิทธิ์)
**สิ่งที่บล็อกหนักที่สุดตอนนี้:** มีบัญชีผู้ใช้ในองค์กรเพียง 2 บัญชี ⇒ ทดสอบการอนุมัติ 2 ระดับและสิทธิ์ 8 บทบาทไม่ได้เลย
**ปลดบล็อกสำเร็จ 26 ส.ค. 2569:** `create_worksheet` ยังพังอยู่ แต่พบทางเลือกสำรองที่ใช้งานได้ 100% (`create_app_items`→`update_worksheet`[removeFields+addFields รวมคำสั่งเดียว]) — ใช้สร้าง HR-00 ครบ 10 ตาราง และล่าสุด 3 ตารางใบลา (P3-1) สำเร็จอีกครั้ง
**ปลดบล็อกเพิ่ม 27 ส.ค. 2569:** WF-HR-02/WF-HR-03 สร้าง+publish+ทดสอบยิงจริงสำเร็จ — พบและแก้บั๊กแพลตฟอร์มใหม่ 2 ตัวระหว่างทาง (ดู Change Log และ `04-CLAUDE-memory.md`/`nocoly-shared/00-HAP-Working-Guide.md`)
**พบข้อจำกัดแพลตฟอร์มใหม่ 27 ส.ค. 2569 (P3-8):** ทั้ง Business Rules และ Dynamic Default ("Query worksheet") ไม่รองรับการอ้างอิง **Relation-type field ของฟอร์มปัจจุบัน** เป็นค่าเปรียบเทียบ (Dynamic Value) เลย — field picker ไม่แสดงให้เลือก ⇒ lookup ข้ามตารางที่อ้างอิง relation field ต้องย้ายไปทำที่ระดับ workflow เสมอ (ดูรายละเอียดในแถว P3-8 และ Known Issues ของ `04-CLAUDE-memory.md`)
**ทำต่อ 27 ส.ค. 2569 (ตามคำขอผู้ใช้ "เน้นงานที่ผ่าน MCP/CLI ได้ก่อน"):** P3-4 seed ครบ · P4-1 `hr_attendance` และ P4-2 `hr_ot_request` สร้างโครงสร้างครบผ่าน MCP + hap CLI (view rename) — ดูรายละเอียดแถว P4-1/P4-2 และ Change Log
**ทำต่อ 27 ส.ค. 2569 (ต่อเนื่อง — หลัง D-8/D-13 บล็อก เปลี่ยนไปทำงานที่ไม่ต้องใช้ `sub_process`):** เพิ่มฟิลด์ `hr_ot_rate.day_type` ที่ขาดหายไปสำเร็จผ่าน `addFields`+`editFields` (ไม่ต้องรอ Browser) · **P4-3/WF-HR-04 (อนุมัติ OT 2 ระดับ) สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ** ด้วยสถาปัตยกรรม gate-branch เดียวกับ WF-HR-01 บวกเทคนิคใหม่ "share inner approval flow ข้าม 2 เส้นทาง day-type ผ่าน `mode:use_existing`" — พบ+แก้บั๊กใหม่ `dateDiff` compute node ตัด `precision` ทิ้งเงียบ ๆ (workaround: คำนวณเป็นนาทีก่อนด้วย `dateDiff`, แล้วหารเป็นชั่วโมงด้วย compute node ชนิด `number` ที่มี `precision` แยกอีกตัว) — ดูรายละเอียดแถว P4-3 และ Change Log

---

## 2. P0 — เตรียมความพร้อมและปลดบล็อก 🔴 ทำก่อนทุกอย่าง

| # | งาน | สถานะ | effort | ขึ้นกับ | ผู้รับผิดชอบ | หลักฐานที่ต้องมี |
|---|---|---|---|---|---|---|
| P0-1 | ยืนยันข้อสมมติ 18 ข้อ (A-HR-01…18) กับผู้ใช้ แล้วเขียนกลับ BRD §11 | ⬜ | S | — | ผู้ใช้ + agent | BRD §11 ไม่มีคำว่า "ยังไม่ได้รับการยืนยัน" |
| P0-2 | **ขอเพิ่มบัญชีผู้ใช้ทดสอบ 4 บัญชี** (พนักงาน · หัวหน้า · HR · Payroll) | ⬜ | S | ผู้ดูแลองค์กร | ผู้ใช้ | `get_role_list` เห็นบัญชีใหม่ |
| P0-3 | เพิ่ม option `PAYROLL_ACCRUAL` / `PAYROLL_PAYMENT` ใน `ac_posting_rule.event_code` | ✅ **28 ส.ค. 2569 (ต่อเนื่อง 13) — ฝ่ายบัญชีทำแล้ว** | S | Backup ก่อน | ฝ่ายบัญชี (ทีม tilsna-accounting) | ✅ **แก้ไข: ไม่ใช่ Browser-only อย่างที่ HR เข้าใจผิดตอนแรก** — `event_code` เป็น **inline SingleSelect** (fieldId `6a85518c055f2288c5b7430b`) ไม่ใช่ shared optionset จึงแก้ผ่าน MCP `editFields` ได้ตรง ๆ — ฝ่ายบัญชีเพิ่มให้แล้ว: `PAYROLL_ACCRUAL` (key `ad4850c4-f1c1-4a65-adbb-fb0054a6e0c8`), `PAYROLL_PAYMENT` (key `04a14b13-73c2-408b-a5fb-bb81ff630a06`) — อ้างอิง `tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md` |
| P0-4 | เพิ่ม option สำหรับ HR ใน `ac_voucher.source_doc_type` **ผ่านหน้าจอ** (ทางเลือก — ถ้าไม่ทำใช้ `source_module` แทน) | ⬜ | S | Backup | agent | เห็น option ใหม่ |
| P0-5 | ยืนยันอัตราประกันสังคม เพดานฐานค่าจ้าง และขั้นบันไดภาษีที่บังคับใช้ กับฝ่ายบัญชี | 🟡 **28 ส.ค. 2569 (ต่อเนื่อง 7)** | M | — | ผู้ใช้ | ✅ cross-check กับกฎหมายทางการแล้ว (ราชกิจจานุเบกษา + rd.go.th) ตรงกับที่ seed ไว้ทุกจุด — ⚠️ ยังไม่ใช่ตัวเลขที่ฝ่ายบัญชี**ลงนามรับรองอย่างเป็นทางการ** ยังต้องขอ sign-off ก่อน go-live · `hr_company_tax_id`/`hr_sso_employer_no` หาจากเน็ตไม่ได้ ยังเป็น placeholder |
| P0-6 | **Backup แอปก่อนเริ่มสร้าง object** | ⬜ | S | — | agent | มีจุด restore ในวันที่เริ่ม |

---

## 3. P1 — โครงสร้างพื้นฐานและตารางตั้งค่า

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P1-1 | สร้างกลุ่มในแอป 8 กลุ่ม (HR + HR-00…HR-06) | ✅ | S | P0-6 | `get_app_info` ยืนยันแล้ว 26 ส.ค. 2569 — Section ID เติมใน BuildSpec §1.2 ครบ |
| P1-2 | สร้าง optionset ใหม่ 24 ชุด | ✅ | M | — | ยืนยันแล้ว 26 ส.ค. 2569 — `get_optionset_list` เห็นครบ 24 · option key ทุกตัวคัดลงใน BuildSpec §1.6 / §1.6.1 แล้ว |
| P1-3 | สร้างตารางตั้งค่า HR-00 ทั้ง 10 ตาราง (ผูก optionset ตอน `addFields`) | 🚧 | M | P1-2 | ✅ **10/10 ตารางสร้างแล้ว 26 ส.ค. 2569** ผ่านทางเลือกสำรอง `create_app_items`→`get_worksheet_structure`→`removeFields`→`addFields`→verify (เพราะ `create_worksheet` ยังพัง) · ws ID + field ID ครบใน BuildSpec §1.3/§2 · ⚠️ ค้าง 2 ฟิลด์ Dropdown ที่ต้องผูก shared optionset (`hr_leave_type.carry_policy` · `hr_ot_rate.employment_type`) — ทำได้เฉพาะใน Browser · ✅ **27 ส.ค. 2569 (ต่อเนื่อง) — `hr_ot_rate.day_type` เพิ่มสำเร็จผ่าน API** (พบว่าตารางนี้ขาดฟิลด์นี้ไปตั้งแต่แรก ไม่ใช่ปัญหา Browser-only) ผ่าน `addFields` เป็น inline options (label อังกฤษตรงกับ `OS_HR_OT_DAY_TYPE` — ลบ+สร้างใหม่ 1 ครั้งเพื่อแก้ label ที่พลาดเป็นไทยรอบแรก) fieldId `6a8fd7608b6633ef76f13156` — seed 3 record เดิมอัปเดต `day_type` ครบแล้ว — ยังไม่ bind shared optionset จริง (เหมือน D-11/D-12) · ⚠️ ค้าง relation 2 ฟิลด์รอ P2-1 สร้างตารางปลายทาง (`hr_leave_type.pay_component`→`hr_pay_component`, `hr_leave_policy.job_level`→`hr_job_level`) |
| P1-4 | seed ข้อมูลตั้งค่า: `hr_setting` 1 record · วันหยุด 1 ปี · กะ 1 กะ · ประเภทการลา 6 · นโยบายสิทธิ · อัตรา OT 3 · อัตรา SSO · ขั้นบันไดภาษี · ค่าลดหย่อน | ✅ | M | P1-3 · P0-5 | ✅ **เสร็จ 26 ส.ค. 2569** — 55 record ใน 9/10 ตาราง (`hr_approval_rule` ยกไปรอ role) ยืนยันด้วย `get_record_pivot_data` COUNT ทุกตาราง · รายละเอียดใน BuildSpec §2 ต่อ FR · ⚠️ ตัวเลข SSO/ภาษียังเป็น "ยืนยันจากแหล่งข่าวสาธารณะ" ไม่ใช่ตัวเลขที่ฝ่ายบัญชีเซ็นรับรอง (G-05 ยังเปิดอยู่) · ⚠️ `hr_company_tax_id`/`hr_sso_employer_no` เป็น placeholder |
| P1-5 | สร้าง unique index ของตารางตั้งค่า (IX-01.1 · IX-02.1 · IX-07.1) | ⬜ | S | P1-4 | `create_record` ซ้ำถูกปฏิเสธ |

---

## 4. P2 — Core HR (ทะเบียนพนักงาน)

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P2-1 | สร้าง `hr_position` · `hr_job_level` + seed | ✅ | S | P1-3 | ws ID เติมใน §1.3/§2 แล้ว · 6 job_level + 8 position record seeded 26 ส.ค. 2569 ยืนยันด้วย `get_record_pivot_data` COUNT |
| P2-2 | สร้าง `hr_employee` (36 ฟิลด์) — ฟิลด์ `supervisor` ชี้ตัวเองเพิ่มทีหลังด้วย `addFields` | ✅ | L | P2-1 | `get_worksheet_structure` ยืนยันครบ 36 ฟิลด์ + supervisor self-relation 26 ส.ค. 2569 · field ID เติมใน §2 FR-HR-03 แล้ว |
| P2-3 | ผูกฟิลด์ Dropdown ของ `hr_employee` (`gender`/`marital_status`/`employment_type`/`emp_status`) เข้ากับ shared optionset ที่มีอยู่ (🔴 Browser UI เท่านั้น) | ⬜ | M | P2-2 | ยืนยันข้อจำกัดแล้ว 26 ส.ค. 2569 ผ่าน hap CLI + ตรวจ WireControl JSON schema โดยตรง — สรุปชัดเจนว่าไม่มีทาง API/CLI ใด ๆ ทำได้ ต้องทำผ่าน Browser UI เท่านั้น |
| P2-4 | ตั้ง default 0 ให้ฟิลด์ธงทุกตัว (Browser) | ⬜ | S | P2-2 | เปิดฟอร์มเห็นค่า 0 |
| P2-5 | สร้าง unique index IX-03.1 (`emp_code`) · IX-03.2 (`national_id`) + ทดสอบผ่าน API | ⬜ | S | P2-2 | **AC-15 ส่วนแรก** |
| P2-6 | สร้าง `hr_employment_contract` · `hr_employment_event` · `hr_dependent` · `hr_bank_account` + seed พนักงานทดสอบ 4 คน | 🔶 | M | P2-2 · P0-2 | ✅ ทั้ง 4 ตารางสร้างแล้ว 26 ส.ค. 2569 (ws/field ID เติมใน §1.3/§2 แล้ว) · ✅ **แก้ Data Name ภาษาจีน (`记录`) เป็นไทยแล้ว 26 ส.ค. 2569** (พบค้างตอนตรวจซ้ำรอบ 2 ตามคำขอผู้ใช้ — ใช้ `update_worksheet.addRecordButtonName` เหมือนตารางใบลา) · ⬜ ยังไม่ seed พนักงานทดสอบ EMP-T1…T4 — บล็อกบางส่วนโดย G-06 (มีบัญชีผู้ใช้จริงในองค์กรแค่ 2 บัญชี) |

---

## 5. P3 — การลา 🎯 **โมดูลที่ควรทำ demo แรก**

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P3-1 | สร้าง `hr_leave_balance` · `hr_leave_request` · `hr_leave_ledger` | 🚧 | M | P1-3 · P2-2 | ✅ **โครงสร้างฟิลด์ครบทั้ง 3 ตาราง 26 ส.ค. 2569** ผ่านทางเลือกสำรอง `create_app_items`→`update_worksheet`(removeFields+addFields รวมคำสั่งเดียว — ยืนยันใหม่ว่ารวมได้ ไม่ต้องแยก 2 คำสั่ง)→verify · ws ID + field ID ครบใน `02b-BuildSpec-Addendum-P3-1.md` (ไม่ได้แก้ไฟล์ `02-BuildSpec-FRS.md` หลักตรง ๆ เพื่อลดความเสี่ยงเขียนทับ — ไฟล์ addendum override ค่า `<TBD>` ของแถว #20–22 ใน §1.3 และฟิลด์ FR-HR-08/09) · ⚠️ **ยังไม่เสร็จสมบูรณ์:** ยังไม่ seed record ทดสอบเพื่อยืนยัน defaultValue จริง (readback md ไม่แสดงค่า default) · `leave_unit`/`leave_status`/`ledger_type` เป็น **inline options** ยังไม่ผูก shared optionset (`OS_HR_LEAVE_UNIT`/`OS_HR_REQUEST_STATUS`/`OS_HR_LEDGER_TYPE`) — Browser-only · `hr_leave_ledger.ledger_no` เป็น **Text ชั่วคราว** แทน AutoNumber ที่สเปกกำหนด (API ทำ AutoNumber ไม่ได้) · ✅ ตั้ง `addRecordButtonName` (Data Name ไทย) ครบแล้วทั้ง 3 ตาราง 26 ส.ค. 2569 ผ่าน `update_worksheet.addRecordButtonName` ยืนยันด้วย `hap worksheet info` |
| P3-2 | สร้างฟิลด์ `AutoNumber` ของ ledger ใน Browser + ทดสอบเลขจริง | ⬜ | S | P3-1 | 🔴 **เปลี่ยนสถานะ:** ตอนนี้ `hr_leave_ledger.ledger_no` เป็น Text ชั่วคราวแล้ว (ใช้งานได้จริงแม้ไม่ใช่ AutoNumber) — งานนี้กลายเป็น "ทางเลือก" ไม่ใช่ blocker: ตัดสินใจว่าจะแปลงเป็น AutoNumber จริงใน Browser หรือใช้ Text ต่อไปโดยให้ workflow เขียนสตริงเอง |
| P3-3 | สร้าง unique index IX-08.1 · IX-09.1 | ⛔ | S | P3-1 | **27 ส.ค. 2569 — ตัดสินใจ "ยังไม่เปิดใช้" IX-08.1** ดูรายละเอียดเหตุผลในแถว P3-8 (สรุป: `leave_no` ไม่ required + ยังไม่ auto-generate ⇒ เปิด unique index ตอนนี้จะบล็อกการสร้างใบลา Draft ใบที่ 2 ขึ้นไป) — รอ D-9/D-10 ก่อน |
| P3-4 | seed `hr_leave_balance` ให้พนักงานทดสอบครบทุกประเภท | ✅ | S | P3-1 · P2-6 | ✅ **เสร็จ+ยืนยัน 27 ส.ค. 2569** — TEST-EMP1 และ TEST-EMP2 มีครบ 6 record/คน (UNPAID=30, ORDINATION=15, MATERNITY=98, PERSONAL=3, SICK=30, ANNUAL=6 ตามนโยบายที่ seed ไว้ — ยกเว้น TEST-EMP1.ANNUAL คงค่าเดิม 10 ที่ใช้ทดสอบ WF-HR-02/03 ไปแล้วไว้เหมือนเดิม ไม่แตะ) ผ่าน `batch_create_records`(`triggerWorkflow:false`) · ยืนยันด้วย `get_record_pivot_data`(group by employee, COUNT rowid) = 6 ทั้งสองคน |
| P3-5 | **WF-HR-01 อนุมัติใบลา 2 ระดับ** | 🟢 **D-17 แก้ไขแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 16) — ไม่ใช่ปัญหาระบบ เป็นบั๊ก `submitted_flag` ว่างบน record ทดสอบ (ดู §11 D-17) — ยิงสำเร็จแล้วหลังแก้ ขั้นหัวหน้างานผ่านจริง มี To-do ถึง Wanadtapong.l อีกครั้ง** ~~🔴 D-17: re-verify 28 ส.ค. 2569 (ต่อเนื่อง 11) พบว่าไม่ยิง — แต่เป็นปัญหาระดับระบบ ไม่ใช่ WF-HR-01 เจาะจง~~ | L | P3-4 | หลักฐานเดิม (27 ส.ค.) ที่เคยผ่าน: `_updatedBy` = **`user-workflow`** ✅ (record `4a226f18-5f2b-4af4-b212-4c016f50b9ec`) · `get_approval_list_by_row` มี To-do จริงถึง Wanadtapong.l ✅ · **AC-01 (ขั้นหัวหน้างาน) ผ่าน** · ⬜ ขั้น HR (`appr_block_2`) ยังไม่ทดสอบ — รอ `hr_approval_rule` seed (ต้องมี role ก่อน P8-1←G-06) และรอมนุษย์กด Approve จริงใน To-do (A-HR-15) · รายละเอียดสถาปัตยกรรมและบั๊กที่แก้ระหว่างทางอยู่ใน `04-CLAUDE-memory.md` แถว #12 และ `nocoly-shared/00-HAP-Working-Guide.md` §2 ข้อ 16-17 · 🔴 **workflow ต้องอ้าง option key แบบ inline ของ `leave_status`/`leave_unit` ที่บันทึกใน `02b-BuildSpec-Addendum-P3-1.md` ไม่ใช่ key ของ `OS_HR_REQUEST_STATUS`/`OS_HR_LEAVE_UNIT` ใน BuildSpec §1.6.1 (คนละชุดกันจนกว่าจะรีไบนด์ผ่าน Browser)** · **28 ส.ค. 2569 (ต่อเนื่อง 11): re-verify ด้วย record ทดสอบใหม่สะอาด (`TEST-LR-D15-REVERIFY`) พบว่าไม่ยิงเลย (68+ วินาที ไม่มี `user-workflow`) แต่โครงสร้าง (main+inner) ตรวจสอบผ่าน `get_workflow_structure` แล้วว่า published/สมบูรณ์ 100% ไม่มี warning — sanity-check กับ WF-HR-04 (แถว P4-3) ก็ไม่ยิงเช่นกัน ⇒ สรุปว่าเป็นปัญหา session/environment/platform กว้างกว่า WF-HR-01 เอง ไม่ใช่ความเสียหายจาก BR-08.3/D-15 — ต้อง re-verify ซ้ำอีกครั้งใน session ถัดไปก่อนเชื่อสถานะเดิม** |
| P3-6 | **WF-HR-02 ตัดสิทธิ** + **WF-HR-03 คืนสิทธิ** | 🟢 **สร้าง+publish+ทดสอบยิงจริงสำเร็จทั้งคู่ 27 ส.ค. 2569** | M | P3-5 | **AC-01 (ส่วนตัดสิทธิ) · AC-03 ผ่านทั้งคู่** — WF-HR-02 `6a8fa97cfdab77a41c51cb3f` (publish v3) · WF-HR-03 `6a8fab800e97bf440dddb744` (publish v1) · หลักฐาน: balance `56c008e2-…` 10.0→9.0 (ตัด)→10.0 (คืน) ทั้งคู่ `_updatedBy`=`user-workflow` · ledger มีคู่กัน 2 แถว (`-1.0` Leave taken balance_after=9.0 · `+1.0` Leave returned balance_after=10.0) ทั้งคู่ relation ชี้ balance/leave_request ถูกต้อง (ไม่ใช่ "已删除") · ทดสอบยิงซ้ำ (idempotency) แล้วไม่ตัดซ้ำ (balance/ledger ไม่เปลี่ยน) · 🔴 **ระหว่างทางพบบั๊กแพลตฟอร์มใหม่ 2 ตัว** (ดู Change Log ด้านล่างและ `04-CLAUDE-memory.md`/`00-HAP-Working-Guide.md`) — แก้ในทั้ง 2 workflow แล้ว · ⚠️ **เลื่อนออกจากขอบเขตรอบนี้ (ลองแก้ 27 ส.ค. 2569 รอบเย็นแล้วชนบั๊กใหม่ — ดู D-8/D-13):** node เดิมของสเปกที่อ้าง `hr_attendance` ตอนนั้นยังไม่มีตาราง — P4-1 เสร็จแล้วแต่การเพิ่ม node กลับต้องใช้ `sub_process` ซึ่งชนบั๊ก `NodeAppIsNull` ใหม่ที่ยังไม่มีทางแก้ — revert กลับ publish v4/v2 (logic เดิมไม่เปลี่ยน) รอทางแก้ |
| P3-7 | ปุ่ม "ส่งคำขอ" / "ยกเลิกใบลา" + view 4 มุมมองของใบลา | ✅ | M | P3-1 | ✅ **ยืนยันแล้ว 27 ส.ค. 2569** — เปิดฟอร์ม TEST-LR-0001 แล้วดูปุ่มไม่โผล่ตอน `leave_status`=Cancelled (ถูกต้อง) → ตั้งชั่วคราวเป็น Draft ผ่าน MCP แล้วเปิดฟอร์มซ้ำ → ปุ่ม "ส่งคำขอ" และ "ยกเลิกใบลา" โผล่ทั้งคู่ตาม `enableWhen` จริง → revert กลับ Cancelled · view ครบ 4 มุมมองตามสเปก (ใบลาของฉัน `6a8fb8451378964f9984a0b5` · รออนุมัติ หัวหน้างาน `6a8fb845ae2a0e3743a0c34f` · รออนุมัติ HR `6a8fb8458b6633ef76f12f64` · ปฏิทินการลาของทีม `6a8fb845353e1b0e4a507c38`) · 🧹 **ทำความสะอาด ghost views ซ้ำ 8 อัน (2 ชุด × 4)** ที่ค้างเป็นผลข้างเคียงจาก Agent tool call ที่ถูก reject ในเซสชันก่อนหน้า — ลบครบแล้วเหลือ 5 view ที่ถูกต้อง (ทั้งหมด + 4 มุมมองจริง) — ดู Known Issues ใน `04-CLAUDE-memory.md` |
| P3-8 | Business Rules ของใบลา (BR-08.1…5) + Dynamic default DV-08.1 + unique index IX-08.1 | 🔶 | M | P3-1 | 🔶 **บางส่วนเสร็จ 27 ส.ค. 2569 — AC-02 บางส่วน** · ✅ **BR-08.1** (validation: `leave_to` is before `leave_from` → block) · ✅ **BR-08.4** (interaction: `leave_status` ≠ Draft → Read-only all) · ✅ **BR-08.5** (interaction: `leave_unit` ≠ Full day → Hide `leave_to` / Show `leave_hours`) — ทั้ง 3 ข้อสร้างและ **ยืนยันผ่าน Role Debugging (role Member)** แล้ว: BR-08.4/8.5 ยืนยันด้วยการเปิด record จริงเห็นฟอร์ม read-only + hide/show ตามเงื่อนไข, BR-08.1 ยืนยันด้วยการอ่านค่ากลับจากแผง Business Rules (condition ตรงสเปก) · ⛔ **BR-08.2** (บล็อกเมื่อ `leave_days` > สิทธิคงเหลือ) และ **BR-08.3** (บังคับแนบไฟล์ตาม `leave_type.require_document`) **ทำเป็น Business Rule ไม่ได้** — field picker ของ Business Rules ไม่รองรับการอ้างอิงฟิลด์บนตารางที่เชื่อมผ่าน Relation field (ทดสอบแล้ว: ค้นหาฟิลด์ของ `leave_type` ในเงื่อนไข ได้ "No search results") ⇒ ต้องย้ายไปบังคับที่ **WF-HR-01** (BR-08.2 มีแผนอยู่แล้วใน BuildSpec) และ **WF-HR-06 หรือโหนดใหม่** (BR-08.3) แทน · ⛔ **DV-08.1 ทำไม่ได้ผ่าน UI Dynamic Default เช่นกัน (พบใหม่)** — ทดสอบจริงที่ Field "ผู้อนุมัติที่ระบบกำหนด" (`approver_user`) → Default Value → Query worksheet → เลือก worksheet `ทะเบียนพนักงาน` ได้ปกติ แต่ในขั้น Query conditions ช่อง "Dynamic Value" ที่ต้องเทียบ `Record ID` ของ hr_employee กับฟิลด์ `พนักงาน` (`employee`, Relation) ของฟอร์มปัจจุบัน **ไม่แสดง field ชนิด Relation ให้เลือกเลย** (ค้นหาด้วยชื่อฟิลด์ตรง ๆ ก็ไม่เจอ — เห็นเฉพาะ Text/Date/Number) ⇒ ข้อจำกัดเดียวกับ Business Rules ขยายมาถึง Dynamic Default ด้วย — ต้องย้ายทั้ง `approver_user` (จาก `employee.supervisor_user`) และ `balance_snapshot` (จาก query `hr_leave_balance` filter employee+leave_type+leave_year) ไปทำที่ **WF-HR-01** แทน (query worksheet + update_record) · ⛔ **IX-08.1 ตัดสินใจ "ยังไม่เปิดใช้" (พบใหม่)** — เปิดหน้า Search Acceleration → Create Index → hover tooltip ของ "Unique Index" ยืนยันข้อความจริงจากระบบ: *"Once a unique index is established, the value of the field cannot be duplicated. If the field is not required, the entire worksheet can only have one empty record to ensure the uniqueness of the data."* เนื่องจาก `leave_no` เป็น Text ธรรมดา **ไม่ required และไม่ auto-generate ตอนสร้าง record** (ผู้ใช้ต้องปล่อยว่างตอน Draft แล้วรอระบบ/workflow ใส่เลขทีหลัง) การเปิด unique index ตอนนี้จะทำให้สร้างใบลา Draft ได้แค่ใบเดียวทั้งระบบที่ `leave_no` ว่าง — **ไม่เปิดใช้จนกว่าจะมีกลไก auto-generate `leave_no` ทันทีตอนสร้าง record** (ดู D-9/D-10) · 🔴🆕 **28 ส.ค. 2569 — ลองย้าย BR-08.3 เข้า WF-HR-01 จริง พบปัญหา 2 ชั้นแล้ว revert กลับสถานะเดิมปลอดภัย (รายละเอียดเต็มดู D-15)**: (1) แทรก branch node กลางเส้นทางเดี่ยวที่มีปลายทางเดิมอยู่แล้ว ทำให้ปลายทางเดิม (`set_submitted`) ถูก auto-relink ไปอ้าง node ของ branch เองแทน path ย่อยที่ตั้งใจ (`validate_process` ผ่านสะอาดแม้แปลก) (2) **ทดสอบยิงจริงด้วย record ใหม่ 2 ใบไม่พบ `_updatedBy`=`user-workflow` เลยแม้รอเกิน 3 นาที ทั้งก่อน revert (v4) และหลัง revert กลับโครงสร้างเดิม 100% (v5)** — ตรวจแล้วว่า approval instance เดิมของ TEST-LR-0001 ไม่กระทบ (`get_approval_list_by_row` ยังเห็น To-do เดิมถึง Wanadtapong.l ปกติ) — **สรุป: BR-08.3 ยังไม่ได้เพิ่ม (revert สำเร็จทุกจุด) แต่พบประเด็นใหม่ต้องเฝ้าระวัง: ต้องตรวจสอบซ้ำ session หน้าว่า WF-HR-01 ยิง trigger จริงได้ปกติหรือไม่ ก่อนเชื่อสถานะ 🟢 เดิม** |

---

## 6. P4 — ลงเวลาและล่วงเวลา

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P4-1 | สร้าง `hr_attendance` + unique index IX-10.1 (`employee`, `work_date`) | 🔶 | M | P2-2 | ✅ **โครงสร้างฟิลด์ครบ 27 ส.ค. 2569** — สร้างผ่าน `create_app_items`(`6a8fcd67353e1b0e4a507d32`)→`update_worksheet`(removeFields 3 default + addFields 16 ฟิลด์ครบตาม FR-HR-10)→`get_worksheet_structure` verify · relation ทุกตัวชี้ถูก (`employee`→`hr_employee` `6a8efa5e9762533b5b7185c1` · `shift`→`hr_shift` `6a8eebf79762533b5b7184c5` · `leave_request`→`hr_leave_request` `6a8f2dbaae2a0e3743a0beaa` · `ot_request`→`hr_ot_request` `6a8fcca48b6633ef76f13033` — ตัวหลังนี้ต้องสร้าง P4-2 ก่อนจึงเรียงลำดับสร้าง `hr_ot_request` ก่อน `hr_attendance`) · view "全部"→"ทั้งหมด" (`6a8fcd67353e1b0e4a507d36`) แก้ผ่าน hap CLI แล้ว · ⚠️ **ยังไม่เสร็จ:** IX-10.1 unique index (Browser-only) · `att_status`/`att_source` เป็น **inline options** (label อังกฤษตรงกับ `OS_HR_ATT_STATUS`/`OS_HR_ATT_SOURCE` แต่ยังไม่ผูก shared optionset จริง — Browser-only) · BR-10.1/BR-10.2 (Business Rules) · ✅ `addRecordButtonName` (Data Name ไทย) = "บันทึกลงเวลา" ตั้งสำเร็จ (ยืนยันด้วย `hap worksheet info` → `entityName`) — **AC-15** · 🔶 **D-8 ลองทำแล้ว 27 ส.ค. 2569 (รอบเย็น) แต่ชนบั๊ก `sub_process`→`NodeAppIsNull` ใหม่ — ยังไม่ปิดงานได้ ดู D-13** |
| P4-2 | สร้าง `hr_ot_request` + IX-11.1 | 🔶 | M | P2-2 · P1-3 | ✅ **โครงสร้างฟิลด์ครบ 27 ส.ค. 2569** — สร้างผ่าน `create_app_items`(`6a8fcca48b6633ef76f13033`)→`update_worksheet`(removeFields 3 default + addFields 16 ฟิลด์ครบตาม FR-HR-11B)→`get_worksheet_structure` verify · relation `employee`→`hr_employee` ถูกต้อง · `day_type`/`ot_status` เป็น **inline options** (label อังกฤษตรงกับ `OS_HR_OT_DAY_TYPE`/`OS_HR_REQUEST_STATUS` แต่ยังไม่ผูก shared optionset จริง — Browser-only; **แก้ไข 27 ส.ค. 2569:** สร้าง `day_type` เป็นภาษาไทยผิดพลาดครั้งแรก ["OT วันทำงาน"ฯลฯ ตามชื่อ record ของ `hr_ot_rate`] แล้วแก้เป็นภาษาอังกฤษให้ตรง optionset ภายหลังด้วย `editFields`) · view "全部"→"ทั้งหมด" (`6a8fcca48b6633ef76f13037`) แก้ผ่าน hap CLI แล้ว · ⚠️ **ยังไม่เสร็จ:** IX-11.1 unique index (`employee`,`ot_date`,`ot_from`) (Browser-only) · BR-11.1/11.2/11.3 · `pay_period` relation (รอ P5-1 สร้าง `hr_pay_period`) · ✅ `addRecordButtonName` = "ใบขออนุมัติล่วงเวลา" ตั้งสำเร็จ (ยืนยันด้วย `hap worksheet info` → `entityName`) |
| P4-3 | **WF-HR-04 อนุมัติ OT 2 ระดับ** (พร้อมหา `day_type` และตัวคูณจากตาราง) | 🟢 **D-17 แก้ไขแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 16) — sanity-check ที่ "ไม่ยิง" เกิดเพราะ record ทดสอบ `TEST-OT-SYSCHK-01` มี `hr_ot_submitted_flag` ว่างจริง (ไม่ใช่ 0) ทั้งที่ field ตั้ง defaultValue:0 ไว้แล้ว — **สรุปใหม่: ฟิลด์ธงเหล่านี้**ไม่มี `defaultValue` เลย** (`default:""` — ยืนยันด้วย `hap worksheet fields <ws_id> --raw` 28 ส.ค. 2569) รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกไว้ว่าตั้ง `defaultValue:0` แล้วเมื่อ 27 ส.ค. ⇒ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องกับบั๊กที่รู้อยู่แล้วว่า `editFields` เขียนทับทั้ง field object) — ยังพิสูจน์ไม่ได้ว่า API เพิกเฉยต่อ default เพราะไม่มีฟิลด์ไหนมี default ให้ทดสอบเลย ⇒ ในทางปฏิบัติ ต้องส่งค่าฟิลด์ธงตรง ๆ เองเสมอเมื่อ seed record ผ่าน API** — แก้แล้วยิงสำเร็จ ขั้นหัวหน้างานผ่านจริง (ดู §11 D-17)** ~~🔴 D-17: ใช้เป็น sanity-check 28 ส.ค. 2569 (ต่อเนื่อง 11) แล้วพบว่าไม่ยิงเช่นกัน — ยืนยันว่าปัญหาไม่ยิงเป็นระดับระบบ~~ | L | P4-2 | หลักฐานเดิม (27 ส.ค.) ที่เคยผ่าน: ตัวคูณเปลี่ยนตามตาราง ไม่ใช่ค่าฝัง (**AC-19 ส่วนแรก — ผ่านแล้ว**) — main process `6a8fd8d35f8564a68c3c1909` publish v2 · inner supervisor-approval `6a8fd9325f8564a68c3c1cec` publish v1 (ใช้ร่วมกัน 2 เส้นทาง day-type ผ่าน `mode:"use_existing"`) · inner HR-approval `6a8fd9325f8564a68c3c1ced` publish v1 (สร้างแล้วแต่**ยังไม่ทดสอบ** — รอ `hr_approval_rule` seed ซึ่งรอ role/G-06 เหมือน WF-HR-01) · **สถาปัตยกรรมเหมือน WF-HR-01** (single-flow gate-branch ตาม `hr_ot_status`) บวกเทคนิคใหม่: fork แยกตาม day_type (holiday/workday) ที่ไม่มี merge node ในแพลตฟอร์มนี้ ⇒ ทำซ้ำแค่ chain สั้น ๆ ต่อเส้นทาง แต่ใช้ inner approval_block ร่วมกันตัวเดียวผ่าน create-once-then-`use_existing` · **ทดสอบยิงจริง 3 เคส:** `OT-TEST-HOLIDAY-01` (4h, Holiday outside hours, ×3.0), `OT-TEST-WORKDAY-01` (2.5h, Working day, ×1.5), `OT-TEST-FRACTIONAL-01` (2.5h — ยืนยัน `hr_ot_hours`="2.50" ถูกต้องหลังแก้บั๊ก precision) ทั้ง 3 มี To-do จริงถึง Wanadtapong.l (`get_approval_list_by_row`) และ `_updatedBy`=`user-workflow` ครบ — เก็บไว้เป็นหลักฐานทดสอบ ไม่ลบ · 🔴 **พบ+แก้บั๊กใหม่ระหว่างทาง 2 ตัว** (ดู Known Issues `04-CLAUDE-memory.md`): (1) `dateDiff` compute node ตัด `precision` ทิ้งเงียบ ๆ — แก้ด้วย 2-step compute (นาที→หารเป็นชั่วโมงด้วย `number` compute ที่มี precision) (2) เจอซ้ำ: `hr_ot_submitted_flag`/`hr_paid_flag` ไม่มี defaultValue ทำให้ `ne` branch condition ไม่ match เงียบ ๆ workflow ไม่ยิงเลย — แก้ด้วย `editFields` ใส่ `defaultValue:0` |
| P4-4 | **WF-HR-05 สรุปเวลาทำงานรายวัน** | 🟢⬜ | L | P4-1 · P3-6 | **AC-04** — วันที่ลาแสดง On leave · ทดสอบ 5 เคส · 🟢🆕 **28 ส.ค. 2569 (ต่อเนื่อง 12) — สร้าง+publish สำเร็จจริง!** main `6a910184730d20c5b7710fa8` v1 + inner `6a910197730d20c5b7711028` v1 — validate สะอาด 0 error ทั้งคู่ ยืนยัน D-16 fix ใช้ได้กับ build จริง — ปรับจากดราฟต์เดิม: ใช้ `code` node (JS) แทน Duration/Function calculation เพราะ `hr_shift.hr_start_time`/`hr_end_time` เป็น Text "HH:mm" ไม่ใช่ Time field · ตัดขั้น get-associated-shift ออกเพราะมี `hr_att_shift` บน `hr_attendance` อยู่แล้ว · ส่ง grace/halfday minutes เข้า sub_process ผ่าน subprocess parameter — ⚠️ **ยังไม่ได้ live-fire test เพราะ D-17** (ดู §11) — Test recipe เดิม (seed 5 เคส + Execute now) รอรันจริงเมื่อ D-17 คลี่คลาย |
| P4-5 | ปุ่ม "ส่งขอล่วงเวลา" + view ลงเวลาผิดปกติ / OT รออนุมัติ | ✅ **สร้าง+verify ผ่านจริง 27 ส.ค. 2569 (ต่อเนื่อง)** | S | P4-2 | ปุ่ม "ส่งขอล่วงเวลา" บน `hr_ot_request` (`create_custom_actions` — actionId `6a8fde0aae2a0e3743a0c62f`) + view "OT รออนุมัติ" (`6a8fde0b353e1b0e4a50803e`, filter `hr_ot_status` in Pending supervisor/Pending HR) + view "บันทึกลงเวลาที่ผิดปกติ" บน `hr_attendance` (`6a8fde0c1378964f9984a42c`, filter `hr_att_status` in Late/Absent) — ทั้งหมดสร้างผ่าน MCP สำเร็จ · 🔴 **เจอกับดักเดิมของโมดูลบัญชีซ้ำในโมดูล HR เป็นครั้งแรก:** ปุ่มที่สร้างผ่าน `create_custom_actions` มี Scope เริ่มต้นเป็น "Unassigned View" (ไม่โผล่บนหน้าจอเลย) ต้องเข้า Browser (Form Settings → Custom Action → Scope → All Records) ก่อนถึงจะโผล่ — แก้แล้วผ่าน Claude in Chrome (มี session ที่ login ค้างอยู่แล้ว ไม่ต้อง login ใหม่) · **verify จริง:** toggle `hr_ot_status` ของ `OT-TEST-FRACTIONAL-01` เป็น Draft ชั่วคราวผ่าน MCP (`triggerWorkflow:false`) → เปิด record ใน Browser เห็นปุ่ม "ส่งขอล่วงเวลา" โผล่จริงตาม `enableWhen` → revert กลับ Pending supervisor เดิม · view "OT รออนุมัติ" แสดง 3 record ถูกต้อง · view "บันทึกลงเวลาที่ผิดปกติ" render คอลัมน์ถูกต้อง (0 record เพราะ `hr_attendance` ยังไม่มี seed data) |
| P4-6 | **WF-HR-06 เตือนคำขอค้างเกิน SLA** | 🟢⬜ | M | P3-5 · P4-3 | **AC-14** · 🟢🆕 **28 ส.ค. 2569 (ต่อเนื่อง 12) — สร้าง+publish สำเร็จจริง!** main `6a910222730d20c5b77115e5` v1 + inner `6a9102345f8564a68c449ee7` v1 — validate สะอาด 0 error — **v1 จำกัดขอบเขตเหลือแค่ `hr_leave_request`** เพราะพบ gap ใหม่ D-18: `hr_ot_request` ไม่มีฟิลด์ `submitted_at` ให้คำนวณ SLA ได้ตรงความหมาย (`hr_claim` ตัดออกโดยธรรมชาติเพราะยังไม่มีตาราง — P6) — ⚠️ **ยังไม่ได้ live-fire test เพราะ D-17** (ดู §11) — ต้องตัดสินใจเรื่อง D-18 ก่อนขยายขอบเขตไป OT/claim |

---

## 7. P5 — เงินเดือนและภาษี (เฟสที่หนักที่สุด)

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P5-1 | สร้าง `hr_pay_period` · `hr_pay_component` · `hr_salary_structure` | 🔶 | M | P2-2 | ✅ **โครงสร้างฟิลด์ครบทั้ง 3 ตาราง 27 ส.ค. 2569** — สร้างผ่าน `create_app_items`→`update_worksheet`(removeFields 3 default + addFields)→`get_worksheet_structure` verify: `hr_pay_component` (`6a8ff2868b6633ef76f13871`, view `6a8ff2868b6633ef76f13875`, 11 ฟิลด์ รวม `biz_coa_account`→`ac_coa` bidirectional) · `hr_pay_period` (`6a8ff2878b6633ef76f1387b`, view `6a8ff2878b6633ef76f1387f`, 22 ฟิลด์ รวม `biz_ac_period_ref`→`ac_period` และ `biz_voucher_ref`→`ac_voucher` ทั้งคู่ bidirectional) · `hr_salary_structure` (`6a8ff287353e1b0e4a508550`, view `6a8ff287353e1b0e4a508554`, 9 ฟิลด์ รวม `biz_employee`→`hr_employee` และ `biz_pay_component`→`hr_pay_component` ทั้งคู่ bidirectional) · flag/total Number fields ทั้งหมดตั้ง `defaultValue:0` เชิงป้องกันตั้งแต่แรก (บทเรียนจาก P4) · `addRecordButtonName` ตั้งสำเร็จทั้ง 3 ตาราง ("องค์ประกอบค่าจ้าง"/"งวดจ่ายเงินเดือน"/"โครงสร้างเงินเดือน") · ⚠️ **ยังไม่เสร็จ:** `biz_component_type`/`biz_calc_method`/`biz_period_status` เป็น **inline options** (label อังกฤษตรงกับ `OS_HR_COMPONENT_TYPE`/`OS_HR_COMPONENT_CALC`/`OS_HR_PAY_PERIOD_STATUS` แต่ยังไม่ผูก shared optionset จริง — Browser-only) · unique index IX-12.1 (`period_year`,`period_month`) และ IX-12.2 (`employee`,`pay_component`,`effective_from`) ยังไม่ตั้ง (Browser-only) · `hr_leave_type.pay_component` และ `hr_leave_policy.job_level` (relation ย้อนกลับที่ค้างจาก FR-HR-07) ยังไม่ได้เพิ่ม |
| P5-2 | สร้าง `hr_payslip` · `hr_payslip_line` + unique index IX-12.3 | 🔶 **27 ส.ค. 2569** | M | P5-1 | **AC-15** — โครงสร้างตารางสร้างครบผ่าน MCP (`hr_payslip` `6a904c85353e1b0e4a50ba05` 25 ฟิลด์ · `hr_payslip_line` `6a904c858b6633ef76f16a0b` 8 ฟิลด์) relation bidirectional ครบ 6 จุด · เงินยอดใช้ Currency(THB) แทน Number ตามสเปกเดิม · `payslip_no` เป็น AutoNumber · flag/total ตั้ง defaultValue:0 เชิงรุก · `addRecordButtonName` ตั้งครบ · BuildSpec §1.3/FR-HR-12 12D/12E sync แล้ว — ⬜ เหลือ unique index IX-12.3 (Browser-only, **AC-15 ยังไม่ผ่าน**) · ผูก `payslip_status`→`OS_HR_PAYSLIP_STATUS` จริง · BR-12.4/12.5 |
| P5-3 | สร้าง `hr_pnd1_filing` · `hr_wht_cert` + IX-12.5 · IX-12.6 | 🔶 **27 ส.ค. 2569** | M | P5-2 | โครงสร้างตารางสร้างครบผ่าน MCP (`hr_pnd1_filing` `6a904e72ae2a0e3743a0fd06` 13 ฟิลด์ · `hr_wht_cert` `6a904e73353e1b0e4a50ba83` 9 ฟิลด์) relation bidirectional ครบ 3 จุด (รวม multi-relation `pay_periods`) · ยอดเงินใช้ Currency(THB) แทน Number ตามสเปกเดิม · `addRecordButtonName` ตั้งครบ · **กลุ่มเงินเดือนครบ 7/7 ตารางแล้ว** · BuildSpec §1.3/FR-HR-12 12I/12J sync แล้ว — ⬜ เหลือ unique index IX-12.5/IX-12.6 (Browser-only) · ผูก dropdown 4 ฟิลด์เข้า shared optionset จริง |
| P5-4 | seed องค์ประกอบค่าจ้างพร้อมผูก `coa_account` เข้าผังบัญชีจริง | ✅ **28 ส.ค. 2569 (ต่อเนื่อง 13) — ปิดครบ 6/6** | M | P5-1 | seed `hr_pay_component` 6 รายการผ่าน MCP `batch_create_records`(`triggerWorkflow:false`) สำเร็จ: SALARY/OT/SSO_ER/SSO_EE/WHT/ABSENCE_DEDUCT — ผูก `coa_account` ครบ 6/6: SALARY/OT/ABSENCE_DEDUCT→`530101 เงินเดือน` (26-27 ส.ค.) · **28 ส.ค. 2569 (ต่อเนื่อง 13):** ฝ่ายบัญชีตอบกลับ (`tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`) สร้างบัญชีใหม่ 3 รายการในผังบัญชีจริงแล้ว → ผูกต่อสำเร็จ: SSO_ER→`530102` (rowId `233626b4-d272-4cb5-8f77-9d8d2ddef804`), SSO_EE→`210402` (rowId `c6386fe7-1b3c-44db-ab80-9dfd3308a6a5`), WHT→`210304` (rowId `777ea001-5300-4f5c-857a-71a45db2076b`) ผ่าน `update_record`(fieldId `6a8ff3049762533b5b7193a2`, `triggerWorkflow:false`) ทั้ง 3 record + ยืนยันด้วย `get_record_details` เห็นชื่อบัญชีจริงไม่ใช่ "已删除" — **P5-4 ปิดงานสมบูรณ์ ปลดบล็อก P5-5 เต็มที่** |
| P5-5 | สร้างกฎการผ่านรายการของเงินเดือนใน `ac_posting_rule` | ⬜ | S | ~~P0-3~~ · P5-4 | ปลดบล็อกแล้ว (P0-3 และ P5-4 เสร็จทั้งคู่) — ฝ่ายบัญชียืนยัน `ac_posting_rule` เป็น **source of truth หลัก** (จับคู่ด้วย `event_code`+conditions), `hr_pay_component.biz_coa_account` เป็น fallback เมื่อไม่มีกฎตรง — ใช้ `event_code`: `PAYROLL_ACCRUAL` (key `ad4850c4-f1c1-4a65-adbb-fb0054a6e0c8`)/`PAYROLL_PAYMENT` (key `04a14b13-73c2-408b-a5fb-bb81ff630a06`) · `get_record_list` เห็นกฎที่ `event_code` = PAYROLL_ACCRUAL |
| P5-6 | seed โครงสร้างเงินเดือนของพนักงานทดสอบ 4 คน | 🔶 **28 ส.ค. 2569 (ต่อเนื่อง 15) — 2/4 คน** | S | P5-1 | seed `hr_salary_structure` 2 record ผ่าน `batch_create_records`(`triggerWorkflow:false`): TEST-EMP1 (`f36de845-596d-4a87-b8b1-55f19f1bd790`, 30,000 บาท) · TEST-EMP2 (`14efbacf-3f36-426d-a98a-7d714cebb14f`, 35,000 บาท) — ทั้งคู่ผูก `biz_pay_component`=SALARY (`4f6e08db-9316-4d22-88ac-a3656c29a182`), `biz_effective_from`=2026-01-01, `biz_is_active`=1 — ยืนยันด้วย `get_record_list` เห็น relation ชี้ employee/pay component ถูกต้อง ไม่ใช่ "已删除" · ⚠️ **ตัวเลข 30,000/35,000 เป็นค่าทดสอบสมมติกลม ๆ สำหรับ QA เท่านั้น ไม่ใช่เงินเดือนจริงของพนักงาน** (ตามหลักปฏิบัติเดิมของโปรเจกต์ที่ใช้ตัวเลขสมมติสำหรับ test data — ไม่ใช่การ "เดา" ข้อมูลจริง) · ⬜ ยังมีแค่ 2/4 คน — TEST-EMP3/4 ยังไม่มี (รอ P2-6 seed พนักงานทดสอบเพิ่ม ซึ่งบล็อกบางส่วนโดย G-06 บัญชีผู้ใช้จริงในองค์กรมีแค่ 2 บัญชี) |
| P5-7 | **เตรียมกระดาษคำนวณ 3 เคสด้วยมือ** (เงินเดือนอย่างเดียว / มี OT / มีขาดงาน) | ⬜ | M | P0-5 | เอกสารกระดาษคำนวณที่ลงนามรับรอง |
| P5-8 | **WF-HR-07 สร้างสลิปทั้งงวด** | ⬜ | M | P5-2 · P5-6 | จำนวนสลิป = จำนวนพนักงาน active · ยิงซ้ำไม่เพิ่ม |
| P5-9 | **WF-HR-08 คำนวณสลิป** 🔴 XL — workflow ที่ซับซ้อนที่สุด 13 กลุ่ม node | ⬜ | XL | P5-8 · P5-7 · P4-3 | **AC-05 · AC-06 · AC-19** — ทุกยอดตรงกระดาษคำนวณ 100% |
| P5-10 | **WF-HR-09 อนุมัติงวดจ่าย** | ⬜ | L | P5-9 · P0-2 | approval 1 ขั้นโดย HR Manager · สลิปทุกใบเป็น Approved |
| P5-11 | **WF-HR-10 ผ่านรายการเข้าบัญชี** | ⛔ | L | **P0-3** · P5-10 | **AC-07 · AC-08** — ใบสำคัญดุล · `source_module` = Payroll · ยิงซ้ำไม่เกิดใบที่สอง |

---

## 8. P6 — สวัสดิการและการเบิกจ่าย

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P6-1 | สร้าง `hr_welfare_scheme` · `hr_welfare_balance` + IX-18.1 | ⬜ | M | P2-1 | |
| P6-2 | สร้าง `hr_claim` · `hr_claim_line` + Rollup RU-19.1 (🔴 **Browser** — ห้ามแตะผ่าน API อีก) | ⬜ | M | P6-1 | Rollup คำนวณจริง (แก้บรรทัดแล้วหัวขยับ) |
| P6-3 | seed สวัสดิการและวงเงินของพนักงานทดสอบ | ⬜ | S | P6-1 | |
| P6-4 | Business Rules ของใบเบิก (BR-19.1…4) + DV-19.1 | ⬜ | M | P6-2 | **AC-10 ส่วนแรก** — บล็อกเมื่อเกินวงเงิน |
| P6-5 | **WF-HR-11 อนุมัติใบเบิก + ตัดวงเงิน** | ⬜ | L | P6-4 | วงเงินลดถูกต้อง · ยิงซ้ำไม่ตัดซ้ำ |
| P6-6 | **WF-HR-12 ส่งใบเบิกเข้า `ac_pay_req`** | ⬜ | M | P6-5 | **AC-10 ส่วนหลัง** — เกิด `ac_pay_req` 1 ใบ · ยิงซ้ำไม่เกิดใบที่สอง |

---

## 9. P7 — สรรหา ประเมิน อบรม

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P7-1 | สร้าง `hr_job_requisition` · `hr_candidate` · `hr_interview` | ⬜ | M | P2-1 | |
| P7-2 | **WF-HR-15 อนุมัติใบขออัตรากำลัง** | ⬜ | M | P7-1 · P0-2 | approval 2 ขั้น |
| P7-3 | **WF-HR-16 เลื่อนสถานะผู้สมัคร + แจ้งนัดสัมภาษณ์** | ⬜ | M | P7-1 | แจ้งครั้งเดียว ยิงซ้ำไม่แจ้งซ้ำ |
| P7-4 | **WF-HR-17 สร้างทะเบียนพนักงานจากผู้สมัคร** | ⬜ | M | P7-1 · P2-2 | **AC-17** |
| P7-5 | สร้าง `hr_appraisal_cycle` · `hr_appraisal` · `hr_appraisal_item` + IX-22.1/2 | ⬜ | M | P2-2 | |
| P7-6 | **WF-HR-18 เปิดรอบประเมินสร้างแบบให้ทุกคน** | ⬜ | M | P7-5 | จำนวนแบบ = จำนวนพนักงาน |
| P7-7 | **WF-HR-19 เดินสถานะแบบประเมิน 3 ขั้น** | ⬜ | L | P7-6 · P0-2 | `final_score` ถ่วงน้ำหนักถูกต้อง |
| P7-8 | สร้าง `hr_course` · `hr_training` + IX-23.1 + view Kanban ผู้สมัคร | ⬜ | M | P2-2 | ⚠️ verify view Kanban โผล่จริง |

---

## 10. P8 — สิทธิ์ รายงาน เอกสาร และ go-live

| # | งาน | สถานะ | effort | ขึ้นกับ | หลักฐาน |
|---|---|---|---|---|---|
| P8-1 | **สร้าง role 8 บทบาทพร้อม permission matrix** | ⛔ | L | **P0-2** · ทุกตาราง | `get_role_details` ตรง matrix §1.7 |
| P8-2 | ตั้ง field-level visibility ปิดบังข้อมูลอ่อนไหว (Browser) | ⛔ | M | **P0-2** · P8-1 | **AC-11** — Role Debugging ยืนยัน |
| P8-3 | ผูกบัญชีทดสอบเข้าบทบาทและทดสอบสิทธิ์จริงทั้ง 8 บทบาท | ⛔ | M | **P0-2** · P8-1 | **AC-11 · AC-12 · AC-13** |
| P8-4 | **WF-HR-13A/13B เตือนสัญญาและทดลองงาน** | ⬜ | M | P2-6 | แจ้งเตือนถึงผู้รับถูกคน |
| P8-5 | **WF-HR-14 ยกยอดสิทธิลาต้นปี** | ⬜ | L | P3-6 | **AC-20** — ทดสอบด้วย Execute now |
| P8-6 | **WF-HR-20 ออกหนังสือรับรองหักภาษีประจำปี** | ⬜ | M | P5-11 | 1 ใบต่อพนักงาน · ยอดตรงผลรวมทุกงวด |
| P8-7 | รายงานและแดชบอร์ด 7 รายการ | ⬜ | M | ทุกตาราง | ⚠️ `create_chart` ยังไม่เคยเรียกสำเร็จ — **เปิดหน้าจอดูทุกครั้ง** |
| P8-8 | Print template เอกสารราชการ 4 แบบ (🔴 Browser) + **App Lock** | ⬜ | M | P5-11 | **AC-16** — พิมพ์ออกมาถูกรูปแบบ |

---

## 11. หนี้ทางเทคนิคและงานที่ยกไปเฟสถัดไป

| # | รายการ | เหตุผล | ความเสี่ยงถ้าไม่ทำ |
|---|---|---|---|
| D-1 | workflow ลบข้อมูลผู้สมัครที่หมดอายุตาม PDPA (G-08) | ไม่อยู่ในขอบเขตเฟสแรก | ไม่เป็นไปตาม PDPA ในระยะยาว |
| D-2 | เชื่อมเครื่องบันทึกเวลาอัตโนมัติ | Out of scope | ต้องนำเข้าไฟล์ด้วยมือทุกเดือน |
| D-3 | ไฟล์นำส่งธนาคารและไฟล์ยื่นภาษีอิเล็กทรอนิกส์ | Out of scope | ยังต้องกรอกในเว็บหน่วยงานเอง |
| D-4 | หน้าแรกของพนักงาน (Custom Page) | Browser · ทำหลังโครงสร้างนิ่ง | NFR-HR-11 (ยื่นใบลา ≤ 3 คลิก) อาจไม่ผ่าน |
| D-5 | จัดลำดับฟิลด์บนฟอร์มให้อ่านง่าย | ต้องทำผ่านหน้าจอหลังสร้างครบ | ฟอร์มยาวและอ่านยาก |
| D-6 | 🆕 รีไบนด์ `leave_unit`/`leave_status`/`ledger_type` เข้า shared optionset ผ่าน Browser | inline options ใช้งานได้ชั่วคราว แต่ค่า key ไม่ตรงกับ `OS_HR_*` ใน registry กลาง | workflow/report ที่อ้าง key ของ `OS_HR_*` จะไม่ match กับข้อมูลจริงในตารางนี้ จนกว่าจะรีไบนด์ |
| D-7 | 🆕 ตัดสินใจ `hr_leave_ledger.ledger_no`: คงเป็น Text หรือแปลงเป็น AutoNumber จริงใน Browser | AutoNumber สร้างผ่าน API ไม่ได้ | ถ้าต้องการเลขที่รันอัตโนมัติจริงต้องแปลงทีหลัง (อาจต้องลบสร้างใหม่ ข้อมูลเดิมหาย) |
| D-8 | 🔶 **พยายามทำแล้ว 27 ส.ค. 2569 (รอบเย็น) แต่ชนบั๊กใหม่ — revert กลับสถานะเดิมปลอดภัยแล้ว** — เพิ่ม node sync สถานะ "On leave" เข้า `hr_attendance` กลับเข้า WF-HR-02/03 | `hr_attendance` มีตารางแล้ว แต่ node ชนิด `sub_process` (จำเป็นสำหรับวน update ทีละ attendance record) สร้างผ่าน `batch_create_process_nodes` แล้ว `validate_process` ค้าง error `NodeAppIsNull` เสมอที่ตัว container node เอง (internal child process ของมันเองกลับ valid+canPublish:true ปกติ) — ทดลองแก้ 3 รูปแบบ (worksheetId ที่ `process.start` / worksheetId ที่ config ระดับบนของ sub_process / target แบบ nodeAlias-only vs nodeId+alias) ยังไม่หาย ดู D-13 และ G-13 ใน RTM สำหรับรายละเอียดเต็ม | AC-04 ("วันที่ลาแสดงเป็น 'ลา' ไม่ใช่ 'ขาดงาน'") ยังทำไม่ได้จนกว่าจะแก้บั๊ก `sub_process`/`NodeAppIsNull` ได้ — งานนี้ยัง**บล็อกอยู่จริง** ไม่ใช่ "พร้อมทำ" อีกต่อไป |
| D-9 | 🆕 27 ส.ค. 2569 — ย้าย DV-08.1 (เติม `approver_user` จาก `employee.supervisor_user` + เติม `balance_snapshot` จาก `hr_leave_balance`) ไปทำใน **WF-HR-01** แทน Dynamic Default UI | Dynamic Default "Query worksheet" ไม่รองรับ Relation-type field ของฟอร์มปัจจุบันเป็นค่าเปรียบเทียบ (field picker ไม่แสดงให้เลือก — ทดสอบยืนยันแล้ว) | ถ้าไม่เพิ่ม node นี้ใน WF-HR-01 ฟิลด์ `approver_user`/`balance_snapshot` จะว่างเสมอตอนสร้างใบลาใหม่ (ตอนนี้ WF-HR-01 คำนวณ `approver_user`/`balance_snapshot` ในสายอนุมัติอยู่แล้วบางส่วน — ต้อง cross-check ว่าครอบคลุมครบ 100% หรือยังต้องเพิ่ม node เฉพาะสำหรับ default ตอนสร้าง) |
| D-10 | 🆕 27 ส.ค. 2569 — เปิดใช้ IX-08.1 (unique index บน `leave_no`) หลังมีกลไก auto-generate `leave_no` ทันทีตอนสร้าง record (workflow เขียนสตริงเอง หรือแปลงเป็น AutoNumber จริงใน Browser — ดู D-7) | เปิด unique index ตอนนี้ (ฟิลด์ไม่ required) จะจำกัดทั้งตารางให้มี record ที่ `leave_no` ว่างได้แค่ 1 แถว — ยืนยันจาก tooltip ของระบบเองที่หน้า Search Acceleration | ถ้าเปิดก่อนมี auto-generate จะบล็อกการสร้างใบลา Draft ใบที่ 2 เป็นต้นไปทันที (error ตอน `create_record`/บันทึกฟอร์ม) |
| D-11 | 🆕 27 ส.ค. 2569 — รีไบนด์ `hr_attendance.att_status`/`att_source` และ `hr_ot_request.day_type`/`ot_status` เข้า shared optionset จริง (`OS_HR_ATT_STATUS`/`OS_HR_ATT_SOURCE`/`OS_HR_OT_DAY_TYPE`/`OS_HR_REQUEST_STATUS`) ผ่าน Browser | inline options ที่สร้างไว้ใช้ label ภาษาอังกฤษตรงกับ optionset กลางแล้ว แต่ option key (GUID) เป็นคนละชุด — ใช้งานได้ชั่วคราวแต่ workflow/report ที่อ้าง key ของ `OS_HR_*` จะไม่ match | เหมือน D-6 — จนกว่าจะรีไบนด์ ทุก workflow ที่ต้องเทียบ key ต้องอ้างอิง key ของ inline options ในตารางนี้เอง ไม่ใช่ key ของ `OS_HR_*` ใน BuildSpec §1.6.1 — **27 ส.ค. 2569 (ต่อเนื่อง): เพิ่ม `hr_ot_rate.day_type` เข้ารายการรีไบนด์นี้ด้วย** (สร้างเป็น inline option เช่นกันตอนทำ WF-HR-04) |
| D-13 | 🟢 **คลี่คลายแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 10) — ดู D-16** ~~🆕🔴 27 ส.ค. 2569 — สืบสวน/แก้บั๊กแพลตฟอร์มใหม่ `sub_process` container node → `NodeAppIsNull` ตอน `validate_process`/`publish_process` (พบระหว่างพยายามทำ D-8)~~ | สร้าง `sub_process` node ผ่าน `batch_create_process_nodes` (ทั้งใน WF-HR-02 ×2 จุดและ WF-HR-03 ×1 จุด) สำเร็จทุกครั้งในแง่ create แต่ `validate_process` ค้าง error `NodeAppIsNull`/"节点关联对象不存在或不可用" ที่ตัว container node เสมอ — internal child process ของมันเอง (`get_workflow_structure`/`validate_process` เรียกตรงด้วย internal processId) กลับ valid+canPublish:true ปกติทุกครั้ง ⇒ **[แก้ไข] ไม่ใช่บั๊ก — เราไม่เคยเรียก `publish_process` ที่ inner processId จริง ๆ เลยสักครั้ง แค่ validate แล้วเข้าใจผิดว่า "ข้างในถูกต้องแล้ว" — ดู D-16 สำหรับคำตอบที่ถูกต้อง** | ~~D-8 และ WF-HR-05 ทำต่อไม่ได้~~ **[แก้ไข] ปลดบล็อกแล้ว — ดู D-16** |
| D-16 | 🆕🟢 28 ส.ค. 2569 (ต่อเนื่อง 10) — พบ root cause ที่แท้จริงของ D-13 จากเอกสาร skill `hap-mcp-app-builder` (ตามคำขอผู้ใช้ "ลองยืนยันข้อมูลใน skill ชุด hap ทั้ง mcp และ cli") | ไฟล์ `workflow_rules.md` ของ skill นั้นระบุชัดว่า: **ต้อง `publish_process` ที่ inner processId ของ `sub_process`/`approval_block` ก่อนเสมอ แล้วจึงค่อย publish outer/main process — ถ้า publish outer ตรง ๆ โดยไม่ publish inner ก่อน จะได้ `NodeAppIsNull` แน่นอน** (`nocoly-hybrid-builder-v2/references/workflow-authoring-mcp.md` บรรทัด 155 ก็มีกฎเดียวกันบันทึกไว้สั้น ๆ อยู่แล้ว แต่ตีความ/ปฏิบัติผิดมาตลอดว่า validate อย่างเดียวพอ) — **ทดสอบยืนยันจริง:** สร้าง process ทดสอบใหม่ (outer `6a90fb59fdab77a41c63662d` trigger+get_multiple(`hr_employee`)+sub_process, inner `6a90fb5dfdab77a41c636699` 1 update_record) → validate(inner)=valid:true → **publish(inner)=published:true v1** → validate(outer)=valid:true → **publish(outer)=published:true v1 ไม่มี `NodeAppIsNull` เลย** — ลบ process ทดสอบทั้ง 2 ตัวทิ้งสะอาดแล้ว (`delete_process` ไม่ cascade ลบ inner อัตโนมัติ ต้องลบแยกทั้งคู่) | **P4-4 (WF-HR-05) และ P4-6 (WF-HR-06) ปลดบล็อกแล้ว** — กฎใหม่บังคับ: ทุกครั้งที่มี `sub_process`/`approval_block` ต้อง publish inner processId ก่อนเสมอ (ไม่ใช่แค่ validate) แล้วจึง validate/publish outer — D-8 (sync "On leave" เข้า `hr_attendance`) ก็พร้อมกลับมาทำใหม่ด้วยลำดับที่ถูกต้องนี้ |
| D-14 | 🆕 27 ส.ค. 2569 (ต่อเนื่อง) — ตรวจสอบ (audit) ฟิลด์ธง/Number ทุกตัวที่สร้างผ่าน workaround `create_app_items`+`addFields` ในตารางอื่น (โดยเฉพาะ `hr_attendance` ที่สร้างพร้อมกันกับ `hr_ot_request` ใน P4-1/P4-2) ว่ามี `defaultValue` ครบหรือยัง ก่อนเริ่มสร้าง WF-HR-05 | พบซ้ำเป็นครั้งที่ 2 ว่าฟิลด์ธง/Number ที่สร้างผ่าน workaround นี้ไม่ได้ตั้ง `defaultValue` อัตโนมัติ — คราวนี้เจอที่ `hr_ot_request.hr_ot_submitted_flag`/`hr_paid_flag` (ระหว่างสร้าง WF-HR-04) ทำให้ workflow ไม่ยิงเลยเงียบ ๆ (เพราะ `ne` branch condition เทียบกับค่าว่างไม่ match) — จุดเดิมเคยเจอกับ `submitted_flag` ของ WF-HR-01 มาแล้วครั้งหนึ่ง | ถ้าไม่ audit ก่อน WF-HR-05 อาจเจอบั๊ก "workflow ไม่ยิงเลย" แบบเดิมซ้ำเป็นครั้งที่ 3 บนฟิลด์ธงของ `hr_attendance` (เช่น `summarised_flag` หรือฟิลด์ธงอื่นที่สร้างพร้อมกัน) — เสียเวลาสืบสวนซ้ำโดยไม่จำเป็น ทั้งที่รู้ pattern แล้ว |
| D-18 | 🟡 **28 ส.ค. 2569 (ต่อเนื่อง 12) — พบระหว่างสร้าง WF-HR-06 — ต้องตัดสินใจก่อนขยายขอบเขต** `hr_ot_request` ไม่มีฟิลด์ "วันที่ส่งคำขอ" (`submitted_at`) | `hr_leave_request` มี `submitted_at` (DateTime) ให้ WF-HR-06 คำนวณ SLA ค้างอนุมัติได้ตรงความหมาย แต่ `hr_ot_request` มีแค่ `hr_approved_at` — ใช้ `_createdAt` ระบบแทนได้แค่คร่าว ๆ ไม่ตรงความหมายถ้า Draft ค้างไว้นานก่อนกดส่งจริง | WF-HR-06 v1 สร้างแล้วจำกัดขอบเขตเหลือแค่ `hr_leave_request` เท่านั้น — ต้องตัดสินใจ (a) เพิ่มฟิลด์ `submitted_at` จริงใน `hr_ot_request` ผ่าน MCP แล้วให้ WF-HR-04 เขียนค่าตอน submit หรือ (b) ยอมรับ `_createdAt` เป็นตัวแทน ก่อนจะขยาย WF-HR-06 ให้ครอบคลุม OT (และ `hr_claim` เมื่อสร้างตารางในเฟส P6) |
| D-17 | 🟢 **แก้ไขแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 16) — ไม่ใช่ปัญหาแพลตฟอร์ม/session เลย เป็นบั๊กข้อมูลเฉพาะ record ทดสอบ 2 ใบนี้ — ดูการแก้ไขท้ายแถว** ~~🔴🔴 28 ส.ค. 2569 (ต่อเนื่อง 11) — สำคัญที่สุดตอนนี้ — worksheet_event workflow ทั้งหมด (ไม่ใช่แค่ WF-HR-01) ไม่ยิงเลย เป็นปัญหาระดับ session/environment/platform~~ | ทำตามคำแนะนำ "ทำตามที่แนะนำ" โดย re-verify WF-HR-01 ก่อน (ตามที่ D-15 ทิ้งไว้ว่าต้องเช็คซ้ำ) — สร้าง record ทดสอบใหม่สะอาด (`hr_leave_request` rowId `72e70392-6833-4921-932c-5db3fa446b50`, `TEST-LR-D15-REVERIFY`) → set `leave_status`="Pending supervisor" ผ่าน `update_record(triggerWorkflow:true)` → รอ 68+ วินาที → `get_record_details`/`get_record_logs`/`get_approval_list_by_row` ไม่มีร่องรอย workflow ทำงานเลย (`_updatedBy`=`user-api` ตลอด) — **เพื่อแยกว่าเป็นปัญหาเฉพาะ WF-HR-01 (อาจเกี่ยวกับการแก้ไข/revert ของ D-15) หรือทั้งระบบ จึง sanity-check กับ WF-HR-04 ที่ไม่เคยถูกแตะต้องเลยตั้งแต่ยืนยันทำงานสำเร็จ 27 ส.ค.** สร้าง record OT ทดสอบใหม่สะอาดเช่นกัน (`hr_ot_request` rowId `f9d1b750-4a25-4642-82f5-552524a8ea62`, `TEST-OT-SYSCHK-01`) → set `hr_ot_status`="Pending supervisor" → **ก็ไม่ยิงเช่นกัน** (ไม่มี log, ไม่มี approval, `_updatedBy`=`user-api`) — ตรวจ `get_workflow_structure` ของ WF-HR-01 ทั้ง main และ inner appr_block_1 ยืนยันว่า `published:true`, trigger config ถูกต้องทุกจุด, ไม่มี warning ใด ๆ ⇒ **โครงสร้างสมบูรณ์ 100% แต่ execution engine ไม่ทำงาน — ปัญหานี้ไม่เกี่ยวกับการแก้ไข WF-HR-01/BR-08.3 (D-15) เลย เป็นปัญหาที่กว้างกว่านั้นมาก** | (1) **ห้ามเชื่อว่า workflow ใด ๆ ในแอปนี้ทำงานได้จริงจนกว่าจะ re-verify สำเร็จ** — ไม่ใช่แค่ WF-HR-01 (2) P4-4/P4-6 (WF-HR-05/06): สร้าง/publish โครงสร้างทำต่อได้ปกติ (ไม่พึ่ง D-17) แต่ **ขั้นทดสอบยิงจริงจะพิสูจน์ผลไม่ได้จนกว่า D-17 จะคลี่คลาย** (3) เก็บ record ทดสอบทั้ง 2 ไว้เป็นหลักฐาน (ตั้งชื่อชัดเจนว่าเป็น test) ไม่ลบ (4) **กฎใหม่บังคับ:** ก่อนเริ่มงานที่ต้องพึ่งพา worksheet_event trigger ทำงานจริง ต้อง sanity-check ด้วย workflow ที่เคยพิสูจน์ทำงานมาก่อนเสมอ — ถ้า sanity-check ก็ไม่ยิง แปลว่าเป็นปัญหาระดับ environment ไม่ใช่ workflow ที่กำลังทดสอบ (5) แนะนำ: รอ/ตรวจสอบใหม่ใน session ถัดไป — อาจเป็นปัญหาชั่วคราวของ session/เวลานี้ · 🔴🔴 **28 ส.ค. 2569 (ต่อเนื่อง 15) — re-verify ครั้งที่ 2/3 ตามคำสั่ง "ดำเนินการต่อไปก่อน" ยังไม่คลี่คลาย ข้ามช่วง session แล้ว (>7 ชม.):** reset `TEST-LR-D15-REVERIFY` (`hr_leave_request`) จาก Draft→Pending supervisor อีกครั้งผ่าน `update_record(triggerWorkflow:true)` → รอ >60 วินาที → `get_record_details`/`get_approval_list_by_row` ยังคง `_updatedBy`=`user-api`, `submitted_at`/`approver_user` ว่าง, `todoList` ว่าง — sanity-check ซ้ำด้วย `TEST-OT-SYSCHK-01` (`hr_ot_request`, WF-HR-04) เช่นกัน (reset `hr_ot_day_type`+`hr_ot_status`) ก็ไม่ยิงเหมือนกันทุกประการ — **นี่คือครั้งที่ 3 ติดต่อกันที่ re-verify ล้มเหลว และครั้งแรกที่ยืนยันข้ามขอบเขต session (สร้าง test เดิมไว้ ~11:15 ของ session ก่อน, re-check รอบนี้ ~18:46-18:47 ของ session ปัจจุบัน)** — **อัปเดตคำแนะนำ:** ไม่ใช่ปัญหาชั่วคราวของ session/เวลาเดียวอีกต่อไป **แนะนำให้ผู้ใช้ตรวจสอบสถานะแพลตฟอร์ม Nocoly/HAP โดยตรง** (สถานะ subscription/billing ขององค์กร, ติดต่อ platform support, หรือเปิด Browser ดูว่า workflow ขึ้นสถานะ "Enabled"/"Active" จริงหรือไม่) — เป็นสิ่งที่ agent ทำเองผ่าน MCP/CLI ไม่ได้แล้ว ต้องมีผู้ใช้เข้าไปช่วยตรวจสอบ · 🟢🆕 **[แก้ไข 28 ส.ค. 2569 (ต่อเนื่อง 16)] ข้อสรุปข้างต้นทั้งหมดผิด — ไม่ใช่ปัญหาแพลตฟอร์ม/session เลย:** ผู้ใช้ส่งภาพหน้าจอ Workflow History ของ `TEST-LR-D15-REVERIFY` มาถามว่าใช่ workflow ที่พูดถึงหรือไม่ — เปิดดูพบว่า process สถานะ **"Done"** จริง เข้า node `trigger`→`gate` (branch) ครบ ไม่ใช่ "ไม่ยิง" ตามที่เข้าใจผิดมาตลอด — ไปตรวจ `get_workflow_structure` ของ `gate` node พบเงื่อนไข path `leg_supervisor` ต้องการ `leave_status`=Pending supervisor **AND** `submitted_flag` ne "1" — ตรวจ `get_record_details` พบว่า `submitted_flag` ของ record นี้เป็น **ค่าว่างจริง ("" ไม่ใช่ 0)** เพราะสร้างด้วย `create_record`/`update_record` ตรง ๆ ไม่เคยผ่าน workflow set ค่าเลย ⇒ **ตรงกับบั๊กที่เคยพบมาก่อนแล้ว (`เงื่อนไข ne กับฟิลด์ที่ค่าว่างไม่ผ่าน` — ดู Known Issues) — ค่าว่างไม่ match `ne "1"` จึงตกไป path `leg_default` ("อื่นๆ ไม่ทำอะไร") เงียบ ๆ ไม่ error** เหมือนกันทุกประการกับ `TEST-OT-SYSCHK-01`/WF-HR-04 (`hr_ot_submitted_flag` ว่างเช่นกัน) — **ทดสอบแก้จริง:** set `submitted_flag`/`hr_ot_submitted_flag`=0 ทั้ง 2 record (`triggerWorkflow:false`) → รีเซ็ตสถานะเป็น Draft แล้วตั้งกลับเป็น Pending supervisor (`triggerWorkflow:true`) → **ทั้งคู่ยิงสำเร็จทันที** `_updatedBy`=`user-workflow`, `approver_user`/`hr_approver_user`=Wanadtapong.l, มี To-do จริงใน `get_approval_list_by_row` ทั้งคู่ — **สรุป: D-17 ไม่เคยเป็นปัญหาระดับระบบเลยตลอด 3 รอบที่บันทึกไว้ก่อนหน้า — เป็นเพราะ record ทดสอบที่สร้างขึ้นเองผ่าน MCP ไม่ผ่าน flow ปกติ ทำให้ flag ค้างเป็นค่าว่างแทนที่จะเป็น 0** ขออภัยที่แนะนำให้ผู้ใช้ไปตรวจสอบสถานะแพลตฟอร์ม/subscription ทั้งที่ไม่จำเป็น — **บทเรียนสำคัญ: ควรเปิด Workflow History (Browser) ตรวจสอบก่อนสรุปว่า "workflow ไม่ยิง" ทุกครั้ง อย่าพึ่ง `_updatedBy`/`get_approval_list_by_row` อย่างเดียว เพราะ field เหล่านี้ว่างได้ทั้งกรณี "ไม่ยิงเลย" และกรณี "ยิงแล้วแต่ไปจบที่ path ที่ไม่ทำอะไร" ซึ่งพฤติกรรมต่างกันโดยสิ้นเชิง** · **ผลกระทบ:** WF-HR-01/WF-HR-04 ทำงานได้ปกติมาตลอด ไม่เคยเสียหายจาก D-15/BR-08.3 เลย — P4-4/P4-6 (WF-HR-05/06) ไม่ได้ถูกบล็อกจาก "ปัญหาระบบ" อีกต่อไป (แต่ยังต้องเช็คฟิลด์ธงของ `hr_attendance`/sub_process เองว่ามีค่าว่างแบบเดียวกันหรือไม่ก่อนเชื่อผลทดสอบ) — เก็บ record ทดสอบทั้ง 2 ไว้เป็นหลักฐานเช่นเดิม (ตอนนี้มี approval instance จริงค้างอยู่ที่ Wanadtapong.l แล้ว) |
| D-15 | 🆕🔴 28 ส.ค. 2569 — (a) ยืนยันซ้ำ D-13 (`sub_process`→`NodeAppIsNull`) ยังไม่หายจริง ด้วย minimal test บน process ใหม่ (trigger+get_multiple+sub_process 1 node) — validate ค้าง error เดิมทุกประการ ลบ process ทิ้งสะอาด (b) ระหว่างลองย้าย BR-08.3 เข้า WF-HR-01 (P3-8) พบ auto-relink พฤติกรรมแปลกของ `batch_create_process_nodes`: แทรก branch node ใหม่กลางเส้นทางเดี่ยวที่มีปลายทาง node เดิมอยู่แล้ว (single edge A→B, แทรก C ด้วย prevNode=A) ทำให้ B ถูกย้าย prevNode ไปอ้างอิง **node ของ branch เอง** (ไม่ใช่ path ย่อยเฉพาะที่ตั้งใจ เช่น `doc_ok`) — `validate_process` ผ่านสะอาดไม่ error (c) **หลัง publish (v4) แล้วทดสอบยิงจริงด้วย record ใหม่ 2 ใบ ไม่มี node ใดทำงานเลย (`get_record_logs` ไม่มี `user-workflow`, `get_approval_list_by_row` ว่าง) แม้รอนานเกิน 3 นาที** — สงสัยว่า auto-relink (b) อาจทำให้ runtime engine ปฏิเสธ execution ทั้งเส้นแบบเงียบ (ไม่ error ที่ validate เพราะเป็นแค่ structural check ไม่ใช่ runtime check) (d) **ลบ node ที่เพิ่มทั้งหมดคืนสถานะเดิม (`delete_process_node` 4 ครั้ง) → validate สะอาด → publish v5 (โครงสร้างเดิมทุกจุด เทียบเท่า v3)** แต่ **ทดสอบยิงซ้ำด้วย record ใหม่หลัง revert ก็ยังไม่มี node ใดทำงานเลยเช่นกัน แม้รอนานเกิน 3 นาทีอีกครั้ง** — ตรวจแล้วว่า approval instance เดิมของ TEST-LR-0001 (real pending To-do ถึง Wanadtapong.l) ไม่กระทบ ยังอยู่ปกติ | (1) D-13 ยังบล็อก WF-HR-05/WF-HR-06 (sub_process ทุกจุด) ต่อไป (2) **สถานะ "🟢 WF-HR-01 ทดสอบยิงจริงสำเร็จ" ที่บันทึกไว้ก่อนหน้า (27 ส.ค.) อาจไม่ใช่สถานะปัจจุบันอีกต่อไป — ต้อง re-verify การยิง trigger จริงของ WF-HR-01 ใน session ถัดไปก่อนเชื่อ/อ้างอิงสถานะเดิม** ทั้งที่ structure ยืนยันแล้วว่าเหมือน v3 ทุกประการ (สาเหตุที่แท้จริงยังไม่ทราบ — อาจเป็นปัญหาเฉพาะ session/เวลานี้ ไม่ใช่ความเสียหายถาวร) (3) **บทเรียนใหม่: การแทรก node กลางเส้นทางเดี่ยวที่มีปลายทางอยู่แล้วเสี่ยงเกิด auto-relink ที่ไม่ตรงเจตนา — ควรตรวจ `prevNode` ของ node ปลายทางเดิมหลังแทรกเสมอ (ผ่าน `get_workflow_structure`) ไม่พึ่ง validate_process อย่างเดียว** |
| D-19 | 🔴🔴 **28 ส.ค. 2569 (ต่อเนื่อง 17) — เร่งด่วนที่สุดตอนนี้ — branch ที่เทียบ `<flag> ne "1"` พังกับ record ที่ธงยังว่าง ยังไม่ได้แก้ทั้งโมดูล** (พบจากการ scrutinize การแก้ D-17) | สำรวจด้วย `hap worksheet fields <ws_id> --raw` ครบทุกตารางแล้วพบว่า **ฟิลด์ธงทุกตัวมี `default:""`** ไม่มีตัวไหนได้ `defaultValue:0` จริงเลย (`submitted_flag`/`deducted_flag`/`returned_flag`/`hr_ot_submitted_flag`/`hr_paid_flag`/`hr_summarised_flag`/`probation_alert_flag`) — D-17 แก้ไปแค่ 2 record ทดสอบ ไม่ได้แก้รูปแบบนี้ทั้งโมดูล · **MCP `get_worksheet_structure` ไม่คืนค่า `defaultValue` เลย** จึงยืนยันเรื่องนี้ผ่าน MCP ไม่ได้ ต้องใช้ hap CLI `--raw` เท่านั้น (ดู Known Issues ใน `04-CLAUDE-memory.md` ต่อเนื่อง 17 สำหรับรายละเอียดเต็ม) | 🔴 **WF-HR-02 จะไม่ตัดสิทธิเมื่อ To-do ที่ค้างอยู่ที่ Wanadtapong.l ถูกกด Approve** (gate = `leave_status eq Approved` AND `deducted_flag ne "1"` · `deducted_flag` ของ `TEST-LR-D15-REVERIFY` = ว่าง) แล้วจะเห็นอาการ "workflow ไม่ทำงาน" อีกครั้ง เสี่ยงวินิจฉัยผิดซ้ำรอบที่ 5 · WF-HR-03/05/13 เสี่ยงเหมือนกัน · **AC-01/AC-03 พังตรงนี้** · **path ผู้ใช้จริงที่กรอกผ่านฟอร์มยังไม่เคยทดสอบเลยสักครั้ง** — กฎ "ใส่ค่าธงตอน seed" ป้องกันเฉพาะ record ที่ agent สร้าง · **ทางแก้ที่แนะนำ: สลับโครง branch เป็น path แรก `flag eq "1"` → ไม่ทำอะไร, path หลัง catch-all → ทำงาน** (ผลตรรกะเดิม แต่ทนค่าว่าง ไม่ต้อง backfill ไม่ต้องพึ่ง default) — **ยังไม่ลงมือ รอผู้ใช้อนุมัติเพราะต้องแตะ workflow ที่ publish แล้ว** · ✅🔴 **30 ส.ค. 2569 — ยืนยันแล้วด้วย A/B controlled test (agent-hr, claim `HR/D19-A`)**: ใบลา 2 ใบเหมือนกันทุกฟิลด์ต่างแค่ `deducted_flag` แล้วเปลี่ยน Draft→Approved เท่ากัน — ใบที่ flag=`"0"` ถูกตัดสิทธิครบสาย (`deducted_flag`→`"1"`, used 0→1, remaining 10→9) · ใบที่ flag=`""` **ไม่เกิดอะไรเลย เงียบสนิท ไม่มี error** ⇒ `"" ne "1"` = FALSE ตกไป branch catch-all `อื่นๆ (ไม่ทำอะไร)` · **แก้รายชื่อ workflow ที่กระทบให้ถูกต้อง (จากการอ่านโครงจริงทุกตัว): WF-HR-01 / WF-HR-02 / WF-HR-03 / WF-HR-04** — เดิมเขียนว่า WF-HR-05/WF-HR-13 เสี่ยงด้วยนั้น **ไม่ถูก** (WF-HR-05 branch ใช้ `conditionId 7/8` = ตรวจว่าง/ไม่ว่าง ไม่ใช่ `ne` · WF-HR-13 ยังไม่มีอยู่จริงในแอป) ส่วน **WF-HR-04 กระทบด้วยแต่ไม่เคยถูกลิสต์ไว้** · ล้าง test data + คืนยอดสิทธิกลับ used=0/remaining=10 แล้ว · หลักฐานเต็ม ตาราง branch/nodeId ที่กระทบ และ 3 ทางเลือกแก้ (พร้อมคำสั่ง `hap workflow node save` ที่แก้ node เดิมในที่ได้ ไม่ต้องลบ-สร้างใหม่) → `16-D19-Empty-Flag-Branch.md` · ✅✅ **30 ส.ค. 2569 — แก้แล้วและยืนยันผลแล้ว (claim `HR/D19-B`, ผู้ใช้อนุมัติ "patch data ก่อน + แก้เงื่อนไข")**: ①patch ธงว่างเป็น `0` ครบทุก record ที่ค้าง (`TEST-LR-D15-REVERIFY`/`TEST-LR-DOCOK`/`TEST-LR-BR083`/`TEST-OT-SYSCHK-01`) ②เพิ่ม OR group `is empty` (`conditionId 8`) เข้า branch ของ WF-HR-01/02/03/04 ด้วย `hap workflow node save --type 2` แล้ว `hap workflow publish` ครบ 4 ตัว (v6/v5/v3/v3 · ยืนยัน `enabled=true, deleted=false` · สำรอง structure+node get ก่อนแก้ครบ) ③ยิงจริงเคสเดิมที่เคยพัง: WF-HR-02 ใบลา `deducted_flag=""` → Approved **ตัดสิทธิสำเร็จ** (used 0→1) · WF-HR-03 `returned_flag=""` → Cancelled **คืนสิทธิสำเร็จ** (used 1→0) · WF-HR-01 `submitted_flag=""` → Pending supervisor **เปิด approval สำเร็จ** · ⚠️ WF-HR-04 แก้+publish+อ่านกลับตรงแล้วแต่ยังไม่ live-fire · ล้าง test data + คืนยอดสิทธิเรียบร้อย · **ยังเปิดอยู่:** `defaultValue` ยัง persist ไม่ได้ (record ใหม่ยังเกิดพร้อมธงว่าง แต่ workflow ทนแล้ว) และยังไม่เคยทดสอบ path ผู้ใช้จริงผ่านฟอร์ม |
| D-20 | 🧹 **28 ส.ค. 2569 (ต่อเนื่อง 18) — ส่งต่อฝ่ายบัญชี: พบ orphan/workflow ซ้ำฝั่งบัญชี 12 ตัว (HR ไม่แตะ)** | ระหว่างล้าง orphan ฝั่ง HR สำรวจทั้งแอปพบของฝั่งบัญชีที่ไม่มี parent อ้างถึงหรือเป็นฉบับซ้ำที่ปิดใช้งานไว้: **inner ไม่มี parent 7 ตัว** — `สร้างบรรทัดกลับรายการ` ×5 (`6a8f4286…`/`6a8f42d2…`/`6a8f4310…` ปิดอยู่ · `6a8f437d…`/`6a8f45ba…` **เปิดอยู่**) · `ทำเครื่องหมาย GL ว่าถูกกลับรายการ` ×1 (`6a8f43e4…` เปิดอยู่) · `สร้างรายการภาษีติดลบ` ×1 (`6a8f43c5…` เปิดอยู่) — และ **ฉบับซ้ำระดับบนที่ปิดใช้งาน 5 ตัว** — `WF-AC-02 ผ่านรายการใบสำคัญฯ` (`6a861cfe…`) · `ปิดงวด` (`6a903eb8…`) · `ปิดปีและยกยอด` ×2 (`6a90ef43…`/`6a90f50e…`) · `กลับรายการ` (`6a8fb9ea…`) · ✅ ตรวจแล้วว่า `อนุมัติใบสำคัญ (WF-AC-01)` (`6a8eaa76…`) **ไม่ใช่ตัวซ้ำ** — เป็น approval sub-flow ที่ WF-AC-01 เรียกผ่าน `triggerId` (`appType:9`) | HR **ไม่ลบให้** เพราะเป็น object ของโมดูลบัญชี และมี 3 ตัวที่ `enabled:true` ซึ่งอาจยังถูกอ้างผ่านเส้นทางที่ตรวจไม่พบ — ส่งเรื่องให้ฝ่ายบัญชีตัดสินใจเอง (เอกสาร `tilsna-hr/14-HR-to-Accounting-Orphan-Workflows.md`) · ถ้าปล่อยไว้: รก workflow list ทำให้ audit ยากขึ้น และเสี่ยงแก้ผิดตัวเวลามี process ชื่อซ้ำกัน 5-6 ตัว |

---

## 12. Change Log

| วันที่ | ผู้/agent | การเปลี่ยนแปลง |
|---|---|---|
| 26 ส.ค. 2569 | `nocoly-build-docs` | สร้างชุดเอกสารครั้งแรก · ดึงโครงสร้างจริงของโมดูลบัญชี 6 ตารางมาผูกเป็นจุดเชื่อม · ระบุ Gap G-01…G-08 · ยังไม่มี object ของ HR บนเซิร์ฟเวอร์ |
| 26 ส.ค. 2569 | build agent (MCP) | **P1-1 เสร็จ** — สร้างกลุ่ม HR + HR-00…HR-06 ครบ 8 กลุ่มผ่าน `create_app_sections` · ยืนยันด้วย `get_app_info` |
| 26 ส.ค. 2569 | build agent (MCP) | **P1-2 เสร็จ** — สร้าง optionset ใหม่ 24 ชุดผ่าน `create_optionset` ครบทุกตัว · ยืนยันด้วย `get_optionset_list` · คัดลอก ID + option key ทุกตัวลง BuildSpec §1.6 / §1.6.1 แล้ว |
| 26 ส.ค. 2569 | build agent (MCP) | 🔴 **พบบั๊ก** `create_worksheet` — เรียกทุกครั้งพัง `structuredContent does not match tool outputSchema: required property 'appId' not found` ยืนยันข้ามconnector (`ERP_-_TILSNA`, `API-Lab`) และข้ามแอป ไม่ใช่ปัญหา payload — `update_worksheet` ยังทำงานปกติ |
| 26 ส.ค. 2569 | ผู้ใช้ + build agent (MCP) | ✅ **ปลดบล็อกสำเร็จ** — workaround `create_app_items`→`removeFields`→`addFields`→verify ทดสอบสำเร็จ 10/10 ตาราง HR-00 · ค้นพบเพิ่ม: Checkbox และ Role สร้างผ่าน `addFields` ได้จริง · ยืนยัน shared-optionset Dropdown ยังต้องทำใน Browser เท่านั้น · **P1-3 → 🚧** |
| 26 ส.ค. 2569 | build agent (MCP) | ✅ **P1-4 เสร็จ** — seed ข้อมูลตั้งค่า 55 record ใน 9/10 ตาราง HR-00 · ยืนยันจำนวนด้วย `get_record_pivot_data` COUNT ทุกตาราง · SingleSelect เขียนด้วย option key GUID ได้ถูกต้อง · `hr_approval_rule` ยกไป seed พร้อม P8-1 |
| 26 ส.ค. 2569 | build agent (MCP) | ✅ **P2-1 เสร็จ** — seed `hr_job_level` 6 record + `hr_position` 8 record · ✅ **P2-2 เสร็จ (โครงสร้าง)** — `hr_employee` 36 ฟิลด์ + supervisor self-relation · 🔶 **P2-6 บางส่วน** — 4 ตาราง (contract/event/dependent/bank_account) สร้างครบ · 🟢 ปิดการสอบสวน hap CLI/optionset-binding (Browser-only ยืนยันแน่ชัด) |
| 26 ส.ค. 2569 | build agent (MCP) | 🆕 ✅ **P3-1 เสร็จ (โครงสร้าง)** — สร้าง `hr_leave_balance` (`6a8f2dba353e1b0e4a507757`) · `hr_leave_request` (`6a8f2dbaae2a0e3743a0beaa`) · `hr_leave_ledger` (`6a8f2dba9762533b5b718675`) ครบทั้ง 3 ตาราง ผ่าน `create_app_items`→`update_worksheet`(removeFields+addFields **รวมคำสั่งเดียว** — ค้นพบใหม่ว่าไม่ต้องแยก 2 คำสั่ง)→`get_worksheet_structure` verify · relation ทุกฟิลด์ตั้ง `bidirectional:true` สร้าง reverse field อัตโนมัติครบ (เชื่อมกับ `hr_employee`/`hr_leave_type`/ข้ามกันเองระหว่าง 3 ตารางนี้) · บันทึก field ID ทั้งหมดไว้ที่ไฟล์ใหม่ `tilsna-hr/02b-BuildSpec-Addendum-P3-1.md` (ไม่แก้ `02-BuildSpec-FRS.md` ตัวหลักโดยตรง เพื่อลดความเสี่ยงเขียนทับไฟล์ขนาดใหญ่ผิดพลาด) · ยังไม่เสร็จ: seed record ทดสอบยืนยัน defaultValue จริง · unique index (IX-08.1/09.1) · form rules · การผูก Dropdown 3 ฟิลด์ (`leave_unit`/`leave_status`/`ledger_type`) เข้า shared optionset (ยังเป็น inline options — Browser-only) · `hr_leave_ledger.ledger_no` เป็น Text ชั่วคราวแทน AutoNumber ที่สเปกเดิมกำหนด (API ทำ AutoNumber ไม่ได้) · `addRecordButtonName` ตั้งเป็นไทยครบแล้วทั้ง 3 ตาราง (แก้ตามคำขอผู้ใช้ วันเดียวกัน — ดู Change Log แถวถัดไป) |
| 26 ส.ค. 2569 | build agent (MCP) | ✅ **แก้ Data Name ภาษาจีน (`记录`) ของ 3 ตารางใบลาเป็นไทย** (ตามคำขอผู้ใช้ "แก้ภาษาจีนตาม Data Name / Record Name") — เรียก `update_worksheet.addRecordButtonName` แยกทีละตาราง: `hr_leave_request`→"ใบลา" · `hr_leave_balance`→"สิทธิและยอดคงเหลือการลา" · `hr_leave_ledger`→"รายการเคลื่อนไหวสิทธิลา" — ยืนยันด้วย `hap worksheet info` ทั้ง 3 ตาราง (name/entityName ตรงกันเป็นไทยแล้ว) — ตอนนี้ทั้งแอปมี Data Name ไทยครบ 47/47 ตาราง (44 เดิม + 3 ใบลา) |
| 26 ส.ค. 2569 | ผู้ใช้ + build agent (MCP) | ✅ **แก้ Data Name ภาษาจีนรอบ 2** (ผู้ใช้ทักว่า "ยังเป็นภาษาจีนอยู่ อย่างเหตุการณ์การจ้าง และชีทใกล้ๆ กัน") — ตรวจซ้ำทุกตาราง HR ที่มีอยู่ (HR-00 10 · position/job_level/employee 3 · P2-6 4 · ใบลา 3) พบว่า **4 ตาราง P2-6** (`hr_employment_contract`/`hr_employment_event`/`hr_dependent`/`hr_bank_account`) ยังเป็น `记录` ค้างอยู่ (สร้างด้วย workaround `create_app_items` รอบก่อนหน้ารอบใบลา จึงพลาดจุดเดียวกัน) — แก้ครบด้วย `update_worksheet.addRecordButtonName` ยืนยันด้วย `hap worksheet info` ทั้ง 4 ตาราง · ตรวจ HR-00/position/job_level/employee ซ้ำแล้วยืนยันว่าถูกต้องอยู่แล้ว **ทั้งแอปตอนนี้มี Data Name ไทยครบ 54/54 ตาราง** (44 เดิม + 3 ใบลา + 3 position/job_level/employee + 4 P2-6) |
| 26 ส.ค. 2569 | ผู้ใช้ + build agent (MCP) | ✅ **แก้ view name ภาษาจีนด้วย** (ผู้ใช้ถามต่อว่า view name แก้ผ่าน CLI ได้ไหม แล้วให้เช็ค/แก้ทั้งหมด) — พบว่า view เริ่มต้นที่ `create_app_items` สร้างให้ทุกตารางมีชื่อจริงเป็น `全部` (จีน) ไม่ใช่ "ทั้งหมด" ตามที่เอกสารเคยเข้าใจผิดไว้ — ใช้ `hap worksheet view update <ws_id> <view_id> --name "ทั้งหมด" -a <app_id>` (hap CLI เท่านั้น MCP ไม่มี `update_view`) แก้ครบ 10 ตารางที่ยังค้าง: `hr_leave_request`/`hr_leave_balance`/`hr_leave_ledger`/`hr_employment_contract`/`hr_employment_event`/`hr_dependent`/`hr_bank_account`/`hr_position`/`hr_job_level`/`hr_employee` — ยืนยันด้วย `hap worksheet view list` ทุกตาราง (HR-00 10 ตารางมี view ไทยอยู่แล้วตั้งแต่ต้น ไม่ต้องแก้) · **บทเรียนใหม่:** workaround `create_app_items` ต้องเช็คให้ครบ 2 จุดเสมอ — (1) `addRecordButtonName` ระดับตาราง (2) ชื่อ view default `全部`→"ทั้งหมด" |
| 27 ส.ค. 2569 | build agent (MCP, งานต่อเนื่องจากผู้ใช้เลือก "เริ่มสร้าง WF-HR-01") | 🆕 ✅ **WF-HR-01 สร้าง+publish+ทดสอบยิงจริงสำเร็จ** — 3 process (main `6a8f36bc0e97bf440dda64eb` v3 + inner approval ขั้นหัวหน้างาน `6a8f373b730d20c5b7647131` + ขั้น HR `6a8f3757730d20c5b7647392`) ออกแบบใหม่เป็น single-flow gate-branch (เลี่ยง nested approval_block ที่ไม่เคยพิสูจน์) · seed พนักงานทดสอบ 2 คนผูกบัญชีจริงขององค์กร (`TEST-EMP1`→Kunlasatri.c, `TEST-EMP2`→Wanadtapong.l เป็นหัวหน้า) + `hr_leave_balance` 10 วัน แล้วยิงทดสอบจริง ผ่านขั้นหัวหน้างานสมบูรณ์ (`_updatedBy`=`user-workflow` · to-do ถึง Wanadtapong.l จริง) · 🔴 **ระหว่างทางพบและแก้บั๊กใหม่ 2 ตัวที่กระทบทุกโปรเจกต์ Nocoly** — (1) `get_single` filter ด้วย `rowid`+`in` เทียบ Relation field ของ node อื่นล้มเหลวเงียบ (ต้องใช้ reverse-relation field + `contains` แทน) (2) `FieldPatch.value{kind:field}` ที่ลืมใส่ `node` เขียนค่าว่างเงียบไม่ error ตอน validate — บันทึกทั้งคู่ไว้ใน `nocoly-shared/00-HAP-Working-Guide.md` §2 ข้อ 16-17 แล้ว (ใช้ได้กับทุกโปรเจกต์ที่เหลือ) · ค้นพบเพิ่ม: ลบ branch node ด้วย `delete_process_node` cascade ลบทั้ง subtree ใต้ paths (ยืนยัน M-04 บางส่วน) · ⬜ ยังไม่ทดสอบขั้น HR และ path ปฏิเสธทั้ง 4 เส้น — ต้องมี `hr_approval_rule` seed (บล็อกด้วย G-06→role) และมนุษย์กด Approve ใน To-do ก่อน |
| 27 ส.ค. 2569 | build agent (MCP, งาน P3-6) | 🆕 ✅ **WF-HR-02 (`6a8fa97cfdab77a41c51cb3f`, publish v3) และ WF-HR-03 (`6a8fab800e97bf440dddb744`, publish v1) สร้าง+publish+ทดสอบยิงจริงสำเร็จทั้งคู่** — ใช้ record ทดสอบเดิม `4a226f18-…` (TEST-LR-0001) และ balance `56c008e2-…` (TEST-EMP1-ANNUAL-2026) เดิมจาก WF-HR-01 · **ลำดับทดสอบ:** Approved (WF-HR-02 ตัดสิทธิ 10→9) → ยิงซ้ำ Approved อีกครั้ง (ยืนยัน idempotency: ไม่ตัดซ้ำ) → Cancelled (WF-HR-03 คืนสิทธิ 9→10) · หลักฐานครบ: `_updatedBy`=`user-workflow` ทั้งบน balance และ trigger record ทุกจุด · ledger 2 แถวคู่กัน (`ledger_days` -1.0/+1.0, `balance_after` 9.0/10.0) relation ชี้ balance/leave_request ถูกต้อง (แสดงชื่อจริง ไม่ใช่ "已删除") · `deducted_flag`/`returned_flag` เซ็ตถูกต้องตามลำดับ · **เลื่อนออกจากขอบเขต:** ตัด node sync `hr_attendance` (ยังไม่มีตาราง รอ P4-1) ออกจากทั้ง 2 workflow — บันทึกเป็น D-8 · 🔴🔴 **พบบั๊กแพลตฟอร์มใหม่ 2 ตัวระหว่างทาง แก้ได้ทั้งคู่ก่อน publish จริง:** (A) **compute/get_single node ไม่ใช่ snapshot แบบ freeze** — ถ้า node ที่เขียนทับฟิลด์ต้นทางของ compute (เช่น `update_record` เขียน `used_days`) รันก่อน node ปลายทางที่อ้างผลลัพธ์ compute เดิมซ้ำ (เช่น `add_record` ledger อ้าง `calc_remaining-number_fx_id` อีกครั้งหลัง update ไปแล้ว) ค่าที่ได้จะถูกคำนวณใหม่จากค่าล่าสุด (หลัง update) ไม่ใช่ค่า ณ ตอนที่ compute node รันครั้งแรก — ทำให้ `ledger.balance_after` เพี้ยน (พบจริง: balance จริง=9.0 แต่ ledger บันทึก 8.0) **วิธีแก้ที่พิสูจน์แล้ว:** เรียง node ให้ `add_record` (ledger) รันก่อน `update_record` (balance) เสมอ อย่าให้มี write คั่นระหว่าง compute กับจุดที่ต้องใช้ผลลัพธ์นั้นซ้ำ (B) **`FieldPatch.value{kind:"record"}` เขียนลง Relation field แล้วได้ค่าขยะ** — ค่าที่บันทึกจริงกลายเป็น literal string ที่มี **node ID ภายใน** (เช่น `["6a8fa9a9fdab77a41c51cd9e"]`) ไม่ใช่ rowid ของ record จริง ทำให้ relation แสดง "已删除" (ไม่พบ) แม้ `validate_process`/`publish_process` ผ่านปกติ **วิธีแก้ที่พิสูจน์แล้ว:** ใช้ `{kind:"field", node:{nodeAlias:<upstream>}, fieldId:"rowid"}` แทน `{kind:"record", node:{...}}` เมื่อต้องเขียนค่าเข้า Relation field ที่ชี้ไปยัง record ของ node ต้นน้ำ (ไม่ใช่การ copy ค่าจาก relation field ที่มีอยู่แล้ว ซึ่งใช้ kind:"field" ปกติได้อยู่แล้ว) — บันทึกทั้งคู่ไว้ใน `04-CLAUDE-memory.md` และ `nocoly-shared/00-HAP-Working-Guide.md` §2 ข้อ 18-19 แล้ว (ใช้ได้กับทุกโปรเจกต์ Nocoly) |
| 27 ส.ค. 2569 | build agent (Browser + MCP, งาน P3-7/P3-8) | 🆕 ✅ **P3-7 เสร็จ+ยืนยัน** — ล้าง ghost views ซ้ำ 8 อันจากเซสชันก่อนหน้า (ที่ Agent tool call ถูก reject แต่ยังสร้าง side-effect ทิ้งไว้) เหลือ 5 view ที่ถูกต้อง · ยืนยันปุ่ม "ส่งคำขอ"/"ยกเลิกใบลา" render ตาม `enableWhen` จริงด้วยการ toggle `leave_status` ผ่าน MCP แล้วเปิดฟอร์มดู · 🔶 **P3-8 บางส่วนเสร็จ** — สร้าง+ยืนยัน BR-08.1/8.4/8.5 ผ่าน Role Debugging (role Member: เปิด record จริงเห็น read-only all เมื่อ Cancelled, เห็น hide `leave_to`/show `leave_hours` เมื่อ `leave_unit`=Hourly) · พบข้อจำกัดแพลตฟอร์มใหม่ที่สำคัญ: (1) BR-08.2/BR-08.3 ทำเป็น Business Rule ไม่ได้ (field picker ไม่รองรับ relation-field lookup ข้ามตาราง) (2) **Dynamic Default "Query worksheet" ก็มีข้อจำกัดเดียวกัน** — ทดสอบสร้าง DV-08.1 จริงจนถึงขั้น Query conditions แล้วพบว่าช่อง Dynamic Value ไม่แสดง Relation-type field ของฟอร์มปัจจุบัน (`employee`) ให้เลือกเปรียบเทียบกับ `Record ID` ของ worksheet ที่ query เลย — ยกเลิกการตั้งค่านี้ (Cancel ไม่ Save) แล้วบันทึกเป็น D-9 ต้องย้ายไปทำที่ WF-HR-01 แทน (3) **ทดสอบเปิด IX-08.1 จริงจนถึงหน้า Create Index** แล้วอ่าน tooltip ของระบบยืนยันว่าฟิลด์ไม่ required + ไม่ auto-generate จะเปิด unique index ไม่ได้โดยไม่เสี่ยงบล็อกการสร้าง Draft ใบที่ 2 — ตัดสินใจไม่เปิดใช้ (Cancel ไม่ Save) บันทึกเป็น D-10 · ยังพบและบันทึก UI quirk ใหม่ 2 ข้อระหว่างการสร้าง Business Rules (ดู Known Issues ของ `04-CLAUDE-memory.md`): ต้องใช้คีย์บอร์ด (Down+Return) เพื่อเลือก "Dynamic Value" แทนการคลิกเมาส์ตรง ๆ · ปุ่ม Escape ขณะแผงสร้างกฎเปิดอยู่จะปิดทั้งแผงและทิ้งข้อมูลที่ยังไม่ได้ Save ทันที |
| 27 ส.ค. 2569 | build agent (MCP, งาน P3-4) | 🆕 ✅ **P3-4 เสร็จ+ยืนยัน** — seed `hr_leave_balance` ให้ TEST-EMP1/TEST-EMP2 ครบทั้ง 6 ประเภทการลา (ก่อนหน้ามีแค่ TEST-EMP1.ANNUAL 1 แถว) ผ่าน `batch_create_records`(`triggerWorkflow:false`) เอาค่า entitlement จาก `hr_leave_policy` ที่ seed ไว้แล้ว (UNPAID=30·ORDINATION=15·MATERNITY=98·PERSONAL=3·SICK=30·ANNUAL=6) — คงค่า TEST-EMP1.ANNUAL เดิม=10 ไว้ไม่แตะ (ใช้ทดสอบ WF-HR-02/03 ไปแล้ว) · ยืนยันด้วย `get_record_pivot_data`(group employee, COUNT rowid) = 6 ทั้งสองคน |
| 27 ส.ค. 2569 | build agent (MCP + hap CLI, งาน P4-1/P4-2 — ตามคำขอผู้ใช้ "เน้นงานที่ผ่าน MCP, CLI ได้ก่อน") | 🆕 ✅ **P4-2 `hr_ot_request` (`6a8fcca48b6633ef76f13033`) และ P4-1 `hr_attendance` (`6a8fcd67353e1b0e4a507d32`) สร้างโครงสร้างครบ** (สร้าง `hr_ot_request` ก่อนเพราะ `hr_attendance.ot_request` ต้องอ้างถึง) ผ่าน `create_app_items`→`update_worksheet`(removeFields+addFields รวมคำสั่งเดียว)→`get_worksheet_structure` verify — `hr_ot_request` 16 ฟิลด์ตาม FR-HR-11B ครบ (relation `employee`→`hr_employee`) · `hr_attendance` 16 ฟิลด์ตาม FR-HR-10 ครบ (relation `employee`/`shift`/`leave_request`/`ot_request` ชี้ถูกทั้ง 4 จุด) · ทั้งคู่ตั้ง view "全部"→"ทั้งหมด" ผ่าน `hap worksheet view update` แล้ว · 🆕 **พบและแก้ระหว่างทาง:** สร้าง `hr_ot_request.day_type` ผิดพลาดครั้งแรกเป็น label ภาษาไทย (คัดลอกมาจากชื่อ record ของ `hr_ot_rate` แทนที่จะเป็น label ของ optionset `OS_HR_OT_DAY_TYPE`) — ตรวจพบก่อนใช้งานจริง แก้ด้วย `editFields` เป็นภาษาอังกฤษ (Working day / Holiday within hours / Holiday outside hours) ให้ตรงกับ optionset กลาง (แม้ยังเป็น inline options ไม่ได้ผูกจริง) · ✅ **`addRecordButtonName` ยืนยันซ้ำว่ายังใช้ได้ปกติแม้ไม่อยู่ใน schema ของ `update_worksheet`** — ตอนแรกลองเช็ค hap CLI (`worksheet update`/`form-settings`) ไม่พบช่องนี้ ชวนสงสัยว่า MCP อาจใช้ไม่ได้แล้ว แต่ทดสอบยิงตรงก็ยังสำเร็จปกติทั้ง 2 ตาราง (`hr_ot_request`→"ใบขออนุมัติล่วงเวลา", `hr_attendance`→"บันทึกลงเวลา") ยืนยันด้วย `hap worksheet info` → `entityName` ตรงกัน · ✅ **ปลดบล็อก D-8** — `hr_attendance` มีตารางแล้ว พร้อมเพิ่ม node sync "On leave" กลับเข้า WF-HR-02/03 |

| 27 ส.ค. 2569 (รอบเย็น) | build agent (MCP, งาน D-8 — ตามคำขอผู้ใช้ "จัดการต่อเลย") | 🆕🔴 **พยายามทำ D-8 (เพิ่ม node sync "On leave" กลับเข้า WF-HR-02/03) แล้วชนบั๊กแพลตฟอร์มใหม่ — revert กลับสถานะเดิมปลอดภัย ไม่มีข้อมูลเสียหาย** — สร้าง `get_multiple`(หา `hr_attendance` ที่ทับช่วงวันลา) + `sub_process`(`sequential_each`, update `att_status`→"On leave"+ผูก `leave_request`) สำเร็จทั้ง 2 จุดของ WF-HR-02 (path `bal_missing`/`bal_found`) และ 1 จุดของ WF-HR-03 (revert: clear `att_status`/`leave_request`+set `summarised_flag`=0) แต่ `validate_process` ค้าง error `NodeAppIsNull` ที่ตัว `sub_process` container node เสมอ (internal child process ของมันเองกลับ valid+canPublish:true ปกติทุกครั้งเมื่อ validate ตรงด้วย internal processId) — ลองแก้ 3 รูปแบบ (worksheetId ที่ `process.start` / worksheetId ที่ config ระดับบนสุด / target แบบ nodeAlias-only vs nodeId+alias) และลอง validate ซ้ำหลังรอ (ตัดทิ้งความเป็นไปได้ eventual-consistency) ก็ยังไม่หาย — **ตัดสินใจ revert:** `delete_process_node` ทุก node ที่เพิ่ม (sub_process ก่อน แล้วค่อย get_multiple) คืน WF-HR-02/WF-HR-03 กลับโครงสร้างเดิมทุกจุด → `validate_process` สะอาด (issueCount:0) → `publish_process` สำเร็จทั้งคู่ (WF-HR-02 publish v4, WF-HR-03 publish v2 — logic ทำงานเดิมทุกอย่าง ไม่มีการเปลี่ยนแปลงจริงในผลลัพธ์ที่ผู้ใช้เห็น) — บันทึกบั๊กนี้เป็น **D-13** (Roadmap) และ **G-13** (RTM) พร้อมรายละเอียดเต็มสำหรับลองต่อภายหลัง (Browser UI / รอแพตช์ / workaround เลี่ยง sub_process) — 🔴 **ผลกระทบ:** D-8 และ WF-HR-05 (ต้องใช้ sub_process เช่นกัน) ทำต่อไม่ได้จนกว่าจะแก้บั๊กนี้ — เปลี่ยนแผนไปทำ WF-HR-04 (อนุมัติ OT 2 ระดับ ไม่ต้องใช้ sub_process) แทนในลำดับถัดไป |
| 27 ส.ค. 2569 (ต่อเนื่อง) | build agent (MCP, งาน P4-3/WF-HR-04 — ตามคำแนะนำเดิม "เน้นงานที่ผ่าน MCP/CLI ได้ก่อน") | 🆕🟢 **P4-3/WF-HR-04 สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ** — ก่อนเริ่ม แก้ prerequisite ที่ค้าง: เพิ่มฟิลด์ `hr_ot_rate.day_type` ที่ขาดหายไปทั้งหมด (ไม่ใช่ gap เดิม) ผ่าน `addFields` (ลบ+สร้างใหม่ 1 ครั้งเพื่อแก้ label ภาษาไทยผิดพลาดรอบแรกให้ตรง `OS_HR_OT_DAY_TYPE`) fieldId `6a8fd7608b6633ef76f13156` แล้ว backfill 3 seed record เดิม · ดึงโครงสร้าง live จริงของ WF-HR-01 (`get_workflow_structure` ทั้ง 3 process) มาใช้เป็นต้นแบบยืนยันว่าสถาปัตยกรรมจริงคือ single-flow gate-branch (ไม่ใช่ nested approval_block) · สร้าง WF-HR-04: main `6a8fd8d35f8564a68c3c1909` + inner supervisor-approval `6a8fd9325f8564a68c3c1cec` + inner HR-approval `6a8fd9325f8564a68c3c1ced` · 🆕 **คิดค้นเทคนิคใหม่แก้ข้อจำกัด "fork แล้วไม่มี merge node"**: เมื่อ 2 เส้นทาง (holiday/workday) ต้องไปจบที่ approval_block เดียวกัน ให้สร้าง approval_block ตัวแรกด้วย `mode:"create"` (เก็บ inner processId ที่ระบบคืนมา) แล้วสร้าง approval_block ตัวที่สองด้วย `mode:"use_existing"` อ้าง processId เดียวกัน — ประหยัดไม่ต้องสร้าง inner flow ซ้ำ 2 รอบ · **พบ+แก้บั๊กใหม่ 2 ตัวระหว่างทดสอบ:** (1) WF ไม่ยิงเลยรอบแรก เพราะ `hr_ot_submitted_flag`/`hr_paid_flag` ไม่มี `defaultValue` (ธง `ne` condition เทียบค่าว่างไม่ match) — แก้ด้วย `editFields` ใส่ `defaultValue:0` (เจอ `editFields` ล้าง `name`/`alias` อีกครั้งระหว่างแก้ ต้องส่งคืนครบ) (2) `dateDiff` compute node (`outputUnit:"h"`) **ตัดพารามิเตอร์ `precision` ทิ้งเงียบ ๆ** ทำให้ 2.5 ชม. กลายเป็น 2.00 — แก้ด้วยลบ 9 node ปลายน้ำแล้วสร้างใหม่เป็น 2 ขั้น: `dateDiff`(นาที) → `number`(หาร 60, precision:2) — ยืนยันด้วยเคสทดสอบ 2.5h ได้ผลถูกต้อง "2.50" · **ทดสอบยิงจริง 3 เคส** (`OT-TEST-HOLIDAY-01`/`WORKDAY-01`/`FRACTIONAL-01`) ยืนยัน `applied_multiplier` ดึงจากตาราง `hr_ot_rate` ถูกต้องตาม day_type (×3.0/×1.5) — **AC-19 ส่วนแรกผ่าน** — เก็บ record ทดสอบไว้เป็นหลักฐาน (มี To-do จริงถึง Wanadtapong.l) · ⬜ ขั้น HR (`appr_block_2`) ยังไม่ทดสอบ (รอ `hr_approval_rule` seed → รอ role → รอ G-06) — บันทึกบทเรียนใหม่เป็น D-14 (audit ฟิลด์ธงตารางอื่นก่อน WF-HR-05) |
| 27 ส.ค. 2569 (ต่อเนื่อง 2) | build agent (MCP + Claude in Chrome, งาน P4-5 — ตามคำขอผู้ใช้ "ดำเนินการต่อ") | 🆕✅ **P4-5 เสร็จ+verify ผ่านจริง** — สร้างปุ่ม "ส่งขอล่วงเวลา" (`create_custom_actions` บน `hr_ot_request`, actionId `6a8fde0aae2a0e3743a0c62f`) + view "OT รออนุมัติ" (`6a8fde0b353e1b0e4a50803e`) + view "บันทึกลงเวลาที่ผิดปกติ" บน `hr_attendance` (`6a8fde0c1378964f9984a42c`) ทั้งหมดผ่าน MCP สำเร็จในครั้งแรก · 🔴 **พบว่ากับดัก "Scope เริ่มต้น Unassigned View" ของ `create_custom_actions` ที่เคยพบในโมดูลบัญชี (26 ส.ค.) เกิดซ้ำในโมดูล HR เป็นครั้งแรก** — ปุ่มที่สร้างไม่โผล่บนหน้าจอเลยจนกว่าจะเข้า Browser ตั้ง Scope เป็น "All Records" · **ใช้ Claude in Chrome แก้ปัญหานี้สำเร็จ** — พบว่ามี browser session ที่ login ค้างอยู่แล้ว (ไม่ต้อง login ใหม่) เข้า Form Settings → Custom Action → Scope → เลือก All Records (บันทึกอัตโนมัติ ไม่มีปุ่ม Save แยก ตรงกับที่บันทึกไว้ในโมดูลบัญชี) · **verify DoD ครบ:** toggle `hr_ot_status` ของ record ทดสอบเป็น Draft ชั่วคราวผ่าน MCP (`triggerWorkflow:false`) → เปิดใน Browser เห็นปุ่มโผล่จริงตาม `enableWhen` → revert กลับค่าเดิม (ไม่กระทบ evidence เดิม) · view ทั้ง 2 ตัวยืนยันด้วยสายตาผ่าน Browser (filter/columns ถูกต้อง) |

| 28 ส.ค. 2569 | build agent (MCP, งาน P5-4 — ตามคำขอ "เดินงานส่วนที่ใช้ MCP, CLI ก่อน") | 🆕🔶 **P5-4 seed `hr_pay_component` 6 รายการผ่าน `batch_create_records` + verify ด้วย `get_record_list`** — ผูก `coa_account`→`ac_coa` `530101 เงินเดือน` สำเร็จ 3/6 (`SALARY`/`OT`/`ABSENCE_DEDUCT`) · `SSO_ER`/`SSO_EE`/`WHT` เว้นว่างเพราะตรวจ `ac_coa` ครบ 79 record แล้วไม่มีบัญชีรองรับจริง — ส่งสรุปปัญหาให้ผู้ใช้/ฝ่ายบัญชีเป็นไฟล์แยกแล้ว |
| 28 ส.ค. 2569 (ต่อเนื่อง) | build agent (MCP, งาน P4-4/WF-HR-05 — ตามคำแนะนำ "ทำต่อตามลำดับที่แนะนำ") | 🆕🔴 **D-15(a): ยืนยันซ้ำ D-13 `sub_process`→`NodeAppIsNull` ด้วย minimal test บน process ใหม่ (trigger+get_multiple+sub_process 1 node) — error เดิมทุกประการ ลบ process ทิ้งสะอาด ไม่มี draft ค้าง** — P4-4(WF-HR-05)/P4-6(WF-HR-06) เปลี่ยนเป็น ⛔ จนกว่า D-13 จะแก้ |
| 28 ส.ค. 2569 (ต่อเนื่อง 2) | build agent (MCP, งาน P3-8/BR-08.3 บน WF-HR-01) | 🆕🔴 **D-15(b-d): ลองย้าย BR-08.3 (บังคับแนบเอกสาร) เข้า WF-HR-01 จริง** — เพิ่ม node `chk_doc`/`br_document`/`reject_doc`/`notify_doc` สำเร็จ, validate สะอาด, publish v4 — **แต่พบ auto-relink ผิดเจตนา** (ปลายทางเดิม `set_submitted` ถูกย้ายไปอ้าง node ของ branch เองแทน path `doc_ok`) **และทดสอบยิงจริงด้วย record ใหม่ไม่มี node ใดทำงานเลย แม้รอเกิน 3 นาที** — **ตัดสินใจ revert ทันที**: `delete_process_node` 4 ครั้งคืนโครงสร้างเดิม 100% → validate สะอาด → publish v5 — **ทดสอบยิงซ้ำหลัง revert ก็ยังไม่มี node ใดทำงานเช่นกัน** (ปริศนายังไม่คลี่คลาย) — ยืนยันแล้วว่า approval instance จริงของ TEST-LR-0001 (To-do ถึง Wanadtapong.l) ไม่กระทบ, ลบ/cancel test record ทั้ง 2 ใบที่สร้างไว้แล้ว — **BR-08.3 ยังไม่ได้เพิ่ม, ต้อง re-verify WF-HR-01 trigger ใน session หน้าก่อนเชื่อสถานะ 🟢 เดิม** (รายละเอียดเต็ม D-15 ในตาราง §11) |
| 28 ส.ค. 2569 (ต่อเนื่อง 3) | build agent (ตรวจเอกสาร skill + MCP, ตามคำขอผู้ใช้ "ส่วนที่ติด Block ลองยืนยันข้อมูลใน skill ชุด hap ได้ไหม ทั้ง mcp และ cli") | 🆕🟢 **D-16: พบ+ยืนยัน root cause จริงของ D-13 — คลี่คลายแล้ว ไม่ใช่บั๊กแพลตฟอร์ม!** ตรวจเอกสารอ้างอิงของ skill `hap-mcp-app-builder` (`workflow_rules.md`) พบระบุชัดเจนว่าต้อง `publish_process` ที่ inner processId ของ `sub_process`/`approval_block` **ก่อน** แล้วจึงค่อย publish outer/main — publish outer ตรง ๆ โดยไม่ publish inner ก่อนจะได้ `NodeAppIsNull` แน่นอน (กฎเดียวกันมีบันทึกสั้น ๆ อยู่แล้วใน `nocoly-hybrid-builder-v2/references/workflow-authoring-mcp.md` บรรทัด 155 แต่ตีความผิดมาตลอดว่า validate อย่างเดียวพอ) — **ทดสอบยืนยันจริงด้วย minimal process ใหม่:** สร้าง outer (`6a90fb59fdab77a41c63662d`, trigger+get_multiple(`hr_employee`)+sub_process) + inner (`6a90fb5dfdab77a41c636699`, 1 update_record) → validate(inner)=valid:true → **publish(inner)=published:true v1** → validate(outer)=valid:true → **publish(outer)=published:true v1 ไม่มี `NodeAppIsNull`** — ลบ process ทดสอบทั้ง 2 ทิ้งสะอาดแล้ว (`delete_process` ไม่ cascade ลบ inner อัตโนมัติ ต้องลบแยกทั้งคู่ ไม่งั้นเหลือ orphan) — **P4-4 (WF-HR-05) และ P4-6 (WF-HR-06) ปลดบล็อกแล้ว** พร้อมสร้างจริงในรอบถัดไปด้วยลำดับ publish inner→outer เสมอ |
| 28 ส.ค. 2569 (ต่อเนื่อง 4) | build agent (MCP, ตามคำแนะนำ "ทำตามที่แนะนำ" — re-verify WF-HR-01 ก่อนสร้าง WF-HR-05) | 🆕🔴🔴 **D-17: ค้นพบว่า worksheet_event workflow ทั้งหมดไม่ยิงเลยตอนนี้ — เป็นปัญหาระดับระบบ ไม่ใช่ WF-HR-01 เจาะจง** — re-verify WF-HR-01 ด้วย record ทดสอบใหม่สะอาด (`TEST-LR-D15-REVERIFY`) ไม่ยิง (68+ วินาที) → sanity-check ด้วย WF-HR-04 (untouched, เคยยิงสำเร็จ 27 ส.ค.) ด้วย record ใหม่ (`TEST-OT-SYSCHK-01`) ก็ไม่ยิงเช่นกัน → โครงสร้าง WF-HR-01 ตรวจสอบแล้วสมบูรณ์ 100% (published, ไม่มี warning) ⇒ สรุปว่าเป็นปัญหา session/environment/platform กว้างกว่า WF-HR-01 ไม่ใช่ผลจากการแก้ไข BR-08.3/D-15 — เก็บ record ทดสอบทั้ง 2 ไว้เป็นหลักฐาน ไม่ลบ — **P4-4/P4-6 สร้างโครงสร้างต่อได้ปกติ แต่ทดสอบยิงจริงต้องรอ D-17 คลี่คลาย** — ดูรายละเอียดเต็มใน §11 D-17 |
| 28 ส.ค. 2569 (ต่อเนื่อง 5) | build agent (MCP, ตามคำสั่ง "ดำเนินการต่อตามที่แนะนำ" — สร้างโครงสร้าง WF-HR-05/06 ผ่าน MCP แม้ D-17 จะบล็อกการทดสอบยิงจริง) | 🆕🟢 **สร้าง+publish WF-HR-05 และ WF-HR-06 สำเร็จจริงทั้งคู่ผ่าน MCP** — WF-HR-05 main `6a910184730d20c5b7710fa8` v1 + inner `6a910197730d20c5b7711028` v1 (sub_process สรุปสถานะรายวัน) · WF-HR-06 main `6a910222730d20c5b77115e5` v1 + inner `6a9102345f8564a68c449ee7` v1 (sub_process แจ้งเตือน SLA) — `validate_process` ทั้ง 4 process สะอาด 0 error ทั้งคู่ publish สำเร็จ ยืนยัน D-16 fix ใช้ได้จริงกับ build งานจริง ไม่ใช่แค่ minimal test — **ปรับโครงสร้างจากดราฟต์เดิม:** ใช้ node `code` (JavaScript) แทน `Duration`/`Function calculation` ใน WF-HR-05 เพราะ `hr_shift.hr_start_time`/`hr_end_time` เป็น Text "HH:mm" ไม่ใช่ Time field (คำนวณผ่าน epoch ms + offset +7h เอง กัน timezone bug) · ตัดขั้น "get associated shift" ออกเพราะ `hr_attendance` มี `hr_att_shift` อยู่แล้ว · ส่ง `hr_setting` (grace/halfday minutes, SLA days) เข้า sub_process ผ่าน subprocess parameter แทนการ query ซ้ำทุก record — 🆕🟡 **พบ gap ใหม่ D-18 ระหว่างสร้าง WF-HR-06:** `hr_ot_request` ไม่มีฟิลด์ `submitted_at` ⇒ **WF-HR-06 v1 จำกัดขอบเขตเหลือแค่ `hr_leave_request`** ตัด OT/claim ออกจากรอบนี้ รอผู้ใช้ตัดสินใจ — ⚠️ **ทั้งคู่ยังไม่ได้ live-fire test เพราะ D-17 ยังไม่คลี่คลาย** (schedule trigger ยังไม่เคยพิสูจน์ว่ากระทบจาก D-17 ด้วยหรือไม่) — ดูรายละเอียดเต็มใน `02-BuildSpec-FRS.md` WF-HR-05/06 และ `04-CLAUDE-memory.md` |
| 28 ส.ค. 2569 (ต่อเนื่อง 6) | build agent (MCP, รับ response จากฝ่ายบัญชี `tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md` — ปิด Gap G-02) | 🆕✅ **P5-4/G-02 ปิดครบ — ผูก `hr_pay_component.biz_coa_account` สำเร็จ 6/6** — ฝ่ายบัญชีสร้างบัญชีใหม่ 3 รายการในผังบัญชีจริงตามที่ HR ส่งเรื่องไป (`SSO_ER`→`530102`, `SSO_EE`→`210402`, `WHT`→`210304`) และเพิ่ม `event_code` = `PAYROLL_ACCRUAL`/`PAYROLL_PAYMENT` ใน `ac_posting_rule` ให้แล้ว (แก้ไขความเข้าใจผิดเดิมของ HR ว่าต้องทำผ่าน Browser — จริง ๆ เป็น inline field แก้ผ่าน MCP ได้ตรง ๆ) — HR ทำตามที่คอมมิตไว้ในเอกสาร handoff เดิม: อ่าน `get_worksheet_structure(hr_pay_component)` หา fieldId ของ `biz_coa_account` (`6a8ff3049762533b5b7193a2`) แล้ว `update_record`(`triggerWorkflow:false`) 3 ครั้งผูกบัญชีให้ `SSO_ER`/`SSO_EE`/`WHT` ครบ · ยืนยันด้วย `get_record_details` ทั้ง 3 record เห็นชื่อบัญชีจริง (`530102`/`210402`/`210304`) ไม่ใช่ "已删除" · **ฝ่ายบัญชียังยืนยันการตัดสินใจ WF-HR-10 source-of-truth ด้วย: `ac_posting_rule` เป็นหลัก (จับคู่ event_code+conditions), `hr_pay_component.biz_coa_account` เป็น fallback** ตรงกับที่ HR เสนอไว้ในเอกสาร handoff — **ยังเปิดอยู่ (ไม่แตะ ตามคำสั่งฝ่ายบัญชี "ห้ามเดา"):** อัตราเงินสมทบ SSO (นายจ้าง+ลูกจ้าง)/เพดานฐานค่าจ้าง/ขั้นบันไดภาษี WHT/`hr_company_tax_id`/`hr_sso_employer_no` ยังไม่มีตัวเลขที่ฝ่ายบัญชีเซ็นรับรอง (G-05/P0-5 ยังเปิด) |
| 28 ส.ค. 2569 (ต่อเนื่อง 7) | build agent (WebSearch/WebFetch, ตามคำขอผู้ใช้ "ดึงข้อมูลจากในเน็ตได้ไหม เอาตามกฎหมายก่อน") | 🆕🟡 **G-05 cross-check กับกฎหมายทางการ — ตัวเลขที่ seed ไว้ตรงกับกฎหมายจริงทุกจุด** — ตรวจสอบผ่าน WebSearch/WebFetch: (1) อัตราเงินสมทบประกันสังคม ม.33 5%(นายจ้าง)/5%(ลูกจ้าง) + เพดานฐานค่าจ้างใหม่ 17,500 บาท/เดือน (ขึ้นจาก 15,000 เดิม) มีผล 1 ม.ค. 2569 ตามราชกิจจานุเบกษาประกาศ 12 ธ.ค. 2568 (เฟส 1 ปี 2569-2571 เพดาน 17,500/เงินสมทบสูงสุด 875 บาท; เฟส 2 ปี 2572-2574 เพดาน 20,000; เฟส 3 ปี 2575+ เพดาน 23,000) — ตรวจ `hr_sso_rate` ที่ seed ไว้แล้ว (26 ส.ค.) พบว่า**ตรงกับตัวเลขกฎหมายจริงทุกจุดอยู่แล้ว** (5%/5%, min 1,650, max 17,500, effective 2026-01-01) (2) ขั้นบันไดภาษีเงินได้บุคคลธรรมดา 8 ขั้น ตรวจกับหน้าทางการของกรมสรรพากร (rd.go.th/59670.html) ยืนยันขั้นสุดท้ายคือ 2,000,001-5,000,000=30% และ over 5,000,000=35% — ตรวจ `hr_tax_bracket` ที่ seed ไว้แล้วพบว่า**ตรงกับกฎหมายจริงทุกขั้น** — ⚠️ **พบแหล่งข้อมูลรองขัดแย้งกัน** (เอกสาร AEC tax comparison ของ rd.go.th เองและเว็บ Statrys ระบุขั้นสุดท้ายผิดเป็น 2,000,001-4,000,000/over 4,000,000) — ตัดสินใจเชื่อหน้าอัตราภาษีทางการโดยตรง (rd.go.th/59670.html) เพราะเจาะจงกว่าและเป็นเอกสารหลักของกรมสรรพากรเอง — **สรุป: ไม่ใช่การ "เดา" แต่เป็นการ cross-check กับกฎหมาย/ประกาศทางการจริง — ยังไม่เท่ากับ "ยืนยัน/เซ็นรับรองจากฝ่ายบัญชี" ที่ P0-5 ต้องการและที่ฝ่ายบัญชีขอไว้ใน response document (ห้ามเดาตัวเลข)** — แนะนำให้ขอ sign-off อย่างเป็นทางการจากฝ่ายบัญชีก่อน go-live จริง · **`hr_company_tax_id`/`hr_sso_employer_no` เป็นเลขเฉพาะของบริษัท TILSNA เอง หาจากอินเทอร์เน็ตไม่ได้ — ต้องได้จากเอกสารทะเบียนจริงของบริษัทเท่านั้น** ยังคงเป็น placeholder ต่อไป |
| 28 ส.ค. 2569 (ต่อเนื่อง 8) | build agent (MCP, ตามคำสั่ง "ดำเนินการต่อไปก่อน") | 🆕🔴🔴 **D-17 re-verify ครั้งที่ 2/3 — ยังไม่คลี่คลาย ยืนยันข้ามช่วง session แล้ว (>7 ชม.)** — reset `TEST-LR-D15-REVERIFY` (Draft→Pending supervisor) และ `TEST-OT-SYSCHK-01` (Draft→Pending supervisor) อีกครั้งผ่าน `update_record(triggerWorkflow:true)` → รอ >60 วินาที → ทั้งคู่ `_updatedBy`=`user-api` เท่านั้น ไม่มีฟิลด์ workflow ใดถูกเติม `get_approval_list_by_row` ว่างทั้งคู่ — **นี่คือความล้มเหลวครั้งที่ 3 ติดต่อกัน และครั้งแรกที่ยืนยันคร่อมขอบเขต session เดิม (test สร้างไว้ ~11:15 ของ session ก่อนหน้า, re-check นี้ ~18:46-18:47 ของ session ปัจจุบัน)** — สรุปว่าไม่ใช่ปัญหาชั่วคราวของ session/เวลาเดียวอีกต่อไป แนะนำให้ผู้ใช้ตรวจสอบสถานะแพลตฟอร์ม Nocoly/HAP โดยตรง (subscription/billing, platform support, หรือเปิด Browser ดูสถานะ Enable ของ workflow) — ดูรายละเอียดเต็มใน §11 D-17 |
| 28 ส.ค. 2569 (ต่อเนื่อง 9) | build agent (MCP, งาน P5-6 — ตามคำสั่ง "ดำเนินการต่อไปก่อน") | 🆕🔶 **P5-6 seed `hr_salary_structure` 2/4 คน** — สร้าง TEST-EMP1 (30,000 บาท) และ TEST-EMP2 (35,000 บาท) ผ่าน `batch_create_records`(`triggerWorkflow:false`) ผูก `biz_pay_component`=SALARY, `biz_effective_from`=2026-01-01 ยืนยันด้วย `get_record_list` เห็น relation ถูกต้องไม่ orphan — ตัวเลขเป็นค่าทดสอบสมมติกลม ๆ (ตามหลักปฏิบัติเดิมของโปรเจกต์ ไม่ใช่การเดาข้อมูลจริง) — เหลือ TEST-EMP3/4 ที่ยังไม่มีตัวพนักงานทดสอบ (บล็อกบางส่วนโดย G-06) · **P5-5 (posting rule) พิจารณาแล้วตัดสินใจไม่ทำในรอบนี้** — ตรวจ `get_worksheet_structure`(`ac_posting_rule`) พบว่า schema รองรับแค่ debit/credit account เดียวต่อ 1 rule record แต่ WF-HR-10 ในสเปกออกแบบไว้เป็นใบสำคัญ 5 บรรทัด (salary expense/net pay liability/SSO payable/WHT payable/SSO employer expense) — สร้างตอนนี้ต้องเดาการออกแบบ mapping หลายบรรทัดเอง ซึ่งขัดกับกฎ "ห้ามเดา" ของโปรเจกต์ ต้องรอการตัดสินใจร่วมกับฝ่ายบัญชี/ผู้ใช้ก่อน |
| 28 ส.ค. 2569 (ต่อเนื่อง 16) | build agent (MCP, ตามภาพ Workflow History ที่ผู้ใช้ส่งมาถาม "D-17 ชื่อ Workflow อะไรนะ") | 🆕🟢 **D-17 แก้ไขแล้ว — ไม่ใช่ปัญหาแพลตฟอร์ม/session เลยตลอด 3 รอบก่อนหน้า** — ผู้ใช้ส่งภาพ Workflow History ของ `TEST-LR-D15-REVERIFY` มาถามชื่อ workflow พบว่า process จริงมีสถานะ **"Done"** (ไม่ใช่ "ไม่ยิง" ตามที่บันทึกผิดมาตลอด) เข้า node `trigger`→`gate` (branch) ครบ — ตรวจ `get_workflow_structure` พบเงื่อนไข path `leg_supervisor` ต้องการ `submitted_flag` ne "1" ตรวจ `get_record_details` พบว่า `submitted_flag` เป็นค่าว่างจริง ("" ไม่ใช่ 0) ⇒ ตรงกับบั๊กเดิมที่เคยพบแล้ว ("`ne` เทียบฟิลด์ว่างไม่ผ่าน") จึงตกไป path `leg_default` เงียบ ๆ — ตรวจ WF-HR-04/`TEST-OT-SYSCHK-01` พบสาเหตุเดียวกันทุกประการ (`hr_ot_submitted_flag` ว่าง ทั้งที่เคยตั้ง `defaultValue:0` ไว้แล้วตอนสร้าง WF-HR-04 27 ส.ค.) — **สรุปใหม่สำคัญ: ฟิลด์ธงเหล่านี้**ไม่มี `defaultValue` เลย** (`default:""` — ยืนยันด้วย `hap worksheet fields <ws_id> --raw` 28 ส.ค. 2569) รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกไว้ว่าตั้ง `defaultValue:0` แล้วเมื่อ 27 ส.ค. ⇒ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องกับบั๊กที่รู้อยู่แล้วว่า `editFields` เขียนทับทั้ง field object) — ยังพิสูจน์ไม่ได้ว่า API เพิกเฉยต่อ default เพราะไม่มีฟิลด์ไหนมี default ให้ทดสอบเลย ⇒ ในทางปฏิบัติ ต้องตั้งค่าฟิลด์ธงตรง ๆ เองเสมอตอน seed ห้ามพึ่ง defaultValue อย่างเดียว** — **ทดสอบแก้จริง:** set flag=0 ทั้ง 2 record (`triggerWorkflow:false`) → reset Draft→Pending supervisor (`triggerWorkflow:true`) → **ทั้งคู่ยิงสำเร็จทันที** `_updatedBy`=`user-workflow`, `approver_user`=Wanadtapong.l, มี To-do จริงใน `get_approval_list_by_row` ทั้งคู่ — WF-HR-01/WF-HR-04 ทำงานได้ปกติมาตลอด ไม่เคยเสียหายจาก D-15/BR-08.3 — ขออภัยที่แนะนำผิดให้ผู้ใช้ไปตรวจสถานะแพลตฟอร์ม/subscription ในรอบก่อน — บทเรียน: ต้องเปิด Workflow History (Browser) ตรวจก่อนสรุปว่า "ไม่ยิง" เสมอ อย่าพึ่ง `_updatedBy`/`get_approval_list_by_row` อย่างเดียว |
| 28 ส.ค. 2569 (ต่อเนื่อง 17) | build agent (hap CLI + MCP, ตามคำสั่งผู้ใช้ "จัดการให้เรียบร้อย" หลัง /scrutinize) | 🆕🔴🔴 **แก้ข้อสรุป D-17 ที่เขียนผิดเมื่อรอบก่อน + เปิด D-19** — /scrutinize จับได้ว่าข้อสรุปที่เพิ่งเขียนลง 3 ไฟล์เมื่อ ต่อเนื่อง 16 (*"`defaultValue` ไม่ถูกใช้เมื่อสร้าง record ผ่าน MCP"*) **พิสูจน์ไม่ได้** — ตรวจด้วย `hap worksheet fields <ws_id> --raw` พบว่า **ฟิลด์ธงทุกตัวในทุกตาราง HR มี `default:""`** ไม่มีตัวไหนได้ `defaultValue:0` จริงเลย รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกว่าตั้งแล้วเมื่อ 27 ส.ค. ⇒ **สาเหตุจริงคือค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องบั๊กที่รู้แล้วว่า `editFields` เขียนทับทั้ง field object) และเนื่องจากไม่มีฟิลด์ไหนมี default เหลือเลย จึง**ยังพิสูจน์ไม่ได้**ว่า API เพิกเฉยต่อ default หรือไม่ — **แก้ข้อความผิดครบ 7 จุดใน 3 ไฟล์** (RTM 2 · memory 2+1 · Roadmap 3) ยืนยันด้วย grep = 0 จุดค้าง · 🔴 **ค้นพบเพิ่ม 3 เรื่องที่ยังไม่เคยบันทึก:** (1) **MCP `get_worksheet_structure` ไม่คืนคีย์ `defaultValue` เลย** ทั้ง json/md ⇒ ห้ามใช้ MCP ยืนยันการตั้ง default — นี่คือเหตุผลที่ D-14 ถูกเชื่อว่าแก้แล้วมา 4 วัน (2) **D-19: D-17 แก้ไปแค่ 2 record ทดสอบ ไม่ได้แก้รูปแบบนี้ทั้งโมดูล** — WF-HR-02 (`deducted_flag`)/WF-HR-03 (`returned_flag`)/WF-HR-05 (`hr_summarised_flag`)/WF-HR-13 (`probation_alert_flag`) ยังเสี่ยงทั้งหมด · **WF-HR-02 จะไม่ตัดสิทธิเมื่อ To-do ที่ค้างอยู่ที่ Wanadtapong.l ถูกกด Approve** (3) **path ผู้ใช้จริงที่กรอกผ่านฟอร์มยังไม่เคยทดสอบเลยสักครั้ง** — record ทดสอบทุกใบสร้างผ่าน API · เพิ่ม D-19 ใน §11 · G-19 ใน RTM §E · Known Issues ต่อเนื่อง 17 ใน memory · ปรับ AC-01 (บล็อกใหม่) และ AC-03 (จำกัดขอบเขตหลักฐาน) — **ทางแก้ที่แนะนำ (ยังไม่ลงมือ รอผู้ใช้อนุมัติเพราะต้องแตะ workflow ที่ publish แล้ว): สลับโครง branch เป็น path แรก `flag eq "1"` → ไม่ทำอะไร, path หลัง catch-all → ทำงาน** |
| 28 ส.ค. 2569 (ต่อเนื่อง 18) | build agent (hap CLI + MCP, ตามคำขอผู้ใช้ "ช่วยเช็ค workflow ซ้ำ, ขยะ แล้วจัดการหน่อย") | 🧹✅ **ล้าง orphan workflow ฝั่ง HR สำเร็จ 9 ตัว — workflow ที่ใช้งานจริงครบ 12/12 ไม่เสียหาย** — สำรวจทั้งแอปด้วย `hap workflow list` + `workflow structure` ทีละตัว (51 process) พบ orphan ฝั่ง HR 9 ตัว: `Sync attendance to On leave (bal_missing/bal_found path)` อย่างละ 3 ตัว + `Revert attendance sync on leave cancel` 2 ตัว (**ตกค้างจากการ revert D-8 เมื่อ 27 ส.ค. — ลบ `sub_process` node แล้ว inner process ไม่ถูกลบตาม ทั้ง 3 รอบที่ลองแก้**) และ `WF-HR-05 inner - summarise one attendance record` 1 ตัว (ตกค้างจากการสร้าง WF-HR-05 รอบแรก) — **ยืนยันก่อนลบทุกตัว**: grep หา id แบบ full-text ในโครงสร้างของ process ทั้ง 51 ตัว ไม่มีใครอ้างถึงเลยแม้แต่ตัวเดียว · สำรอง `workflow structure` ลงไฟล์ก่อนลบ · ลบผ่าน MCP `delete_process` สำเร็จทั้ง 9 · **ตรวจหลังลบ: WF-HR-01/02/03/04/05/06 พร้อม inner ครบ 12 process โครงสร้างสมบูรณ์ (36/12/14/26/17/28/12/14/9/22/9/9 node)** · 🔴 **พบข้อควรระวังใหม่ 3 ข้อ (บันทึกใน Known Issues ต่อเนื่อง 18):** (1) `delete_process_node` ที่ลบ `sub_process` ทิ้ง inner เป็น orphan เสมอ ต้องลบเองทุกครั้ง (2) **`hap workflow list` ให้ผลไม่คงที่/ไม่ครบ** — เรียก 3 ครั้งได้ 50/37/36 รายการ · inner ของ WF-HR-05 ไม่โผล่ในครั้งแรกทั้งที่มีอยู่จริง ⇒ ห้ามใช้ผล list สรุปว่า "ไม่มี" (3) คีย์อ้าง inner ต่างกันตามเครื่องมือ: MCP ใช้ `config.process.processId` · hap CLI ใช้ `subProcessId` — และ approval sub-flow ผูกผ่าน `triggerId` ไม่ใช่ทั้งสองตัว จึงต้อง grep แบบ full-text · 🤝 **พบ orphan/ฉบับซ้ำฝั่งบัญชีอีก 12 ตัว — ไม่แตะ ส่งเรื่องให้ฝ่ายบัญชี** (ดู D-20 และ `tilsna-hr/14-HR-to-Accounting-Orphan-Workflows.md`) |
| 28 ส.ค. 2569 (ต่อเนื่อง 19) | build agent (hap CLI, หลังผู้ใช้แจ้งว่า "ฝั่งบัญชีก็กำลังเคลียร์ออกพร้อมๆ กัน") | 🔴🆕 **ตรวจซ้ำแล้วพบว่าวิธียืนยันการลบที่บันทึกไว้เมื่อ ต่อเนื่อง 18 ผิด — แก้แล้ว** — ตรวจสถานะหลังบัญชีเคลียร์พร้อมกัน พบว่า process ที่ถูกลบแล้ว **`hap workflow structure <id>` ยังคืนโครงสร้างเต็มเหมือนตัวที่ใช้งานอยู่ทุกประการ** ไม่มีอะไรบอกความต่าง — เกือบสรุปผิดว่า "`delete_process` ไม่ได้ลบจริง" · หาเครื่องมือที่ถูกได้แล้ว: **`hap workflow get <id>` มีฟิลด์ `deleted` (true/false) + `enabled`** เป็นตัวเดียวที่เชื่อถือได้ · และยืนยันว่า **`delete_process` เป็น soft-delete** (ได้ `deleted:true, enabled:false` หายจาก list แต่ object ยังดึงด้วย id ได้) ไม่ใช่ลบถาวร — **ผลตรวจด้วยวิธีที่ถูก:** HR ลบสำเร็จครบ 9/9 (`deleted:true` ทุกตัว) · ฝ่ายบัญชีเคลียร์ไปแล้ว 9/12 เหลือ 3 ตัวที่ยัง `deleted:false` (`ปิดงวด` `6a903eb8…` · `ปิดปีและยกยอด` `6a90f50e…` · `กลับรายการ` `6a8fb9ea…` — ปิดใช้งานอยู่ทั้งหมด ไม่เร่งด่วน) · ✅ **การเคลียร์พร้อมกันของสองทีมไม่ทำให้ของที่ใช้งานจริงเสียหายเลย** — workflow บัญชีที่ใช้จริง 9 ตัวปลอดภัยครบ · workflow HR 12 process โครงสร้างสมบูรณ์ครบ — แก้ Known Issues ต่อเนื่อง 18 ข้อ 2 และเอกสาร handoff ให้ตรงแล้ว |

---
*🔴 กฎเหล็ก: ทุกครั้งที่เริ่มหรือปิดงาน ต้องแก้ไฟล์นี้ + ตาราง "สถานะงานล่าสุด" ใน `04-CLAUDE-memory.md` + `03-RTM-Status.md` ให้ตรงกันทั้งสามที่*
</content>
