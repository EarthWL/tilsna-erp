# Requirements Traceability Matrix (RTM) / Status — โมดูล HR · TILSNA ERP
> เทียบ BRD ↔ สถานะจริงบนเซิร์ฟเวอร์ (ตรวจด้วย MCP/UI) · อัปเดต 28 สิงหาคม 2569 (ต่อเนื่อง 10) — 🔴🔴 **แก้ข้อสรุป G-17 ที่ผิด + เปิด G-19** — จากการ scrutinize: ข้อสรุปเดิม *"defaultValue ไม่ถูกใช้ตอนสร้าง record ผ่าน MCP"* **ผิด** — `hap worksheet fields --raw` แสดงว่าฟิลด์ธงทุกตัวมี `default:""` ไม่มีตัวไหนได้ `defaultValue:0` จริง ⇒ สาเหตุจริง = `editFields` ไม่ persist · **MCP `get_worksheet_structure` ไม่คืน `defaultValue` เลย** · 🔴 **G-19: WF-HR-02 จะไม่ตัดสิทธิเมื่อ To-do ที่ค้างถูก Approve** (`deducted_flag` ว่าง) WF-HR-03/05/13 เสี่ยงเหมือนกัน — **AC-01/AC-03 ถูกบล็อกใหม่** · path ผู้ใช้จริงผ่านฟอร์มยังไม่เคยทดสอบ — ดู G-19 ใน §E — ก่อนหน้านั้น: อัปเดต 28 สิงหาคม 2569 (ต่อเนื่อง 9) — 🟢 **G-17 แก้ไขแล้ว — ไม่ใช่ปัญหาแพลตฟอร์ม/session เลย** ผู้ใช้ส่งภาพ Workflow History ของ `TEST-LR-D15-REVERIFY` มาถามชื่อ workflow → พบว่า process จริงสถานะ "Done" เข้า `gate` (branch) แล้วตกไป path "ไม่ทำอะไร" เพราะ `submitted_flag`/`hr_ot_submitted_flag` เป็นค่าว่างจริง (ไม่ใช่ 0) — ตรงกับบั๊ก "`ne` เทียบฟิลด์ว่างไม่ผ่าน" ที่เคยพบมาก่อน — **ฟิลด์ธงเหล่านี้**ไม่มี `defaultValue` เลย** (`default:""` — ยืนยันด้วย `hap worksheet fields <ws_id> --raw` 28 ส.ค. 2569) รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกไว้ว่าตั้ง `defaultValue:0` แล้วเมื่อ 27 ส.ค. ⇒ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องกับบั๊กที่รู้อยู่แล้วว่า `editFields` เขียนทับทั้ง field object) — ยังพิสูจน์ไม่ได้ว่า API เพิกเฉยต่อ default เพราะไม่มีฟิลด์ไหนมี default ให้ทดสอบเลย ⇒ ในทางปฏิบัติ** ต้องส่งค่าฟิลด์ธงเองเสมอ — ทดสอบแก้จริง (set flag=0 แล้ว re-trigger) ทั้ง WF-HR-01/WF-HR-04 ยิงสำเร็จทันที ขั้นหัวหน้างานผ่านจริง มี To-do ถึง Wanadtapong.l — ดู G-17 ในตาราง §E — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 8) — 🔴🔴 **G-17 re-verify ครั้งที่ 2/3 (ตามคำสั่ง "ดำเนินการต่อไปก่อน") ยังไม่คลี่คลาย — ยืนยันข้ามช่วง session แล้ว (>7 ชม.)**: reset+re-trigger `TEST-LR-D15-REVERIFY` (WF-HR-01) และ `TEST-OT-SYSCHK-01` (WF-HR-04) อีกครั้ง รอ 60+ วินาที ยังไม่มี `user-workflow` เลยทั้งคู่ — ไม่ใช่ปัญหาชั่วคราวของ session เดียวอีกต่อไป แนะนำผู้ใช้ตรวจสอบสถานะแพลตฟอร์ม Nocoly/HAP โดยตรง (ดู G-17) · ✅ P5-6 seed `hr_salary_structure` ให้ TEST-EMP1/TEST-EMP2 (2/4 คน) สำเร็จ — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 7) — 🟡 **G-05 cross-check กับกฎหมายทางการแล้ว (SSO 5%/5%+เพดาน 17,500 ตามราชกิจจานุเบกษา 2569 · ขั้นบันไดภาษี 8 ขั้นตาม rd.go.th) พบว่าตัวเลขที่ seed ไว้ตรงกับกฎหมายจริงทุกจุด — ยังไม่ใช่ accounting sign-off อย่างเป็นทางการ** · `hr_company_tax_id`/`hr_sso_employer_no` หาจากเน็ตไม่ได้ (เฉพาะบริษัท) ยังเป็น placeholder — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 6) — ✅ **Gap G-02 ปิดแล้ว — ฝ่ายบัญชีตอบกลับ (`tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`) สร้างบัญชีใหม่ 3 รายการในผังบัญชีจริง (`530102`/`210402`/`210304`) + เพิ่ม `event_code`=PAYROLL_ACCRUAL/PAYROLL_PAYMENT** — HR ผูก `hr_pay_component.biz_coa_account` ต่อสำเร็จครบ 6/6 (FR-HR-12/P5-4 ปิดเต็ม) — WF-HR-10 source-of-truth ยืนยันแล้ว (`ac_posting_rule` หลัก/`hr_pay_component` fallback) — ⚠️ ยังเปิดอยู่: อัตรา SSO/ภาษี WHT/เลขทะเบียนบริษัท (G-05) ฝ่ายบัญชีระบุห้ามเดา — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 5) — 🟢 **WF-HR-05 และ WF-HR-06 สร้าง+publish สำเร็จจริงผ่าน MCP** (main×2 + inner×2, validate สะอาด 0 error) — WF-HR-06 v1 จำกัดเหลือแค่ `hr_leave_request` เพราะพบ gap ใหม่ **G-18** (`hr_ot_request` ไม่มีฟิลด์ `submitted_at`) — AC-04/AC-14 ยังไม่ปิด ✅ เพราะยังไม่ได้ live-fire test (บล็อกด้วย G-17) — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 4) — 🔴🔴 **G-17: worksheet_event workflow ทั้งหมดไม่ยิงเลยตอนนี้ — เป็นปัญหาระดับ session/environment/platform ไม่ใช่ WF-HR-01 เจาะจง** re-verify WF-HR-01 ด้วย record ทดสอบใหม่สะอาดพบว่าไม่ยิง → sanity-check ด้วย WF-HR-04 (untouched, เคยยิงสำเร็จ 27 ส.ค.) ก็ไม่ยิงเช่นกัน → โครงสร้าง WF-HR-01 ตรวจสอบผ่าน `get_workflow_structure` แล้วสมบูรณ์ 100% ⇒ ไม่ใช่ความเสียหายจาก BR-08.3/G-15 — บล็อกการพิสูจน์ AC ที่ต้องพึ่ง live-fire ทั้งหมดจนกว่าจะคลี่คลาย (สร้าง/publish โครงสร้างใหม่ยังทำต่อได้ปกติ) — ดู G-17 ในตาราง §E — ก่อนหน้านั้น: 28 สิงหาคม 2569 (ต่อเนื่อง 3) — 🟢 **G-16: G-13 (`sub_process`→`NodeAppIsNull`) คลี่คลายแล้ว ไม่ใช่บั๊กแพลตฟอร์ม** ตามคำขอผู้ใช้ตรวจเอกสาร skill `hap-mcp-app-builder` พบ root cause จริง (ต้อง publish inner sub_process ก่อน outer เสมอ) ทดสอบยืนยันสำเร็จ 100% — P4-4(WF-HR-05)/P4-6(WF-HR-06) ปลดบล็อกแล้ว
> สัญลักษณ์: ✅ ทำจริง+ยืนยันแล้ว · 🔶 บางส่วน · ⚠️ มีแต่ยังไม่พิสูจน์ว่าทำงาน · ⬜ ยังไม่เริ่ม · ⛔ ถูกบล็อก

---

## สรุปภาพรวม

| กลุ่ม | ✅ | 🔶 | ⚠️ | ⬜ | ⛔ | รวม |
|---|---|---|---|---|---|---|
| Business (BR) | 1 | 0 | 0 | 21 | 0 | 22 |
| Functional (FR) | 0 | 7 | 0 | 20 | 0 | 27 |
| Non-functional (NFR) | 1 | 1 | 0 | 11 | 0 | 13 |
| Interfaces | 0 | 0 | 0 | 7 | 0 | 7 |
| Acceptance Criteria | 1 | 2 | 0 | 14 | 3 | 20 |

**สถานะโดยรวม:** โมดูลการลา (P3) เริ่มเดินได้จริงแล้ว — WF-HR-01 (ขั้นหัวหน้างาน) + WF-HR-02/03 (ตัด/คืนสิทธิ) ยิงจริงสำเร็จ 27 ส.ค. 2569 · ปุ่ม "ส่งคำขอ"/"ยกเลิกใบลา" + view 4 มุมมองยืนยันแล้ว (P3-7) และ Business Rules หลัก 3/5 ข้อ (BR-08.1/8.4/8.5) สร้าง+ยืนยันผ่าน Role Debugging แล้ว (P3-8 บางส่วน) — พบข้อจำกัดแพลตฟอร์มใหม่ 2 เรื่อง (relation-field เป็น Dynamic Value ไม่ได้ในทั้ง Business Rules และ Dynamic Default, unique index บนฟิลด์ไม่ required จำกัดทั้งตาราง) ทำให้ BR-08.2/8.3/DV-08.1/IX-08.1 ต้องเลื่อนไปทำที่ workflow/รอเงื่อนไขอื่น · **เพิ่มเติม 27 ส.ค. 2569 (รอบเย็น — ตามคำขอผู้ใช้ "เน้นงานที่ผ่าน MCP/CLI ได้ก่อน"):** P3-4 seed `hr_leave_balance` ครบทั้ง 6 ประเภท×2 พนักงานแล้ว · เปิดเฟส 4 — โครงสร้าง `hr_ot_request` (FR-HR-11B) และ `hr_attendance` (FR-HR-10) สร้างครบผ่าน MCP + hap CLI (view rename + Data Name ไทย) แล้ว รวม relation เชื่อมกลับ `hr_leave_request` ให้พร้อมสำหรับ WF-HR-05 — ยังไม่ทำ unique index/Business Rules/workflow ของเฟส 4 · 🔴 **พบบั๊กแพลตฟอร์มใหม่ระหว่างพยายามทำ D-8 (sync "On leave" เข้า WF-HR-02/03):** node ชนิด `sub_process` สร้างผ่าน MCP สำเร็จแต่ publish ไม่ได้ (`NodeAppIsNull`) — revert กลับสถานะเดิมปลอดภัยแล้ว (WF-HR-02/03 ยังทำงานถูกต้องเหมือนเดิมทุกอย่าง ไม่มีข้อมูลเสียหาย) แต่ D-8/AC-04 และ WF-HR-05 ยังทำต่อไม่ได้จนกว่าจะแก้บั๊กนี้ — ดู G-13 · **เพิ่มเติม 27 ส.ค. 2569 (ต่อเนื่อง):** เปลี่ยนไปทำ **WF-HR-04 (อนุมัติ OT 2 ระดับ)** แทนเพราะไม่ต้องใช้ `sub_process` — **สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ** ด้วยสถาปัตยกรรม gate-branch เดียวกับ WF-HR-01 บวกเทคนิคใหม่แชร์ inner approval_block ข้าม 2 เส้นทาง day-type ผ่าน `mode:"use_existing"` — ทดสอบยิงจริง 3 เคสยืนยัน AC-19 ส่วนแรกผ่าน (ตัวคูณดึงจากตาราง `hr_ot_rate` จริง) — ระหว่างทางพบบั๊กแพลตฟอร์มใหม่อีก 1 ตัว: `dateDiff` compute node ตัด `precision` ทิ้งเงียบ (ดู G-14) · **28 ส.ค. 2569 — ทำตามลำดับที่แนะนำ P4-4→P4-6→P3-8:** P4-4 (WF-HR-05) ยืนยันซ้ำว่าบล็อกด้วยบั๊ก `sub_process`→`NodeAppIsNull` เดิม (ทดสอบแยกด้วย minimal isolated process แล้วลบทิ้งสะอาด) — P4-6 (WF-HR-06) สรุปบล็อกเดียวกันโดยไม่ต้องทดสอบซ้ำ (ออกแบบต้องใช้ `sub_process` เหมือนกัน) — **ลองเพิ่ม BR-08.3 เข้า WF-HR-01 จริง (publish v4) แล้วพบว่า workflow หยุดยิงบน trigger ใหม่ทั้งหมด** revert กลับโครงสร้างเดิม (publish v5) แต่อาการไม่ยิงยังคงอยู่แม้หลัง revert — สาเหตุยังไม่ทราบ ยืนยันว่า approval instance จริง TEST-LR-0001 ไม่ถูกกระทบ — 🔴 **สถานะ ✅ ของแถว "WF-HR-01 อนุมัติใบลา (ขั้นหัวหน้างาน)" ด้านล่างต้องตรวจสอบซ้ำก่อนเชื่อ** ดูรายละเอียดเต็มใน `04-CLAUDE-memory.md` (D-15) และ `05-Roadmap-Tracker.md`

---

## หลักฐานสถานะปัจจุบัน (ตรวจเมื่อ 26–27 ส.ค. 2569)

| สิ่งที่ตรวจ | คำสั่ง | ผลจริง |
|---|---|---|
| กลุ่มและ worksheet ในแอป | `get_app_info` | มีกลุ่ม **Accounting (AC)** 34 worksheet + **Human Resources (HR)** พร้อม HR-00…HR-03 มีตารางแล้ว (HR-04…HR-06 ยังว่าง) |
| optionset | `get_optionset_list` | 35 ชุดเดิม (AC) + 24 ชุดใหม่ของ HR (`OS_HR_*`) |
| role | `get_role_list` | 5 org role + 8 custom role ของบัญชี (AC-R1…AC-R8) · **ยังไม่มี HR-R*** |
| บัญชีผู้ใช้ในองค์กร | `get_role_list` › 管理员 | **มีเพียง 2 บัญชี**: `Kunlasatri.c` `202df0a6-…`/`3664d2b8-…` (TEST-EMP1) · `Wanadtapong.l` `4dc40ac1-…` (TEST-EMP2, หัวหน้า) |
| จุดเชื่อมที่เตรียมไว้แล้ว | `get_worksheet_structure(ac_cost_center)` | ✅ มีฟิลด์ **`hrms_unit_id`** `6a85452b055f2288c5b741bc` รออยู่แล้ว |
| ระบบต้นทาง Payroll | `get_optionset_list` › `OS_SOURCE_MODULE` | ✅ มี option **Payroll** key `914f5226-dc4c-4572-bd4d-18bb278414b5` พร้อมใช้ |
| **WF-HR-01 อนุมัติใบลา (ขั้นหัวหน้างาน)** | `get_record_details(includeSystemFields:true)` บน `4a226f18-…` (เดิม) และ `72e70392-…` (re-verify) | 🔴 **28 ส.ค. 2569 (ต่อเนื่อง 4) — re-verify ด้วย record ใหม่สะอาดแล้วไม่ยิงจริง (G-17) แต่เป็นปัญหาระดับระบบ ไม่ใช่ WF-HR-01 เจาะจง** (WF-HR-04 ที่ไม่เคยถูกแตะก็ไม่ยิงเช่นกัน — sanity-check เทียบ) — โครงสร้าง (main+inner) ตรวจสอบผ่าน `get_workflow_structure` แล้วว่าสมบูรณ์ published 100% — หลักฐาน ✅ เดิม (27 ส.ค., ก่อน D-15/G-17): `_updatedBy`=`user-workflow` · `submitted_flag`=1 · `approver_user`=Wanadtapong.l · `get_approval_list_by_row` มี To-do จริง — ขั้น HR ยังไม่ทดสอบ (รอ role/G-06) — **ต้องรอ G-17 คลี่คลายก่อน re-verify ซ้ำอีกครั้ง** |
| **WF-HR-02 ตัดสิทธิลาเมื่ออนุมัติ** | `get_record_details` บน balance `56c008e2-…` + ledger | ✅ `_updatedBy`=`user-workflow` · `used_days` 0→1 · `remaining_days` 10→9 · ledger 1 แถว (`ledger_days`=-1.0, `balance_after`=9.0) · ทดสอบยิงซ้ำแล้วไม่ตัดซ้ำ |
| **WF-HR-03 คืนสิทธิลาเมื่อยกเลิก** | `get_record_details` บน balance เดิม + ledger | ✅ `_updatedBy`=`user-workflow` · `used_days` 1→0 · `remaining_days` 9→10 · ledger เพิ่มอีก 1 แถว (`ledger_days`=+1.0, `balance_after`=10.0) — รวม 2 แถวคู่กัน |
| **P3-7 ปุ่ม + view ของใบลา** | Role Debugging (role Member) บน TEST-LR-0001 | ✅ ปุ่ม "ส่งคำขอ"/"ยกเลิกใบลา" ไม่โผล่เมื่อ `leave_status`=Cancelled · โผล่ทั้งคู่เมื่อตั้งชั่วคราวเป็น Draft (ตาม `enableWhen`) · view ครบ 4 มุมมองตามสเปก · ล้าง ghost views ซ้ำ 8 อันจากเซสชันก่อนหน้าแล้ว |
| **P3-8 BR-08.1/8.4/8.5** | Role Debugging (role Member) + อ่านค่ากลับจากแผง Business Rules | ✅ BR-08.4: record ที่ `leave_status`≠Draft แสดง read-only ทั้งฟอร์ม · ✅ BR-08.5: `leave_unit`=Hourly ซ่อน `leave_to`/โชว์ `leave_hours` · ✅ BR-08.1: condition ที่บันทึกไว้ตรงสเปก (validate ผ่าน UI แผง Business Rules โดยตรง เนื่องจากทดสอบ live-save-block ผ่าน record modal ทำไม่สำเร็จ) |
| **DV-08.1 / IX-08.1** | ทดสอบจริงถึงขั้น Query conditions ของ Dynamic Default และหน้า Create Index ของ Search Acceleration | ⛔ DV-08.1: field picker "Select fields from current form" ไม่แสดง Relation-type field (`employee`) ให้เลือกเป็น Dynamic Value เลย · ⛔ IX-08.1: tooltip ของ "Unique Index" ยืนยันข้อความ "ฟิลด์ไม่ required ⇒ ทั้งตารางมี record ว่างได้แค่ 1 แถว" — ตัดสินใจไม่เปิดใช้ |
| **P3-4 seed hr_leave_balance** | `get_record_pivot_data`(group by employee, COUNT rowid, viewId ของ `hr_leave_balance`) | ✅ TEST-EMP1 = 6 · TEST-EMP2 = 6 (ครบทั้ง 6 ประเภทการลาต่อคน) |
| **P4-1/P4-2 `hr_attendance`/`hr_ot_request`** | `get_worksheet_structure` ทั้งสองตาราง + `hap worksheet info` (`entityName`) | ✅ `hr_ot_request` (`6a8fcca48b6633ef76f13033`) 16 ฟิลด์ตรงสเปก FR-HR-11B ครบ · ✅ `hr_attendance` (`6a8fcd67353e1b0e4a507d32`) 16 ฟิลด์ตรงสเปก FR-HR-10 ครบ รวม relation `leave_request`/`ot_request` ชี้ถูกทั้งคู่ · ✅ `entityName` เป็นไทยทั้ง 2 ตาราง ("ใบขออนุมัติล่วงเวลา"/"บันทึกลงเวลา") · ✅ view "全部"→"ทั้งหมด" ยืนยันด้วย `hap worksheet view update` response ทั้งคู่ |
| **P4-3/WF-HR-04 อนุมัติ OT (ขั้นหัวหน้างาน)** | `get_record_details(includeSystemFields:true)` + `get_approval_list_by_row` บน 3 record ทดสอบ | ✅ `OT-TEST-HOLIDAY-01`/`OT-TEST-WORKDAY-01`/`OT-TEST-FRACTIONAL-01` ทั้ง 3 มี `_updatedBy`=`user-workflow` · `hr_ot_hours`/`hr_applied_multiplier`/`hr_ot_day_type` คำนวณถูกต้องตามตาราง `hr_ot_rate` (×3.0 Holiday outside hours, ×1.5 Working day) · `get_approval_list_by_row` มี To-do จริงถึง Wanadtapong.l ทั้ง 3 — ขั้น HR ยังไม่ทดสอบ (รอ role/G-06) |
| **P4-5 ปุ่ม "ส่งขอล่วงเวลา" + view OT รออนุมัติ/ลงเวลาผิดปกติ** | Claude in Chrome — เปิด record จริงหลัง toggle สถานะชั่วคราว | ✅ ปุ่ม "ส่งขอล่วงเวลา" โผล่จริงตาม `enableWhen` (`hr_ot_status`=Draft) หลังแก้ Scope เป็น "All Records" (เจอกับดัก Scope="Unassigned View" ซ้ำจากโมดูลบัญชี — ดู `04-CLAUDE-memory.md`) · view "OT รออนุมัติ" แสดง 3 record ตรงสเปก · view "บันทึกลงเวลาที่ผิดปกติ" render คอลัมน์ถูกต้อง (0 record เพราะยังไม่มี seed data ใน `hr_attendance`) |

---

## A. Business Requirements

| รหัส | ความต้องการ (ย่อ) | สถานะ | หลักฐาน / งานที่เหลือ |
|---|---|---|---|
| BR-01 | ทะเบียนพนักงานเดียวผูกกับบัญชีผู้ใช้ | ⬜ | ต้องสร้าง `hr_employee` (FR-HR-03) |
| BR-02 | อนุมัติ 2 ระดับพร้อมร่องรอย | ⬜ | WF-HR-01 · 04 · 11 · 15 |
| BR-03 | ผู้อนุมัติขั้น 1 มาจากฟิลด์ผู้บังคับบัญชา | ⬜ | ฟิลด์ `supervisor_user` + WF-HR-01 node 3–4 |
| BR-04 | ตัด/คืนสิทธิลาอัตโนมัติ | ✅ **27 ส.ค. 2569** | WF-HR-02 `6a8fa97cfdab77a41c51cb3f` (publish v3) + WF-HR-03 `6a8fab800e97bf440dddb744` (publish v1) ยิงจริงสำเร็จทั้งคู่ — ดู RTM ด้านบนและ `04-CLAUDE-memory.md` แถว #13 · ⚠️ ยังไม่ได้ทดสอบเคส "ไม่พบ hr_leave_balance" (branch create-if-missing ใน WF-HR-02 validate ผ่านแต่ยังไม่ยิงจริง) |
| BR-05 | บล็อกใบลาเกินสิทธิ/ซ้อนทับ | ⬜ | BR-08.2 (กฎฟอร์ม) + WF-HR-01 node 5–9 · **27 ส.ค. 2569 — ยืนยันแล้วว่า BR-08.2 ทำเป็น Business Rule ไม่ได้** (relation-field lookup ข้าม `leave_type`/`hr_leave_balance` ไม่รองรับใน field picker) ต้องย้ายไปทำเป็น workflow validation ที่ WF-HR-01 เท่านั้น |
| BR-06 | OT คิดจากใบที่อนุมัติแล้วเท่านั้น | ⬜ | ธง `paid_flag` + WF-HR-08 node 7 · `hr_ot_request.paid_flag` มีฟิลด์แล้ว (P4-2) — logic ยังไม่สร้าง |
| BR-07 | เงินเดือนจากโครงสร้างที่มีวันที่มีผล | ⬜ | `hr_salary_structure` + WF-HR-08 node 6 · **28 ส.ค. 2569 (ต่อเนื่อง 8):** seed ข้อมูลทดสอบแล้ว (TEST-EMP1=30,000/TEST-EMP2=35,000, effective 2026-01-01) แต่ logic WF-HR-08 ยังไม่สร้าง — สถานะ ⬜ ยังคงเดิม |
| BR-08 | ประกันสังคม/ภาษีจากตารางอัตรา | ⬜ | `hr_sso_rate` · `hr_tax_bracket` · `hr_tax_allowance` |
| BR-09 | สร้างใบสำคัญตามกฎการบันทึกบัญชี | ⬜ | ✅ **ปลดบล็อกแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 6) — Gap G-02 ปิดแล้ว** — `event_code` = `PAYROLL_ACCRUAL`/`PAYROLL_PAYMENT` เพิ่มโดยฝ่ายบัญชีแล้ว — ยังต้องสร้าง `ac_posting_rule` จริง (P5-5) + WF-HR-10 เอง |
| BR-10 | ใบเบิกเข้ากระบวนการจ่ายของบัญชี | ⬜ | WF-HR-12 → `ac_pay_req` `6a8677b19b6999a714d2aa83` |
| BR-11 | กันเบิกเกินวงเงินสวัสดิการ | ⬜ | BR-19.1 + WF-HR-11 |
| BR-12 | ปิดบังข้อมูลอ่อนไหว | ⬜ | ⛔ **บล็อกด้วย Gap G-06** — ทดสอบไม่ได้ถ้าไม่มีบัญชีทดสอบ |
| BR-13 | SoD | ⬜ | `02-BuildSpec-FRS.md` §1.7 permission matrix |
| BR-14 | อัตราทุกชนิดเก็บเป็นข้อมูล | ⬜ | ตารางตั้งค่า HR-00 ทั้ง 10 ตาราง |
| BR-15 | สลิปที่ปิดงวดแล้วแก้ไม่ได้ | ⬜ | BR-12.4 Business Rule lock |
| BR-16 | พนักงานเห็นสลิปและสิทธิของตนเอง | ⬜ | View "สลิปของฉัน" + role R1 |
| BR-17 | แจ้งเตือน SLA และสัญญาใกล้ครบ | ⬜ | WF-HR-06 · WF-HR-13A/13B |
| BR-18 | พิมพ์เอกสารรูปแบบราชการ | ⬜ | 🔴 Print Template — **Browser เท่านั้น** |
| BR-19 | สร้างพนักงานจากผู้สมัคร | ⬜ | WF-HR-17 |
| BR-20 | ผลประเมินและอบรมผูกกับทะเบียน | ⬜ | `hr_appraisal` · `hr_training` |
| BR-21 | ร่องรอยการแก้ไข | ⬜ | ระบบมีให้อัตโนมัติ (`_updatedBy` / record log) — ยืนยันแล้วบางส่วนผ่าน WF-HR-01/02/03 (`_updatedBy`=`user-workflow` ทุกครั้ง) |
| BR-22 | ยกยอดสิทธิลาต้นปี | ⬜ | WF-HR-14 |

---

## B. Functional Requirements

| รหัส | โมดูล | สถานะ | หลักฐาน / งานที่เหลือ |
|---|---|---|---|
| FR-HR-01 | ตั้งค่าโมดูลบุคคล | 🔶 | `hr_setting` สร้าง+seed แล้ว 26 ส.ค. 2569 (1 record) — เหลือ Index Acceleration ของ `hr_setting_name` (IX-01.1) |
| FR-HR-02 | ปฏิทินวันหยุดและกะทำงาน | 🔶 | `hr_holiday` (21 record) · `hr_shift` (1 record) สร้าง+seed แล้ว 26 ส.ค. 2569 — เหลือ Index Acceleration ของ `hr_holiday_date` (IX-02.1) |
| FR-HR-03 | ทะเบียนพนักงาน | 🔶 | `hr_employee` 36 ฟิลด์สร้างแล้ว 26 ส.ค. 2569 · seed พนักงานทดสอบ 2 คน (TEST-EMP1/TEST-EMP2) ผูกบัญชีจริง — เหลือ seed พนักงานทดสอบเพิ่ม (รอ G-06) และผูก Dropdown 4 ฟิลด์เข้า shared optionset (Browser) |
| FR-HR-04 | ตำแหน่งและระดับ | ✅ | `hr_position` (8 seed) · `hr_job_level` (6 seed) สร้าง+seed แล้ว 26 ส.ค. 2569 |
| FR-HR-05 | สัญญาจ้างและเหตุการณ์ | 🔶 | `hr_employment_contract` · `hr_employment_event` สร้างโครงสร้างแล้ว 26 ส.ค. 2569 — ยังไม่ seed |
| FR-HR-06 | ผู้ติดตามและบัญชีธนาคาร | 🔶 | `hr_dependent` · `hr_bank_account` สร้างโครงสร้างแล้ว 26 ส.ค. 2569 — ยังไม่ seed |
| FR-HR-07 | ประเภทการลาและนโยบาย | 🔶 | `hr_leave_type` (6) · `hr_leave_policy` (6) สร้าง+seed แล้ว 26 ส.ค. 2569 — เหลือ `carry_policy`/`pay_component` ผูก optionset/relation (Browser + รอ `hr_pay_component`) |
| FR-HR-08 | ใบลา | 🔶 **27 ส.ค. 2569 (อัปเดต)** | `hr_leave_request` สร้างโครงสร้างครบ (P3-1) · **WF-HR-01 (ขั้นหัวหน้างาน) + WF-HR-02 (ตัดสิทธิ) + WF-HR-03 (คืนสิทธิ) ยิงจริงสำเร็จ** · ✅ **P3-7 เสร็จ+ยืนยันแล้ว** — ปุ่ม "ส่งคำขอ"/"ยกเลิกใบลา" render ตาม `enableWhen` จริง (ยืนยันผ่าน Role Debugging) + view ครบ 4 มุมมอง + ล้าง ghost views 8 อัน · 🔶 **P3-8 บางส่วนเสร็จ**: ✅ BR-08.1 (validation `leave_to` ก่อน `leave_from` → บล็อก)/BR-08.4 (lock ทั้งฟอร์มเมื่อ `leave_status`≠Draft)/BR-08.5 (hide `leave_to`/show `leave_hours` เมื่อ `leave_unit`≠Full day) สร้าง+ยืนยันผ่าน Role Debugging แล้วทั้ง 3 ข้อ · ⛔ BR-08.2 (บล็อกเกินสิทธิ)/BR-08.3 (บังคับแนบไฟล์ตาม `leave_type.require_document`) **ทำเป็น Business Rule ไม่ได้** — field picker ไม่รองรับการอ้างอิงฟิลด์ข้ามตารางผ่าน Relation field — ต้องย้ายไปทำที่ WF-HR-01 (BR-08.2) และ WF-HR-06/โหนดใหม่ (BR-08.3) แทน · ⛔ **DV-08.1 (dynamic default `approver_user`/`balance_snapshot`) ทำผ่าน UI Dynamic Default ไม่ได้เช่นกัน** (ข้อจำกัดเดียวกันขยายมาจาก Business Rules — ยืนยันด้วยการทดสอบจริงถึงขั้น Query conditions) ต้องย้ายไปทำที่ WF-HR-01 แทน (ดู D-9 ใน Roadmap Tracker) · ⛔ **IX-08.1 ตัดสินใจ "ยังไม่เปิดใช้"** — ยืนยันจาก tooltip ของระบบว่าฟิลด์ไม่ required จะจำกัดทั้งตารางให้มี record ว่างได้แค่ 1 แถว (ดู D-10) · เหลือ: ขั้นอนุมัติ HR ของ WF-HR-01 (รอ G-06), ผูก Dropdown เข้า shared optionset |
| FR-HR-09 | สิทธิและยอดคงเหลือ | 🔶 **27 ส.ค. 2569 (อัปเดต)** | `hr_leave_balance`/`hr_leave_ledger` สร้างโครงสร้างครบ (P3-1) · **WF-HR-02/03 เขียน `used_days`/`remaining_days` ถูกต้องและมี `hr_leave_ledger` เป็นบัญชีแยกประเภทคู่กันทุกรายการ (ยิงจริงยืนยันแล้ว)** · ✅ **P3-4 เสร็จ+ยืนยันแล้ว** — seed `hr_leave_balance` ครบทั้ง 6 ประเภทการลา×TEST-EMP1/TEST-EMP2 (`get_record_pivot_data` COUNT=6 ยืนยันทั้งคู่) — เหลือ unique index IX-09.1 (P3-3 — ยังไม่ทดสอบ tooltip ของฟิลด์นี้โดยตรง ต่างจาก IX-08.1) |
| FR-HR-10 | บันทึกลงเวลา | 🔶 **27 ส.ค. 2569** | `hr_attendance` (`6a8fcd67353e1b0e4a507d32`) สร้างโครงสร้างครบ 16 ฟิลด์ผ่าน MCP workaround — relation `employee`/`shift`/`leave_request`/`ot_request` ชี้ถูกทั้ง 4 จุด · `att_status`/`att_source` เป็น inline options (label อังกฤษตรงกับ `OS_HR_ATT_STATUS`/`OS_HR_ATT_SOURCE` แต่ยังไม่ผูก shared optionset จริง) · Data Name ไทย + view "ทั้งหมด" ตั้งครบแล้ว · **G-09 ตารางพร้อมแล้วแต่ node sync ยังเพิ่มไม่ได้ — บล็อกด้วย G-13 (`sub_process`→`NodeAppIsNull`, ลองแล้ว 27 ส.ค. 2569 รอบเย็น revert กลับสถานะเดิม)** · ✅ **view "บันทึกลงเวลาที่ผิดปกติ" สร้าง+verify แล้ว 27 ส.ค. 2569 (ต่อเนื่อง 2, P4-5)** · เหลือ: unique index IX-10.1 (`employee`+`work_date`, Browser-only) · Business Rules BR-10.1/10.2 · WF-HR-05 (สรุปเวลาทำงานรายวัน — บล็อกด้วย G-13 เช่นกัน) · seed record ทดสอบ |
| FR-HR-11 | ใบขอล่วงเวลา | 🔶 **27 ส.ค. 2569 (ต่อเนื่อง)** | 11A `hr_ot_rate` สร้าง+seed แล้ว 26 ส.ค. 2569 (3 record) — ✅ **`day_type` เพิ่มสำเร็จผ่าน API 27 ส.ค. 2569 (ต่อเนื่อง)** (fieldId `6a8fd7608b6633ef76f13156`, inline options, backfill 3 record ครบ) — เหลือ `employment_type` (Browser) + rebind ทั้งคู่เข้า shared optionset จริง · 11B `hr_ot_request` (`6a8fcca48b6633ef76f13033`) **สร้างโครงสร้างครบ 27 ส.ค. 2569** (16 ฟิลด์ผ่าน MCP workaround, relation `employee` ถูกต้อง, Data Name ไทย + view "ทั้งหมด" ตั้งครบ) — `day_type`/`ot_status` เป็น inline options (label อังกฤษตรงกับ optionset กลาง แก้จากที่สร้างผิดเป็นไทยครั้งแรกแล้ว) · ✅ **WF-HR-04 (อนุมัติ OT 2 ระดับ) สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ 27 ส.ค. 2569 (ต่อเนื่อง)** — ดู `04-CLAUDE-memory.md` ID Workflow WF-HR-04 · ✅ **ปุ่ม "ส่งขอล่วงเวลา" + view "OT รออนุมัติ" สร้าง+verify ผ่านจริง 27 ส.ค. 2569 (ต่อเนื่อง 2, P4-5)** — เหลือ unique index IX-11.1 · Business Rules BR-11.1/11.2/11.3 · ขั้น HR ของ WF-HR-04 ยังไม่ทดสอบ (รอ role/G-06) · `pay_period` relation (รอเฟส 5) |
| FR-HR-12 | งวดจ่ายและองค์ประกอบค่าจ้าง | 🔶 **28 ส.ค. 2569 (ต่อเนื่อง 6)** | `hr_pay_component` (`6a8ff2868b6633ef76f13871`, 11 ฟิลด์) · `hr_pay_period` (`6a8ff2878b6633ef76f1387b`, 22 ฟิลด์) สร้างโครงสร้างครบผ่าน MCP workaround — relation `coa_account`→`ac_coa` · `ac_period_ref`→`ac_period` · `voucher_ref`→`ac_voucher` ทั้งหมด bidirectional ผูกสำเร็จ · Data Name ไทยครบ · ✅ **seed องค์ประกอบค่าจ้าง (P5-4) เสร็จ+verify แล้ว 6/6 record — ผูก `coa_account` ครบ 6/6 แล้ว 28 ส.ค. 2569 (ต่อเนื่อง 6)**: `SALARY`/`OT`/`ABSENCE_DEDUCT`→`530101` (26-27 ส.ค.) + `SSO_ER`→`530102`/`SSO_EE`→`210402`/`WHT`→`210304` (ฝ่ายบัญชีสร้างบัญชีให้ตาม `tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`) — Gap G-02 ปิดแล้ว — เหลือ unique index IX-12.1 · dropdown 3 ฟิลด์ (`component_type`/`calc_method`/`period_status`) ผูก shared optionset จริง (Browser) |
| FR-HR-13 | โครงสร้างเงินเดือน | 🔶 **27 ส.ค. 2569** | `hr_salary_structure` (`6a8ff287353e1b0e4a508550`, 9 ฟิลด์) สร้างโครงสร้างครบ — relation `employee`→`hr_employee` และ `pay_component`→`hr_pay_component` ทั้งคู่ bidirectional ผูกสำเร็จ · Data Name ไทยครบ — เหลือ unique index IX-12.2 · seed โครงสร้างเงินเดือนพนักงานทดสอบ 4 คน (P5-6) |
| FR-HR-14 | สลิปเงินเดือน | 🔶 **27 ส.ค. 2569 (P5-2)** | `hr_payslip` (`6a904c85353e1b0e4a50ba05`, 25 ฟิลด์ + reverse relation) · `hr_payslip_line` (`6a904c858b6633ef76f16a0b`, 8 ฟิลด์) สร้างโครงสร้างครบผ่าน MCP workaround — relation `employee`/`pay_period`/`cost_center`/`bank_account`→`hr_payslip` และ `payslip`/`pay_component`→`hr_payslip_line` ทั้งหมด bidirectional ผูกสำเร็จ · เงินทุกยอดใช้ Currency (THB) แทน Number ตามสเปกเดิม (ปรับให้ตรงของจริงแล้วใน BuildSpec) · flag/total ทุกตัวตั้ง `defaultValue:0` เชิงรุก · Data Name ไทย + add-record button ตั้งครบ — เหลือ: unique index IX-12.3 (`employee`+`pay_period`) · ผูก `payslip_status` เข้า `OS_HR_PAYSLIP_STATUS` จริง (Browser) · Business Rules BR-12.4/12.5 · Dynamic default snapshot ของ `cost_center` · WF-HR-07/08 (ยังไม่ได้สร้าง) |
| FR-HR-15 | ประกันสังคมและภาษี | 🔶 **28 ส.ค. 2569 (ต่อเนื่อง 7) — cross-check กับกฎหมายจริงแล้ว** | `hr_sso_rate` (1) · `hr_tax_bracket` (8) · `hr_tax_allowance` (8) สร้าง+seed แล้ว 26 ส.ค. 2569 — ✅ **ตรวจสอบซ้ำกับแหล่งกฎหมายทางการแล้ว 28 ส.ค. 2569 พบว่าตัวเลขที่ seed ไว้ตรงกับกฎหมาย/ประกาศจริงทุกจุด:** อัตราประกันสังคม 5%/5% + เพดานฐานค่าจ้าง 17,500 บาท (ตรงกับราชกิจจานุเบกษา 12 ธ.ค. 2568 มีผล 1 ม.ค. 2569) · ขั้นบันไดภาษี 8 ขั้น (0-150,000 ยกเว้น … 2,000,001-5,000,000=30% … 5,000,001 ขึ้นไป=35%) ตรงกับหน้าอัตราภาษีทางการของกรมสรรพากร (rd.go.th) — ⚠️ **ยังไม่ใช่ "การยืนยัน/เซ็นรับรองจากฝ่ายบัญชี" ตามที่ G-05 ต้องการ** เป็นแค่ cross-check กับกฎหมายสาธารณะ ไม่ใช่ guess — ยังแนะนำให้ฝ่ายบัญชีเซ็นรับรองอย่างเป็นทางการก่อน go-live ดู G-05 |
| FR-HR-16 | ภ.ง.ด.1 และ 50 ทวิ | 🔶 **27 ส.ค. 2569 (P5-3)** | `hr_pnd1_filing` (`6a904e72ae2a0e3743a0fd06`, 13 ฟิลด์) · `hr_wht_cert` (`6a904e73353e1b0e4a50ba83`, 9 ฟิลด์) สร้างโครงสร้างครบผ่าน MCP workaround — relation `pay_periods`(multi)/`employee`/`pnd1_filing` bidirectional ผูกสำเร็จ · ยอดเงินใช้ Currency (THB) แทน Number ตามสเปกเดิม — เหลือ unique index IX-12.5/IX-12.6 · ผูก dropdown 4 ฟิลด์เข้า shared optionset จริง (Browser) |
| FR-HR-17 | เชื่อมเงินเดือนเข้าบัญชี | ⬜ | เฟส 5 · ✅ **ปลดบล็อก Gap G-02 แล้ว 28 ส.ค. 2569 (ต่อเนื่อง 6)** — `event_code`=PAYROLL_ACCRUAL/PAYROLL_PAYMENT พร้อมใช้ + source-of-truth ยืนยันแล้ว (`ac_posting_rule` หลัก/`hr_pay_component` fallback) — ยังต้องรอ P5-8…P5-10 (สลิป+อนุมัติงวด) ก่อนจึงจะสร้าง WF-HR-10 ได้จริง |
| FR-HR-18 | สวัสดิการและวงเงิน | ⬜ | เฟส 6 |
| FR-HR-19 | ใบเบิก | ⬜ | เฟส 6 |
| FR-HR-20 | ใบขออัตรากำลังและสรรหา | ⬜ | เฟส 7 |
| FR-HR-21 | สัมภาษณ์และการจ้าง | ⬜ | เฟส 7 |
| FR-HR-22 | รอบประเมินและแบบประเมิน | ⬜ | เฟส 7 |
| FR-HR-23 | หลักสูตรและการอบรม | ⬜ | เฟส 7 |
| FR-HR-24 | บทบาทและสิทธิ์ | ⬜ | เฟส 8 · ⛔ **บล็อกด้วย Gap G-06** |
| FR-HR-25 | รายงานและแดชบอร์ด | ⬜ | เฟส 8 · ⚠️ `create_chart` ยังไม่เคยเรียกสำเร็จในแอปนี้ |
| FR-HR-26 | การแจ้งเตือน | 🔶 | `send_internal_notice` ใน WF-HR-01/02/03 ยิงจริงสำเร็จบางส่วน (แจ้งพนักงาน/ผู้อนุมัติเมื่อตัด/คืนสิทธิ) — ยังไม่ครบทุก workflow |
| FR-HR-27 | เอกสารรูปแบบราชการ | ⬜ | เฟส 8 · 🔴 Browser เท่านั้น |

---

## C. Non-functional

| รหัส | ด้าน | สถานะ | หมายเหตุ |
|---|---|---|---|
| NFR-HR-01 | สิทธิ์และขอบเขต | ⬜ | ⛔ ทดสอบไม่ได้จนกว่าจะมีบัญชีทดสอบ 4 บัญชี |
| NFR-HR-02 | ปิดบังข้อมูลอ่อนไหว | ⬜ | field-level visibility ตั้งได้เฉพาะ UI |
| NFR-HR-03 | audit trail | 🔶 | แพลตฟอร์มมีให้อัตโนมัติ — ยืนยันแล้วผ่าน `_updatedBy`=`user-workflow` บน WF-HR-01/02/03 |
| NFR-HR-04 | SLA | ⬜ | WF-HR-06 |
| NFR-HR-05 | PDPA | ⬜ | ⚠️ **Gap** — ยังไม่มี workflow ลบข้อมูลผู้สมัครที่หมดอายุ (A-HR-18) |
| NFR-HR-06 | ความถูกต้องการคำนวณ | ⬜ | ต้องเตรียมกระดาษคำนวณ 3 เคสก่อนทดสอบ WF-HR-08 |
| NFR-HR-07 | ความสมบูรณ์ของข้อมูล | ⬜ | 15 unique index · ⚠️ **27 ส.ค. 2569** — พบว่า unique index บนฟิลด์ที่ไม่ required มีความเสี่ยงจำกัดทั้งตารางให้มี record ว่างได้แค่ 1 แถว (ยืนยันจาก tooltip จริง) ต้องเช็คทุก index ที่เหลือว่าฟิลด์ required หรือ auto-generate หรือยัง ก่อนเปิดใช้ (ดู G-11) — IX-10.1/IX-11.1 ของเฟส 4 ไม่ติดกับดักนี้เพราะฟิลด์ทั้งหมดที่ใช้เป็น key required อยู่แล้ว |
| NFR-HR-08 | ห้ามลบเอกสารที่อนุมัติแล้ว | ⬜ | |
| NFR-HR-09 | ทุก logic บน Nocoly | ✅ | สเปกทั้งฉบับใช้เฉพาะ worksheet/workflow/business rule/role — ยืนยันต่อเนื่องผ่าน WF-HR-01/02/03 ที่สร้างทั้งหมดผ่าน MCP workflow AST ไม่มีสคริปต์ภายนอก |
| NFR-HR-10 | เขตเวลาและปฏิทิน | ⬜ | |
| NFR-HR-11 | การใช้งาน | ⬜ | |
| NFR-HR-12 | SoD | ⬜ | |
| NFR-HR-13 | การกู้คืน | ⬜ | 🔴 ต้อง Backup ก่อนงานที่แตะโครงสร้างทุกครั้ง |

---

## D. Interfaces

| ระบบ / โมดูล | สถานะ | หมายเหตุ |
|---|---|---|
| โมดูลบัญชี — ใบสำคัญเงินเดือน | ⬜ | ✅ Gap G-02 ปิดแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 6) · ID ปลายทางพร้อมแล้วครบ (`02-BuildSpec-FRS.md` §1.4) · รอ P5-8…P5-10 ก่อนสร้าง WF-HR-10 จริง |
| โมดูลบัญชี — ใบขออนุมัติเบิกจ่าย | ⬜ | ID ปลายทางพร้อมแล้ว · ไม่มี blocker |
| โมดูลบัญชี — ศูนย์ต้นทุน | 🔶 | ✅ ฟิลด์ `hrms_unit_id` เตรียมไว้แล้ว · ยังไม่มีข้อมูล |
| โมดูลบัญชี — ผังบัญชีและกฎการผ่านรายการ | 🔶 **28 ส.ค. 2569 (ต่อเนื่อง 6)** | ผังบัญชีมี 79+3 record (ฝ่ายบัญชีเพิ่ม `530102`/`210402`/`210304` ให้ HR แล้ว) · กฎ `ac_posting_rule` เงินเดือนยังไม่สร้าง (P5-5) แต่ `event_code`=PAYROLL_ACCRUAL/PAYROLL_PAYMENT พร้อมใช้แล้ว |
| เครื่องบันทึกเวลา | ⬜ | Out of scope เฟสนี้ — นำเข้าไฟล์ |
| ธนาคาร | ⬜ | Out of scope — ออกไฟล์เท่านั้น |
| สรรพากร / ประกันสังคม | ⬜ | Out of scope — ออกรายงานเท่านั้น |

---

## E. ความไม่ตรงกันและช่องว่างที่ตรวจพบ

> บันทึกทุกครั้งที่เอกสาร/สมมติฐานไม่ตรงกับของจริงบนเซิร์ฟเวอร์ — กัน agent เชื่อข้อมูลผิด

| รหัส | สิ่งที่คาดไว้ | ของจริง (ตรวจแล้ว) | แก้ที่ไหน |
|---|---|---|---|
| **G-01** | `ac_voucher.source_doc_type` น่าจะมีค่าสำหรับเงินเดือน | มีแค่ AC_AP · AC_AR · AC_PAY · AC_DEPR · AC_CLOSE · Manual — **ไม่มี HR** | เพิ่ม option ผ่านหน้าจอ · ชั่วคราวใช้ `source_module` = Payroll key แทน (`02-BuildSpec-FRS.md` §1.12) |
| **G-02** | `ac_posting_rule.event_code` น่าจะมี PAYROLL | ✅ **ปิดแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 6)** — ฝ่ายบัญชีเพิ่ม `PAYROLL_ACCRUAL`(key `ad4850c4-f1c1-4a65-adbb-fb0054a6e0c8`)/`PAYROLL_PAYMENT`(key `04a14b13-73c2-408b-a5fb-bb81ff630a06`) ให้แล้ว (`tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`) — **แก้ไข: `event_code` เป็น inline SingleSelect ไม่ใช่ shared optionset** แก้ผ่าน MCP `editFields` ได้ตรง ๆ ไม่ต้อง Browser | ~~ต้องเพิ่มผ่านหน้าจอ~~ ปิดแล้ว — พร้อมสร้าง `ac_posting_rule` จริง (P5-5) |
| **G-03** | `OS_FORM_TYPE` น่าจะมี ภ.ง.ด.1 | มีแค่ ภ.พ.30 · ภ.พ.36 · ภ.ง.ด.3 · 53 · 54 | สร้าง `OS_HR_FILING_FORM` แยก (ไม่แตะ optionset เดิมที่ผูก 4 ตาราง) |
| **G-05** | อัตราประกันสังคมและขั้นบันไดภาษี | 🟡 **28 ส.ค. 2569 (ต่อเนื่อง 7) — cross-check กับกฎหมายทางการแล้ว ยังไม่ใช่ accounting sign-off:** ตามคำขอผู้ใช้ "ดึงข้อมูลจากในเน็ต เอาตามกฎหมายก่อน" ตรวจสอบผ่าน WebSearch/WebFetch: (1) อัตราเงินสมทบประกันสังคม ม.33 5%/5% + เพดานฐานค่าจ้างใหม่ 17,500 บาท/เดือน (จากเดิม 15,000) มีผล 1 ม.ค. 2569 ตามราชกิจจานุเบกษาที่ประกาศ 12 ธ.ค. 2568 (เฟส 1 ปี 2569-2571; เฟส 2 ปี 2572-2574 เพดาน 20,000; เฟส 3 ปี 2575+ เพดาน 23,000) — **ตรงกับที่ seed ไว้ในระบบทุกจุดอยู่แล้ว** (2) ขั้นบันไดภาษีเงินได้บุคคลธรรมดา 8 ขั้น (สูงสุด 2,000,001-5,000,000=30%, over 5,000,000=35%) ตามหน้าทางการของกรมสรรพากร rd.go.th (มีผลตั้งแต่ปีภาษี 2560 ต่อเนื่องถึงปัจจุบัน) — **ตรงกับที่ seed ไว้แล้วเช่นกัน** — ⚠️ พบแหล่งรองอื่น (AEC comparison PDF ของ rd.go.th เอง, Statrys) ระบุขั้นสุดท้ายผิดเป็น 2,000,001-4,000,000/over 4,000,000 — ใช้หน้าอัตราภาษีทางการโดยตรง (rd.go.th/59670.html) เป็นหลักเพราะเจาะจงกว่า | 🟡 **ยกระดับจาก "ไม่ยืนยันเลย" เป็น "cross-check กับกฎหมายสาธารณะแล้วตรงกัน" — ยังไม่ใช่การเซ็นรับรองอย่างเป็นทางการจากฝ่ายบัญชีตามที่ขอไว้ใน `tilsna-accounting/12-HR-Handoff-Response-COA-Gap.md`** แนะนำให้ฝ่ายบัญชียืนยันซ้ำอย่างเป็นทางการก่อน go-live จริง · `hr_company_tax_id`/`hr_sso_employer_no` เป็นเลขเฉพาะของบริษัท **หาจากอินเทอร์เน็ตไม่ได้** ต้องได้จากเอกสารจริงของบริษัทเท่านั้น ยังเป็น placeholder ต่อไป |
| **G-06** | มีบัญชีผู้ใช้พอทดสอบ 8 บทบาท | **มีเพียง 2 บัญชี** | 🔴 ขอเพิ่มอย่างน้อย 4 บัญชีทดสอบ — ⛔ บล็อก AC-11 AC-12 AC-13 และการทดสอบอนุมัติ 2 ระดับ (ขั้น HR ของ WF-HR-01) |
| **G-07** | `create_view` / `create_custom_actions` / `create_chart` ใช้ได้ | มี tool แต่ **ยังไม่เคยเรียกสำเร็จในแอปนี้** · REST route คู่ขนานเคยตอบ 405 | Surface = `MCP (unverified)` · **DoD ทุกรายการต้องมีขั้นเปิดหน้าจอดูว่าโผล่จริง** |
| **G-08** | นโยบายลบข้อมูลผู้สมัครตาม PDPA | ยังไม่มี workflow | เพิ่มในเฟสถัดไป — บันทึกไว้เป็นหนี้ทางเทคนิค |
| **G-09** | `hr_attendance` มีอยู่แล้วให้ WF-HR-02/03 sync สถานะ "On leave" | 🟢 **ปลดบล็อกแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 3)** — G-13 คลี่คลาย (ไม่ใช่บั๊ก แค่ขาดขั้น publish inner ก่อน — ดู G-16) — พร้อมกลับมาเพิ่ม node sync "On leave" ใหม่ในรอบถัดไป | ~~บล็อกอยู่จริง~~ ปลดบล็อกแล้ว — ดู G-16 |
| **G-13** 🆕 | `sub_process` node สร้างผ่าน `batch_create_process_nodes` แล้ว publish ได้ปกติเหมือน node ชนิดอื่น | 🟢 **คลี่คลายแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 3) — ไม่ใช่บั๊กแพลตฟอร์ม** ~~27 ส.ค. 2569 — ไม่จริง: สร้างสำเร็จแต่ `validate_process`/`publish_process` ของ workflow แม่ค้าง error `NodeAppIsNull` ที่ตัว container node เสมอ~~ **[แก้ไข] root cause จริง = ไม่เคยเรียก `publish_process` ที่ inner processId เลย มัวแต่ `validate_process` แล้วเข้าใจผิดว่าพอ — ดู G-16** | ~~กระทบ D-8/WF-HR-05~~ **[แก้ไข] ปลดบล็อกแล้ว — ดู G-16** |
| **G-16** 🆕 | หลัง `publish_process` ที่ **inner** processId ของ `sub_process` สำเร็จก่อนแล้ว การ `validate_process`/`publish_process` ที่ **outer** ควรผ่านโดยไม่มี `NodeAppIsNull` | **28 ส.ค. 2569 (ต่อเนื่อง 3) — ยืนยันจริงว่าใช่**: ตามคำขอผู้ใช้ "ลองยืนยันข้อมูลใน skill ชุด hap ทั้ง mcp และ cli" ตรวจเอกสารอ้างอิงของ skill `hap-mcp-app-builder` (`workflow_rules.md`) พบระบุชัดเจนว่าต้อง publish inner ก่อน outer เสมอ (กฎเดียวกันมีบันทึกสั้น ๆ อยู่แล้วใน `nocoly-hybrid-builder-v2/references/workflow-authoring-mcp.md` บรรทัด 155 แต่ตีความผิดมาตลอด) — ทดสอบยืนยันด้วย minimal process ใหม่ (outer `6a90fb59fdab77a41c63662d` + inner `6a90fb5dfdab77a41c636699`): validate(inner)=valid:true → **publish(inner)=published:true v1** → validate(outer)=valid:true → **publish(outer)=published:true v1 ไม่มี error** — ลบ process ทดสอบทั้ง 2 ทิ้งสะอาด (ลบแยกทั้ง outer+inner เพราะลบ outer ไม่ cascade ลบ inner) | **P4-4 (WF-HR-05) และ P4-6 (WF-HR-06) ปลดบล็อกแล้ว** — กฎใหม่บังคับทุกโปรเจกต์ Nocoly: มี `sub_process`/`approval_block` ต้อง publish inner processId ก่อนเสมอแล้วจึง validate/publish outer |
| **G-15** 🆕 | หลังแก้+revert node graph ของ workflow ที่ publish อยู่แล้ว (WF-HR-01) กลับสู่โครงสร้างเดิม workflow ควรกลับมายิงทำงานปกติ | **28 ส.ค. 2569 — ไม่จริง**: เพิ่ม BR-08.3 (4 node) เข้า WF-HR-01 แล้ว publish v4 → ยิงทดสอบไม่มี `user-workflow` log เลย → revert `delete_process_node`×4 คืนโครงสร้าง=v3 เดิมทุกจุด publish v5 → ยิงทดสอบซ้ำ **ยังคงไม่ยิงเหมือนเดิม** (รอ 3+ นาทีสะสม) — สาเหตุยังไม่ทราบ ไม่ยืนยันว่าเกี่ยวกับการแก้ไขจริง (อาจเป็น session/environment ชั่วคราว) | 🔴 **[อัปเดต 28 ส.ค. 2569 ต่อเนื่อง 4 — ดู G-17] สถานะ ✅/🟢 เดิมของ WF-HR-01 ต้องตรวจสอบซ้ำก่อนเชื่อ ห้ามแก้ node ของ WF-HR-01 เพิ่มจนกว่าจะเข้าใจสาเหตุ** — ยืนยันแล้วว่า approval instance จริง TEST-LR-0001 ไม่ถูกกระทบตลอดกระบวนการ — **G-17 พิสูจน์แล้วว่าสาเหตุไม่ใช่การแก้ไข WF-HR-01/BR-08.3 นี้ — เป็นปัญหาระดับระบบที่กว้างกว่า (WF-HR-04 ที่ไม่เคยถูกแตะก็ไม่ยิงเช่นกัน)** — ดู `04-CLAUDE-memory.md` D-15/D-17 สำหรับรายละเอียดเต็ม |
| **G-17** 🟢 **แก้ไขแล้ว (ต่อเนื่อง 9)** | worksheet_event workflow ที่ published สมบูรณ์ (structure ผ่าน `get_workflow_structure` ครบ ไม่มี warning) ควรยิงทำงานจริงเมื่อ trigger field เปลี่ยนค่าตามเงื่อนไข | **28 ส.ค. 2569 (ต่อเนื่อง 4) — ไม่จริงตอนนี้ ทั้งระบบ**: re-verify WF-HR-01 ตามที่ G-15 ทิ้งไว้ว่าต้องตรวจซ้ำ — สร้าง record ทดสอบใหม่สะอาด (`hr_leave_request` `72e70392-6833-4921-932c-5db3fa446b50`, `TEST-LR-D15-REVERIFY`) → set `leave_status`="Pending supervisor" (`triggerWorkflow:true`) → รอ 68+ วินาที → `get_record_details`/`get_record_logs`/`get_approval_list_by_row` ไม่มีร่องรอย workflow เลย (`_updatedBy`=`user-api` ตลอด) — **เพื่อแยกว่าเป็นปัญหาเฉพาะ WF-HR-01 (จาก G-15) หรือกว้างกว่านั้น จึง sanity-check ด้วย WF-HR-04** (ไม่เคยถูกแตะเลยตั้งแต่ยืนยันสำเร็จ 27 ส.ค.) ด้วย record ทดสอบใหม่สะอาดเช่นกัน (`hr_ot_request` `f9d1b750-4a25-4642-82f5-552524a8ea62`, `TEST-OT-SYSCHK-01`) → set `hr_ot_status`="Pending supervisor" → **ก็ไม่ยิงเช่นกัน** (ไม่มี log, ไม่มี approval, `_updatedBy`=`user-api`) — ตรวจ `get_workflow_structure` ของ WF-HR-01 ทั้ง main+inner ยืนยัน `published:true`, trigger config ถูกต้องทุกจุด, ไม่มี warning | 🔴🔴 **ห้ามเชื่อว่า workflow ใด ๆ ในแอปนี้ทำงานได้จริงจนกว่าจะ re-verify สำเร็จในรอบถัดไป — ไม่ใช่แค่ WF-HR-01** — บล็อกการทดสอบยิงจริง (live-fire test) ของทุก AC/workflow ที่ยังไม่ปิด ✅ (AC-01 ขั้น HR, AC-04/WF-HR-05 เมื่อสร้างเสร็จ, WF-HR-06 เมื่อสร้างเสร็จ ฯลฯ) — **การสร้าง/publish โครงสร้าง workflow ใหม่ (เช่น WF-HR-05/06) ยังทำต่อได้ปกติ ไม่พึ่ง G-17** — เก็บ record ทดสอบทั้ง 2 ไว้เป็นหลักฐาน (ตั้งชื่อชัดว่าเป็น test) ไม่ลบ — **กฎใหม่บังคับ:** ก่อนเริ่มงานที่ต้องพึ่งพา worksheet_event trigger ทำงานจริง ต้อง sanity-check ด้วย workflow ที่เคยพิสูจน์ทำงานมาก่อนเสมอ · 🔴🔴 **28 ส.ค. 2569 (ต่อเนื่อง 8) — re-verify ครั้งที่ 2/3 ยังไม่คลี่คลาย ข้ามช่วง session แล้ว**: reset `TEST-LR-D15-REVERIFY` และ `TEST-OT-SYSCHK-01` อีกครั้ง (Draft→Pending supervisor ผ่าน `update_record(triggerWorkflow:true)`) รอ >60 วินาที ทั้งคู่ยังคง `_updatedBy`=`user-api`, ไม่มีฟิลด์ workflow ใดถูกเติม, `get_approval_list_by_row` ว่าง — **นี่คือความล้มเหลวครั้งที่ 3 ติดต่อกัน และครั้งแรกที่คร่อมขอบเขต session เดิม** (record ทดสอบสร้างไว้ ~11:15 ของ session ก่อนหน้า, re-check นี้ ~18:46-18:47 ของ session ปัจจุบัน — ห่างกัน >7 ชม.) — **อัปเดตคำแนะนำ: ไม่ใช่ปัญหาชั่วคราวของ session/เวลาเดียวอีกต่อไป — แนะนำให้ผู้ใช้ตรวจสอบสถานะแพลตฟอร์ม Nocoly/HAP โดยตรง** (subscription/billing ขององค์กร, ติดต่อ platform support, หรือเปิด Browser ดูว่า workflow ขึ้นสถานะ Enable/Active จริงหรือไม่) — เกินขอบเขตที่ agent ทำเองผ่าน MCP/CLI ได้แล้ว · 🟢🆕 **[แก้ไข 28 ส.ค. 2569 ต่อเนื่อง 9] ข้อสรุปทั้งหมดข้างต้นผิด — ไม่ใช่ปัญหาแพลตฟอร์ม/session เลย:** ผู้ใช้ส่งภาพ Workflow History ของ `TEST-LR-D15-REVERIFY` มาถามชื่อ workflow → เปิดดูพบสถานะ **"Done" จริง** เข้า node `trigger`→`gate` (branch) ครบ ไม่ใช่ "ไม่ยิง" — ตรวจ `get_workflow_structure` พบเงื่อนไข path `leg_supervisor` ต้องการ `submitted_flag` ne "1" — ตรวจ `get_record_details` พบ `submitted_flag` เป็นค่าว่างจริง ("" ไม่ใช่ 0) ⇒ ตรงกับบั๊ก "`ne` เทียบฟิลด์ว่างไม่ผ่าน" ที่เคยพบมาก่อนแล้ว (เหมือน G-14/D-14) จึงตกไป path "ไม่ทำอะไร" เงียบ ๆ — ตรวจ `TEST-OT-SYSCHK-01`/WF-HR-04 พบสาเหตุเดียวกันทุกประการ (`hr_ot_submitted_flag` ว่าง ทั้งที่เคยตั้ง `defaultValue:0` ไว้แล้ว) **⇒ สรุปใหม่: ฟิลด์ธงเหล่านี้**ไม่มี `defaultValue` เลย** (`default:""` — ยืนยันด้วย `hap worksheet fields <ws_id> --raw` 28 ส.ค. 2569) รวมถึง `hr_ot_submitted_flag` ที่ D-14 บันทึกไว้ว่าตั้ง `defaultValue:0` แล้วเมื่อ 27 ส.ค. ⇒ **ค่าที่ตั้งผ่าน `editFields` ไม่ persist** (สอดคล้องกับบั๊กที่รู้อยู่แล้วว่า `editFields` เขียนทับทั้ง field object) — ยังพิสูจน์ไม่ได้ว่า API เพิกเฉยต่อ default เพราะไม่มีฟิลด์ไหนมี default ให้ทดสอบเลย ⇒ ในทางปฏิบัติ ต้องส่งค่าฟิลด์ธงตรง ๆ เองเสมอ** — **ทดสอบแก้จริง:** set flag=0 ทั้ง 2 record แล้ว re-trigger → **ทั้งคู่ยิงสำเร็จทันที** `_updatedBy`=`user-workflow`, มี To-do จริงถึง Wanadtapong.l — WF-HR-01/WF-HR-04 ทำงานได้ปกติมาตลอด ไม่เคยเสียหายจาก G-15/BR-08.3 — ขออภัยที่แนะนำผิดให้ผู้ใช้ตรวจสถานะแพลตฟอร์ม/subscription รอบก่อน — บทเรียน: ต้องเปิด Workflow History (Browser) ตรวจก่อนสรุปว่า "ไม่ยิง" เสมอ อย่าพึ่ง `_updatedBy`/`get_approval_list_by_row` อย่างเดียว (field เหล่านี้ว่างได้ทั้งกรณี "ไม่ยิงเลย" และ "ยิงแล้วไปจบที่ path ไม่ทำอะไร") |
| **G-18** 🆕 | `hr_ot_request` ควรมีฟิลด์บันทึก "วันที่ส่งคำขอ" (`submitted_at`) เหมือน `hr_leave_request` เพื่อให้ WF-HR-06 คำนวณ SLA ค้างอนุมัติได้ตรงความหมาย | **28 ส.ค. 2569 (ต่อเนื่อง 4) — ไม่มีจริง**: ตรวจ `get_worksheet_structure` ของ `hr_ot_request` พบว่ามีแค่ `hr_approved_at` (วันที่อนุมัติ) ไม่มีฟิลด์ "วันที่ส่งคำขอ" เลย — `_createdAt` ระบบใช้แทนได้แค่คร่าว ๆ ไม่ตรงความหมายถ้า Draft ค้างไว้นานก่อนกดส่งจริง | WF-HR-06 สร้าง v1 แล้วจำกัดขอบเขตเหลือแค่ `hr_leave_request` เท่านั้น (AC-14 ยังไม่ปิดเต็ม) — ต้องตัดสินใจ (a) เพิ่มฟิลด์ `submitted_at` จริงใน `hr_ot_request` แล้วให้ WF-HR-04 เขียนค่าตอน submit หรือ (b) ยอมรับ `_createdAt` เป็นตัวแทน ก่อนขยาย WF-HR-06 ให้ครอบคลุม OT (และ `hr_claim` เมื่อสร้างตารางในเฟส P6) |
| **G-14** 🆕 | `compute` node ชนิด `dateDiff` ที่ส่ง `precision` มาด้วยน่าจะเคารพค่านั้นเหมือน numeric compute | **27 ส.ค. 2569 (ต่อเนื่อง) — ไม่จริง**: ส่ง `outputUnit:"h"` + `precision:2` ตอนสร้าง node แล้วอ่านค่ากลับด้วย `get_workflow_structure` พบว่า `precision` ถูกตัดทิ้งเงียบ ๆ ไม่ error — ผลลัพธ์จริงถูกตัดทศนิยมเสมอ (ยืนยันจริง: กะเวลา 2.5 ชม. ได้ผล "2.00" ไม่ใช่ "2.50") | **ห้ามพึ่ง `precision` ของ `dateDiff` เมื่อ `outputUnit` อาจมีเศษ (h/d)** — แก้ด้วย 2-step compute: `dateDiff`(หน่วยเล็กสุดไม่มีเศษ เช่น นาที) → `computeType:"number"`(`expression` หารแปลงหน่วย, `precision:2` — numeric compute เคารพ `precision` ปกติ) — ยืนยันแล้วว่าใช้ได้กับทุก workflow ที่ต้องคำนวณชั่วโมงทศนิยมจากเวลา (WF-HR-04, และจะกระทบ WF-HR-05/WF-HR-08 ที่ต้องคำนวณชั่วโมงทำงานเช่นกัน) |
| **G-10** 🆕 | Business Rules ทำ cross-table lookup ผ่าน Relation field ไม่ได้ แต่ Dynamic Default ("Query worksheet") น่าจะทำได้เพราะมี query conditions ของตัวเอง | **27 ส.ค. 2569 — ทดสอบจริงแล้วพบว่าไม่ได้เช่นกัน**: field picker "Select fields from current form" ของ Dynamic Default ไม่แสดง Relation-type field ให้เลือกเป็น Dynamic Value เลย (เฉพาะ Text/Date/Number) — ข้อจำกัดเดียวกันขยายจาก Business Rules มาถึง Dynamic Default ด้วย | ทุก cross-table lookup ที่อ้างอิง Relation field ของฟอร์มปัจจุบันต้องย้ายไปทำที่ระดับ **workflow** (query worksheet + update_record node) เสมอ — กระทบ DV-08.1 โดยตรง (ดู D-9 ใน Roadmap) |
| **G-11** 🆕 | Unique Index เป็นแค่กันซ้ำระดับ field ไม่มีผลข้างเคียงอื่น | **27 ส.ค. 2569 — tooltip จริงของระบบที่หน้า Search Acceleration → Create Index ยืนยัน**: *"Once a unique index is established, the value of the field cannot be duplicated. If the field is not required, the entire worksheet can only have one empty record..."* — ฟิลด์ที่ไม่ required จะจำกัดทั้งตารางให้มี record ที่ฟิลด์นั้นว่างได้แค่ 1 แถว | เปิดใช้ unique index ได้เฉพาะฟิลด์ที่ required หรือ auto-generate ค่าเสมอ — `leave_no` (IX-08.1) ยังไม่ auto-generate จึง **ตัดสินใจไม่เปิดใช้** (ดู D-10 ใน Roadmap) จนกว่าจะแก้ · IX-10.1/IX-11.1 ของเฟส 4 ไม่ติดกับดักนี้เพราะฟิลด์ที่ใช้เป็น key (`employee`/`work_date`/`ot_date`/`ot_from`) required ทั้งหมด |
| **G-12** 🆕 | inline-option Dropdown ที่สร้างแทน shared optionset ควรใช้ label ตรงกับ optionset กลางเสมอ | **27 ส.ค. 2569 — พบพลาดจริง 1 ครั้ง**: สร้าง `hr_ot_request.day_type` ครั้งแรกด้วย label ภาษาไทยที่คัดลอกมาจากชื่อ **record** ของ `hr_ot_rate` (`hr_rate_name`) แทนที่จะเป็น label ของ **optionset** `OS_HR_OT_DAY_TYPE` ที่ field สเปกอ้างถึงจริง — ตรวจพบก่อนใช้งานจริงและแก้ด้วย `editFields` เป็นภาษาอังกฤษให้ตรงแล้ว | ก่อนสร้าง inline options ทดแทน shared optionset ให้ตรวจ option-key GUID mapping ใน `02-BuildSpec-FRS.md` §1.6.1 เสมอ อย่าเดา label จากข้อมูล record ที่ดูเกี่ยวข้องกัน |
| **G-19** 🆕🔴🔴 | branch condition `<flag> ne "1"` ควรผ่านเมื่อฟิลด์ธงยังว่าง (ยังไม่เคยตั้งค่า) และฟิลด์ธงที่ตั้ง `defaultValue:0` ไว้แล้วควรได้ค่า 0 จริง | **28 ส.ค. 2569 (ต่อเนื่อง 17) — ไม่จริงทั้งสองข้อ**: (1) ค่าว่างไม่ match `ne "1"` พิสูจน์แล้วจาก WF-HR-01/WF-HR-04 (ตัวแปรเดียวที่ต่างระหว่างรอบล้มเหลวกับรอบสำเร็จคือค่าธง) (2) สำรวจด้วย `hap worksheet fields <ws_id> --raw` ครบทุกตารางพบว่า **ฟิลด์ธงทุกตัวมี `default:""`** ไม่มีตัวไหนได้ `defaultValue:0` จริงเลย รวมถึง `hr_ot_submitted_flag` ที่ G-14/D-14 บันทึกว่าตั้งแล้วเมื่อ 27 ส.ค. ⇒ ค่าที่ตั้งผ่าน `editFields` ไม่ persist · (3) **MCP `get_worksheet_structure` ไม่คืนคีย์ `defaultValue` เลย** ทั้ง json/md จึงยืนยันผ่าน MCP ไม่ได้ — นี่คือเหตุผลที่ D-14 ถูกเชื่อว่าแก้แล้วมา 4 วัน | 🔴🔴 **บล็อก AC-01/AC-03** — WF-HR-02 จะไม่ตัดสิทธิเมื่อ To-do ที่ค้างอยู่ถูกกด Approve (`deducted_flag` ว่าง) · WF-HR-03/05/13 เสี่ยงเหมือนกัน · **path ผู้ใช้จริงผ่านฟอร์มยังไม่เคยทดสอบ** · ทางแก้ที่แนะนำ: สลับโครง branch เป็น `flag eq "1"` → ไม่ทำอะไร + catch-all → ทำงาน (ทนค่าว่าง ไม่ต้อง backfill) — ยังไม่ลงมือ รอผู้ใช้อนุมัติ · ดู D-19 ใน `05-Roadmap-Tracker.md` §11 และ Known Issues ต่อเนื่อง 17 ใน `04-CLAUDE-memory.md` · ✅🔴 **30 ส.ค. 2569 — ยืนยันแล้วด้วย A/B controlled test (agent-hr, claim `HR/D19-A`)**: ใบลา 2 ใบเหมือนกันทุกฟิลด์ต่างแค่ `deducted_flag` แล้วเปลี่ยน Draft→Approved เท่ากัน — ใบที่ flag=`"0"` ถูกตัดสิทธิครบสาย (`deducted_flag`→`"1"`, used 0→1, remaining 10→9) · ใบที่ flag=`""` **ไม่เกิดอะไรเลย เงียบสนิท ไม่มี error** ⇒ `"" ne "1"` = FALSE ตกไป branch catch-all `อื่นๆ (ไม่ทำอะไร)` · **แก้รายชื่อ workflow ที่กระทบให้ถูกต้อง (จากการอ่านโครงจริงทุกตัว): WF-HR-01 / WF-HR-02 / WF-HR-03 / WF-HR-04** — เดิมเขียนว่า WF-HR-05/WF-HR-13 เสี่ยงด้วยนั้น **ไม่ถูก** (WF-HR-05 branch ใช้ `conditionId 7/8` = ตรวจว่าง/ไม่ว่าง ไม่ใช่ `ne` · WF-HR-13 ยังไม่มีอยู่จริงในแอป) ส่วน **WF-HR-04 กระทบด้วยแต่ไม่เคยถูกลิสต์ไว้** · ล้าง test data + คืนยอดสิทธิกลับ used=0/remaining=10 แล้ว · หลักฐานเต็ม ตาราง branch/nodeId ที่กระทบ และ 3 ทางเลือกแก้ (พร้อมคำสั่ง `hap workflow node save` ที่แก้ node เดิมในที่ได้ ไม่ต้องลบ-สร้างใหม่) → `16-D19-Empty-Flag-Branch.md` · ✅✅ **30 ส.ค. 2569 — แก้แล้วและยืนยันผลแล้ว (claim `HR/D19-B`, ผู้ใช้อนุมัติ "patch data ก่อน + แก้เงื่อนไข")**: ①patch ธงว่างเป็น `0` ครบทุก record ที่ค้าง (`TEST-LR-D15-REVERIFY`/`TEST-LR-DOCOK`/`TEST-LR-BR083`/`TEST-OT-SYSCHK-01`) ②เพิ่ม OR group `is empty` (`conditionId 8`) เข้า branch ของ WF-HR-01/02/03/04 ด้วย `hap workflow node save --type 2` แล้ว `hap workflow publish` ครบ 4 ตัว (v6/v5/v3/v3 · ยืนยัน `enabled=true, deleted=false` · สำรอง structure+node get ก่อนแก้ครบ) ③ยิงจริงเคสเดิมที่เคยพัง: WF-HR-02 ใบลา `deducted_flag=""` → Approved **ตัดสิทธิสำเร็จ** (used 0→1) · WF-HR-03 `returned_flag=""` → Cancelled **คืนสิทธิสำเร็จ** (used 1→0) · WF-HR-01 `submitted_flag=""` → Pending supervisor **เปิด approval สำเร็จ** · ⚠️ WF-HR-04 แก้+publish+อ่านกลับตรงแล้วแต่ยังไม่ live-fire · ล้าง test data + คืนยอดสิทธิเรียบร้อย · **ยังเปิดอยู่:** `defaultValue` ยัง persist ไม่ได้ (record ใหม่ยังเกิดพร้อมธงว่าง แต่ workflow ทนแล้ว) และยังไม่เคยทดสอบ path ผู้ใช้จริงผ่านฟอร์ม · 🔴 **แก้ข้อมูลผิดของตัวเอง 30 ส.ค. 2569 (หลัง `/scrutinize`): WF-HR-05 กระทบด้วย** — รอบก่อนเขียนว่า "WF-HR-05 ไม่กระทบ" ซึ่ง**ผิด และไปเขียนทับข้อความเดิมที่ถูกอยู่แล้ว** · สาเหตุ: สแกนด้วย `hap workflow structure` ซึ่งไม่คืน `filters` ของ node ชนิด search/get_multiple · จุดที่กระทบจริงคือ **filter ของ node `หาบันทึกลงเวลาที่ยังไม่สรุป`** (`6a91018d730d20c5b7710fdc`) `สรุปแล้ว ≠ "1"` ⇒ ถ้าไม่แก้ **สรุปเวลาทำงานรายวันจะเลือกได้ 0 แถวตลอดไป เงียบ ๆ** · สแกนใหม่ด้วยวิธีที่ถูก (`node get` ทีละ node ครบ 12 process) ได้จุดที่ใช้ `≠` ทั้งหมด **5 จุด: WF-HR-01/02/03/04 (branch) + WF-HR-05 (filter)** · แก้ WF-HR-05 แล้ว publish v3 · **แต่ยิงพิสูจน์ไม่ได้** เพราะเป็น schedule และ `hap workflow trigger` ไม่รัน flow จริง (ยืนยันด้วย control case) ⇒ **WF-HR-04 และ WF-HR-05 ยังอยู่สถานะ "แก้แล้ว รอพิสูจน์"** ต้องดู Workflow History บนเบราว์เซอร์ · fixture `TEST-ATT-D19-01` เตรียมไว้แล้ว · รายละเอียด → `16-D19-Empty-Flag-Branch.md` §8 |

---

## F. Acceptance Criteria — สถานะ

| รหัส | เกณฑ์ (ย่อ) | สถานะ | บล็อกด้วย |
|---|---|---|---|
| AC-01 | ใบลาผ่าน 2 ระดับ สิทธิลดถูกต้อง | 🟡 **27 ส.ค. 2569 — บางส่วนผ่าน · 28 ส.ค. 2569 (ต่อเนื่อง 4) — re-verify แล้วไม่ยิงจริง แต่เป็นปัญหาระดับระบบ (G-17) ไม่ใช่ WF-HR-01 เจาะจง** | ขั้นหัวหน้างาน (WF-HR-01) + ตัดสิทธิ (WF-HR-02) เคยยิงจริงสำเร็จและยืนยันด้วย `_updatedBy`=`user-workflow` แล้ว (27 ส.ค.) — 28 ส.ค. (ต่อเนื่อง 4) re-verify ด้วย record ทดสอบใหม่สะอาด (`TEST-LR-D15-REVERIFY`) พบว่าไม่ยิงจริง แต่ sanity-check ด้วย WF-HR-04 (untouched) ก็ไม่ยิงเช่นกัน ⇒ **G-17: ปัญหาระดับ session/environment/platform ไม่ใช่ความเสียหายจาก BR-08.3/G-15** โครงสร้าง WF-HR-01 ยืนยันสมบูรณ์ 100% ผ่าน `get_workflow_structure` — เหลือขั้นอนุมัติ HR ของ WF-HR-01 ที่ยังไม่ทดสอบ (⛔ **G-06** ต้องมี role/บัญชีทดสอบก่อน + ต้องมนุษย์กด Approve จริงใน To-do) — 🟢 **28 ส.ค. 2569 (ต่อเนื่อง 9) — G-17 แก้ไขแล้ว ไม่ใช่ปัญหาระบบ (ดู G-17): แก้ `submitted_flag` ว่าง→0 แล้วยิงสำเร็จจริง ขั้นหัวหน้างานผ่าน มี To-do ถึง Wanadtapong.l อีกครั้ง** — เหลือขั้นอนุมัติ HR ที่ยังไม่ทดสอบ (⛔ G-06 ต้องมี role/บัญชีทดสอบก่อน) จึงยังไม่ปิด ✅ เต็มรูปแบบ · 🔴 **28 ส.ค. 2569 (ต่อเนื่อง 10) — บล็อกใหม่ G-19: ส่วน "สิทธิลดถูกต้อง" ของ AC-01 จะไม่ผ่าน** เพราะ WF-HR-02 gate เทียบ `deducted_flag ne "1"` กับค่าว่าง — ต้องแก้ G-19 ก่อนจึงจะพิสูจน์ AC-01 ครบได้ |
| AC-02 | บล็อกใบลาเกินสิทธิ | ⬜ | **27 ส.ค. 2569 — ยังไม่เริ่ม** BR-08.1 ที่สร้างแล้วครอบคลุมเฉพาะ "วันที่สิ้นสุดก่อนวันที่เริ่ม" ไม่ใช่เกณฑ์นี้ · BR-08.2 (บล็อกเมื่อ `leave_days` > สิทธิคงเหลือ) ซึ่งควรครอบคลุม AC-02 โดยตรง **ยืนยันแล้วว่าทำเป็น Business Rule ไม่ได้** (relation-field lookup ข้ามตาราง `leave_type`/`hr_leave_balance` ไม่รองรับในทั้ง Business Rules — ดู G-10) ต้องย้ายไปทำที่ WF-HR-01 (มีแผน node 5–9 อยู่แล้วใน BuildSpec) ซึ่งยังไม่ได้เริ่มสร้าง |
| AC-03 | ยกเลิกใบลาแล้วคืนสิทธิ | ✅ **27 ส.ค. 2569** | WF-HR-03 `6a8fab800e97bf440dddb744` ยิงจริงสำเร็จ — balance คืนสิทธิถูกต้อง (9.0→10.0) `_updatedBy`=`user-workflow` · ledger บันทึก Leave returned คู่กับ Leave taken ครบ · ดู `04-CLAUDE-memory.md` แถว #13 · ⚠️ **28 ส.ค. 2569 (ต่อเนื่อง 10) — ขอบเขตของหลักฐานนี้แคบกว่าที่เคยเข้าใจ (G-19):** หลักฐาน 27 ส.ค. ใช้ `TEST-LR-0001` ซึ่ง `returned_flag`/`deducted_flag` ถูก workflow เขียนค่าไปแล้วก่อนหน้า — **ยังไม่เคยพิสูจน์กับ record ที่ธงยังว่าง** ซึ่งเป็นสภาพของใบลาใหม่ทุกใบ ⇒ ✅ ยังยืนสำหรับเคสที่ทดสอบ แต่ **ไม่ครอบคลุมใบลาใหม่จนกว่าจะแก้ G-19** |
| AC-04 | วันที่ลาแสดงเป็น "ลา" ไม่ใช่ "ขาดงาน" | 🟡 | ~~27 ส.ค. 2569 (รอบเย็น) — บล็อกด้วยบั๊กใหม่ G-13~~ **28 ส.ค. 2569 (ต่อเนื่อง 3) — ปลดบล็อกด้าน structure แล้ว (G-16)**: G-13 ไม่ใช่บั๊กแพลตฟอร์ม แค่ขาดขั้น publish inner sub_process ก่อน outer — ยืนยันด้วย minimal test สำเร็จ 100% — **28 ส.ค. 2569 (ต่อเนื่อง 4): WF-HR-05 สร้าง+publish สำเร็จจริง** (main `6a910184730d20c5b7710fa8` v1 + inner `6a910197730d20c5b7711028` v1, validate สะอาด) — **ยังไม่ปิด ✅ เพราะยังไม่ได้กลับไปสร้าง node sync "On leave" จริงใน WF-HR-02/03 (D-8) และยังไม่ได้ live-fire test WF-HR-05** — 🟢 **28 ส.ค. 2569 (ต่อเนื่อง 9) — G-17 แก้ไขแล้ว ไม่ใช่ปัญหาระบบ** — worksheet_event ของ WF-HR-01/04 ยิงได้ปกติจริง (root cause คือ flag ว่างบน record ทดสอบ ไม่ใช่ platform) — แต่ schedule trigger ของ WF-HR-05 เองยังไม่เคยถูก live-fire test เลย (ยังต้อง seed 5 เคส + Execute now ตาม test recipe เดิม) และควร audit flag field ของ `hr_attendance`/sub_process ก่อนเชื่อผลว่าไม่มีปัญหาเดียวกัน |
| AC-05 | OT ที่ไม่อนุมัติไม่เข้าสลิป | ⬜ | — · `hr_ot_request` มีตารางแล้ว (P4-2) รวมฟิลด์ `paid_flag`/`ot_status` — logic ยังไม่สร้าง (WF-HR-08) |
| AC-06 | ยอดสลิปตรงกับกระดาษคำนวณ 100% | ⬜ | G-05 |
| AC-07 | ใบสำคัญเงินเดือนดุลและอ้างกฎการบันทึกบัญชี | ⬜ | ✅ **G-02 ปิดแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 6)** — รอ P5-5 (สร้าง `ac_posting_rule`) + P5-8…P5-10 ก่อนสร้าง WF-HR-10 จริง |
| AC-08 | ยิงซ้ำไม่เกิดใบสำคัญที่สอง | ⬜ | ✅ **G-02 ปิดแล้ว 28 ส.ค. 2569 (ต่อเนื่อง 6)** — เหมือน AC-07 |
| AC-09 | สลิปในงวดที่ปิดแล้วแก้ไม่ได้ | ⬜ | — |
| AC-10 | ใบเบิกเกินวงเงินถูกบล็อก · อนุมัติแล้วเข้าบัญชี | ⬜ | — |
| AC-11 | พนักงานไม่เห็นเงินเดือนผู้อื่น | ⬜ | ⛔ **G-06** |
| AC-12 | หัวหน้าเห็นเฉพาะลูกทีม | ⬜ | ⛔ **G-06** |
| AC-13 | R5 R8 ไม่มีสิทธิ์อนุมัติ | ⬜ | ⛔ **G-06** |
| AC-14 | แจ้งเตือนคำขอค้างเกิน SLA | 🟡 | **28 ส.ค. 2569 (ต่อเนื่อง 4)** — WF-HR-06 สร้าง+publish สำเร็จ (main `6a910222730d20c5b77115e5` v1 + inner `6a9102345f8564a68c449ee7` v1) แต่ **v1 ครอบคลุมเฉพาะ `hr_leave_request`** (`hr_ot_request`/`hr_claim` ตัดออก — ดู G-18) และยังไม่ได้ live-fire test — 🟢 G-17 แก้ไขแล้ว (28 ส.ค. ต่อเนื่อง 9, ไม่ใช่ปัญหาระบบ) แต่ schedule trigger ของ WF-HR-06 เองยังไม่เคย live-fire test เลย ยังไม่ปิด ✅ |
| AC-15 | กันบันทึกซ้ำแม้ผ่าน API | 🔶 **27 ส.ค. 2569 — บางส่วน** | ยืนยันแล้วเฉพาะ WF-HR-02 (`deducted_flag` กันตัดซ้ำ) และ WF-HR-03 (`returned_flag` กันคืนซ้ำ) ผ่านการทดสอบยิงซ้ำจริง — unique index ของตารางอื่นยังไม่ตั้ง (IX-08.1 ตัดสินใจไม่เปิดใช้ชั่วคราว ดู G-11 · IX-10.1/IX-11.1 ของเฟส 4 ยังไม่สร้างเช่นกัน แม้ฟิลด์ key จะ required ครบแล้วก็ตาม) |
| AC-16 | พิมพ์เอกสารรูปแบบราชการ | ⬜ | Browser |
| AC-17 | ผู้สมัครที่จ้างสร้างทะเบียนอัตโนมัติ | ⬜ | — |
| AC-18 | ร่องรอยการแก้โครงสร้างเงินเดือน | ⬜ | — |
| AC-19 | เปลี่ยนอัตราแล้วผลเปลี่ยนโดยไม่แก้ workflow | 🔶 **27 ส.ค. 2569 (ต่อเนื่อง) — ส่วนแรกผ่าน** | WF-HR-04 ยืนยันแล้วว่า `hr_applied_multiplier` ดึงจากตาราง `hr_ot_rate` จริง ไม่ hardcode — ทดสอบ 2 เคส day_type ต่างกันได้ตัวคูณต่างกันถูกต้อง (×3.0 Holiday outside hours, ×1.5 Working day) — ส่วนที่เหลือ (สลิปเงินเดือน/WF-HR-08) ยังไม่เริ่ม |
| AC-20 | ยกยอดสิทธิลาต้นปี | ⬜ | — |

---

## G. Task List ที่ทำได้ทันที (เรียงตาม value/effort)

| # | งาน | อ้างอิง | effort | ต้องมีอะไรก่อน | สถานะ |
|---|---|---|---|---|---|
| 1 | ยืนยันข้อสมมติ 18 ข้อ (A-HR-01…18) กับผู้ใช้ | `01-BRD.md` §11 | S | — | ⬜ |
| 2 | **ขอเพิ่มบัญชีทดสอบ 4 บัญชี** | G-06 | S | ผู้ดูแลองค์กร | ⬜ 🔴 **ทำก่อน** |
| 3 | เพิ่ม option `PAYROLL_ACCRUAL` / `PAYROLL_PAYMENT` ใน `ac_posting_rule.event_code` | G-02 | S | Backup ก่อน | ✅ **28 ส.ค. 2569 (ต่อเนื่อง 6) — ฝ่ายบัญชีทำผ่าน MCP แล้ว** (ไม่ใช่ Browser-only ตามที่เข้าใจผิดเดิม) |
| 4 | สร้างกลุ่ม HR 8 กลุ่มในแอป | `02-BuildSpec-FRS.md` §1.2 | S | — | ✅ |
| 5 | สร้าง optionset ใหม่ 24 ชุด | `02-BuildSpec-FRS.md` §1.6 | M | — | ✅ |
| 6 | สร้างตารางตั้งค่า HR-00 ทั้ง 10 ตาราง + seed ข้อมูล | FR-HR-01 · 02 · 07 · 11A | M | #5 | 🔶 10/10 ตารางสร้าง + seed 9/10 เสร็จ 26 ส.ค. 2569 (ผ่าน `create_app_items`+`addFields` — `create_worksheet` ยังพัง) · `hr_approval_rule` รอ role |
| 7 | สร้าง `hr_position` · `hr_job_level` · `hr_employee` | FR-HR-03 · 04 | M | #5 #6 | ✅ |
| 8 | สร้าง unique index ของ `hr_employee` (2 ตัว) + ทดสอบด้วย API | IX-03.1/2 · AC-15 | S | #7 | ⬜ |
| 9 | seed พนักงานทดสอบ 4 คนพร้อมสายบังคับบัญชา | `02-BuildSpec-FRS.md` §5 Record ทดสอบ | S | #2 #7 | 🔶 seed แล้ว 2 คน (TEST-EMP1/TEST-EMP2 ผูกบัญชีจริง) — เหลืออีก 2 คนรอ G-06 |
| 10 | สร้างตารางกลุ่มการลา 3 ตาราง + seed `hr_leave_balance` ตั้งต้นให้ครบทุกประเภท | FR-HR-08 · 09 | M | #6 #7 | ✅ โครงสร้างครบ · ✅ **seed `hr_leave_balance` ครบ 6 ประเภท×2 พนักงานแล้ว 27 ส.ค. 2569 (P3-4)** |
| 11 | **WF-HR-01 อนุมัติใบลา 2 ระดับ** | `22-Workflow-Catalog-HR.md` §3 | L | #9 #10 | 🔴 ขั้นหัวหน้างานเคยสำเร็จ (27 ส.ค.) — **28 ส.ค. 2569 (ต่อเนื่อง 4): re-verify แล้วไม่ยิงจริง — G-17 พิสูจน์ว่าเป็นปัญหาระดับระบบ ไม่ใช่ WF-HR-01 เจาะจง (WF-HR-04 sanity-check ก็ไม่ยิงเช่นกัน)** โครงสร้างยืนยันสมบูรณ์ — ต้องรอ G-17 คลี่คลายก่อน re-verify ซ้ำ — ขั้น HR รอ #2 |
| 12 | WF-HR-02 · WF-HR-03 ตัด/คืนสิทธิ | `22-Workflow-Catalog-HR.md` §3 | M | #11 | 🟢 **สำเร็จทั้งคู่ 27 ส.ค. 2569** |
| 13 | ปุ่ม "ส่งคำขอ" / "ยกเลิกใบลา" + view ของใบลา | `02-BuildSpec-FRS.md` §1.9 · `02-BuildSpec-FRS.md` §1.10 | M | #10 | ✅ **27 ส.ค. 2569 — เสร็จ+ยืนยันผ่าน Role Debugging แล้ว** (P3-7) |
| 14 | Business Rules ของใบลา (BR-08.1…5) | FR-HR-08 | M | #10 | 🔶 **27 ส.ค. 2569** — BR-08.1/8.4/8.5 เสร็จ+ยืนยันผ่าน Role Debugging แล้ว · BR-08.2/8.3 ทำเป็น Business Rule ไม่ได้ (relation-field lookup ไม่รองรับ) ต้องย้ายไปทำที่ workflow (WF-HR-01/WF-HR-06) แทน — ยังไม่เริ่ม |
| 15 | สร้าง role 8 บทบาท + permission matrix | `02-BuildSpec-FRS.md` §1.7 | L | #2 ทุกตาราง | ⬜ |
| 16 | สร้างตารางกลุ่มลงเวลา/OT (`hr_attendance`/`hr_ot_request`) + WF-HR-04 · WF-HR-05 | FR-HR-10 · 11 | L | #10 | 🔴 **27 ส.ค. 2569 (ต่อเนื่อง 2) — โครงสร้างทั้ง 2 ตารางเสร็จ+ยืนยันแล้ว + WF-HR-04 สร้าง+publish+ทดสอบขั้นหัวหน้างานสำเร็จ + P4-5 (ปุ่ม+view) เสร็จ+verify ผ่านจริง** (P4-1/P4-2/P4-3/P4-5) — เหลือ: unique index IX-10.1/IX-11.1 · Business Rules BR-10.x/BR-11.x · WF-HR-04 ขั้น HR ยังไม่ทดสอบ (รอ role/G-06) · **WF-HR-05 (สรุปเวลาทำงานรายวัน) — 🟢 28 ส.ค. 2569 (ต่อเนื่อง 4) สร้าง+publish สำเร็จจริงแล้ว** (main `6a910184730d20c5b7710fa8` v1 + inner `6a910197730d20c5b7711028` v1) · dropdown rebind เข้า shared optionset จริง (G-12 pattern) · 🔴 **WF-HR-04 ถูกใช้เป็น sanity-check ของ G-17 แล้วพบว่าไม่ยิงเช่นกัน (`TEST-OT-SYSCHK-01`) — ยืนยันว่าปัญหาไม่ยิงเป็นระดับระบบ ไม่ใช่ WF-HR-04 เจาะจง — live-fire test ของ WF-HR-05 ต้องรอ G-17 คลี่คลายก่อนเช่นกัน** |
| 17 | กลุ่มเงินเดือน 7 ตาราง | FR-HR-12…16 | L | #7 · G-05 | 🔶 **7/7 ตารางสร้างโครงสร้างครบ + seed องค์ประกอบค่าจ้างแล้ว 28 ส.ค. 2569 (P5-1+P5-2+P5-3+P5-4)** — `hr_pay_component`/`hr_pay_period`/`hr_salary_structure`/`hr_payslip`/`hr_payslip_line`/`hr_pnd1_filing`/`hr_wht_cert` — seed `hr_pay_component` 6/6 record verify แล้ว ✅ **ผูก `coa_account` ครบ 6/6 แล้ว 28 ส.ค. 2569 (ต่อเนื่อง 6)** (ดู FR-HR-12) — เหลือ unique index ทั้งชุด (IX-12.1…12.6) · dropdown rebind เข้า shared optionset · seed P5-6 (โครงสร้างเงินเดือนพนักงานทดสอบ) · workflow WF-HR-07…10 ทั้งหมด |
| 18 | **WF-HR-08 คำนวณสลิป** (workflow ที่ซับซ้อนที่สุด) | `22-Workflow-Catalog-HR.md` §3 | XL | #17 | ⬜ |
| 19 | **WF-HR-10 ผ่านรายการเข้าบัญชี** | `22-Workflow-Catalog-HR.md` §3 | L | #3 #18 | ⬜ |
| 20 | กลุ่มสวัสดิการ + WF-HR-11 · WF-HR-12 | FR-HR-18 · 19 | L | #7 | ⬜ |
| 21 | กลุ่มสรรหา/ประเมิน/อบรม 8 ตาราง + WF-HR-15…19 | FR-HR-20…23 | XL | #7 | ⬜ |
| 22 | รายงานและแดชบอร์ด | FR-HR-25 | M | ทุกตาราง | ⬜ ⚠️ G-07 |
| 23 | Print template เอกสารราชการ 4 แบบ | FR-HR-27 | M | #7 #17 | ⬜ Browser |
| 24 | workflow ลบข้อมูลผู้สมัครที่หมดอายุ (PDPA) | G-08 | S | #21 | ⬜ |

---
*ไฟล์นี้ต้องอัปเดตทุกครั้งที่สถานะจริงเปลี่ยน และ sync กับ `05-Roadmap-Tracker.md` กับตาราง "สถานะงานล่าสุด" ใน `04-CLAUDE-memory.md` เสมอ*
