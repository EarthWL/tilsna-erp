# MIGRATION — ย้ายเอกสาร TILSNA เข้า repo

_28 ส.ค. 2569 · รอบนี้ทำเฉพาะ **โครงสร้าง** ไม่แตะเนื้อหาสักไฟล์_

## ทำอะไรไปแล้ว

| จาก | ไป |
|---|---|
| `tilsna-accounting_*.md` (13 ไฟล์) | `projects/accounting/` ตัด prefix ออก |
| `tilsna-hr_*.md` (10 ไฟล์) | `projects/hr/` ตัด prefix ออก |
| `nocoly-shared_00-HAP-Working-Guide.md` | `shared/00-HAP-Working-Guide.md` |

**เนื้อหาทุกไฟล์เหมือนเดิมทุกตัวอักษร** — การตัด prefix ทำให้ลิงก์ภายในเอกสาร (ซึ่งอ้างด้วยชื่อสั้น เช่น `` `04-CLAUDE-memory.md` ``) ชี้ถูกต้องขึ้น ไม่ใช่พัง

## ทำไมไม่แก้เนื้อหาในรอบนี้

ฝั่ง accounting ยังทำงานอยู่ ⇒ การเขียนทับไฟล์ที่ agent กำลังแก้จะชนกันแน่นอน · และการบีบเอกสารเป็น operation ที่กู้ไม่ได้ **จนกว่าจะมี commit แรก** ⇒ ลำดับที่ปลอดภัยคือ **push ขึ้น repo ก่อน แล้วค่อยแก้เนื้อหา** เพราะตอนนั้นทุกอย่างย้อนได้

---

## งานเนื้อหาที่ค้าง (ทำหลังฝั่ง accounting หยุด)

### A. ข้อมูลผิดที่ต้องแก้ — เร่งด่วนสุด

| ที่ | ปัญหา | แก้เป็น |
|---|---|---|
| `accounting/02-BuildSpec-FRS.md` บรรทัด 32, 953, 1077 | สั่งพิสูจน์ workflow ด้วย `get_record_logs` → operator `user-workflow` — **ให้ผลลบลวง** (ยืนยัน 28 ส.ค.: log คืน `user-api` แม้ `_createdBy` เป็น `user-workflow`) | `get_record_details(includeSystemFields:true)` → `_createdBy`/`_updatedBy` |
| ไฟล์เดียวกัน บรรทัด 46 | **เขียนถูกอยู่แล้ว** — ทีมค้นพบก่อนแล้วแต่ไม่ได้ตามไปแก้จุดอื่น | ใช้เป็นต้นแบบ |
| `hr/*` | ตรวจแบบเดียวกัน ยังไม่ได้ไล่ | — |

### B. ยกความรู้ขึ้นชั้นบน

`accounting/04-CLAUDE-memory.md` มีกับดักถึงข้อ 34 · **ข้อ 26–32 ยกขึ้นสกิลแล้ว** (`anti-drift-playbook.md` §9.1/§9.3/§9.4) ที่เหลือต้องไล่ว่าข้อไหนเป็นความรู้แพลตฟอร์ม (→ `shared/00-HAP-Working-Guide.md`) ข้อไหนเฉพาะแอป (อยู่เดิม)

เกณฑ์: ประโยคนั้นมีชื่อตาราง/ฟิลด์ของแอปนี้อยู่ไหม — ถ้าไม่มี = อยู่ผิดที่

### C. บีบเอกสาร (ทำหลัง B เท่านั้น — บีบก่อนยกขึ้น = ความรู้หายถาวร)

วัดเมื่อ 28 ส.ค.:

| | hr | accounting |
|---|---|---|
| `04-CLAUDE-memory.md` | **11,391 คำ** | 5,796 คำ |
| บรรทัด `_อัปเดตล่าสุด:_` เดียว | **1,397 คำ ซ้อน 12 ชั้น** = 12% | 600 คำ ซ้อน 3 ชั้น |
| section "สถานะงานล่าสุด" (สำเนาของ Roadmap) | 2,061 คำ = 18% | 127 คำ |
| Known Issues | 5,132 คำ = 45% | 631 คำ |

ลำดับ: ① ยกความรู้แพลตฟอร์มขึ้น → ② ลบ section สถานะ ใส่ pointer ไป `05-Roadmap-Tracker.md` → ③ แยกการสอบสวนยาว >500 คำ เป็นไฟล์เลข (แบบที่ accounting `07-`–`12-` ทำอยู่แล้ว) → ④ บีบ prose ที่เหลือ → ⑤ ตัด ID ที่ซ้ำกับ BuildSpec §1

เป้าหมาย ~6,000 คำต่อไฟล์ · `_อัปเดตล่าสุด:_` เหลือ 1 รายการ ประวัติอยู่ใน commit

### D. เรื่องที่ต้องตัดสินใจ ไม่ใช่แค่แก้ไฟล์

| # | เรื่อง | หลักฐาน |
|---|---|---|
| D-1 | **API-Lab อยู่ผิด repo** — เป็นงานทดลองคนละแอป ควรแยกออกไป (ยังไม่ได้ย้ายมาใน repo นี้) และเอกสารของมันสั่งพิสูจน์ด้วย `get_record_logs` หลายจุด ผู้ดูแลระบุว่า **outdate** ⇒ ปั๊ม `DEPRECATED` ที่หัวไฟล์แทนการลบ ก่อนที่ agent จะไปดูดวิธีเก่ากลับมาใช้ · 🔶 **ทำแล้วบางส่วน 29 ส.ค. 2569**: ปั๊มแบนเนอร์ DEPRECATED ที่หัว `00-README.md` / `01-BRD.md` / `04-CLAUDE-memory.md` (ตัวที่มีความเสี่ยงสูงสุดที่ agent จะโหลดมาทำตาม โดยเฉพาะ `04` ที่เขียนไว้ว่า "โหลดทุก session") **เหลือ**: `02-BuildSpec-FRS.md` (ไฟล์ใหญ่ที่สุด) · `03-RTM-Status.md` · `05-Roadmap-Tracker.md` ยังไม่ได้ปั๊ม — เนื้อหายังถูกต้องในแง่ข้อเท็จจริงเกี่ยวกับ REST/MCP capability (ถูก patch เข้าสกิลแล้ว) ความเสี่ยงคือมีแค่ประโยค `get_record_logs` เป็นหลักฐาน "workflow ทำงานจริง" กระจายอยู่หลายจุด ⇒ agent ควรอ่านผ่าน lens นี้จนกว่าจะปั๊มครบ · **ยังไม่ได้ย้ายเอกสารออกจาก repo/project นี้จริง** (แค่ทำเครื่องหมาย) เพราะการย้ายจริงต้องตัดสินใจเรื่อง repo ปลายทางก่อน |
| D-2 | **แอป `f21b5c19` "ERP" 35 worksheets ไม่มีเอกสารกำกับเลย** — ✅ **ตรวจเบื้องต้นแล้ว 29 ส.ค. 2569 ผ่าน `hap app info`**: โครงสร้างเกือบเหมือน module บัญชีใน TILSNA เป๊ะ — section เดียวกันทุกชื่อ (AC-00 ตั้งค่า ... AC-08 เชื่อมระบบ), worksheet ชื่อเดียวกันเกือบทั้งหมด (สมุดรายวัน, ผังบัญชี, ใบสำคัญ, ใบสำคัญตั้งหนี้ ฯลฯ) แต่ **section AC-05 ลูกหนี้และรายได้ / AC-06 สินทรัพย์ถาวร / AC-07 ปิดงวด ว่างเปล่าไม่มี worksheet เลย** และ alias ส่วนใหญ่เป็น auto-gen (`worksheet`, `worksheet1`, ...) ไม่ใช่ alias ที่ตั้งใจแบบ TILSNA (`ac_period` ฯลฯ — มีบางตัวเท่านั้นที่ตั้งชื่อแล้ว) **ข้อสรุปที่เป็นไปได้สูงสุด: นี่คือ prototype/ต้นแบบเดิมของ module บัญชีที่สร้างไว้ก่อน แล้วภายหลังงานจริงไปต่อในแอป `deca7391` (TILSNA) แทน โดยไม่ได้ลบแอปนี้ทิ้ง** 🟡 (อนุมานจากโครงสร้าง ยังไม่ได้ยืนยันกับผู้ดูแลหรือเช็ค record/audit log ว่าใครสร้างเมื่อไหร่) **ยังไม่ตัดสินใจ**: ถ้าใช่ต้นแบบเก่าจริง ควร archive/ลบทิ้งเพื่อไม่ให้สับสน — แต่การลบแอปเป็นการกระทำที่ย้อนไม่ได้ ⇒ **รอผู้ดูแลยืนยันก่อนแตะ** ห้าม agent ลบเองเด็ดขาด |
| D-3 | **`ac_gl` ว่าง 0 แถว ทั้งที่ WF-AC-02 เปิดอยู่** — ✅ **ตรวจแล้ว 29 ส.ค. 2569 สรุปว่าไม่ใช่บั๊ก**: `get_record_list` ยืนยัน AC_GL = 0 แถวจริง แต่ไล่ดูใบสำคัญ 5 ใบที่เหลือ (prefix `ZZTEST-`) ทั้งหมดถูกสร้างผ่าน `user-api` (ไม่ใช่ `user-workflow`) คือ seed มาตรงๆ ไม่ผ่าน trigger จริง มีใบเดียว (`ZZTEST-REV-001`) ที่ field `voucher_status` ถูกตั้งเป็น "ผ่านรายการแล้ว" ด้วยมือ/สคริปต์ แต่ `posted_flag`/`posted_at` ยังว่างเปล่าและ `_updatedBy` ยังเป็น `user-api` — แปลว่า WF-AC-02 (workflow ผ่านรายการจริง) ไม่เคยถูกยิงกับข้อมูลชุดนี้เลย จึงไม่มีอะไรไปสร้างแถวใน AC_GL ⇒ ว่างถูกต้องแล้ว ยังไม่ใช่หลักฐานว่า WF-AC-02 พัง 🟢 (ยืนยันด้วยข้อมูลจริงผ่าน MCP) · เรื่องที่ต้องแยกตัดสินใจ: 5 ใบ `ZZTEST-*` นี้เป็น test data ที่ควรลบทิ้งก่อน go-live หรือเก็บไว้เป็น regression fixture — ยังไม่ตัดสิน |
| D-4 | **`_owner = user-undefined` บนแถวที่ workflow สร้าง** ⇒ role ที่ตั้ง scope "เฉพาะของตัวเอง" มองไม่เห็น — ✅ **ตรวจแล้ว 29 ส.ค. 2569 ผ่าน `get_role_details` เทียบ 3 role**: `recordDataScope` เป็น `{read, edit, delete}` แบบตัวเลข (10/20/100 ฯลฯ) ที่ความหมายจริงไม่มีเอกสารในแอป (`get_app_knowledge_list` ว่างเปล่า) แต่เทียบข้าม role แล้วสรุปได้ชัดว่า **`20` = ค่า default ที่ยังไม่ได้ตั้ง** (ไม่ใช่ "เฉพาะของตัวเอง" ตามที่ตั้งใจ) เพราะ: AC-R7 Requester (ควรเป็น own-only) มี `{20,20,20}` **เหมือนกันทุก worksheet ทั้ง 69 ตาราง แม้แต่ตารางตั้งค่าที่ไม่เกี่ยวกับ requester เลย** และ AC-R5 Internal Auditor (ควรเห็นทุกอย่างแบบไม่จำกัด) ก็มี `{20,20,20}` เหมือนกันทุกตารางเช่นกัน — ในขณะที่ AC-R1 Accounting Officer ซึ่งน่าจะถูกปรับมาบ้างแล้ว กลับมีค่าไม่สม่ำเสมอ (`{20,100,100}` บนบางตาราง เช่น "ใบขออนุมัติเบิกจ่าย") พิสูจน์ว่า `100` คือค่าที่ถูกยกสิทธิ์ขึ้นจริง ส่วน `20` คือ baseline **สรุป: ยังไม่มี role ไหนถูกตั้ง scope "เฉพาะของตัวเอง" จริงในระบบ** ⇒ ความเสี่ยงเรื่อง `_owner = user-undefined` มองไม่เห็น **ยังไม่เกิดขึ้นจริงตอนนี้** (เพราะยังไม่มีการจำกัด scope เลย) แต่จะกลายเป็นบั๊กทันทีที่ผู้ดูแลไปตั้งค่า AC-R7 ตาม Build Spec §1.4 ⇒ **ต้องเพิ่ม test case ใน Build Spec**: หลังตั้ง scope "เฉพาะของตัวเอง" แล้ว ต้องทดสอบว่า record ที่ owner เป็น `user-undefined` (สร้างผ่าน workflow) ยังมองเห็น/แก้ไม่ได้ตามที่ตั้งใจหรือไม่ ก่อน sign-off 🟢 (เทียบข้อมูลจริง 3 role ผ่าน MCP) |
| D-5 | **ยังไม่เคยสำรองแอป** — ✅ **ทำแล้ว 29 ส.ค. 2569**: `hap app backup deca7391-1761-424b-9af3-c8d043004ad3` รันจาก cloud sandbox ของ agent (ยืนยันว่า `hap` ใช้งานได้จากที่นั่นและชี้แอปถูกต้อง — ดู `ENV.md`) ยืนยันสำเร็จผ่าน `hap app backup-logs`: ไฟล์ `Manual Backup_ERP - TILSNA_20260829_0206`, 357 แถว, 69 worksheet · **ของค้างต่อ**: นี่คือ manual backup ครั้งเดียว ยังไม่มีรอบสำรองประจำ — ควรตั้งเป็นงาน recurring (cron/scheduled task) แยกต่างหาก |

---

## ก่อนให้ agent เริ่มทำงานจาก repo นี้

```bash
git init && git branch -M main
git remote add origin <private repo>
git config user.name "agent-ac"        # หรือ agent-hr — หนึ่ง clone ต่อหนึ่งสายงาน
git config user.email "agent-ac@tilsna.local"

SKILLDIR=$(dirname "$(find /mnt/skills -name preflight.sh -path '*nocoly-build-docs*' | head -1)")
cp "$SKILLDIR"/*.sh tools/ && chmod +x tools/*.sh
./tools/check-repo.sh                  # ต้องได้ "พร้อมใช้"

git add -A && git commit -m "chore: migrate TILSNA docs into repo (structure only)"
git push -u origin main
```

**commit แรกต้องเป็นสภาพเดิมทุกตัวอักษร** — นั่นคือสิ่งที่ทำให้งานข้อ C (ซึ่งกู้ไม่ได้ในโลกที่ไม่มี git) กลายเป็นงานที่ปลอดภัย
