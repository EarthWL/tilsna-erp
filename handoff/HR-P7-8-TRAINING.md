# HANDOFF · HR/P7-8-TRAINING · agent-hr → ยังไม่มี · 6 ก.ย. 2569

## ยิงไปแล้วบนเซิร์ฟเวอร์ (ย้อนอัตโนมัติไม่ได้ — ห้ามเว้นว่าง)

- **`hr_course`** `6a9d67b41204328eb1ade840` — worksheet ใหม่ 10 ฟิลด์ · กลุ่ม HR-05 Talent · ✅ verify ด้วย `worksheet fields --raw`
- **`hr_training`** `6a9d67bd4720c515252abedb` — worksheet ใหม่ 13 ฟิลด์ · ✅ verify แล้ว
- **View 4 อันบน `hr_training`** — `6a9d686f4a73a3142151a9c0` (board) · `6a9d68711204328eb1ade862` (แผนอบรมที่ยังไม่เกิดขึ้น ✅ 4 แถว) · `6a9d68724a22ad87b727a869` (ใบรับรองที่มีวันหมดอายุ ✅ 1 แถว) · `6a9d68741204328eb1ade864` (calendar)
- **ข้อมูล demo ค้างอยู่ตั้งใจ** — หลักสูตร 5 · การเข้าอบรม 12 (rowid ของหลักสูตรครบใน `../projects/hr/17-ID-Registry-HR.md`)
- **เอกสาร:** `17-ID-Registry-HR.md` · `05-Roadmap-Tracker.md` (P7-8 → ✅ · P7 56%→63%) · `19-Change-Log-HR.md` · `23-Pending-Human-Review.md` (เพิ่ม 1 ข้อ)

## ทำถึงไหน (เทียบ DoD)

- ✅ สร้าง 2 ตารางครบตามสเปก `21-FRS-Modules-HR.md` หัวข้อ 23A/23B
- ✅ view Kanban ผู้สมัคร (ทำไปแล้วรอบก่อน) + view ของโมดูลอบรม
- ⬜ **IX-23.1 unique index** (`หลักสูตร`, `พนักงาน`, `วันที่กำหนดอบรม`) — Browser เท่านั้น ยังไม่ทำ
- ⬜ ผูก `สถานะการอบรม` เข้า shared optionset จริง — Browser เท่านั้น

## ที่ตั้งใจจะทำต่อ

- workflow เตือนใบรับรองใกล้หมดอายุ (date-field trigger บน `6a9d67ed4a22ad87b727a848`) — ฟิลด์เตรียมไว้แล้ว ยังไม่อยู่ใน Roadmap เป็นแถวของตัวเอง
- P6-6 WF-HR-12 เมื่อ agent-ac ตอบคำถาม 3 ข้อใน `HR-P6-5-WF11.md`

## 🟢 แพทเทิร์นใหม่ที่ควรใช้ต่อ (ฝากถึง agent-ac ด้วย)

**ตั้ง option key ของฟิลด์ inline ให้ตรงกับ shared optionset ตั้งแต่ตอนสร้าง** — `สถานะการอบรม` ใช้ key ชุดเดียวกับ `OS_HR_TRAINING_STATUS` `738a7d5c-1c93-4cd2-b79c-468d389949a9` ทุกตัว ⇒ เมื่อผูกเข้า optionset จริงผ่าน Browser ภายหลัง **ข้อมูลที่ seed ไว้จะไม่หลุด** · ตารางที่สร้างก่อนหน้านี้ (รวมฝั่งบัญชี) ที่ใช้ key สุ่มจะต้อง remap ข้อมูลตอนผูก

## ระเบิดที่ฝังไว้ / ข้อควรระวัง

- 🔴 **หมวดหลักสูตร 6 ค่าเป็นค่าที่ agent ตั้งเอง** สเปกไม่ได้ระบุไว้ — ลงใน `23-Pending-Human-Review.md` แล้ว **ถ้าจะแก้ ต้องแก้ก่อนมีข้อมูลจริง** เพราะตอนนี้มี record demo ผูกกับ key เหล่านี้อยู่ 5 แถว
- ยังไม่มี unique index ⇒ **สร้างการอบรมซ้ำ (คนเดิม หลักสูตรเดิม วันเดิม) ได้** จนกว่าจะตั้ง IX-23.1
- `hr_training.ผ่านการอบรม` (Checkbox) ตอนนี้ seed ด้วยมือให้สอดคล้องกับสถานะ — **ยังไม่มี workflow บังคับให้ตรงกัน** ถ้าแก้สถานะเป็น Passed ธงจะไม่ขยับเอง
