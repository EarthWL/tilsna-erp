# Handoff — AC/AR-MODULE · โมดูลลูกหนี้ (AR) + เก็บงาน AP ค้าง

**agent-ac · 6 ก.ย. 2569** · แอป `deca7391-1761-424b-9af3-c8d043004ad3`

## ยิงไปแล้วบนเซิร์ฟเวอร์

| สิ่งที่ทำ | เป้าหมาย | รายละเอียด | ย้อนกลับได้ไหม |
|---|---|---|---|
| สร้างตาราง | 4 ตารางใหม่ | `ac_ar_invoice` `6a9cec4c4a22ad87b7279fb5` · `ac_ar_invoice_line` `6a9cecc44a22ad87b7279fd7` · `ac_ar_receipt` `6a9cece94720c515252ab4cb` · `ac_ar_receipt_line` `6a9cecfe4a73a3142151a141` | ลบตารางได้ (ข้อมูลหายหมด) |
| สร้าง record | `AC_COA` | บัญชี `110303 ภาษีเงินได้ถูกหัก ณ ที่จ่าย` rowid `58c54334-dd98-4d69-9e47-55622b25c211` | ลบได้ถ้ายังไม่มี GL อ้าง (ตอนนี้มีแล้ว 96,550) |
| สร้าง record | `กฎการอนุมัติตามวงเงิน` 3 แถว | บันไดของเอกสาร AP — **ค่าที่ agent เลือกเอง ดู AC-R-21** | ลบได้ |
| เพิ่มฟิลด์ | — | ไม่ได้เพิ่มฟิลด์ในตารางเดิม | — |
| เขียน record | `AC_AP` 32 แถว | เติม `ap_no` = `AP-2569/0001–0032` | ล้างค่าได้ |
| สร้าง record | AR 31 ใบ + 44 บรรทัด · ใบเสร็จ 21 ใบ + 21 บรรทัด · ทะเบียนภาษีขาย 25 แถว | ข้อมูลจำลอง | ลบได้ แต่ต้องกลับรายการใบสำคัญด้วย |
| **ลงบัญชีจริง** | `AC_VOUCHER` +52 ใบ · `AC_GL` **+66 แถว** | SV-2569/0001–0030 · RV-2569/0001–0020 · SV-AR-2569/0031 · RV-RC-2569/0021 · PMV-PAY-2569/0006 | 🔴 กลับรายการเท่านั้น |
| เขียน record | `AC_TAX_FILING` PP30 3 แถว | เติมภาษีขายและภาษีที่ต้องชำระสุทธิ | ล้างค่าได้ |
| เขียน record | `AC_PAYMENT` PAY-2569/0006 | เปลี่ยนสถานะเป็น "จ่ายแล้ว" เพื่อทดสอบ WF-AC-26 (เงินยังไม่ได้จ่ายจริงในโลกจริง — เป็นข้อมูลจำลอง) | เปลี่ยนสถานะกลับ + กลับรายการใบสำคัญ |
| **สร้าง workflow** | 3 ตัว + 2 สายลูก | WF-AC-31 `6a9cf60ad91d10186d8b20c2` (inner `…223f`) · WF-AC-32 `6a9cf4fae589b8933d781665` · WF-AC-26 `6a9cf6edd91d10186d8b28a9` (inner `…2975`) — **เผยแพร่แล้ว = ทำงานอยู่** | ปิดหรือลบ workflow ได้ |
| ลบ record (soft) | `ac_ar_invoice` 1 + line 2 | ใบซ้ำ `AR-2569/0024` ที่เกิดจากสคริปต์ timeout กลางคัน (ไม่มีใบสำคัญ) — อยู่ในถังขยะ กู้คืนได้ | กู้จากถังขยะ |

**ไม่ได้แตะ:** ตาราง HR ทุกตาราง · workflow เดิมทุกตัว · ใบสำคัญและ GL ที่มีอยู่ก่อนหน้า · PAY-2569/0007 (ยังรออนุมัติ)

## สิ่งที่ agent ตัวถัดไปควรรู้

- **D-47** (MIGRATION.md) — วิธีสร้าง `sub_process` ที่ใช้ได้จริง (สร้าง block เปล่า → ใส่โหนดในสายลูก → publish สายลูกก่อน) และการส่ง rowid ผ่าน `process_variable` เข้าฟิลด์ Relation
- alias `ac_ar` / `ac_ar_line` ถูกจองไว้แล้ว สร้างไม่ได้ ต้องใช้ชื่ออื่น
- **AC-R-21 / R-22 / R-23** เป็นสมมติฐานที่ agent ตั้งเอง ต้องให้ฝ่ายบัญชียืนยัน
- งานถัดไปที่ชัดเจน: **WF-AC-30** คิดภาษีขายระดับบรรทัด · ให้ WF-AC-32 ตัดยอดลูกหนี้บนหัวใบแจ้งหนี้ · CN/DN
