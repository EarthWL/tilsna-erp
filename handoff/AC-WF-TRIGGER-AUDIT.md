# HANDOFF · AC/WF-TRIGGER-AUDIT · agent-ac → ยังไม่มี · 7 ก.ย. 2569

## ยิงไปแล้วบนเซิร์ฟเวอร์ (ย้อนอัตโนมัติไม่ได้ — ห้ามเว้นว่าง)

**1. สร้าง workflow ใหม่ · WF-AC-32 ออกใบสำคัญรับเงินจากใบเสร็จรับเงิน (v2)**
`6a9df302d91d10186d917569` · trigger `add_or_update` + triggerFields [สถานะใบเสร็จ] · 16 node · **publish แล้ว = ทำงานอยู่**
โครง node ลอกจากตัวเดิมทั้งหมด ไม่ได้เปลี่ยนตรรกะบัญชี

**2. ลบ workflow เดิม** `6a9cfdb62fe3e8d6b3b6b235` (ปิดก่อนด้วย `--disable` → ทดสอบตัวใหม่ผ่าน → แล้วจึงลบ)

**3. ข้อมูลทดสอบ — สร้างแล้วลบครบ** ใบเสร็จ `ZZTEST-RE-ADD-01` + ใบสำคัญ `RV-ZZTEST-RE-ADD-01` + รายการ 3 + GL 3 แถว
ตรวจหลังลบ: ใบสำคัญ 115 · รายการในใบสำคัญ 388 · GL 386 · **Dr = Cr = 29,960,097.42** · แถวกำพร้า 0 ✓

**ไม่ได้แตะ**: workflow อื่นทั้ง 18 ตัว · ตาราง · ฟิลด์ · ข้อมูลจริง · ฝั่ง HR

## ทำถึงไหน (เทียบ DoD — ผู้ใช้เลือกงาน 4 ข้อ)

- ✅ **ไล่ตรวจ trigger 19 ตัว** — เสร็จ พร้อมแผนที่ `triggerId` ที่ยืนยันแล้ว (D-69)
- ✅ **แก้ AC-R-34** — เสร็จ ทดสอบเคสที่เดิมพังผ่าน
- ❌ **AC-R-33 / AC-R-36 ให้ workflow ออกใบสำคัญเอง** — ยังไม่เริ่ม
- ❌ **AC-R-27 งบการเงินคำนวณใหม่เอง** — ยังไม่เริ่ม
- ❌ **เก็บกวาดทะเบียนของค้าง** — ยังไม่เริ่ม
- ✅ เอกสาร `32-Workflow-Trigger-Audit.md` · D-69/70 · ปิด AC-R-34 · เปิด **AC-R-43**
- ❌ `git push github main` — งานของคน/ปลายวัน

## ที่ตั้งใจจะทำต่อ

1. **AC-R-43** — อีก 13 workflow ที่มองไม่เห็นการสร้างระเบียน · เรียงลำดับความเสี่ยงไว้แล้วในเอกสาร 32 §4 · สูตรทำซ้ำอยู่ §5
2. AC-R-33 / AC-R-36 / AC-R-27 ตามที่ผู้ใช้เลือกไว้

## ระเบิดที่ฝังไว้ / ข้อควรระวัง

- 🔴🔴 **AC-R-43 คือของจริงที่ยังไม่ได้แก้** — เอกสารที่คีย์มาพร้อมสถานะปลายทาง หรือที่ระบบอื่นเขียนผ่าน API **จะไม่ปลุก workflow ตัวไหนเลย** · ที่อันตรายที่สุดคือ **WF-AC-24 กันใบแจ้งหนี้ซ้ำ ที่ไม่ทำงานตอนสร้างเอกสาร** ⇒ อย่าเพิ่งต่อ API รับข้อมูลจากระบบอื่น (AC-08) จนกว่าจะแก้
- ⚠️ **WF-AC-32 v2 เปลี่ยนชื่อไปแล้ว** — มีคำว่า `(v2)` ต่อท้าย · เอกสาร/สคริปต์เก่าที่อ้าง processId `6a9cfdb6…` จะหาไม่เจอ ใช้ `6a9df302d91d10186d917569` แทน
- ⚠️ **trigger กว้างขึ้นแล้ว** — WF-AC-32 v2 ยิงตอนสร้างด้วย · ที่กันซ้ำได้คือ branch `b_go` ที่เช็ค "ยังไม่มีใบสำคัญ" · **ถ้าใครไปแก้ branch นั้นออก จะได้ใบสำคัญซ้ำทันที**
- ⚠️ **ห้ามเดาความหมาย `triggerId`** — ดู D-69 ก่อนอ่านผลจาก `hap workflow get`
