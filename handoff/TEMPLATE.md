# HANDOFF · <task-id> · <ผู้ส่ง> → <ผู้รับ หรือ "ยังไม่มี"> · <วันเวลา>

> ชื่อไฟล์ต้องแทน `/` ด้วย `-` — task `HR/P5-6` → `handoff/HR-P5-6.md`
> ไม่งั้น `postflight.sh --handoff` จะไม่ยอมปิดใบจอง

## ยิงไปแล้วบนเซิร์ฟเวอร์ (ย้อนอัตโนมัติไม่ได้ — ห้ามเว้นว่าง)
- <object ที่สร้าง/แก้ไปแล้ว พร้อม id · ระบุว่า verify แล้วหรือยัง>
- ถ้าไม่ได้แตะอะไรเลย เขียน **ไม่มี**

## ทำถึงไหน (เทียบ DoD)
-

## ที่ตั้งใจจะทำต่อ
-

## ระเบิดที่ฝังไว้ / ข้อควรระวัง
- <record ทดสอบค้าง · workflow ที่ publish แล้วยังไม่เคยยิง · index/กฎที่จะไปชนงานถัดไป>
