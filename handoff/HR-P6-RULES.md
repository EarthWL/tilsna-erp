# HANDOFF · HR/P6-RULES · agent-hr → agent-ac · 6 ก.ย. 2569

## ยิงไปแล้วบนเซิร์ฟเวอร์ (ย้อนอัตโนมัติไม่ได้ — ห้ามเว้นว่าง)

- **Lookup 2 ฟิลด์บน `hr_claim`** — `6a9d206e4a73a3142151a543` (วงเงินสูงสุดต่อครั้ง) · `6a9d206e4a73a3142151a544` (ต้องแนบใบเสร็จ) · ✅ อ่านค่าที่ดึงมาได้จริงทั้ง 3 record
- **Business Rules 4 กฎบน `hr_claim`** — `6a9d20fd4a22ad87b727a403` (BR-19.1) · `6a9d20c04720c515252ab959` (BR-19.2) · `6a9d20fe4720c515252ab95f` (BR-19.3) · `6a9d20ff4720c515252ab961` (BR-19.4) · ✅ **enabled ทั้งหมด** และพิสูจน์บนฟอร์มจริงแล้ว 3 ใน 4 (BR-19.1 พิสูจน์ทางลบ = ไม่ยิงเมื่อไม่ควรยิง)
- **`showControls` ของ relation `6a9d1a084a22ad87b727a373`** ตั้งใหม่ 5 คอลัมน์
- **เอกสาร:** `../shared/00-HAP-Working-Guide.md` §15 (ใหม่) + แก้ตารางเลือก surface ใน §1 · `17-ID-Registry-HR.md` · `05-Roadmap-Tracker.md` (P6-4 → 🔶, P6 42%→50%, รวม 46%→47%) · `19-Change-Log-HR.md`
- ข้อมูลทดสอบ **คืนค่าครบแล้ว** (บรรทัด 1,400→1,200 · Rollup กลับ 1,500 · ล้าง `เหตุผลที่ไม่อนุมัติ`)

## ทำถึงไหน (เทียบ DoD)

- ✅ BR-19.1 · BR-19.2 · BR-19.3 · BR-19.4 สร้างครบและ enabled
- ⬜ **DV-19.1 ยังไม่ทำ** — `balance_snapshot` ยังเป็นค่าที่ seed ด้วยมือ ⇒ **BR-19.1 ยังไม่ผูกกับวงเงินคงเหลือจริงใน `hr_welfare_balance`**
- ⬜ DoD ของ AC-10 ("บล็อกเมื่อเกินวงเงิน · อนุมัติแล้ววงเงินลด · เกิด `ac_pay_req` 1 ใบ") ยังไม่ครบ — ผ่านเฉพาะข้อแรกและเฉพาะเมื่อ snapshot มีค่าถูก

## ที่ตั้งใจจะทำต่อ

- P6-5 **WF-HR-11** อนุมัติใบเบิก + ตัดวงเงิน (จะเติม `balance_snapshot` จาก `hr_welfare_balance` ที่ workflow ด้วย แทน DV-19.1)
- P6-6 **WF-HR-12** ส่งเข้า `ac_pay_req` `6a8677b19b6999a714d2aa83` — 🔴 **ต้องคุยกับ agent-ac ก่อน** เรื่อง relation `pay_req_ref` และรูปแบบใบขออนุมัติเบิกจ่ายที่ฝั่งบัญชียอมรับ

## 🔴 สิ่งที่ agent-ac ควรอ่านทันที

1. **Business Rule สร้างผ่าน `hap worksheet save-rule` ได้แล้ว** — ไกด์ §1 เดิมบอก Browser only · ฝั่งบัญชีที่ยังค้างกฎฟอร์มรอ Browser อยู่ ทำผ่าน CLI ได้เลย (§15.1)
2. 🔴🔴 **`checkType: 1` ไม่บล็อกการเขียนผ่าน API/CLI** — ถ้าฝั่งบัญชีเคยสรุปว่า "กฎทำงาน" เพราะ API เขียนผ่าน/ไม่ผ่าน **ข้อสรุปนั้นใช้ไม่ได้** ต้องเปิดฟอร์มจริงดู (§15.5) · และในทางกลับกัน **workflow/seed script เขียนข้อมูลที่ละเมิดกฎฟอร์มได้เสมอ** ⇒ ถ้าต้องกันจริงที่ระดับข้อมูลต้องมี gate ใน workflow ด้วย
3. 🟢 **ข้อจำกัด "Business Rule/Dynamic Default อ้าง relation field ไม่ได้" ข้ามได้ด้วย Lookup field (type 30)** — สร้างผ่าน CLI ได้ (§15.3) · น่าจะปลดงานฝั่งบัญชีที่เคยติดข้อนี้ได้หลายรายการ
4. **`ruleItems[].type` ที่ยืนยันแล้วมีแค่ 1/2/6/7** — ค่าอื่นห้ามเดา ถ้าอยาก "บังคับกรอก" ให้เขียนเป็น validation แทน (§15.2)

## ระเบิดที่ฝังไว้ / ข้อควรระวัง

- 🔴 **CLM-2569-0001 (และใบเบิกทุกใบที่สวัสดิการตั้ง `require_receipt` = จริง) จะบันทึกจากหน้าฟอร์มไม่ได้จนกว่าจะแนบไฟล์** เพราะ BR-19.3 ทำงานถูกต้อง — ถ้าจะสาธิตการแก้ใบเบิกบนหน้าจอ ต้องแนบไฟล์ก่อน หรือปิดกฎชั่วคราว
- BR-19.1 ตอนนี้เทียบกับ `balance_snapshot` ที่เป็นค่า seed มือ — **อย่าอ่านว่าระบบกันวงเงินได้จริงแล้ว**
- Lookup ทั้ง 2 ฟิลด์เป็น lazy — record ใหม่ที่สร้างผ่าน API ต้องเขียน relation `สวัสดิการ` ซ้ำหนึ่งครั้งค่าถึงจะเข้า มิฉะนั้น BR-19.2/19.3 จะไม่มีค่าไปเทียบและ **ไม่บล็อกทั้งที่ควรบล็อก**
