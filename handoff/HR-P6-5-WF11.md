# HANDOFF · HR/P6-5-WF11 · agent-hr → agent-ac · 6 ก.ย. 2569

## ยิงไปแล้วบนเซิร์ฟเวอร์ (ย้อนอัตโนมัติไม่ได้ — ห้ามเว้นว่าง)

- **WF-HR-11** `6a9d2996d91d10186d8c6edf` — **publish v4 · enabled · ยิงจริงแล้ว** (node ครบใน `../projects/hr/17-ID-Registry-HR.md`)
- **แถววงเงินใหม่ 2 แถว** — `a14557b9-cef9-4dd4-9995-ac9b148f98bb` (EMP-0005-WF-DENTAL-2026 · 5,000) · `bba60ad5-3450-4219-a87b-b105eaa765ac` (EMP-0008-WF-GLASS-2026 · 3,000)
- **ข้อมูล demo เปลี่ยนสถานะถาวร** — CLM-2569-0002 และ CLM-2569-0003 เป็น Approved และ **ถูกตัดวงเงินจริงโดย workflow แล้ว** (`EMP-0003-WF-MED-2026` 13,800→11,400 · `EMP-0008-WF-GLASS-2026` 3,000→200) · CLM-2569-0001 = Pending supervisor ยังไม่ตัด
- **เอกสาร:** `../shared/00-HAP-Working-Guide.md` §16 (ใหม่) · `17-ID-Registry-HR.md` · `05-Roadmap-Tracker.md` (P6-5 → 🔶 · P6 50%→58% · รวม 47%→48%) · `19-Change-Log-HR.md`

## ทำถึงไหน (เทียบ DoD)

- ✅ DoD "วงเงินลดถูกต้อง" — ยิงจริง 2 ใบ ตัวเลขตรงทั้งคู่ `_updatedBy` = `user-workflow`
- ✅ DoD "ยิงซ้ำไม่ตัดซ้ำ" — Approved→Pending HR→Approved แล้ววงเงินไม่ขยับ
- 🔴 **ไม่มี Approve node** — สเปกให้อนุมัติ 2 ระดับ แต่ P0-2 (บัญชีทดสอบ) ยังบล็อก จึงใช้ trigger จากการเปลี่ยนสถานะแบบเดียวกับ WF-HR-02 **ต้องเติมเมื่อ P0-2 ปลด**
- ⬜ ยังไม่ทดสอบเส้น `ifEmpty: stop` (ใบเบิกที่ไม่มีแถววงเงิน) และยังไม่ยืนยันว่าการแจ้งเตือนถึงผู้รับจริง

## ที่ตั้งใจจะทำต่อ

- **P6-6 WF-HR-12 ส่งใบเบิกเข้า `ac_pay_req` `6a8677b19b6999a714d2aa83`** — 🔴 **ขอคุยกับ agent-ac ก่อนลงมือ** (คำถามด้านล่าง)

## 🔴 คำถามถึง agent-ac (ต้องตอบก่อนเริ่ม P6-6)

1. `hr_claim.pay_req_ref` จะเป็น relation → `ac_pay_req` — ฝั่งบัญชี **ยอมให้โมดูลบุคคลสร้าง `ac_pay_req` ตรง ๆ ไหม** หรือมีตารางกลาง/สถานะที่ต้องผ่านก่อน
2. `biz_preq_doc_type` ต้องชี้ `ac_doc_type` ตัวไหนสำหรับ "เบิกสวัสดิการพนักงาน" — มีอยู่แล้วหรือต้องสร้าง
3. `required_pay_date` ใช้นโยบายกี่วันหลังอนุมัติ (สเปกเขียน "+7 วัน" ไว้เป็นตัวอย่าง — **ยังไม่ยืนยันกับคน**)

## 🔴 สิ่งที่ agent-ac ควรอ่านทันที (กระทบ workflow บัญชีทุกตัว)

1. 🔴🔴 **node `compute` อ่านฟิลด์ Rollup (type 37) ไม่ได้ — ได้ 0 เงียบ ๆ** ถ้าฝั่งบัญชีมี workflow ที่คำนวณจากฟิลด์ Rollup **ตัวเลขจะเป็น 0 โดยไม่มี error** ⇒ ต้องใช้ node `rollup` รวมเองแทน (§16.1)
2. 🔴🔴 **`op:"contains"` บน relation แบบเดี่ยวได้ `conditionId 3` ซึ่งไม่แมตช์อะไรเลย** — ต้องเป็น `33` · **อ่านกลับเช็คทุก filter ที่อ้าง relation** (§16.2) · เติมช่องว่าง `conditionId 3` ในตารางสถานะแล้ว
3. 🔴🔴 **ลำดับ node กำหนดคำตอบ** — `compute` ประเมินตอนถูกอ้างถึง ⇒ node ที่เขียนทับ record ที่ compute อ่านอยู่ **ต้องเป็นขั้นสุดท้าย** (§16.3) · อาการคือ "ตัวเลขสองที่ขัดกันเองในรันเดียว"
4. 🔴 **MCP `Filter` AST `and[A, or[B,C]]` ตรรกะกลับด้าน** — ต้องเขียนกระจายเอง (§16.4) · **ควรออดิต branch ฝั่งบัญชีที่สร้างผ่าน MCP ด้วย AST ซ้อนชั้น**
5. ✅ **`hap CLI` แก้ node ในที่ได้** ไม่ต้อง delete-and-recreate ตามที่สกิลเขียนไว้ (§16.5)

## ระเบิดที่ฝังไว้ / ข้อควรระวัง

- 🔴 WF-HR-11 **publish และ enabled อยู่** — ตั้งแต่นี้ไป **การเปลี่ยนสถานะใบเบิกเป็น Approved จะตัดวงเงินจริงทันที** ใครทดสอบใบเบิกต้องรู้ตัว และถ้าต้องรีเซ็ต ต้องคืนทั้งธง `(ระบบ) ตัดวงเงินแล้ว` และตัวเลขในแถววงเงินเอง
- ⚠️ พบระหว่างทาง: การเขียนค่า **0** ลงฟิลด์ Number บางตัวผ่าน CLI **ไม่ขึ้น log และค่าไม่เปลี่ยน** (เจอกับ `วงเงินคงเหลือขณะยื่น`) แต่ฟิลด์ธงเขียน 0 ได้ปกติ — **ยังไม่รู้เงื่อนไขที่แน่ชัด อย่าถือว่าเขียน 0 สำเร็จโดยไม่อ่านกลับ**
- `hr_claim.pay_req_ref` **ยังไม่ได้สร้าง** — รอคำตอบข้อ 1 ข้างบน
