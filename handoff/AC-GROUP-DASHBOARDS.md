# HANDOFF · AC/GROUP-DASHBOARDS · agent-ac → ยังไม่มี · 7 ก.ย. 2569

## ยิงไปแล้วบนเซิร์ฟเวอร์ (ย้อนอัตโนมัติไม่ได้ — ห้ามเว้นว่าง)

**หน้า custom page ใหม่ 4 หน้า** — verify แล้วโดยเปิดดูด้วยตาทุกหน้า

| หน้า | pageId | กลุ่ม | components |
|---|---|---|---|
| แดชบอร์ดลูกหนี้ | `6a9de8c82737a712c93df29d` | AC-05 `6a853a35e16cff5c409bc753` | 5 |
| แดชบอร์ดเจ้าหนี้ | `6a9de920dd75d529e3f64830` | AC-03 `6a8539cac011bf786fef6b59` | 4 |
| แดชบอร์ดภาษี | `6a9de92edd75d529e3f64832` | AC-04 `6a8539e70f7255ac5594e0d9` | 4 |
| แดชบอร์ดสินทรัพย์ | `6a9de93add75d529e3f64834` | AC-06 `6a853ab60f7255ac5594e0dc` | 4 |

**กราฟใหม่ 17 อัน** (report) ผูกกับตารางเดิม — ไม่ได้แก้ตารางหรือข้อมูลใด ๆ
รายชื่อและ reportId ครบอยู่ใน `projects/accounting/31-Group-Dashboards.md`

⚠️ **มีกราฟทดสอบ 1 อันค้างอยู่** — `ยอดค้างรับรวม` `6a9de895720a3fbb2b183338` บนตารางใบแจ้งหนี้ลูกหนี้
สร้างตอนทดสอบว่า CLI สร้างกราฟได้ไหม **ไม่ได้ถูกวางบนหน้าไหน** และมีกราฟชื่อเดียวกัน (`6a9de8b3720a3fbb2b183339`) ที่ใช้งานจริงบนแดชบอร์ดลูกหนี้แล้ว ⇒ **ควรลบทิ้ง** ยังไม่ลบเพราะไม่อยากแตะระหว่างที่หน้ายังไม่ถูกตรวจรับ

**ไม่ได้แตะ**: ตาราง · ฟิลด์ · ข้อมูล · view · workflow · แดชบอร์ดเดิมที่ AC-09 · ทุกอย่างฝั่ง HR

## ทำถึงไหน (เทียบ DoD)

- ✅ ครบทั้ง 3 ข้อที่ผู้ใช้เลือกแล้ว (view ตามสถานะ · รายงานอายุหนี้ · แดชบอร์ดรายกลุ่ม)
- ✅ ทั้ง 17 กราฟขึ้นครบตั้งแต่รอบแรก — เลี่ยง D-54 ด้วยการไม่ส่ง `filter` เลย
- ✅ ตรวจยอดกับข้อมูลจริง: ราคาทุนรวม 5,327,000 = 4,379,000 + 890,000 + 58,000 · สถานะสินทรัพย์ 6/2 ตรงกับที่อ่านจากตาราง
- ✅ เอกสาร `31-Group-Dashboards.md` · D-68 · AC-R-42
- ❌ `git push github main` — งานของคน/ปลายวัน

## ที่ตั้งใจจะทำต่อ

- **ลบกราฟทดสอบ `6a9de895720a3fbb2b183338`** หลังผู้ใช้ตรวจรับหน้าแดชบอร์ด
- **AC-R-34** 🔴 WF-AC-32 trigger `update` เท่านั้น — ยังค้าง ต้องแก้ก่อนให้ลูกค้าคีย์เอง
- **AC-R-40** ผลต่างบัญชีคุมเจ้าหนี้ 686,437.60 · **AC-R-29** ฝั่งลูกหนี้ 1,200,000 — รอฝ่ายบัญชี
- ยังไม่มีแดชบอร์ดให้ AC-07 / AC-08 (ข้อมูลยังน้อยเกินไป)

## ระเบิดที่ฝังไว้ / ข้อควรระวัง

- ⚠️ **กราฟวาดช้ากว่าหน้าโหลด** — ภาพแคปแรกจะเห็นแกนครบแต่ไม่มีแท่ง หน้าตาเหมือน D-54 · **รอแล้วแคปซ้ำก่อนสรุปว่าพัง** (D-68)
- ⚠️ **`page.save_page` แทนที่ทั้งหน้า** — จะเพิ่มกราฟทีหลังต้องอ่าน components เดิมมาต่อท้ายก่อน ไม่งั้นของเดิมหายหมด · และ `version` ต้องตรงกับของปัจจุบัน
- ⚠️ **กราฟที่ต้องแยกสีต้องใส่ทั้ง `split.controlId` และ `splitId`** ไม่งั้นถูกล้างเงียบ ๆ
- ⚠️ **`chart.get_chart` อ่าน config กลับไม่ได้ (D-67)** ⇒ แก้กราฟทีหลังต้องใช้ `hap --json worksheet chart get REPORT_ID --app-id WS_ID` แทน SDK
