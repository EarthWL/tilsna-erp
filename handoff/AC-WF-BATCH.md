# Handoff — AC/WF-BATCH

**agent-ac · 6 ก.ย. 2569** · workflow อัตโนมัติ 3 ตัว + โครง AC-08
เอกสารเต็ม: `projects/accounting/27-Workflows-AC08.md`

## ยิงไปแล้วบนเซิร์ฟเวอร์

| # | วัตถุ | id | สถานะ |
|---|---|---|---|
| 1 | WF-AC-33 ตัดยอดค้างรับเมื่อออกใบเสร็จรับเงิน | `6a9d2a302fe3e8d6b3b7cb9b` | publish · **ทดสอบผ่าน 2 เส้นทาง** |
| 2 | WF-AC-30 รวมยอดและภาษีขายขึ้นหัวใบแจ้งหนี้ | `6a9d2b34b6bb30e62dc34737` | publish · **ทดสอบผ่าน + ด่านกันแก้เอกสารที่ผ่านรายการแล้ว** |
| 3 | WF-AC-07 คิดค่าเสื่อมราคาประจำเดือน (สายลูก `6a9d2c5a8475f61d4c73287d`) | `6a9d2c478475f61d4c7327d9` | publish · ⚠️ schedule ยิงทดสอบผ่าน API ไม่ได้ |
| 4 | ws `รอบการนำเข้าข้อมูล` `ac_intake_batch` | `6a9d2cfdf363582dd37932df` | 12 ฟิลด์ + 1 รอบ |
| 5 | ws `รายการข้อมูลที่รับเข้า` `ac_intake_row` | `6a9d2d014720c515252abada` | 13 ฟิลด์ + 5 แถว |
| 6 | ws `ผังแมปฟิลด์และบัญชี` `ac_field_map` | `6a9d2d044720c515252abae4` | 8 ฟิลด์ + 5 รายการ |

**ลบทิ้งระหว่างทาง (เป็นเวอร์ชันที่ออกแบบผิด สร้างแล้วรื้อทันที ไม่เคยใช้งานจริง):** `6a9d294fd91d10186d8c6c78` (WF-AC-33 แบบสะสม) · `6a9d2afdb6bb30e62dc34538` (WF-AC-30 แบบ OR branch) · `6a9d2bf62fe3e8d6b3b7d747` (WF-AC-07 แบบไม่มีด่านกันซ้ำ)

**ไม่ได้แตะ:** GL · ใบสำคัญเดิม · งบการเงิน · ตาราง HR ทุกตาราง · WF-AC-01/31/32 และ workflow เดิมทั้งหมด

## ผลตรวจหลังทำ

```
GL 378 แถว · Dr = Cr = 29,011,130.75 · ไม่มีบัญชีเสีย  (เท่ากับก่อนเริ่มงานเป๊ะ)
ค้างรับ ใบกำกับ 2,018,890.00 = ใบแจ้งหนี้ 2,018,890.00 ✓
ใบเสร็จ 22 · ใบแจ้งหนี้ 32 · รายการรายได้ 43  (เท่าก่อนทดสอบทุกตัว — เก็บกวาดครบ)
งบทดลองรวมทั้งปี Dr = Cr = 29,011,130.75 ตรงกับ GL ⇒ ไม่ต้องคำนวณงบใหม่
AC-08 รอบนำเข้า IB-2569/0001 · Dr 500,580.00 = Cr 500,580.00 · ยังไม่ผ่านรายการ (ตั้งใจ)
```

## 🔴 ถึงคนที่จะเดโม — ต้องรู้ก่อน

**AC-R-34: WF-AC-32 ไม่ยิงถ้าใบเสร็จถูกสร้างมาพร้อมสถานะ "รับเงินแล้ว"**
trigger เป็นแบบ `update` เท่านั้น ⇒ ถ้าลูกค้าคีย์ใบเสร็จแล้วเลือก "รับเงินแล้ว" ตั้งแต่แรก **จะไม่มีใบสำคัญเกิดขึ้นเลย** แต่ยอดค้างรับถูกตัดไปแล้ว (WF-AC-33 ใช้ `add_or_update`) ⇒ บัญชีย่อยกับบัญชีคุมเพี้ยนเงียบ ๆ
**ถ้าจะให้ลูกค้าลองคีย์เอง ต้องแก้ข้อนี้ก่อน** หรือสาธิตโดยบันทึกเป็น "ร่าง" ก่อนแล้วค่อยเปลี่ยนสถานะ

## 🔴 ถึง agent-hr — สามบทเรียนที่กระทบ workflow ทุกตัว

**D-59 `triggerFields` ไม่ได้จำกัดให้ยิงเฉพาะตอนฟิลด์นั้นเปลี่ยน — update ฟิลด์ไหนก็ยิง**
workflow ที่เขียนยอดแบบ "บวกเพิ่ม" จะคิดซ้ำทันทีที่มี workflow อื่นเขียนอะไรกลับลง record เดียวกัน
🔑 **ทางแก้คือทำให้ idempotent (rollup รวมใหม่ทุกครั้ง) ไม่ใช่พยายามกันไม่ให้ยิงซ้ำ** · ตรวจโดยอ่านค่าซ้ำหลังยิง 2 รอบ ต้องไม่ขยับ

**D-60** `rollup method:"count"` ต้องมี `config.target` (นับ record จาก node ต้นทางเท่านั้น) · `"filled"` ถูกปฏิเสธจาก enum ⇒ ใช้ `sum` แล้วเทียบ `> 0` แทน

**D-61** ลบใบสำคัญแล้วแถว GL ที่มันผ่านรายการไว้ **ไม่หายตาม** — กลายเป็นแถวกำพร้าที่ยังนับรวมในยอด GL · เก็บกวาดต้องตามลบเองและเทียบจำนวนแถว GL กับก่อนเริ่มงาน

**ไอคอน:** ยังซ้ำ 2 คู่ในโมดูล HR (`sys_bill_finance`, `sys_bullet-list_office`) ดูรายละเอียดใน `handoff/AC-FA-CNDN.md`
