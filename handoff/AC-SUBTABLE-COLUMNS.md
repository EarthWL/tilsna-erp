# HANDOFF · AC/SUBTABLE-COLUMNS · agent-ac → ยังไม่มี · 6 ก.ย. 2569

## ยิงไปแล้วบนเซิร์ฟเวอร์ (ย้อนอัตโนมัติไม่ได้ — ห้ามเว้นว่าง)

**เปลี่ยน `showControls` (คอลัมน์ที่ตารางย่อยแสดง) ของฟิลด์ Relation 67 ช่อง ใน 24 worksheet แม่**
— verify แล้วทั้งอ่าน API กลับทีละช่อง สแกนซ้ำทั้งโมดูล และเปิดหน้าจอจริงดูด้วยตา

worksheet แม่ที่ถูกแก้: การกระทบยอดธนาคาร · การตั้งค่าเอกสาร · การยื่นแบบภาษี · คู่ค้า · งวดบัญชี · ช่องทางการจ่ายเงิน ·
บัญชีธนาคารของกิจการ · บัญชีแยกประเภททั่วไป · ประเภทเอกสาร · ผังบัญชี · สกุลเงิน · สินค้าและบริการ · หน่วยงาน/ศูนย์ต้นทุน ·
อัตราภาษีมูลค่าเพิ่ม · แหล่งเงิน · โครงการ · ใบกำกับภาษี · ใบขออนุมัติเบิกจ่าย · ใบวางบิล · ใบสำคัญ · ใบสำคัญจ่าย ·
ใบสำคัญตั้งหนี้ · ใบเสร็จรับเงิน · ใบแจ้งหนี้ลูกหนี้

**เปลี่ยนชื่อฟิลด์ 2 ตัวบน `ผังบัญชี` `6a85516e1049edca1eecd9b7`** (เดิมชื่อ "ประเภทสินทรัพย์" ซ้ำกันทั้งคู่) — verify แล้ว
- `6a9d40374a73a3142151a67e` → ประเภทสินทรัพย์ที่ใช้บัญชีนี้เป็นบัญชีขาดทุนจากการจำหน่าย
- `6a9d40374a73a3142151a680` → ประเภทสินทรัพย์ที่ใช้บัญชีนี้เป็นบัญชีกำไรจากการจำหน่าย

**ไม่ได้แตะ**: ข้อมูลในระเบียน · โครงสร้างฟิลด์ (ไม่ลบ ไม่เพิ่ม) · relation 10 ช่องที่ปลายทางเป็นตารางฝั่ง **HR** · view และกราฟ

## ทำถึงไหน (เทียบ DoD)

- ✅ สแกน 57 ตารางในโมดูล AC พบฟิลด์ Relation แบบรายการ 77 ช่อง
- ✅ ตรวจ alias ที่จะใช้กับของจริงก่อนยิงทุกตัว — รอบแรกจับได้ว่าเดาผิด 11 ตาราง จึงแก้แผนก่อน (เช่น `ใบสำคัญ.status` จริงคือ `voucher_status` ไม่ใช่ `status1` ตามที่ `00-README.md` เขียนไว้ — **ข้อนั้นใน README ผิด**)
- ✅ แก้ 67 ช่อง · สแกนซ้ำ: คอลัมน์ว่าง 0 · คอลัมน์ไร้ชื่อ 0 · เหลือ 6 ช่องที่ไม่มีคอลัมน์เงินซึ่งถูกต้องแล้ว (ข้อมูลหลักที่ไม่มีตัวเงิน)
- ✅ ยืนยันด้วยตาบนหน้าจอ `แหล่งเงิน / FUND-OWN` ทั้ง 2 แท็บ
- ✅ เอกสาร `projects/accounting/29-Subtable-Columns-Audit.md` · D-64 ใน `MIGRATION.md` · AC-R-38
- ❌ **ยังไม่ได้ `git push github main`** — งานของคน/ปลายวัน

## ที่ตั้งใจจะทำต่อ

- **AC-R-38** ฟิลด์ตายไร้ชื่อบน `ใบแจ้งหนี้ลูกหนี้` — เอาออกจากคอลัมน์แล้ว แต่ยังอยู่ในตาราง ต้องตรวจก่อนลบ
- ของค้างเดิม: **AC-R-34** 🔴 · AC-R-36 · AC-R-27

## ระเบิดที่ฝังไว้ / ข้อควรระวัง

- 🔴 **Relation ที่สร้างใหม่หลังจากนี้จะกลับมาเป็นคอลัมน์มั่วอีก** — แพลตฟอร์มเดาให้เสมอ ต้องตั้ง `showControls` เองทุกครั้ง (D-64)
- ⚠️ **CLI/MCP มองไม่เห็น `showControls`** — ตรวจต้องใช้ SDK `GetWorksheetInfo`
- ⚠️ **`00-README.md` ข้อ 1 ในตารางกับดักเขียนว่า `AC_VOUCHER.status` มี alias `status1`** — ของจริงคือ `voucher_status` (`6a86016b1049edca1eed028a`, type 11) · ยังไม่ได้แก้ README เพราะไม่แน่ใจว่ามี agent อื่นอ้างอิงข้อความนั้นอยู่ไหม — **ฝากตรวจ**
- ⚠️ **ตาราง master ฝั่ง AC มี relation ไปตาราง HR 10 ช่อง** (ทะเบียนพนักงาน · สลิปเงินเดือน · เหตุการณ์การจ้าง **ซ้ำ 2 ครั้ง** · ใบขออัตรากำลัง · สวัสดิการ · hr_pay_period ×2 · hr_pay_component · กฎการอนุมัติของโมดูลบุคคล) — คอลัมน์ยังเป็นค่าที่แพลตฟอร์มเดาให้ · **ผมไม่แตะเพราะอยู่นอกขอบเขต ส่ง D-64 ให้ agent-hr ตรวจฝั่งตัวเอง**
