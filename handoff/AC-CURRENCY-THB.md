# HANDOFF · AC/CURRENCY-THB · agent-ac → ยังไม่มี · 6 ก.ย. 2569

## ยิงไปแล้วบนเซิร์ฟเวอร์ (ย้อนอัตโนมัติไม่ได้ — ห้ามเว้นว่าง)

เปลี่ยน `advancedSetting.currency` จาก `{"currencycode":"CNY","symbol":"¥"}` เป็น `{"currencycode":"THB","symbol":"฿"}`
บน **ฟิลด์ Currency 20 ฟิลด์ · 9 ตาราง** — verify แล้วทั้งอ่าน API กลับและเปิดหน้าจอจริงดูด้วยตา

| ตาราง | wid | ฟิลด์ |
|---|---|---|
| งบทดลอง | `6a9d10b04720c515252ab7f6` | `biz_tb_debit` `biz_tb_credit` `biz_tb_balance` |
| งบกำไรขาดทุน | `6a9d10b5f363582dd3792f35` | `biz_is_amount` |
| งบแสดงฐานะการเงิน | `6a9d10b84a73a3142151a3f8` | `biz_bs_amount` |
| รอบการนำเข้าข้อมูล | `6a9d2cfdf363582dd37932df` | `biz_ib_total_amount` |
| รายการข้อมูลที่รับเข้า | `6a9d2d014720c515252abada` | `biz_ir_debit` `biz_ir_credit` |
| ทะเบียนสินทรัพย์ | `6a9d1a634a22ad87b727a37c` | `biz_ab_cost` `biz_ab_salvage` `biz_ab_accum_bf` `biz_ab_accum_depr` `biz_ab_nbv` |
| ตารางค่าเสื่อมราคา | `6a9d1a67f363582dd37930b3` | `biz_ds_amount` `biz_ds_accum_after` `biz_ds_nbv_after` |
| ใบคิดค่าเสื่อมราคา | `6a9d1a6a4a22ad87b727a388` | `biz_dp_total` |
| ใบตัดจำหน่ายสินทรัพย์ | `6a9d1a6df363582dd37930dd` | `biz_dsp_proceeds` `biz_dsp_nbv` `biz_dsp_gain_loss` |

**ไม่ได้แตะ**: ข้อมูลในระเบียน · ทศนิยม (`dot`) · ฟิลด์ Number (type 6) ทั้ง 149 ตัว · กราฟ 17 ตัวบนแดชบอร์ด · **ทุกตารางฝั่ง HR**

## ทำถึงไหน (เทียบ DoD)

- ✅ สแกนทั้งแอป 98 ตาราง (AC 57 · HR 41) ทุกฟิลด์ตัวเลข ดูทั้ง `unit` / `currency` / `prefix` / `suffix`
- ✅ แก้ครบ 20/20 ฟิลด์ · สแกนซ้ำเหลือ 0
- ✅ ตรวจกราฟบนแดชบอร์ดทั้ง 17 ตัว — ไม่มีสัญลักษณ์หยวนฝังอยู่ (ดึงรูปแบบจากฟิลด์)
- ✅ ยืนยันด้วยตาบนหน้าจอจริง 2 ตาราง (ทะเบียนสินทรัพย์ · งบทดลอง) ขึ้น `฿` ถูกต้อง
- ✅ บันทึก D-63 ลง `MIGRATION.md` · ภาคผนวกใน `28-Asset-Disposal.md`
- ⚠️ **ยิงก่อนจองใบจอง** — งานนี้เริ่มแก้ก่อนแล้วค่อยรัน preflight (ผิดลำดับตามโปรโตคอล) · ไม่มี agent อื่นแตะตารางเหล่านี้ระหว่างนั้น ตรวจแล้วไม่ชนกัน

## ที่ตั้งใจจะทำต่อ

- ไม่มีต่อเนื่องจากงานนี้ · ของค้างเดิมยังอยู่: AC-R-34 🔴 · AC-R-36 · AC-R-27

## ระเบิดที่ฝังไว้ / ข้อควรระวัง

- 🔴 **ฟิลด์ Currency ที่สร้างใหม่หลังจากนี้จะกลับไปเป็นหยวนอีก** — ค่าตั้งต้นของแพลตฟอร์มคือ CNY · ทุกครั้งที่สร้างฟิลด์ชนิด 8 ผ่าน API ต้องตั้ง `currency` เป็น THB เอง (D-63)
- ⚠️ **CLI/MCP มองไม่เห็นปัญหานี้** — `hap worksheet fields` ไม่คืน `advancedSetting` · ตรวจต้องใช้ SDK `GetWorksheetInfo`
- ⚠️ ฝั่ง **HR ตรวจแล้วสะอาด 0 ฟิลด์** แต่ถ้า agent-hr สร้างฟิลด์ Currency ใหม่จะเจอปัญหาเดียวกัน — ส่ง D-63 ให้ทราบ
