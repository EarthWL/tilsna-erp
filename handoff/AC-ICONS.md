# Handoff — AC/ICONS

**agent-ac · 6 ก.ย. 2569** · ตั้งไอคอนไซด์บาร์ให้โมดูลบัญชี
เอกสารเต็ม + ตารางไอคอนครบทุกรายการ: `projects/accounting/24-Sidebar-Icons-AC.md`

## ยิงไปแล้วบนเซิร์ฟเวอร์

App `deca7391-1761-424b-9af3-c8d043004ad3` · คำสั่ง `hap worksheet update <id> --icon <ชื่อ> -a <app>`

| # | วัตถุ | จำนวน | ทำอะไร |
|---|---|---|---|
| 1 | กลุ่ม AC-00 … AC-08 | 9 | ตั้งไอคอนจาก `8_4_folder` เดิม เป็นไอคอนเฉพาะกลุ่ม |
| 2 | worksheet ทั้งหมดในโมดูล AC | 47 | ตั้งไอคอนจาก `table` เดิม เป็นไอคอนเฉพาะตาราง |
| 3 | แก้ไอคอนที่ชนกับ HR ระหว่างทาง | 3 | AC-07 → `sys_final-score_activity` · การตั้งค่าเอกสาร → `sys_folder-vector_office` · ทะเบียนภาษีซื้อ–ภาษีขาย → `sys_1_9_address_book` |

รวมยิง **59 ครั้ง สำเร็จ 59/59** · รายการ id ↔ ไอคอน ครบทุกบรรทัดอยู่ใน `24-Sidebar-Icons-AC.md` §6

**ไม่ได้แตะ:** ชื่อตาราง · `entityName` · `alias` · ฟิลด์ · view · ข้อมูลในระเบียน · workflow · role · ตาราง HR

## ผลตรวจหลังทำ

- ไอคอนตรงกับที่ตั้ง **56/56** (อ่านกลับด้วย `hap --json app info` เทียบทีละรายการ)
- **ไม่มีไอคอนซ้ำทั้งแอป** — 102 รายการในไซด์บาร์ ใช้ไอคอนต่างกันครบ 102 แบบ (นับรวมของ HR)
- ชื่อตาราง / `entityName` / ชื่อ view ของทั้ง 47 ตาราง **ไม่เปลี่ยนเลย**
- จำนวนระเบียนคงเดิม: GL 337 · ใบแจ้งหนี้ 32 · ใบสำคัญ 97

## 🔴 ถึง agent-hr — เรื่องที่ต้องรู้

1. **เราตั้งไอคอนชนกัน 3 ตัวระหว่างที่ทำพร้อมกัน** (`sys_folder-check_office` · `sys_folder-settings_office` · `sys_folder-money_office` — ที่ HR ใช้กับกลุ่ม HR-02 / HR-00 / HR-04)
   ฝั่ง AC ถอยให้แล้ว **ของ HR ไม่ถูกแตะ** · แต่ครั้งหน้าที่แตะทรัพยากรร่วมทั้งแอป ให้อ่านสถานะจริงซ้ำหลังยิงจบ ไม่ใช่แค่ก่อนยิง (บันทึกเป็น **D-53**)
2. **`hap worksheet update --icon` ใช้กับ "กลุ่ม" ได้** — HR ยังเหลือ HR-01/03/05/06/07 ที่ยังเป็น `table` อยู่ (ดู **D-52**)
3. **`hap icon search` / `hap icon list` มีแล้วใน hap-cli 0.8.29** — ไม่ต้องงัด DOM ของ icon picker อีก (แต่ CLI มี 426 ชื่อ = subset ของ 997 บน UI)
4. **ไอคอนของ section จริง** (`บัญชี (AC)` = `custom_style` · `ทรัพยากรบุคคลฯ (HR)` = `8_4_folder`) ยังไม่มีใครแตะ — ต้องใช้ `HomeApp/UpdateAppSection` **ขอให้ตกลงกันก่อนว่าใครทำ**
